Index: src/propgrid/advprops.cpp
===================================================================
--- src/propgrid/advprops.cpp	(Revision 72462)
+++ src/propgrid/advprops.cpp	(Arbeitskopie)
@@ -1437,7 +1437,7 @@
     {
         if ( !(argFlags & wxPG_EDITABLE_VALUE ))
         {
-            // This really should not occurr...
+            // This really should not occur...
             // wxASSERT(false);
             ResetNextIndex();
             return false;
@@ -2204,7 +2204,7 @@
 
 wxString wxDateProperty::DetermineDefaultDateFormat( bool showCentury )
 {
-    // This code is basicly copied from datectlg.cpp's SetFormat
+    // This code is basically copied from datectlg.cpp's SetFormat
     //
     wxString format;
 
Index: src/propgrid/property.cpp
===================================================================
--- src/propgrid/property.cpp	(Revision 72462)
+++ src/propgrid/property.cpp	(Arbeitskopie)
@@ -48,7 +48,7 @@
 
 #if wxPG_COMPATIBILITY_1_4
 
-// Used to establish backwards compatiblity
+// Used to establish backwards compatibility
 const char* g_invalidStringContent = "@__TOTALLY_INVALID_STRING__@";
 
 #endif
Index: src/propgrid/propgrid.cpp
===================================================================
--- src/propgrid/propgrid.cpp	(Revision 72462)
+++ src/propgrid/propgrid.cpp	(Arbeitskopie)
@@ -5165,7 +5165,7 @@
     int splitterHitOffset;
     state->HitTestH( x, &splitterHit, &splitterHitOffset );
 
-    // No event type check - basicly calling this method should
+    // No event type check - basically calling this method should
     // just stop dragging.
     // Left up after dragged?
     if ( m_dragStatus >= 1 )
Index: src/propgrid/manager.cpp
===================================================================
--- src/propgrid/manager.cpp	(Revision 72462)
+++ src/propgrid/manager.cpp	(Arbeitskopie)
@@ -2003,7 +2003,7 @@
 
 void wxPropertyGridManager::OnMouseUp( wxMouseEvent &event )
 {
-    // No event type check - basicly calling this method should
+    // No event type check - basically calling this method should
     // just stop dragging.
 
     if ( m_dragStatus >= 1 )
