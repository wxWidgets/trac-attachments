Index: include/wx/msw/calctrl.h
===================================================================
--- include/wx/msw/calctrl.h	(revision 63657)
+++ include/wx/msw/calctrl.h	(working copy)
@@ -60,8 +60,8 @@
 
     virtual bool MSWOnNotify(int idCtrl, WXLPARAM lParam, WXLPARAM *result);
 
-    void MSWOnClick(wxMouseEvent& event);
-    void MSWOnDoubleClick(wxMouseEvent& event);
+    virtual void MSWOnClick(wxMouseEvent& event);
+    virtual void MSWOnDoubleClick(wxMouseEvent& event);
 
 private:
     void Init();
