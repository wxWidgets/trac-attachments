Index: cmdline.cpp
===================================================================
--- cmdline.cpp	(revision 77478)
+++ cmdline.cpp	(working copy)
@@ -667,6 +667,7 @@
 
     // parse everything
     wxString arg;
+    m_data->m_parameters.Empty();
     size_t count = m_data->m_arguments.size();
     for ( size_t n = 1; ok && (n < count); n++ )    // 0 is program name
     {
