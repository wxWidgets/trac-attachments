Index: include/wx/control.h
===================================================================
--- include/wx/control.h	(revision 57702)
+++ include/wx/control.h	(working copy)
@@ -74,7 +74,7 @@
 
     void SetLabelText(const wxString& text)
     {
-        return SetLabel(EscapeMnemonics(text));
+        SetLabel(EscapeMnemonics(text));
     }
 
     // static utilities:
