Index: spinbtn.cpp
===================================================================
--- spinbtn.cpp	(revision 68871)
+++ spinbtn.cpp	(working copy)
@@ -411,6 +411,9 @@
     m_sizerSpin->Add(0, 0, 1);
 
     m_sizerSpin->Layout();
+
+	this->Enable(false);
+	this->Enable(true);
 }
 
 // ----------------------------------------------------------------------------
