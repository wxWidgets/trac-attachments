--- docview.old	Thu Jun  5 14:03:48 2003
+++ docview.cpp	Wed Nov 19 16:21:45 2003
@@ -780,6 +780,7 @@
         // Implicitly deletes the document when the last
         // view is removed (deleted)
         doc->DeleteAllViews();
+        m_currentView = NULL;
 
         // Check document is deleted
         if (m_docs.Member(doc))

 	  	 
