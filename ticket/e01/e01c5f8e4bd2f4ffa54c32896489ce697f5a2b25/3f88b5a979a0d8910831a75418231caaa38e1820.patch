Index: src/cocoa/fontutil.cpp
===================================================================
--- src/cocoa/fontutil.cpp	(revision 64368)
+++ src/cocoa/fontutil.cpp	(working copy)
@@ -249,9 +249,7 @@
 
 void wxFontModule::OnExit()
 {
-    delete g_fontHash;
-
-    g_fontHash = NULL;
+    wxDELETE(g_fontHash);
 }
 
 #endif
Index: src/common/any.cpp
===================================================================
--- src/common/any.cpp	(revision 64368)
+++ src/common/any.cpp	(working copy)
@@ -223,8 +223,7 @@
     }
     virtual void OnExit()
     {
-        delete g_wxAnyValueTypeGlobals;
-        g_wxAnyValueTypeGlobals = NULL;
+        wxDELETE(g_wxAnyValueTypeGlobals);
     }
 private:
 };
Index: src/common/appbase.cpp
===================================================================
--- src/common/appbase.cpp	(revision 64368)
+++ src/common/appbase.cpp	(working copy)
@@ -146,8 +146,7 @@
     // In unicode mode the SetTraceMasks call can cause an apptraits to be
     // created, but since we are still in the constructor the wrong kind will
     // be created for GUI apps.  Destroy it so it can be created again later.
-    delete m_traits;
-    m_traits = NULL;
+    wxDELETE(m_traits);
 #endif
 #endif
 }
@@ -213,11 +212,7 @@
 
 void wxAppConsoleBase::CleanUp()
 {
-    if ( m_mainLoop )
-    {
-        delete m_mainLoop;
-        m_mainLoop = NULL;
-    }
+    wxDELETE(m_mainLoop);
 }
 
 // ----------------------------------------------------------------------------
Index: src/common/appcmn.cpp
===================================================================
--- src/common/appcmn.cpp	(revision 64368)
+++ src/common/appcmn.cpp	(working copy)
@@ -128,8 +128,7 @@
 
     wxDeleteStockLists();
 
-    delete wxTheColourDatabase;
-    wxTheColourDatabase = NULL;
+    wxDELETE(wxTheColourDatabase);
 
     wxAppConsole::CleanUp();
 }
Index: src/common/artprov.cpp
===================================================================
--- src/common/artprov.cpp	(revision 64368)
+++ src/common/artprov.cpp	(working copy)
@@ -204,11 +204,8 @@
         while ( !sm_providers->empty() )
             delete *sm_providers->begin();
 
-        delete sm_providers;
-        sm_providers = NULL;
-
-        delete sm_cache;
-        sm_cache = NULL;
+        wxDELETE(sm_providers);
+        wxDELETE(sm_cache);
     }
 }
 
Index: src/common/cairo.cpp
===================================================================
--- src/common/cairo.cpp	(revision 64368)
+++ src/common/cairo.cpp	(working copy)
@@ -63,8 +63,7 @@
         ms_lib = new wxCairoLibrary();
         if ( !ms_lib->IsOk() )
         {
-            delete ms_lib;
-            ms_lib = NULL;
+            wxDELETE(ms_lib);
         }
     }
 
@@ -73,11 +72,7 @@
 
 /* static */ void wxCairoLibrary::CleanUp()
 {
-    if (ms_lib)
-    {
-        delete ms_lib;
-        ms_lib = NULL;
-    }
+    wxDELETE(ms_lib);
 }
 
 bool wxCairoLibrary::IsOk()
Index: src/common/cmndata.cpp
===================================================================
--- src/common/cmndata.cpp	(revision 64368)
+++ src/common/cmndata.cpp	(working copy)
@@ -223,11 +223,7 @@
 
 void wxPrintData::SetPrivData( char *privData, int len )
 {
-    if (m_privData)
-    {
-        delete [] m_privData;
-        m_privData = NULL;
-    }
+    wxDELETEA(m_privData);
     m_privDataLen = len;
     if (m_privDataLen > 0)
     {
@@ -287,11 +283,7 @@
     m_nativeData = data.GetNativeData();
     m_nativeData->m_ref++;
 
-    if (m_privData)
-    {
-        delete [] m_privData;
-        m_privData = NULL;
-    }
+    wxDELETEA(m_privData);
     m_privDataLen = data.GetPrivDataLen();
     if (m_privDataLen > 0)
     {
Index: src/common/combocmn.cpp
===================================================================
--- src/common/combocmn.cpp	(revision 64368)
+++ src/common/combocmn.cpp	(working copy)
@@ -1959,20 +1959,16 @@
     if ( m_popup )
         m_popup->RemoveEventHandler(m_popupExtraHandler);
 
-    delete m_popupExtraHandler;
+    wxDELETE(m_popupExtraHandler);
+    wxDELETE(m_popupInterface);
 
-    delete m_popupInterface;
-
     if ( m_winPopup )
     {
         m_winPopup->RemoveEventHandler(m_popupWinEvtHandler);
-        delete m_popupWinEvtHandler;
-        m_popupWinEvtHandler = NULL;
+        wxDELETE(m_popupWinEvtHandler);
         m_winPopup->Destroy();
     }
 
-    m_popupExtraHandler = NULL;
-    m_popupInterface = NULL;
     m_winPopup = NULL;
     m_popup = NULL;
 }
Index: src/common/dlgcmn.cpp
===================================================================
--- src/common/dlgcmn.cpp	(revision 64368)
+++ src/common/dlgcmn.cpp	(working copy)
@@ -680,8 +680,7 @@
                     stdButtonSizer->Realize();
                 else
                 {
-                    delete buttonSizer;
-                    buttonSizer = NULL;
+                    wxDELETE(buttonSizer);
                 }
             }
 
Index: src/common/docview.cpp
===================================================================
--- src/common/docview.cpp	(revision 64368)
+++ src/common/docview.cpp	(working copy)
@@ -1877,8 +1877,7 @@
         // deleted directly not by us as indicated by its doc child frame
         // pointer still being set
         m_childView->SetDocChildFrame(NULL);
-        delete m_childView;
-        m_childView = NULL;
+        wxDELETE(m_childView);
     }
 
     m_childDocument = NULL;
Index: src/common/dpycmn.cpp
===================================================================
--- src/common/dpycmn.cpp	(revision 64368)
+++ src/common/dpycmn.cpp	(working copy)
@@ -98,11 +98,7 @@
     virtual bool OnInit() { return true; }
     virtual void OnExit()
     {
-        if ( gs_factory )
-        {
-            delete gs_factory;
-            gs_factory = NULL;
-        }
+        wxDELETE(gs_factory);
     }
 
     DECLARE_DYNAMIC_CLASS(wxDisplayModule)
Index: src/common/dynload.cpp
===================================================================
--- src/common/dynload.cpp	(revision 64368)
+++ src/common/dynload.cpp	(working copy)
@@ -61,8 +61,7 @@
 
     virtual void OnExit()
     {
-        delete wxPluginLibrary::ms_classes;
-        wxPluginLibrary::ms_classes = NULL;
+        wxDELETE(wxPluginLibrary::ms_classes);
         wxPluginManager::ClearManifest();
     }
 
Index: src/common/encconv.cpp
===================================================================
--- src/common/encconv.cpp	(revision 64368)
+++ src/common/encconv.cpp	(working copy)
@@ -116,7 +116,7 @@
     const wxUint16 *in_tbl;
     const wxUint16 *out_tbl = NULL;
 
-    if (m_Table) {delete[] m_Table; m_Table = NULL;}
+    wxDELETEA(m_Table);
 
     if (input_enc == output_enc) {m_JustCopy = true; return true;}
 
Index: src/common/event.cpp
===================================================================
--- src/common/event.cpp	(revision 64368)
+++ src/common/event.cpp	(working copy)
@@ -886,8 +886,7 @@
         delete eTTnode;
     }
 
-    delete[] m_eventTypeTable;
-    m_eventTypeTable = NULL;
+    wxDELETEA(m_eventTypeTable);
 
     m_size = 0;
 }
Index: src/common/fileback.cpp
===================================================================
--- src/common/fileback.cpp	(revision 64368)
+++ src/common/fileback.cpp	(working copy)
@@ -168,11 +168,9 @@
                     m_filelen += count;
 
                     if (count < m_buflen) {
-                        delete m_stream;
-                        m_stream = NULL;
+                        wxDELETE(m_stream);
                         if (count > 0) {
-                            delete[] m_buf;
-                            m_buf = NULL;
+                            wxDELETEA(m_buf);
                             m_buflen = 0;
                         }
                         m_parenterror = wxSTREAM_READ_ERROR;
@@ -182,8 +180,7 @@
                     m_buflen = 0;
 
                     if (!m_stream) {
-                        delete[] m_buf;
-                        m_buf = NULL;
+                        wxDELETEA(m_buf);
                     }
                 }
 
@@ -197,8 +194,7 @@
                     m_parenterror = m_stream->GetLastError();
                     if (m_parenterror == wxSTREAM_NO_ERROR)
                         m_parenterror = wxSTREAM_EOF;
-                    delete m_stream;
-                    m_stream = NULL;
+                    wxDELETE(m_stream);
                 }
             }
 
Index: src/common/framecmn.cpp
===================================================================
--- src/common/framecmn.cpp	(revision 64368)
+++ src/common/framecmn.cpp	(working copy)
@@ -92,27 +92,15 @@
 void wxFrameBase::DeleteAllBars()
 {
 #if wxUSE_MENUS
-    if ( m_frameMenuBar )
-    {
-        delete m_frameMenuBar;
-        m_frameMenuBar = NULL;
-    }
+    wxDELETE(m_frameMenuBar);
 #endif // wxUSE_MENUS
 
 #if wxUSE_STATUSBAR
-    if ( m_frameStatusBar )
-    {
-        delete m_frameStatusBar;
-        m_frameStatusBar = NULL;
-    }
+    wxDELETE(m_frameStatusBar);
 #endif // wxUSE_STATUSBAR
 
 #if wxUSE_TOOLBAR
-    if ( m_frameToolBar )
-    {
-        delete m_frameToolBar;
-        m_frameToolBar = NULL;
-    }
+    wxDELETE(m_frameToolBar);
 #endif // wxUSE_TOOLBAR
 }
 
Index: src/common/fs_arc.cpp
===================================================================
--- src/common/fs_arc.cpp	(revision 64368)
+++ src/common/fs_arc.cpp	(working copy)
@@ -135,10 +135,8 @@
 
 void wxArchiveFSCacheDataImpl::CloseStreams()
 {
-    delete m_archive;
-    m_archive = NULL;
-    delete m_stream;
-    m_stream = NULL;
+    wxDELETE(m_archive);
+    wxDELETE(m_stream);
 }
 
 wxArchiveEntry *wxArchiveFSCacheDataImpl::Get(const wxString& name)
Index: src/common/ftp.cpp
===================================================================
--- src/common/ftp.cpp	(revision 64368)
+++ src/common/ftp.cpp	(working copy)
@@ -199,8 +199,7 @@
     {
         m_lastError = wxPROTO_CONNERR;
         wxLogError(_("Timeout while waiting for FTP server to connect, try passive mode."));
-        delete sock;
-        sock = NULL;
+        wxDELETE(sock);
     }
     else
     {
Index: src/common/gdicmn.cpp
===================================================================
--- src/common/gdicmn.cpp	(revision 64368)
+++ src/common/gdicmn.cpp	(working copy)
@@ -521,8 +521,7 @@
 {
     for (unsigned i = 0; i < ITEMCOUNT; i++)
     {
-        delete ms_stockObject[i];
-        ms_stockObject[i] = NULL;
+        wxDELETE(ms_stockObject[i]);
     }
 }
 
Index: src/common/hash.cpp
===================================================================
--- src/common/hash.cpp	(revision 64368)
+++ src/common/hash.cpp	(working copy)
@@ -135,9 +135,7 @@
 {
     Clear();
 
-    delete[] m_table;
-
-    m_table = NULL;
+    wxDELETEA(m_table);
     m_size = 0;
 }
 
Index: src/common/http.cpp
===================================================================
--- src/common/http.cpp	(revision 64368)
+++ src/common/http.cpp	(working copy)
@@ -251,16 +251,14 @@
     wxIPV4address *addr;
 
     if (m_addr) {
-        delete m_addr;
-        m_addr = NULL;
+        wxDELETE(m_addr);
         Close();
     }
 
     m_addr = addr = new wxIPV4address();
 
     if (!addr->Hostname(host)) {
-        delete m_addr;
-        m_addr = NULL;
+        wxDELETE(m_addr);
         m_lastError = wxPROTO_NETERR;
         return false;
     }
Index: src/common/imagiff.cpp
===================================================================
--- src/common/imagiff.cpp	(revision 64368)
+++ src/common/imagiff.cpp	(working copy)
@@ -121,12 +121,9 @@
 
 void wxIFFDecoder::Destroy()
 {
-    delete m_image;
-    m_image = 0;
-    delete [] databuf;
-    databuf = 0;
-    delete [] decomp_mem;
-    decomp_mem = 0;
+    wxDELETE(m_image);
+    wxDELETEA(databuf);
+    wxDELETEA(decomp_mem);
 }
 
 //---------------------------------------------------------------------------
@@ -420,8 +417,7 @@
         const byte *cmapptr = dataptr + 8;
         colors = chunkLen / 3;                  // calc no of colors
 
-        delete m_image->pal;
-        m_image->pal = 0;
+        wxDELETE(m_image->pal);
         m_image->colors = colors;
         if (colors > 0) {
         m_image->pal = new byte[3*colors];
@@ -479,8 +475,7 @@
         decomprle(bodyptr, decomp_mem, chunkLen, decomp_bufsize);
         bodyptr = decomp_mem;                 // -> uncompressed BODY
         chunkLen = decomp_bufsize;
-        delete [] databuf;
-        databuf = 0;
+        wxDELETEA(databuf);
         }
 
         // the following determines the type of the ILBM file.
Index: src/common/init.cpp
===================================================================
--- src/common/init.cpp	(revision 64368)
+++ src/common/init.cpp	(working copy)
@@ -204,8 +204,7 @@
             free(gs_initData.argv[i]);
         }
 
-        delete [] gs_initData.argv;
-        gs_initData.argv = NULL;
+        wxDELETEA(gs_initData.argv);
         gs_initData.argc = 0;
     }
 }
Index: src/common/intl.cpp
===================================================================
--- src/common/intl.cpp	(revision 64368)
+++ src/common/intl.cpp	(working copy)
@@ -200,8 +200,7 @@
 
 /*static*/ void wxLocale::DestroyLanguagesDB()
 {
-    delete ms_languagesDB;
-    ms_languagesDB = NULL;
+    wxDELETE(ms_languagesDB);
 }
 
 
Index: src/common/mediactrlcmn.cpp
===================================================================
--- src/common/mediactrlcmn.cpp	(revision 64368)
+++ src/common/mediactrlcmn.cpp	(working copy)
@@ -107,8 +107,7 @@
         {
             if (!Load(fileName))
             {
-                delete m_imp;
-                m_imp = NULL;
+                wxDELETE(m_imp);
                 return false;
             }
         }
@@ -172,8 +171,7 @@
 
         if (!Load(location))
         {
-            delete m_imp;
-            m_imp = NULL;
+            wxDELETE(m_imp);
             return false;
         }
 
Index: src/common/mimecmn.cpp
===================================================================
--- src/common/mimecmn.cpp	(revision 64368)
+++ src/common/mimecmn.cpp	(working copy)
@@ -737,8 +737,7 @@
 
         if ( gs_mimeTypesManager.m_impl != NULL )
         {
-            delete gs_mimeTypesManager.m_impl;
-            gs_mimeTypesManager.m_impl = NULL;
+            wxDELETE(gs_mimeTypesManager.m_impl);
             gs_mimeTypesManager.m_fallbacks.Clear();
         }
     }
Index: src/common/object.cpp
===================================================================
--- src/common/object.cpp	(revision 64368)
+++ src/common/object.cpp	(working copy)
@@ -283,8 +283,7 @@
         sm_classTable->Delete(m_className);
         if ( sm_classTable->GetCount() == 0 )
         {
-            delete sm_classTable;
-            sm_classTable = NULL;
+            wxDELETE(sm_classTable);
         }
     }
 }
Index: src/common/paper.cpp
===================================================================
--- src/common/paper.cpp	(revision 64368)
+++ src/common/paper.cpp	(working copy)
@@ -374,8 +374,7 @@
 
 void wxPrintPaperModule::OnExit()
 {
-    delete wxThePrintPaperDatabase;
-    wxThePrintPaperDatabase = NULL;
+    wxDELETE(wxThePrintPaperDatabase);
 }
 
 #endif // wxUSE_PRINTING_ARCHITECTURE
Index: src/common/radiocmn.cpp
===================================================================
--- src/common/radiocmn.cpp	(revision 64368)
+++ src/common/radiocmn.cpp	(working copy)
@@ -189,8 +189,7 @@
         if ( tooltip )
         {
             // delete the tooltip
-            delete tooltip;
-            tooltip = NULL;
+            wxDELETE(tooltip);
         }
         else // nothing to do
         {
Index: src/common/regex.cpp
===================================================================
--- src/common/regex.cpp	(revision 64368)
+++ src/common/regex.cpp	(working copy)
@@ -646,8 +646,7 @@
     if ( !m_impl->Compile(expr, flags) )
     {
         // error message already given in wxRegExImpl::Compile
-        delete m_impl;
-        m_impl = NULL;
+        wxDELETE(m_impl);
 
         return false;
     }
Index: src/common/socket.cpp
===================================================================
--- src/common/socket.cpp	(revision 64368)
+++ src/common/socket.cpp	(working copy)
@@ -1789,8 +1789,7 @@
 
     if (m_impl->CreateServer() != wxSOCKET_NOERROR)
     {
-        delete m_impl;
-        m_impl = NULL;
+        wxDELETE(m_impl);
 
         wxLogTrace( wxTRACE_Socket, wxT("*** CreateServer() failed") );
         return;
@@ -2030,8 +2029,7 @@
 
     if ( m_impl->CreateUDP() != wxSOCKET_NOERROR )
     {
-        delete m_impl;
-        m_impl = NULL;
+        wxDELETE(m_impl);
         return;
     }
 
Index: src/common/strconv.cpp
===================================================================
--- src/common/strconv.cpp	(revision 64368)
+++ src/common/strconv.cpp	(working copy)
@@ -3013,10 +3013,9 @@
 void wxCSConv::Clear()
 {
     free(m_name);
-    delete m_convReal;
+    wxDELETE(m_convReal);
 
     m_name = NULL;
-    m_convReal = NULL;
 }
 
 void wxCSConv::SetName(const char *charset)
Index: src/common/url.cpp
===================================================================
--- src/common/url.cpp	(revision 64368)
+++ src/common/url.cpp	(working copy)
@@ -365,8 +365,7 @@
         if ( ms_proxyDefault )
         {
             ms_proxyDefault->Close();
-            delete ms_proxyDefault;
-            ms_proxyDefault = NULL;
+            wxDELETE(ms_proxyDefault);
         }
     }
     else
@@ -487,8 +486,7 @@
 void wxURLModule::OnExit()
 {
 #if wxUSE_PROTOCOL_HTTP
-    delete wxURL::ms_proxyDefault;
-    wxURL::ms_proxyDefault = NULL;
+    wxDELETE(wxURL::ms_proxyDefault);
 #endif // wxUSE_PROTOCOL_HTTP
 }
 
Index: src/common/utilscmn.cpp
===================================================================
--- src/common/utilscmn.cpp	(revision 64368)
+++ src/common/utilscmn.cpp	(working copy)
@@ -355,8 +355,7 @@
 
 void wxPlatform::ClearPlatforms()
 {
-    delete sm_customPlatforms;
-    sm_customPlatforms = NULL;
+    wxDELETE(sm_customPlatforms);
 }
 
 /// Function for testing current platform
Index: src/common/wincmn.cpp
===================================================================
--- src/common/wincmn.cpp	(revision 64368)
+++ src/common/wincmn.cpp	(working copy)
@@ -364,8 +364,7 @@
         // This removes any dangling pointers to this window in other windows'
         // constraintsInvolvedIn lists.
         UnsetConstraints(m_constraints);
-        delete m_constraints;
-        m_constraints = NULL;
+        wxDELETE(m_constraints);
     }
 #endif // wxUSE_CONSTRAINTS
 
@@ -1254,8 +1253,7 @@
 
     if ( deleteHandler )
     {
-        delete firstHandler;
-        firstHandler = NULL;
+        wxDELETE(firstHandler);
     }
 
     return firstHandler;
@@ -2076,8 +2074,7 @@
             node = next;
         }
 
-        delete m_constraintsInvolvedIn;
-        m_constraintsInvolvedIn = NULL;
+        wxDELETE(m_constraintsInvolvedIn);
     }
 }
 
Index: src/common/zstream.cpp
===================================================================
--- src/common/zstream.cpp	(revision 64368)
+++ src/common/zstream.cpp	(working copy)
@@ -335,12 +335,9 @@
  {
   DoFlush(true);
    deflateEnd(m_deflate);
-   delete m_deflate;
+   wxDELETE(m_deflate);
+   wxDELETEA(m_z_buffer);
 
-  m_deflate = NULL;
-   delete[] m_z_buffer;
-  m_z_buffer = NULL;
-
   return wxFilterOutputStream::Close() && IsOk();
  }
 
Index: src/dfb/bitmap.cpp
===================================================================
--- src/dfb/bitmap.cpp	(revision 64368)
+++ src/dfb/bitmap.cpp	(working copy)
@@ -696,8 +696,7 @@
     wxCHECK_RET( GetDepth() > 1 && GetDepth() <= 8, wxT("cannot set palette for bitmap of this depth") );
 
     AllocExclusive();
-    delete M_BITMAP->m_palette;
-    M_BITMAP->m_palette = NULL;
+    wxDELETE(M_BITMAP->m_palette);
 
     if ( !palette.Ok() ) return;
 
Index: src/generic/dirctrlg.cpp
===================================================================
--- src/generic/dirctrlg.cpp	(revision 64368)
+++ src/generic/dirctrlg.cpp	(working copy)
@@ -1496,11 +1496,7 @@
     bool OnInit() { wxTheFileIconsTable = new wxFileIconsTable; return true; }
     void OnExit()
     {
-        if (wxTheFileIconsTable)
-        {
-            delete wxTheFileIconsTable;
-            wxTheFileIconsTable = NULL;
-        }
+        wxDELETE(wxTheFileIconsTable);
     }
 };
 
Index: src/generic/dragimgg.cpp
===================================================================
--- src/generic/dragimgg.cpp	(revision 64368)
+++ src/generic/dragimgg.cpp	(working copy)
@@ -358,8 +358,7 @@
 #else
         m_windowDC->DestroyClippingRegion();
 #endif
-        delete m_windowDC;
-        m_windowDC = NULL;
+        wxDELETE(m_windowDC);
     }
 
 #ifndef wxHAS_NATIVE_OVERLAY
Index: src/generic/grid.cpp
===================================================================
--- src/generic/grid.cpp	(revision 64368)
+++ src/generic/grid.cpp	(working copy)
@@ -2100,8 +2100,7 @@
             m_table = NULL;
         }
 
-        delete m_selection;
-        m_selection = NULL;
+        wxDELETE(m_selection);
 
         m_ownTable = false;
         m_numRows = 0;
Index: src/generic/helpext.cpp
===================================================================
--- src/generic/helpext.cpp	(revision 64368)
+++ src/generic/helpext.cpp	(working copy)
@@ -154,8 +154,7 @@
             node = m_MapList->GetFirst();
         }
 
-        delete m_MapList;
-        m_MapList = NULL;
+        wxDELETE(m_MapList);
     }
 }
 
Index: src/generic/htmllbox.cpp
===================================================================
--- src/generic/htmllbox.cpp	(revision 64368)
+++ src/generic/htmllbox.cpp	(working copy)
@@ -66,8 +66,7 @@
     void InvalidateItem(size_t n)
     {
         m_items[n] = (size_t)-1;
-        delete m_cells[n];
-        m_cells[n] = NULL;
+        wxDELETE(m_cells[n]);
     }
 
 public:
Index: src/generic/listctrl.cpp
===================================================================
--- src/generic/listctrl.cpp	(revision 64368)
+++ src/generic/listctrl.cpp	(working copy)
@@ -4326,9 +4326,7 @@
     {
         GetSizer()->Detach( m_headerWin );
 
-        delete m_headerWin;
-
-        m_headerWin = NULL;
+        wxDELETE(m_headerWin);
     }
 }
 
Index: src/generic/mdig.cpp
===================================================================
--- src/generic/mdig.cpp	(revision 64368)
+++ src/generic/mdig.cpp	(working copy)
@@ -74,11 +74,7 @@
     wxDELETE(m_clientWindow);
 
 #if wxUSE_MENUS
-    if (m_pMyMenuBar)
-    {
-        delete m_pMyMenuBar;
-        m_pMyMenuBar = NULL;
-    }
+    wxDELETE(m_pMyMenuBar);
 
     RemoveWindowMenu(GetMenuBar());
 #endif // wxUSE_MENUS
Index: src/generic/progdlgg.cpp
===================================================================
--- src/generic/progdlgg.cpp	(revision 64368)
+++ src/generic/progdlgg.cpp	(working copy)
@@ -621,8 +621,7 @@
 {
     if ( HasFlag(wxPD_APP_MODAL) )
     {
-        delete m_winDisabler;
-        m_winDisabler = NULL;
+        wxDELETE(m_winDisabler);
     }
     else
     {
Index: src/generic/renderg.cpp
===================================================================
--- src/generic/renderg.cpp	(revision 64368)
+++ src/generic/renderg.cpp	(working copy)
@@ -183,10 +183,7 @@
 
 void wxRendererGeneric::Cleanup()
 {
-    if (sm_rendererGeneric)
-        delete sm_rendererGeneric;
-
-    sm_rendererGeneric = NULL;
+    wxDELETE(sm_rendererGeneric);
 }
 
 wxRendererGeneric* wxRendererGeneric::sm_rendererGeneric = NULL;
Index: src/generic/scrlwing.cpp
===================================================================
--- src/generic/scrlwing.cpp	(revision 64368)
+++ src/generic/scrlwing.cpp	(working copy)
@@ -910,11 +910,7 @@
 void wxScrollHelperBase::StopAutoScrolling()
 {
 #if wxUSE_TIMER
-    if ( m_timerAutoScroll )
-    {
-        delete m_timerAutoScroll;
-        m_timerAutoScroll = NULL;
-    }
+    wxDELETE(m_timerAutoScroll);
 #endif
 }
 
Index: src/generic/spinctlg.cpp
===================================================================
--- src/generic/spinctlg.cpp	(revision 64368)
+++ src/generic/spinctlg.cpp	(working copy)
@@ -248,8 +248,7 @@
         delete text;
     }
 
-    delete m_spinButton;
-    m_spinButton = NULL;
+    wxDELETE(m_spinButton);
 }
 
 // ----------------------------------------------------------------------------
Index: src/generic/timer.cpp
===================================================================
--- src/generic/timer.cpp	(revision 64368)
+++ src/generic/timer.cpp	(working copy)
@@ -273,7 +273,7 @@
 public:
     wxTimerModule() {}
     bool OnInit() { return true; }
-    void OnExit() { delete gs_scheduler; gs_scheduler = NULL; }
+    void OnExit() { wxDELETE(gs_scheduler); }
 };
 
 IMPLEMENT_DYNAMIC_CLASS(wxTimerModule, wxModule)
Index: src/gtk1/clipbrd.cpp
===================================================================
--- src/gtk1/clipbrd.cpp	(revision 64368)
+++ src/gtk1/clipbrd.cpp	(working copy)
@@ -216,8 +216,7 @@
         {
             wxLogTrace(TRACE_CLIPBOARD, wxT("wxClipboard will get cleared" ));
 
-            delete wxTheClipboard->m_data;
-            wxTheClipboard->m_data = NULL;
+            wxDELETE(wxTheClipboard->m_data);
         }
     }
 
@@ -383,11 +382,7 @@
             while (m_waiting) gtk_main_iteration();
         }
 
-        if (m_data)
-        {
-            delete m_data;
-            m_data = NULL;
-        }
+        wxDELETE(m_data);
 
 #if wxUSE_THREADS
         /* re-enable GUI threads */
Index: src/gtk1/evtloop.cpp
===================================================================
--- src/gtk1/evtloop.cpp	(revision 64368)
+++ src/gtk1/evtloop.cpp	(working copy)
@@ -79,8 +79,7 @@
     OnExit();
 
     int exitcode = m_impl->GetExitCode();
-    delete m_impl;
-    m_impl = NULL;
+    wxDELETE(m_impl);
 
     return exitcode;
 }
Index: src/gtk/clipbrd.cpp
===================================================================
--- src/gtk/clipbrd.cpp	(revision 64368)
+++ src/gtk/clipbrd.cpp	(working copy)
@@ -485,11 +485,7 @@
 void wxClipboard::GTKClearData(Kind kind)
 {
     wxDataObject *&data = Data(kind);
-    if ( data )
-    {
-        delete data;
-        data = NULL;
-    }
+    wxDELETE(data);
 }
 
 bool wxClipboard::SetSelectionOwner(bool set)
Index: src/gtk/dataview.cpp
===================================================================
--- src/gtk/dataview.cpp	(revision 64368)
+++ src/gtk/dataview.cpp	(working copy)
@@ -4303,11 +4303,7 @@
 
 bool wxDataViewCtrl::AssociateModel( wxDataViewModel *model )
 {
-    if ( m_internal )
-    {
-        delete m_internal;
-        m_internal = NULL;
-    }
+    wxDELETE(m_internal);
 
     if (!wxDataViewCtrlBase::AssociateModel( model ))
         return false;
Index: src/gtk/gnome/gprint.cpp
===================================================================
--- src/gtk/gnome/gprint.cpp	(revision 64368)
+++ src/gtk/gnome/gprint.cpp	(working copy)
@@ -2028,8 +2028,7 @@
 
 void wxGnomePrintModule::OnExit()
 {
-    delete gs_libGnomePrint;
-    gs_libGnomePrint = NULL;
+    wxDELETE(gs_libGnomePrint);
 }
 
 IMPLEMENT_DYNAMIC_CLASS(wxGnomePrintModule, wxModule)
Index: src/html/htmlcell.cpp
===================================================================
--- src/html/htmlcell.cpp	(revision 64368)
+++ src/html/htmlcell.cpp	(working copy)
@@ -237,8 +237,7 @@
 
 void wxHtmlCell::SetLink(const wxHtmlLinkInfo& link)
 {
-    if (m_Link) delete m_Link;
-    m_Link = NULL;
+    wxDELETE(m_Link);
     if (link.GetHref() != wxEmptyString)
         m_Link = new wxHtmlLinkInfo(link);
 }
Index: src/html/htmlpars.cpp
===================================================================
--- src/html/htmlpars.cpp	(revision 64368)
+++ src/html/htmlpars.cpp	(working copy)
@@ -250,8 +250,7 @@
     }
     m_Tags = m_CurTag = NULL;
 
-    delete m_TextPieces;
-    m_TextPieces = NULL;
+    wxDELETE(m_TextPieces);
 }
 
 void wxHtmlParser::DoParsing()
Index: src/html/htmltag.cpp
===================================================================
--- src/html/htmltag.cpp	(revision 64368)
+++ src/html/htmltag.cpp	(working copy)
@@ -203,8 +203,7 @@
     for ( wxHtmlTagsCacheData::iterator i = Cache().begin();
           i != Cache().end(); ++i )
     {
-        delete[] i->Name;
-        i->Name = NULL;
+        wxDELETEA(i->Name);
     }
 }
 
Index: src/html/htmlwin.cpp
===================================================================
--- src/html/htmlwin.cpp	(revision 64368)
+++ src/html/htmlwin.cpp	(working copy)
@@ -471,11 +471,10 @@
     m_Parser->SetDC(dc);
     if (m_Cell)
     {
-        delete m_Cell;
         // notice that it's important to set m_Cell to NULL here before calling
         // Parse() below, even if it will be overwritten by its return value:
         // without this we may crash if it's used from inside Parse()
-        m_Cell = NULL;
+        wxDELETE(m_Cell);
     }
     m_Cell = (wxHtmlContainerCell*) m_Parser->Parse(newsrc);
     delete dc;
Index: src/html/winpars.cpp
===================================================================
--- src/html/winpars.cpp	(revision 64368)
+++ src/html/winpars.cpp	(working copy)
@@ -581,8 +581,7 @@
 #endif
                             ))
     {
-        delete *fontptr;
-        *fontptr = NULL;
+        wxDELETE(*fontptr);
     }
 
     if (*fontptr == NULL)
@@ -644,11 +643,7 @@
     m_nbsp = 0;
 
     m_InputEnc = m_OutputEnc = wxFONTENCODING_DEFAULT;
-    if (m_EncConv)
-    {
-        delete m_EncConv;
-        m_EncConv = NULL;
-    }
+    wxDELETE(m_EncConv);
 
     if (enc == wxFONTENCODING_DEFAULT)
         return;
@@ -713,8 +708,7 @@
         wxLogError(_("Failed to display HTML document in %s encoding"),
                    wxFontMapper::GetEncodingName(enc).c_str());
         m_InputEnc = m_OutputEnc = wxFONTENCODING_DEFAULT;
-        delete m_EncConv;
-        m_EncConv = NULL;
+        wxDELETE(m_EncConv);
     }
 }
 #endif
Index: src/mgl/app.cpp
===================================================================
--- src/mgl/app.cpp	(revision 64368)
+++ src/mgl/app.cpp	(working copy)
@@ -120,8 +120,7 @@
     g_displayDC = new MGLDisplayDC(mode, 1, refresh);
     if ( !g_displayDC->isValid() )
     {
-        delete g_displayDC;
-        g_displayDC = NULL;
+        wxDELETE(g_displayDC);
         return false;
     }
 
@@ -139,11 +138,7 @@
         MGL_wmDestroy(g_winMng);
         g_winMng = NULL;
     }
-    if ( g_displayDC )
-    {
-        delete g_displayDC;
-        g_displayDC = NULL;
-    }
+    wxDELETE(g_displayDC);
 }
 
 //-----------------------------------------------------------------------------
Index: src/mgl/bitmap.cpp
===================================================================
--- src/mgl/bitmap.cpp	(revision 64368)
+++ src/mgl/bitmap.cpp	(working copy)
@@ -498,8 +498,7 @@
     wxCHECK_RET( GetDepth() > 1 && GetDepth() <= 8, wxT("cannot set palette for bitmap of this depth") );
 
     AllocExclusive();
-    delete M_BMPDATA->m_palette;
-    M_BMPDATA->m_palette = NULL;
+    wxDELETE(M_BMPDATA->m_palette);
 
     if ( !palette.Ok() ) return;
 
Index: src/mgl/evtloop.cpp
===================================================================
--- src/mgl/evtloop.cpp	(revision 64368)
+++ src/mgl/evtloop.cpp	(working copy)
@@ -143,8 +143,7 @@
     OnExit();
 
     int exitcode = m_impl->GetExitCode();
-    delete m_impl;
-    m_impl = NULL;
+    wxDELETE(m_impl);
 
     return exitcode;
 }
Index: src/mgl/settings.cpp
===================================================================
--- src/mgl/settings.cpp	(revision 64368)
+++ src/mgl/settings.cpp	(working copy)
@@ -34,8 +34,7 @@
     virtual bool OnInit() { return true; }
     virtual void OnExit()
     {
-        delete gs_fontDefault;
-        gs_fontDefault = NULL;
+        wxDELETE(gs_fontDefault);
     }
 
 private:
Index: src/motif/accel.cpp
===================================================================
--- src/motif/accel.cpp	(revision 64368)
+++ src/motif/accel.cpp	(working copy)
@@ -45,8 +45,7 @@
 
 wxAcceleratorRefData::~wxAcceleratorRefData()
 {
-    delete[] m_entries;
-    m_entries = NULL;
+    wxDELETEA(m_entries);
     m_count = 0;
 }
 
Index: src/motif/app.cpp
===================================================================
--- src/motif/app.cpp	(revision 64368)
+++ src/motif/app.cpp	(working copy)
@@ -269,8 +269,7 @@
 {
     wxAppBase::CleanUp();
 
-    delete wxWidgetHashTable;
-    wxWidgetHashTable = NULL;
+    wxDELETE(wxWidgetHashTable);
 
     delete m_mainLoop;
 
Index: src/motif/dialog.cpp
===================================================================
--- src/motif/dialog.cpp	(revision 64368)
+++ src/motif/dialog.cpp	(working copy)
@@ -307,8 +307,7 @@
     // Now process all events in case they get sent to a destroyed dialog
     wxFlushEvents( display );
 
-    delete m_eventLoop;
-    m_eventLoop = NULL;
+    wxDELETE(m_eventLoop);
 
     // TODO: is it safe to call this, if the dialog may have been deleted
     // by now? Probably only if we're using delayed deletion of dialogs.
Index: src/motif/evtloop.cpp
===================================================================
--- src/motif/evtloop.cpp	(revision 64368)
+++ src/motif/evtloop.cpp	(working copy)
@@ -121,8 +121,7 @@
     OnExit();
 
     int exitcode = m_impl->GetExitCode();
-    delete m_impl;
-    m_impl = NULL;
+    wxDELETE(m_impl);
 
     return exitcode;
 }
Index: src/motif/frame.cpp
===================================================================
--- src/motif/frame.cpp	(revision 64368)
+++ src/motif/frame.cpp	(working copy)
@@ -267,15 +267,10 @@
     if (m_frameMenuBar)
     {
         m_frameMenuBar->DestroyMenuBar();
-        delete m_frameMenuBar;
-        m_frameMenuBar = NULL;
+        wxDELETE(m_frameMenuBar);
     }
 
-    if (m_frameStatusBar)
-    {
-        delete m_frameStatusBar;
-        m_frameStatusBar = NULL;
-    }
+    wxDELETE(m_frameStatusBar);
 
     PreDestroy();
 
Index: src/motif/mdi/lib/XsMDICanvas.C
===================================================================
--- src/motif/mdi/lib/XsMDICanvas.C	(revision 64368)
+++ src/motif/mdi/lib/XsMDICanvas.C	(working copy)
@@ -171,9 +171,8 @@
 
 // Delete and reset the list
 
-   delete [] _list;
+   wxDELETEA(_list);
 
-   _list = 0;
    _num = 0;
    _max = 0;
 }
Index: src/motif/spinbutt.cpp
===================================================================
--- src/motif/spinbutt.cpp	(revision 64368)
+++ src/motif/spinbutt.cpp	(working copy)
@@ -180,8 +180,7 @@
         return;
 
     wxArrowButton* btn = (wxArrowButton*)clientData;
-    delete btn->m_timer;
-    btn->m_timer = 0;
+    wxDELETE(btn->m_timer);
 }
 
 bool wxArrowButton::Create( wxSpinButton* parent,
Index: src/motif/toolbar.cpp
===================================================================
--- src/motif/toolbar.cpp	(revision 64368)
+++ src/motif/toolbar.cpp	(working copy)
@@ -244,8 +244,7 @@
 
 wxToolBar::~wxToolBar()
 {
-    delete wxTheToolBarTimer;
-    wxTheToolBarTimer = NULL;
+    wxDELETE(wxTheToolBarTimer);
 }
 
 bool wxToolBar::Realize()
Index: src/msw/bitmap.cpp
===================================================================
--- src/msw/bitmap.cpp	(revision 64368)
+++ src/msw/bitmap.cpp	(working copy)
@@ -238,8 +238,7 @@
         }
     }
 
-    delete m_bitmapMask;
-    m_bitmapMask = NULL;
+    wxDELETE(m_bitmapMask);
 }
 
 // ----------------------------------------------------------------------------
Index: src/msw/cursor.cpp
===================================================================
--- src/msw/cursor.cpp	(revision 64368)
+++ src/msw/cursor.cpp	(working copy)
@@ -105,8 +105,7 @@
 
     virtual void OnExit()
     {
-        delete gs_globalCursor;
-        gs_globalCursor = NULL;
+        wxDELETE(gs_globalCursor);
     }
 };
 
Index: src/msw/dialup.cpp
===================================================================
--- src/msw/dialup.cpp	(revision 64368)
+++ src/msw/dialup.cpp	(working copy)
@@ -597,11 +597,7 @@
         m_hThread = 0;
     }
 
-    if ( m_data )
-    {
-        delete m_data;
-        m_data = NULL;
-    }
+    wxDELETE(m_data);
 }
 
 // ----------------------------------------------------------------------------
Index: src/msw/fdrepdlg.cpp
===================================================================
--- src/msw/fdrepdlg.cpp	(revision 64368)
+++ src/msw/fdrepdlg.cpp	(working copy)
@@ -443,8 +443,7 @@
         wxLogError(_("Failed to create the standard find/replace dialog (error code %d)"),
                    ::CommDlgExtendedError());
 
-        delete m_impl;
-        m_impl = NULL;
+        wxDELETE(m_impl);
 
         return false;
     }
Index: src/msw/listctrl.cpp
===================================================================
--- src/msw/listctrl.cpp	(revision 64368)
+++ src/msw/listctrl.cpp	(working copy)
@@ -497,8 +497,7 @@
     {
         m_textCtrl->UnsubclassWin();
         m_textCtrl->SetHWND(0);
-        delete m_textCtrl;
-        m_textCtrl = NULL;
+        wxDELETE(m_textCtrl);
     }
 }
 
@@ -1539,8 +1538,7 @@
     if ( !hWnd )
     {
         // failed to start editing
-        delete m_textCtrl;
-        m_textCtrl = NULL;
+        wxDELETE(m_textCtrl);
 
         return NULL;
     }
@@ -2235,8 +2233,7 @@
                         if ( m_internalData[n] == data )
                         {
                             m_internalData.erase(m_internalData.begin() + n);
-                            delete data;
-                            data = NULL;
+                            wxDELETE(data);
                             break;
                         }
                     }
Index: src/msw/main.cpp
===================================================================
--- src/msw/main.cpp	(revision 64368)
+++ src/msw/main.cpp	(working copy)
@@ -333,8 +333,7 @@
             free(argv[i]);
         }
 
-        delete [] argv;
-        argv = NULL;
+        wxDELETEA(argv);
         argc = 0;
     }
 
Index: src/msw/mdi.cpp
===================================================================
--- src/msw/mdi.cpp	(revision 64368)
+++ src/msw/mdi.cpp	(working copy)
@@ -369,8 +369,7 @@
     }
 
 #if wxUSE_ACCEL
-    delete m_accelWindowMenu;
-    m_accelWindowMenu = NULL;
+    wxDELETE(m_accelWindowMenu);
 
     if ( menu && menu->HasAccels() )
         m_accelWindowMenu = menu->CreateAccelTable();
Index: src/msw/mediactrl.cpp
===================================================================
--- src/msw/mediactrl.cpp	(revision 64368)
+++ src/msw/mediactrl.cpp	(working copy)
@@ -2193,11 +2193,7 @@
 //---------------------------------------------------------------------------
 void wxAMMediaBackend::Clear()
 {
-    if (m_pTimer)
-    {
-        delete m_pTimer;
-        m_pTimer = NULL;
-    }
+    wxDELETE(m_pTimer);
 }
 
 //---------------------------------------------------------------------------
@@ -3923,11 +3919,7 @@
 {
     m_bPlaying = false;
 
-    if (m_timer)
-    {
-        delete m_timer;
-        m_timer = NULL;
-    }
+    wxDELETE(m_timer);
 
     m_lib.StopMovie(m_movie);
 
Index: src/msw/mediactrl_qt.cpp
===================================================================
--- src/msw/mediactrl_qt.cpp	(revision 64368)
+++ src/msw/mediactrl_qt.cpp	(working copy)
@@ -1068,11 +1068,7 @@
 {
     m_bPlaying = false;
 
-    if (m_timer)
-    {
-        delete m_timer;
-        m_timer = NULL;
-    }
+    wxDELETE(m_timer);
 
     m_lib.StopMovie(m_movie);
 
Index: src/msw/printwin.cpp
===================================================================
--- src/msw/printwin.cpp	(revision 64368)
+++ src/msw/printwin.cpp	(working copy)
@@ -243,8 +243,7 @@
     if (sm_abortWindow)
     {
         sm_abortWindow->Show(false);
-        delete sm_abortWindow;
-        sm_abortWindow = NULL;
+        wxDELETE(sm_abortWindow);
     }
 
     delete dc;
Index: src/msw/region.cpp
===================================================================
--- src/msw/region.cpp	(revision 64368)
+++ src/msw/region.cpp	(working copy)
@@ -384,13 +384,8 @@
     m_current = 0;
     m_region = region;
 
-    if (m_rects)
-    {
-        delete[] m_rects;
+    wxDELETEA(m_rects);
 
-        m_rects = NULL;
-    }
-
     if (m_region.Empty())
         m_numRects = 0;
     else
Index: src/msw/settings.cpp
===================================================================
--- src/msw/settings.cpp	(revision 64368)
+++ src/msw/settings.cpp	(working copy)
@@ -91,8 +91,7 @@
 
 void wxSystemSettingsModule::OnExit()
 {
-    delete gs_fontDefault;
-    gs_fontDefault = NULL;
+    wxDELETE(gs_fontDefault);
 }
 
 // ----------------------------------------------------------------------------
Index: src/msw/sound.cpp
===================================================================
--- src/msw/sound.cpp	(revision 64368)
+++ src/msw/sound.cpp	(working copy)
@@ -144,11 +144,7 @@
 
 void wxSound::Free()
 {
-    if ( m_data )
-    {
-        delete m_data;
-        m_data = NULL;
-    }
+    wxDELETE(m_data);
 }
 
 bool wxSound::CheckCreatedOk()
Index: src/msw/statbmp.cpp
===================================================================
--- src/msw/statbmp.cpp	(revision 64368)
+++ src/msw/statbmp.cpp	(working copy)
@@ -239,9 +239,7 @@
 
 void wxStaticBitmap::Free()
 {
-    delete m_image;
-
-    m_image = NULL;
+    wxDELETE(m_image);
 }
 
 wxSize wxStaticBitmap::DoGetBestSize() const
Index: src/msw/statusbar.cpp
===================================================================
--- src/msw/statusbar.cpp	(revision 64368)
+++ src/msw/statusbar.cpp	(working copy)
@@ -179,11 +179,7 @@
     // delete existing tooltips
     for (size_t i=0; i<m_tooltips.size(); i++)
     {
-        if (m_tooltips[i])
-        {
-            delete m_tooltips[i];
-            m_tooltips[i] = NULL;
-        }
+        wxDELETE(m_tooltips[i]);
     }
 
     wxDELETE(m_pDC);
@@ -212,11 +208,7 @@
     // reset all current tooltips
     for (size_t i=0; i<m_tooltips.size(); i++)
     {
-        if (m_tooltips[i])
-        {
-            delete m_tooltips[i];
-            m_tooltips[i] = NULL;
-        }
+        wxDELETE(m_tooltips[i]);
     }
 
     // shrink/expand the array:
@@ -354,8 +346,7 @@
             else
             {
                 // delete the tooltip associated with this pane; it's not needed anymore
-                delete m_tooltips[nField];
-                m_tooltips[nField] = NULL;
+                wxDELETE(m_tooltips[nField]);
             }
         }
         else
Index: src/msw/thread.cpp
===================================================================
--- src/msw/thread.cpp	(revision 64368)
+++ src/msw/thread.cpp	(working copy)
@@ -1285,18 +1285,15 @@
         wxLogLastError(wxT("TlsFree failed."));
     }
 
-    delete gs_critsectThreadDelete;
-    gs_critsectThreadDelete = NULL;
+    wxDELETE(gs_critsectThreadDelete);
 
     if ( gs_critsectGui )
     {
         gs_critsectGui->Leave();
-        delete gs_critsectGui;
-        gs_critsectGui = NULL;
+        wxDELETE(gs_critsectGui);
     }
 
-    delete gs_critsectWaitingForGui;
-    gs_critsectWaitingForGui = NULL;
+    wxDELETE(gs_critsectWaitingForGui);
 }
 
 // ----------------------------------------------------------------------------
Index: src/msw/toolbar.cpp
===================================================================
--- src/msw/toolbar.cpp	(revision 64368)
+++ src/msw/toolbar.cpp	(working copy)
@@ -405,11 +405,7 @@
         m_hBitmap = 0;
     }
 
-    if ( m_disabledImgList )
-    {
-        delete m_disabledImgList;
-        m_disabledImgList = NULL;
-    }
+    wxDELETE(m_disabledImgList);
 
     Realize();
 }
@@ -610,11 +606,7 @@
 
 void wxToolBar::CreateDisabledImageList()
 {
-    if (m_disabledImgList != NULL)
-    {
-        delete m_disabledImgList;
-        m_disabledImgList = NULL;
-    }
+    wxDELETE(m_disabledImgList);
 
     // as we can't use disabled image list with older versions of comctl32.dll,
     // don't even bother creating it
Index: src/msw/treectrl.cpp
===================================================================
--- src/msw/treectrl.cpp	(revision 64368)
+++ src/msw/treectrl.cpp	(working copy)
@@ -2029,8 +2029,7 @@
 
         m_textCtrl->UnsubclassWin();
         m_textCtrl->SetHWND(0);
-        delete m_textCtrl;
-        m_textCtrl = NULL;
+        wxDELETE(m_textCtrl);
 
         m_idEdited.Unset();
     }
@@ -2051,8 +2050,7 @@
     // returned false
     if ( !hWnd )
     {
-        delete m_textCtrl;
-        m_textCtrl = NULL;
+        wxDELETE(m_textCtrl);
         return NULL;
     }
 
@@ -3134,8 +3132,7 @@
                 if ( m_dragImage )
                 {
                     m_dragImage->EndDrag();
-                    delete m_dragImage;
-                    m_dragImage = NULL;
+                    wxDELETE(m_dragImage);
 
                     // generate the drag end event
                     wxTreeEvent event(wxEVT_COMMAND_TREE_END_DRAG,
@@ -3275,8 +3272,7 @@
             if ( m_dragImage )
             {
                 m_dragImage->EndDrag();
-                delete m_dragImage;
-                m_dragImage = NULL;
+                wxDELETE(m_dragImage);
 
                 // if we don't do it, the tree seems to think that 2 items
                 // are selected simultaneously which is quite weird
Index: src/msw/wince/menuce.cpp
===================================================================
--- src/msw/wince/menuce.cpp	(revision 64368)
+++ src/msw/wince/menuce.cpp	(working copy)
@@ -54,11 +54,7 @@
 
 wxTopLevelWindowMSW::ButtonMenu::~ButtonMenu()
 {
-    if(m_menu)
-    {
-        delete m_menu;
-        m_menu = NULL;
-    };
+    wxDELETE(m_menu);
 }
 
 void wxTopLevelWindowMSW::SetLeftMenu(int id, const wxString& label, wxMenu *subMenu)
Index: src/msw/window.cpp
===================================================================
--- src/msw/window.cpp	(revision 64368)
+++ src/msw/window.cpp	(working copy)
@@ -3983,8 +3983,7 @@
     {
         m_dropTarget->Revoke(m_hWnd);
 
-        delete m_dropTarget;
-        m_dropTarget = NULL;
+        wxDELETE(m_dropTarget);
     }
 #endif // wxUSE_DRAG_AND_DROP
 
Index: src/os2/app.cpp
===================================================================
--- src/os2/app.cpp	(revision 64368)
+++ src/os2/app.cpp	(working copy)
@@ -386,8 +386,7 @@
 //
 void wxApp::CleanUp()
 {
-    delete[] wxBuffer;
-    wxBuffer = NULL;
+    wxDELETEA(wxBuffer);
 
     //
     // PM-SPECIFIC CLEANUP
@@ -414,8 +413,7 @@
 // TODO:        ::DeleteObject( wxDisableButtonBrush );
     }
 
-    delete wxWinHandleHash;
-    wxWinHandleHash = NULL;
+    wxDELETE(wxWinHandleHash);
 
     // Delete Message queue
     if (wxTheApp->m_hMq)
Index: src/os2/bitmap.cpp
===================================================================
--- src/os2/bitmap.cpp	(revision 64368)
+++ src/os2/bitmap.cpp	(working copy)
@@ -84,11 +84,7 @@
             wxLogLastError(wxT("GpiDeleteBitmap(hbitmap)"));
         }
     }
-    if (m_pBitmapMask)
-    {
-        delete m_pBitmapMask;
-        m_pBitmapMask = NULL;
-    }
+    wxDELETE(m_pBitmapMask);
 } // end of wxBitmapRefData::Free
 
 // ----------------------------------------------------------------------------
Index: src/os2/dialog.cpp
===================================================================
--- src/os2/dialog.cpp	(revision 64368)
+++ src/os2/dialog.cpp	(working copy)
@@ -180,11 +180,7 @@
         // and we will lose activation
         m_modalData->ExitLoop();
 #if 0
-        if (m_pWindowDisabler)
-        {
-            delete m_pWindowDisabler;
-            m_pWindowDisabler = NULL;
-        }
+        wxDELETE(m_pWindowDisabler);
 #endif
     }
 
Index: src/os2/listctrl.cpp
===================================================================
--- src/os2/listctrl.cpp	(revision 64368)
+++ src/os2/listctrl.cpp	(working copy)
@@ -140,8 +140,7 @@
 
     ~CListItemInternalData()
     {
-        delete m_pAttr;
-        m_pAttr = NULL;
+        wxDELETE(m_pAttr);
     }
 
     wxListItemAttr*                 m_pAttr;
@@ -965,8 +964,7 @@
     {
         m_pTextCtrl->SetHWND(0);
         m_pTextCtrl->UnsubclassWin();
-        delete m_pTextCtrl;
-        m_pTextCtrl = NULL;
+        wxDELETE(m_pTextCtrl);
     }
 
     if (m_bOwnsImageListNormal)
Index: src/os2/settings.cpp
===================================================================
--- src/os2/settings.cpp	(revision 64368)
+++ src/os2/settings.cpp	(working copy)
@@ -70,8 +70,7 @@
 {
     sm_optionNames.Clear();
     sm_optionValues.Clear();
-    delete gs_fontDefault;
-    gs_fontDefault = NULL;
+    wxDELETE(gs_fontDefault);
 }
 
 wxColour wxSystemSettingsNative::GetColour(
Index: src/os2/statbmp.cpp
===================================================================
--- src/os2/statbmp.cpp	(revision 64368)
+++ src/os2/statbmp.cpp	(working copy)
@@ -141,9 +141,7 @@
 
 void wxStaticBitmap::Free()
 {
-    if (m_pImage)
-        delete m_pImage;
-    m_pImage = NULL;
+    wxDELETE(m_pImage);
 } // end of wxStaticBitmap::Free
 
 wxSize wxStaticBitmap::DoGetBestSize() const
Index: src/os2/toolbar.cpp
===================================================================
--- src/os2/toolbar.cpp	(revision 64368)
+++ src/os2/toolbar.cpp	(working copy)
@@ -423,11 +423,7 @@
 
 wxToolBar::~wxToolBar()
 {
-    if (m_pToolTip)
-    {
-        delete m_pToolTip;
-        m_pToolTip = NULL;
-    }
+    wxDELETE(m_pToolTip);
 } // end of wxToolBar::~wxToolBar
 
 bool wxToolBar::Realize()
Index: src/os2/treectrl.cpp
===================================================================
--- src/os2/treectrl.cpp	(revision 64368)
+++ src/os2/treectrl.cpp	(working copy)
@@ -80,11 +80,7 @@
     wxTreeItemInternalData() {}
     ~wxTreeItemInternalData()
     {
-        if(m_pAttr)
-        {
-            delete m_pAttr;
-            m_pAttr = NULL;
-        }
+        wxDELETE(m_pAttr);
     }
 
     wxTreeItemAttr*                 m_pAttr;
Index: src/os2/window.cpp
===================================================================
--- src/os2/window.cpp	(revision 64368)
+++ src/os2/window.cpp	(working copy)
@@ -3038,11 +3038,7 @@
     // Delete our drop target if we've got one
     //
 #if wxUSE_DRAG_AND_DROP
-    if (m_dropTarget != NULL)
-    {
-        delete m_dropTarget;
-        m_dropTarget = NULL;
-    }
+    wxDELETE(m_dropTarget);
 #endif // wxUSE_DRAG_AND_DROP
 
     //
Index: src/osx/carbon/clipbrd.cpp
===================================================================
--- src/osx/carbon/clipbrd.cpp	(revision 64368)
+++ src/osx/carbon/clipbrd.cpp	(working copy)
@@ -61,11 +61,7 @@
 
 void wxClipboard::Clear()
 {
-    if (m_data != NULL)
-    {
-        delete m_data;
-        m_data = NULL;
-    }
+    wxDELETE(m_data);
 
     OSStatus err = PasteboardClear( m_pasteboard );
     if (err != noErr)
@@ -139,11 +135,7 @@
     // Get rid of cached object.
     // If this is not done, copying data from
     // another application will only work once
-    if (m_data)
-    {
-        delete m_data;
-        m_data = NULL;
-    }
+    wxDELETE(m_data);
 }
 
 bool wxClipboard::IsSupported( const wxDataFormat &dataFormat )
Index: src/osx/carbon/combobox.cpp
===================================================================
--- src/osx/carbon/combobox.cpp	(revision 64368)
+++ src/osx/carbon/combobox.cpp	(working copy)
@@ -230,17 +230,8 @@
     // delete the controls now, don't leave them alive even though they would
     // still be eventually deleted by our parent - but it will be too late, the
     // user code expects them to be gone now
-    if (m_text != NULL)
-    {
-        delete m_text;
-        m_text = NULL;
-    }
-
-    if (m_choice != NULL)
-    {
-        delete m_choice;
-        m_choice = NULL;
-    }
+    wxDELETE(m_text);
+    wxDELETE(m_choice);
 }
 
 // ----------------------------------------------------------------------------
@@ -471,10 +462,9 @@
     {
         m_text = new wxComboBoxText( this );
     }
-    else if ( ( m_text != NULL ) && !editable )
+    else if ( !editable )
     {
-        delete m_text;
-        m_text = NULL;
+        wxDELETE(m_text);
     }
 
     int currentX, currentY;
Index: src/osx/carbon/combobxc.cpp
===================================================================
--- src/osx/carbon/combobxc.cpp	(revision 64368)
+++ src/osx/carbon/combobxc.cpp	(working copy)
@@ -478,10 +478,9 @@
     {
         m_text = new wxComboBoxText( this );
     }
-    else if ( ( m_text != NULL ) && !editable )
+    else if ( !editable )
     {
-        delete m_text;
-        m_text = NULL;
+        wxDELETE(m_text);
     }
 
     int currentX, currentY;
Index: src/osx/carbon/dataview.cpp
===================================================================
--- src/osx/carbon/dataview.cpp	(revision 64368)
+++ src/osx/carbon/dataview.cpp	(working copy)
@@ -2092,8 +2092,7 @@
                   dataObject->Add(textDataObject);
                 else
                 {
-                  delete textDataObject;
-                  textDataObject = NULL;
+                  wxDELETE(textDataObject);
                 }
               }
             } /* block */
@@ -2124,8 +2123,7 @@
                       dataObject->Add(textDataObject);
                     else
                     {
-                      delete textDataObject;
-                      textDataObject = NULL;
+                      wxDELETE(textDataObject);
                     }
                   }
                   else // overwrite data because the 'utxt' flavor has priority over the 'TEXT' flavor
Index: src/osx/carbon/dcscreen.cpp
===================================================================
--- src/osx/carbon/dcscreen.cpp	(revision 64368)
+++ src/osx/carbon/dcscreen.cpp	(working copy)
@@ -56,8 +56,7 @@
 
 wxScreenDCImpl::~wxScreenDCImpl()
 {
-    delete m_graphicContext;
-    m_graphicContext = NULL;
+    wxDELETE(m_graphicContext);
 #if wxOSX_USE_COCOA_OR_IPHONE
 #else
     DisposeWindow((WindowRef) m_overlayWindow );
Index: src/osx/carbon/dirmac.cpp
===================================================================
--- src/osx/carbon/dirmac.cpp	(revision 64368)
+++ src/osx/carbon/dirmac.cpp	(working copy)
@@ -237,8 +237,7 @@
 
 wxDir::~wxDir()
 {
-    delete m_data;
-    m_data = NULL;
+    wxDELETE(m_data);
 }
 
 // ----------------------------------------------------------------------------
Index: src/osx/carbon/mediactrl.cpp
===================================================================
--- src/osx/carbon/mediactrl.cpp	(revision 64368)
+++ src/osx/carbon/mediactrl.cpp	(working copy)
@@ -875,11 +875,7 @@
 void wxQTMediaBackend::Cleanup()
 {
     m_bPlaying = false;
-    if (m_timer)
-    {
-        delete m_timer;
-        m_timer = NULL;
-    }
+    wxDELETE(m_timer);
 
     // Stop the movie:
     // Apple samples with CreateMovieControl typically
Index: src/osx/carbon/metafile.cpp
===================================================================
--- src/osx/carbon/metafile.cpp	(revision 64368)
+++ src/osx/carbon/metafile.cpp	(working copy)
@@ -318,8 +318,7 @@
 
 wxMetaFile *wxMetafileDCImpl::Close()
 {
-    delete m_graphicContext;
-    m_graphicContext = NULL;
+    wxDELETE(m_graphicContext);
     m_ok = false;
 
     M_METAFILEREFDATA(*m_metaFile)->Close();
Index: src/osx/carbon/region.cpp
===================================================================
--- src/osx/carbon/region.cpp	(revision 64368)
+++ src/osx/carbon/region.cpp	(working copy)
@@ -346,11 +346,7 @@
 
 wxRegionIterator::~wxRegionIterator()
 {
-    if (m_rects)
-    {
-        delete [] m_rects;
-        m_rects = NULL;
-    }
+    wxDELETEA(m_rects);
 }
 
 wxRegionIterator::wxRegionIterator(const wxRegionIterator& iterator)
@@ -375,11 +371,7 @@
  */
 void wxRegionIterator::SetRects(long numRects, wxRect *rects)
 {
-    if (m_rects)
-    {
-        delete [] m_rects;
-        m_rects = NULL;
-    }
+    wxDELETEA(m_rects);
 
     if (rects && (numRects > 0))
     {
@@ -483,11 +475,7 @@
     m_current = 0;
     m_region = region;
 
-    if (m_rects)
-    {
-        delete [] m_rects;
-        m_rects = NULL;
-    }
+    wxDELETEA(m_rects);
 
     if (m_region.IsEmpty())
     {
Index: src/osx/carbon/taskbar.cpp
===================================================================
--- src/osx/carbon/taskbar.cpp	(revision 64368)
+++ src/osx/carbon/taskbar.cpp	(working copy)
@@ -409,11 +409,7 @@
 //-----------------------------------------------------------------------------
 bool wxDockTaskBarIcon::RemoveIcon()
 {
-    if (m_pMenu)
-    {
-        delete m_pMenu;
-        m_pMenu = NULL;
-    }
+    wxDELETE(m_pMenu);
 
     // restore old icon to the dock
     OSStatus err = RestoreApplicationDockTileImage();
Index: src/osx/carbon/thread.cpp
===================================================================
--- src/osx/carbon/thread.cpp	(revision 64368)
+++ src/osx/carbon/thread.cpp	(working copy)
@@ -1229,12 +1229,10 @@
         }
 
         gs_critsectGui->Leave();
-        delete gs_critsectGui;
-        gs_critsectGui = NULL;
+        wxDELETE(gs_critsectGui);
     }
 
-    delete gs_critsectWaitingForGui;
-    gs_critsectWaitingForGui = NULL;
+    wxDELETE(gs_critsectWaitingForGui);
 }
 
 // ----------------------------------------------------------------------------
Index: src/osx/carbon/tooltip.cpp
===================================================================
--- src/osx/carbon/tooltip.cpp	(revision 64368)
+++ src/osx/carbon/tooltip.cpp	(working copy)
@@ -199,11 +199,7 @@
 wxMacToolTip::~wxMacToolTip()
 {
 #if wxUSE_TIMER
-    if ( m_timer )
-    {
-        delete m_timer ;
-        m_timer = NULL;
-    }
+    wxDELETE(m_timer);
 #endif // wxUSE_TIMER
     if ( m_backpict )
         Clear() ;
@@ -251,11 +247,7 @@
 {
     m_mark++ ;
 #if wxUSE_TIMER
-    if ( m_timer )
-    {
-        delete m_timer ;
-        m_timer = NULL ;
-    }
+    wxDELETE(m_timer);
 #endif // wxUSE_TIMER
     if ( !m_shown )
         return ;
Index: src/osx/core/bitmap.cpp
===================================================================
--- src/osx/core/bitmap.cpp	(revision 64368)
+++ src/osx/core/bitmap.cpp	(working copy)
@@ -782,11 +782,7 @@
         m_hBitmap = NULL ;
     }
 
-    if (m_bitmapMask)
-    {
-        delete m_bitmapMask;
-        m_bitmapMask = NULL;
-    }
+    wxDELETE(m_bitmapMask);
 }
 
 wxBitmapRefData::~wxBitmapRefData()
Index: src/osx/core/dcmemory.cpp
===================================================================
--- src/osx/core/dcmemory.cpp	(revision 64368)
+++ src/osx/core/dcmemory.cpp	(working copy)
@@ -58,8 +58,7 @@
     if ( m_selected.Ok() )
     {
         m_selected.EndRawAccess() ;
-        delete m_graphicContext ;
-        m_graphicContext = NULL ;
+        wxDELETE(m_graphicContext);
     }
 }
 
@@ -68,8 +67,7 @@
     if ( m_selected.Ok() )
     {
         m_selected.EndRawAccess() ;
-        delete m_graphicContext ;
-        m_graphicContext = NULL ;
+        wxDELETE(m_graphicContext);
     }
 
     m_selected = bitmap;
Index: src/osx/core/hidjoystick.cpp
===================================================================
--- src/osx/core/hidjoystick.cpp	(revision 64368)
+++ src/osx/core/hidjoystick.cpp	(working copy)
@@ -157,8 +157,7 @@
     }
     else
     {
-        delete m_hid;
-        m_hid = NULL;
+        wxDELETE(m_hid);
     }
 }
 
Index: src/osx/core/printmac.cpp
===================================================================
--- src/osx/core/printmac.cpp	(revision 64368)
+++ src/osx/core/printmac.cpp	(working copy)
@@ -718,8 +718,7 @@
     if (sm_abortWindow)
     {
         sm_abortWindow->Show(false);
-        delete sm_abortWindow;
-        sm_abortWindow = NULL;
+        wxDELETE(sm_abortWindow);
     }
 
     wxEndBusyCursor();
Index: src/osx/listbox_osx.cpp
===================================================================
--- src/osx/listbox_osx.cpp	(revision 64368)
+++ src/osx/listbox_osx.cpp	(working copy)
@@ -108,8 +108,7 @@
     m_blockEvents = false;
 
     // make sure no native events get sent to a object in destruction
-    delete m_peer;
-    m_peer = NULL;
+    wxDELETE(m_peer);
 
     if ( IsSorted() )
         delete m_strings.sorted;
Index: src/osx/nonownedwnd_osx.cpp
===================================================================
--- src/osx/nonownedwnd_osx.cpp	(revision 64368)
+++ src/osx/nonownedwnd_osx.cpp	(working copy)
@@ -167,8 +167,7 @@
 
     DestroyChildren();
 
-    delete m_nowpeer;
-    m_nowpeer = NULL;
+    wxDELETE(m_nowpeer);
 
     // avoid dangling refs
     if ( s_macDeactivateWindow == this )
Index: src/osx/printdlg_osx.cpp
===================================================================
--- src/osx/printdlg_osx.cpp	(revision 64368)
+++ src/osx/printdlg_osx.cpp	(working copy)
@@ -67,10 +67,9 @@
 
 wxMacPrintDialog::~wxMacPrintDialog()
 {
-    if (m_destroyDC && m_printerDC)
+    if (m_destroyDC)
     {
-        delete m_printerDC;
-        m_printerDC = NULL;
+        wxDELETE(m_printerDC);
     }
 }
 
Index: src/osx/sound_osx.cpp
===================================================================
--- src/osx/sound_osx.cpp	(revision 64368)
+++ src/osx/sound_osx.cpp	(working copy)
@@ -73,11 +73,7 @@
 void wxSoundData::Stop()
 {
     DoStop();
-    if ( m_pTimer )
-    {
-        delete m_pTimer;
-        m_pTimer = NULL;
-    }
+    wxDELETE(m_pTimer);
 }
 
 //Time between timer calls
Index: src/osx/spinctrl_osx.cpp
===================================================================
--- src/osx/spinctrl_osx.cpp	(revision 64368)
+++ src/osx/spinctrl_osx.cpp	(working copy)
@@ -281,10 +281,8 @@
     // delete the controls now, don't leave them alive even though they would
     // still be eventually deleted by our parent - but it will be too late, the
     // user code expects them to be gone now
-    delete m_text;
-    m_text = NULL ;
-    delete m_btn;
-    m_btn = NULL ;
+    wxDELETE(m_text);
+    wxDELETE(m_btn);
 }
 
 // ----------------------------------------------------------------------------
Index: src/osx/window_osx.cpp
===================================================================
--- src/osx/window_osx.cpp	(revision 64368)
+++ src/osx/window_osx.cpp	(working copy)
@@ -257,11 +257,7 @@
 
     // delete our drop target if we've got one
 #if wxUSE_DRAG_AND_DROP
-    if ( m_dropTarget != NULL )
-    {
-        delete m_dropTarget;
-        m_dropTarget = NULL;
-    }
+    wxDELETE(m_dropTarget);
 #endif
 
     delete m_peer ;
Index: src/palmos/app.cpp
===================================================================
--- src/palmos/app.cpp	(revision 64368)
+++ src/palmos/app.cpp	(working copy)
@@ -209,8 +209,7 @@
     // unregister the classes now
     UnregisterWindowClasses();
 
-    delete wxWinHandleHash;
-    wxWinHandleHash = NULL;
+    wxDELETE(wxWinHandleHash);
 }
 
 // ----------------------------------------------------------------------------
Index: src/palmos/ownerdrw.cpp
===================================================================
--- src/palmos/ownerdrw.cpp	(revision 64368)
+++ src/palmos/ownerdrw.cpp	(working copy)
@@ -45,8 +45,7 @@
 
     virtual void OnExit()
     {
-        delete ms_systemMenuFont;
-        ms_systemMenuFont = NULL;
+        wxDELETE(ms_systemMenuFont);
     }
 
     static wxFont* ms_systemMenuFont;
Index: src/propgrid/advprops.cpp
===================================================================
--- src/propgrid/advprops.cpp	(revision 64368)
+++ src/propgrid/advprops.cpp	(working copy)
@@ -1818,16 +1818,8 @@
     wxFileProperty::OnSetValue();
 
     // Delete old image
-    if ( m_pImage )
-    {
-        delete m_pImage;
-        m_pImage = NULL;
-    }
-    if ( m_pBitmap )
-    {
-        delete m_pBitmap;
-        m_pBitmap = NULL;
-    }
+    wxDELETE(m_pImage);
+    wxDELETE(m_pBitmap);
 
     wxFileName filename = GetFileName();
 
@@ -1856,8 +1848,7 @@
         {
             m_pImage->Rescale( rect.width, rect.height );
             m_pBitmap = new wxBitmap( *m_pImage );
-            delete m_pImage;
-            m_pImage = NULL;
+            wxDELETE(m_pImage);
         }
 
         dc.DrawBitmap( *m_pBitmap, rect.x, rect.y, false );
Index: src/propgrid/editors.cpp
===================================================================
--- src/propgrid/editors.cpp	(revision 64368)
+++ src/propgrid/editors.cpp	(working copy)
@@ -1508,8 +1508,7 @@
 
 wxSimpleCheckBox::~wxSimpleCheckBox()
 {
-    delete ms_doubleBuffer;
-    ms_doubleBuffer = NULL;
+    wxDELETE(ms_doubleBuffer);
 }
 
 wxBitmap* wxSimpleCheckBox::ms_doubleBuffer = NULL;
Index: src/propgrid/manager.cpp
===================================================================
--- src/propgrid/manager.cpp	(revision 64368)
+++ src/propgrid/manager.cpp	(working copy)
@@ -623,8 +623,7 @@
     END_MOUSE_CAPTURE
 
     //m_pPropGrid->ClearSelection();
-    delete m_pPropGrid;
-    m_pPropGrid = NULL;
+    wxDELETE(m_pPropGrid);
 
     size_t i;
     for ( i=0; i<m_arrPages.size(); i++ )
Index: src/propgrid/propgrid.cpp
===================================================================
--- src/propgrid/propgrid.cpp	(revision 64368)
+++ src/propgrid/propgrid.cpp	(working copy)
@@ -147,7 +147,7 @@
 public:
     wxPGGlobalVarsClassManager() {}
     virtual bool OnInit() { wxPGGlobalVars = new wxPGGlobalVarsClass(); return true; }
-    virtual void OnExit() { delete wxPGGlobalVars; wxPGGlobalVars = NULL; }
+    virtual void OnExit() { wxDELETE(wxPGGlobalVars); }
 };
 
 IMPLEMENT_DYNAMIC_CLASS(wxPGGlobalVarsClassManager, wxModule)
@@ -1123,8 +1123,7 @@
         else
         {
         #if wxPG_DOUBLE_BUFFER
-            delete m_doubleBuffer;
-            m_doubleBuffer = NULL;
+            wxDELETE(m_doubleBuffer);
         #endif
         }
     }
Index: src/richtext/richtextbuffer.cpp
===================================================================
--- src/richtext/richtextbuffer.cpp	(revision 64368)
+++ src/richtext/richtextbuffer.cpp	(working copy)
@@ -7423,11 +7423,7 @@
 
 wxRichTextImageBlock::~wxRichTextImageBlock()
 {
-    if (m_data)
-    {
-        delete[] m_data;
-        m_data = NULL;
-    }
+    wxDELETEA(m_data);
 }
 
 void wxRichTextImageBlock::Init()
@@ -7439,8 +7435,7 @@
 
 void wxRichTextImageBlock::Clear()
 {
-    delete[] m_data;
-    m_data = NULL;
+    wxDELETEA(m_data);
     m_dataSize = 0;
     m_imageType = wxBITMAP_TYPE_INVALID;
 }
@@ -7539,11 +7534,7 @@
 void wxRichTextImageBlock::Copy(const wxRichTextImageBlock& block)
 {
     m_imageType = block.m_imageType;
-    if (m_data)
-    {
-        delete[] m_data;
-        m_data = NULL;
-    }
+    wxDELETEA(m_data);
     m_dataSize = block.m_dataSize;
     if (m_dataSize == 0)
         return;
@@ -7790,8 +7781,7 @@
 
 bool wxRichTextBufferDataObject::SetData(size_t WXUNUSED(len), const void *buf)
 {
-    delete m_richTextBuffer;
-    m_richTextBuffer = NULL;
+    wxDELETE(m_richTextBuffer);
 
     wxString bufXML((const char*) buf, wxConvUTF8);
 
@@ -7802,8 +7792,7 @@
     {
         wxLogError(wxT("Could not read the buffer from an XML stream.\nYou may have forgotten to add the XML file handler."));
 
-        delete m_richTextBuffer;
-        m_richTextBuffer = NULL;
+        wxDELETE(m_richTextBuffer);
 
         return false;
     }
Index: src/richtext/richtexthtml.cpp
===================================================================
--- src/richtext/richtexthtml.cpp	(revision 64368)
+++ src/richtext/richtexthtml.cpp	(working copy)
@@ -85,8 +85,7 @@
         customEncoding = new wxCSConv(GetEncoding());
         if (!customEncoding->IsOk())
         {
-            delete customEncoding;
-            customEncoding = NULL;
+            wxDELETE(customEncoding);
         }
     }
     if (customEncoding)
Index: src/stc/PlatWX.cpp
===================================================================
--- src/stc/PlatWX.cpp	(revision 64368)
+++ src/stc/PlatWX.cpp	(working copy)
@@ -86,8 +86,7 @@
 
 Palette::~Palette() {
     Release();
-    delete [] entries;
-    entries = 0;
+    wxDELETEA(entries);
 }
 
 void Palette::Release() {
@@ -1144,14 +1143,8 @@
 }
 
 ListBoxImpl::~ListBoxImpl() {
-    if (imgList) {
-        delete imgList;
-        imgList = NULL;
-    }
-    if (imgTypeMap) {
-        delete imgTypeMap;
-        imgTypeMap = NULL;
-    }
+    wxDELETE(imgList);
+    wxDELETE(imgTypeMap);
 }
 
 
@@ -1329,14 +1322,8 @@
 }
 
 void ListBoxImpl::ClearRegisteredImages() {
-    if (imgList) {
-        delete imgList;
-        imgList = NULL;
-    }
-    if (imgTypeMap) {
-        delete imgTypeMap;
-        imgTypeMap = NULL;
-    }
+    wxDELETE(imgList);
+    wxDELETE(imgTypeMap);
     if (wid)
         GETLB(wid)->SetImageList(NULL, wxIMAGE_LIST_SMALL);
 }
Index: src/univ/dialog.cpp
===================================================================
--- src/univ/dialog.cpp	(revision 64368)
+++ src/univ/dialog.cpp	(working copy)
@@ -143,11 +143,7 @@
         // if we had disabled other app windows, reenable them back now because
         // if they stay disabled Windows will activate another window (one
         // which is enabled, anyhow) and we will lose activation
-        if ( m_windowDisabler )
-        {
-            delete m_windowDisabler;
-            m_windowDisabler = NULL;
-        }
+        wxDELETE(m_windowDisabler);
 
         if ( IsModal() )
             EndModal(wxID_CANCEL);
Index: src/univ/menu.cpp
===================================================================
--- src/univ/menu.cpp	(revision 64368)
+++ src/univ/menu.cpp	(working copy)
@@ -1096,11 +1096,7 @@
 
 void wxMenu::InvalidateGeometryInfo()
 {
-    if ( m_geometry )
-    {
-        delete m_geometry;
-        m_geometry = NULL;
-    }
+    wxDELETE(m_geometry);
 }
 
 // ----------------------------------------------------------------------------
@@ -2513,8 +2509,7 @@
     ms_evtLoopPopup = new wxEventLoop;
     ms_evtLoopPopup->Run();
 
-    delete ms_evtLoopPopup;
-    ms_evtLoopPopup = NULL;
+    wxDELETE(ms_evtLoopPopup);
 
     // remove the handler
     PopEventHandler(true /* delete it */);
Index: src/univ/scrolbar.cpp
===================================================================
--- src/univ/scrolbar.cpp	(revision 64368)
+++ src/univ/scrolbar.cpp	(working copy)
@@ -933,11 +933,7 @@
 
     m_btnCapture = -1;
 
-    if ( m_timerScroll )
-    {
-        delete m_timerScroll;
-        m_timerScroll = NULL;
-    }
+    wxDELETE(m_timerScroll);
 
     // unpress the arrow and highlight the current element
     Press(control, false);
Index: src/unix/dialup.cpp
===================================================================
--- src/unix/dialup.cpp	(revision 64368)
+++ src/unix/dialup.cpp	(working copy)
@@ -336,8 +336,7 @@
         m_DialPId = (int)wxExecute(cmd, false, m_DialProcess);
         if(m_DialPId == 0)
         {
-            delete m_DialProcess;
-            m_DialProcess = NULL;
+            wxDELETE(m_DialProcess);
             return false;
         }
         else
@@ -379,8 +378,7 @@
    bool rc = m_timer->Start(nSeconds*1000);
    if(! rc)
    {
-      delete m_timer;
-      m_timer = NULL;
+      wxDELETE(m_timer);
    }
    return rc;
 }
@@ -390,8 +388,7 @@
    if(m_timer != NULL)
    {
       m_timer->Stop();
-      delete m_timer;
-      m_timer = NULL;
+      wxDELETE(m_timer);
    }
 }
 
Index: src/unix/fontutil.cpp
===================================================================
--- src/unix/fontutil.cpp	(revision 64368)
+++ src/unix/fontutil.cpp	(working copy)
@@ -1465,9 +1465,7 @@
 
 void wxFontModule::OnExit()
 {
-    delete g_fontHash;
-
-    g_fontHash = NULL;
+    wxDELETE(g_fontHash);
 }
 
 #endif // GTK 2.0/1.x
Index: src/unix/fswatcher_inotify.cpp
===================================================================
--- src/unix/fswatcher_inotify.cpp	(revision 64368)
+++ src/unix/fswatcher_inotify.cpp	(working copy)
@@ -91,8 +91,7 @@
         wxCHECK_RET( IsOk(),
                     "Inotify not initialized or invalid inotify descriptor" );
 
-        delete m_source;
-        m_source = NULL;
+        wxDELETE(m_source);
 
         if ( close(m_ifd) != 0 )
         {
Index: src/unix/fswatcher_kqueue.cpp
===================================================================
--- src/unix/fswatcher_kqueue.cpp	(revision 64368)
+++ src/unix/fswatcher_kqueue.cpp	(working copy)
@@ -115,8 +115,7 @@
             wxLogSysError(_("Error closing kqueue instance"));
         }
 
-        delete m_source;
-        m_source = NULL;
+        wxDELETE(m_source);
     }
 
     virtual bool DoAdd(wxSharedPtr<wxFSWatchEntryKq> watch)
@@ -419,11 +418,7 @@
 {
     if (!Init())
     {
-        if (m_service)
-        {
-            delete m_service;
-            m_service = NULL;
-        }
+        wxDELETE(m_service);
         return;
     }
 
Index: src/unix/net.cpp
===================================================================
--- src/unix/net.cpp	(revision 64368)
+++ src/unix/net.cpp	(working copy)
@@ -236,8 +236,7 @@
    bool rc = m_timer->Start(nSeconds*1000);
    if(! rc)
    {
-      delete m_timer;
-      m_timer = NULL;
+      wxDELETE(m_timer);
    }
    return rc;
 }
@@ -247,8 +246,7 @@
 {
    wxASSERT(m_timer != NULL);
    m_timer->Stop();
-   delete m_timer;
-   m_timer = NULL;
+   wxDELETE(m_timer);
 }
 
 
Index: src/unix/sound.cpp
===================================================================
--- src/unix/sound.cpp	(revision 64368)
+++ src/unix/sound.cpp	(working copy)
@@ -570,8 +570,7 @@
 
         Stop();
 
-        delete ms_backend;
-        ms_backend = NULL;
+        wxDELETE(ms_backend);
 #if wxUSE_LIBSDL && wxUSE_PLUGINS
         delete ms_backendSDL;
 #endif
Index: src/x11/app.cpp
===================================================================
--- src/x11/app.cpp	(revision 64368)
+++ src/x11/app.cpp	(working copy)
@@ -195,10 +195,8 @@
 
 void wxApp::CleanUp()
 {
-    delete wxWidgetHashTable;
-    wxWidgetHashTable = NULL;
-    delete wxClientWidgetHashTable;
-    wxClientWidgetHashTable = NULL;
+    wxDELETE(wxWidgetHashTable);
+    wxDELETE(wxClientWidgetHashTable);
 
     wxAppBase::CleanUp();
 }
Index: src/x11/bitmap.cpp
===================================================================
--- src/x11/bitmap.cpp	(revision 64368)
+++ src/x11/bitmap.cpp	(working copy)
@@ -1072,8 +1072,7 @@
                 wxT("cannot set palette for bitmap of this depth"));
 
     AllocExclusive();
-    delete M_BMPDATA->m_palette;
-    M_BMPDATA->m_palette = NULL;
+    wxDELETE(M_BMPDATA->m_palette);
 
     if (!palette.Ok()) return;
 
Index: src/x11/clipbrd.cpp
===================================================================
--- src/x11/clipbrd.cpp	(revision 64368)
+++ src/x11/clipbrd.cpp	(working copy)
@@ -215,8 +215,7 @@
         {
             wxLogTrace(TRACE_CLIPBOARD, wxT("wxClipboard will get cleared" ));
 
-            delete wxTheClipboard->m_data;
-            wxTheClipboard->m_data = NULL;
+            wxDELETE(wxTheClipboard->m_data);
         }
     }
 
@@ -348,11 +347,7 @@
         }
 #endif
 
-        if (m_data)
-        {
-            delete m_data;
-            m_data = NULL;
-        }
+        wxDELETE(m_data);
 
 #if wxUSE_THREADS
         /* re-enable GUI threads */
Index: src/x11/evtloop.cpp
===================================================================
--- src/x11/evtloop.cpp	(revision 64368)
+++ src/x11/evtloop.cpp	(working copy)
@@ -162,8 +162,7 @@
     OnExit();
 
     int exitcode = m_impl->GetExitCode();
-    delete m_impl;
-    m_impl = NULL;
+    wxDELETE(m_impl);
 
     return exitcode;
 }
