--- choice.cpp.orig	2003-11-01 14:06:55.000000000 +0100
+++ choice.cpp	2003-11-01 14:05:07.000000000 +0100
@@ -86,4 +86,6 @@
     SetBackgroundColour(wxSystemSettings::GetColour(wxSYS_COLOUR_WINDOW));
 
+    SetSize(pos.x, pos.y, size.x, size.y);
+
     for ( int i = 0; i < n; i++ )
     {
@@ -91,6 +93,4 @@
     }
 
-    SetSize(pos.x, pos.y, size.x, size.y);
-
     return TRUE;
 }

 	  	 
