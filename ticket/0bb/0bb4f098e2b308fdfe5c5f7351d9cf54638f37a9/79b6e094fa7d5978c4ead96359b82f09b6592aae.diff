Index: src/gtk1/window.cpp
===================================================================
--- src/gtk1/window.cpp	(Revision 72529)
+++ src/gtk1/window.cpp	(Arbeitskopie)
@@ -454,8 +454,6 @@
     win->GetSize( &w, &h );
     if (w < 2)
         w = 2;
-    if (h < 2)
-        h = 2;
 
     GtkCombo *gcombo = GTK_COMBO(win->m_widget);
 
