Index: interface/wx/scrolwin.h
===================================================================
--- interface/wx/scrolwin.h	(revision 69933)
+++ interface/wx/scrolwin.h	(working copy)
@@ -103,6 +103,8 @@
            Uses a backing pixmap to speed refreshes. Motif only.
     @endStyleTable
 
+    @b Events : this class emits the events described in wxScrollWinEvent
+
     @remarks
     Use wxScrolled for applications where the user scrolls by a fixed amount,
     and where a 'page' can be interpreted to be the current visible portion of
