Index: treebkg.cpp
===================================================================
--- treebkg.cpp	(revision 51420)
+++ treebkg.cpp	(working copy)
@@ -115,7 +115,8 @@
 #endif
                     wxTR_DEFAULT_STYLE |
                     wxTR_HIDE_ROOT |
-                    wxTR_SINGLE
+                    wxTR_SINGLE |
+                    wxTR_EDIT_LABELS
                  );
     GetTreeCtrl()->SetQuickBestSize(false); // do full size calculation
     GetTreeCtrl()->AddRoot(wxEmptyString); // label doesn't matter, it's hidden

 	  	 
