diff -bu1 -r wxWindows/utils/tex2rtf/src/bmputils.h wxWin/utils/tex2rtf/src/bmputils.h
--- wxWindows/utils/tex2rtf/src/bmputils.h	2004-01-08 12:54:29.000000000 +0100
+++ wxWin/utils/tex2rtf/src/bmputils.h	2004-03-04 19:20:49.000000000 +0100
@@ -5,3 +5,3 @@
 // Author:      Julian Smart
-// Modified by:
+// Modified by: Wlodzimiez ABX Skiba 2003/2004 Unicode support
 // Created:     7.9.93
@@ -65,3 +65,3 @@
   int c = getc(fp); int c1 = getc(fp);
-  if (c!='B' || c1!='M') { return FALSE; }
+  if (c!='B' || c1!='M') { return false; }
 
@@ -91,3 +91,3 @@
   
-  return TRUE;
+  return true;
 }
@@ -96,3 +96,3 @@
 
-bool OutputBitmapHeader(FILE *fd, bool isWinHelp = FALSE)
+bool OutputBitmapHeader(FILE *fd, bool isWinHelp = false)
 {
@@ -100,3 +100,3 @@
   if (!GetBMPHeader(fd, &Width, &Height, &Planes, &BitsPerPixel))
-    return FALSE;
+    return false;
 
@@ -122,3 +122,3 @@
   TexOutput(_T("\n"));
-  return TRUE;
+  return true;
 }
@@ -145,3 +145,3 @@
   TexOutput(_T("\n}\n"));
-  return TRUE;
+  return true;
 }
@@ -166,3 +166,3 @@
   {
-    return FALSE;
+    return false;
   }
@@ -173,3 +173,3 @@
   *height = (int)((heightInUnits*1440.0)/theHeader->inch);
-  return TRUE;
+  return true;
 }
@@ -180,3 +180,3 @@
   if (!GetMetafileHeader(handle, &Width, &Height))
-    return FALSE;
+    return false;
 
@@ -214,3 +214,3 @@
   TexOutput(_T("\n"));
-  return TRUE;
+  return true;
 }
@@ -238,3 +238,3 @@
   TexOutput(_T("\n}\n"));
-  return TRUE;
+  return true;
 }
diff -bu1 -r wxWindows/utils/tex2rtf/src/htmlutil.cpp wxWin/utils/tex2rtf/src/htmlutil.cpp
--- wxWindows/utils/tex2rtf/src/htmlutil.cpp	2004-02-22 00:37:01.000000000 +0100
+++ wxWin/utils/tex2rtf/src/htmlutil.cpp	2004-03-04 19:21:32.000000000 +0100
@@ -4,3 +4,3 @@
 // Author:      Julian Smart
-// Modified by:
+// Modified by: Wlodzimiez ABX Skiba 2003/2004 Unicode support
 // Created:     7.9.93
@@ -71,3 +71,3 @@
 int fileId = 0;
-bool subsectionStarted = FALSE;
+bool subsectionStarted = false;
 
@@ -77,3 +77,3 @@
 // Are we in verbatim mode? If so, format differently.
-static bool inVerbatim = FALSE;
+static bool inVerbatim = false;
 
@@ -81,4 +81,4 @@
 // of listoffigures/listoftables
-static bool inFigure = FALSE;
-static bool inTable = FALSE;
+static bool inFigure = false;
+static bool inTable = false;
 
@@ -256,3 +256,3 @@
 {
-  bool changed = FALSE;
+  bool changed = false;
   int ptr = 0;
@@ -272,3 +272,3 @@
       i += 2;
-      changed = TRUE;
+      changed = true;
     }
@@ -278,3 +278,3 @@
       i += 2;
-      changed = TRUE;
+      changed = true;
     }
@@ -284,3 +284,3 @@
       i += 1;
-      changed = TRUE;
+      changed = true;
     }
@@ -292,3 +292,3 @@
       i += 1;
-      changed = TRUE;
+      changed = true;
     }
@@ -300,3 +300,3 @@
       i += 1;
-      changed = TRUE;
+      changed = true;
     }
@@ -341,3 +341,3 @@
       if (def && (def->macroId == ltVERBATIM || def->macroId == ltVERB || def->macroId == ltSPECIAL))
-        inVerbatim = TRUE;
+        inVerbatim = true;
 
@@ -352,3 +352,3 @@
       if (def && (def->macroId == ltVERBATIM || def->macroId == ltVERB || def->macroId == ltSPECIAL))
-        inVerbatim = FALSE;
+        inVerbatim = false;
 
@@ -571,3 +571,3 @@
     {
-      *isPicture = TRUE;
+      *isPicture = true;
       return resStr;
@@ -577,3 +577,3 @@
       wxString token2 = tok.GetNextToken();
-      *isPicture = FALSE;
+      *isPicture = false;
       if (tok.HasMoreTokens())
@@ -624,3 +624,3 @@
   {
-    bool isPicture = FALSE;
+    bool isPicture = false;
     wxChar *s = ParseColourString(backgroundImageString, &isPicture);
@@ -635,3 +635,3 @@
   {
-    bool isPicture = FALSE;
+    bool isPicture = false;
     wxChar *s = ParseColourString(backgroundColourString, &isPicture);
@@ -647,3 +647,3 @@
   {
-    bool isPicture = FALSE;
+    bool isPicture = false;
     wxChar *s = ParseColourString(textColourString, &isPicture);
@@ -657,3 +657,3 @@
   {
-    bool isPicture = FALSE;
+    bool isPicture = false;
     wxChar *s = ParseColourString(linkColourString, &isPicture);
@@ -667,3 +667,3 @@
   {
-    bool isPicture = FALSE;
+    bool isPicture = false;
     wxChar *s = ParseColourString(followedLinkColourString, &isPicture);
@@ -716,3 +716,3 @@
       SetCurrentOutput(NULL);
-      startedSections = TRUE;
+      startedSections = true;
 
@@ -781,3 +781,3 @@
       subsubsectionNo = 0;
-      subsectionStarted = FALSE;
+      subsectionStarted = false;
 
@@ -787,3 +787,3 @@
       SetCurrentOutput(NULL);
-      startedSections = TRUE;
+      startedSections = true;
 
@@ -877,4 +877,4 @@
 
-          startedSections = TRUE;
-          subsectionStarted = TRUE;
+          startedSections = true;
+          subsectionStarted = true;
 
@@ -959,3 +959,3 @@
 
-        startedSections = TRUE;
+        startedSections = true;
 
@@ -1186,6 +1186,7 @@
     {
-      if (TRUE) // (inVerbatim)
+      #if (1) // if(inVerbatim)
         TexOutput(_T("~"));
-      else
+      #else
         TexOutput(_T(" "));
+      #endif
     }
@@ -1455,3 +1456,3 @@
     if (start)
-      TexOutput(_T("&copy;"), TRUE);
+      TexOutput(_T("&copy;"), true);
     break;
@@ -1461,3 +1462,3 @@
     if (start)
-      TexOutput(_T("&reg;"), TRUE);
+      TexOutput(_T("&reg;"), true);
     break;
@@ -1722,4 +1723,4 @@
   {
-    if (start) inFigure = TRUE;
-    else inFigure = FALSE;
+    if (start) inFigure = true;
+    else inFigure = false;
     break;
@@ -1728,4 +1729,4 @@
   {
-    if (start) inTable = TRUE;
-    else inTable = FALSE;
+    if (start) inTable = true;
+    else inTable = false;
     break;
@@ -1759,3 +1760,3 @@
       currentSection = GetArgChunk();
-    return FALSE;
+    return false;
   }
@@ -1884,3 +1885,3 @@
       }
-      return FALSE;
+      return false;
     }
@@ -1891,3 +1892,3 @@
     if (IsArgOptional())
-      return FALSE;
+      return false;
     else if ((GetNoArgs() - arg_no) == 1)
@@ -1896,3 +1897,3 @@
         helpRefText = GetArgChunk();
-      return FALSE;
+      return false;
     }
@@ -1904,5 +1905,5 @@
         TexOutput(_T("<A HREF=\""));
-        inVerbatim = TRUE;
+        inVerbatim = true;
         TraverseChildrenFromChunk(ref);
-        inVerbatim = FALSE;
+        inVerbatim = false;
         TexOutput(_T("\">"));
@@ -1912,3 +1913,3 @@
       }
-      return FALSE;
+      return false;
     }
@@ -1924,3 +1925,3 @@
         helpRefFilename = GetArgChunk();
-      return FALSE;
+      return false;
     }
@@ -1930,3 +1931,3 @@
         helpRefText = GetArgChunk();
-      return FALSE;
+      return false;
     }
@@ -1979,3 +1980,3 @@
       }
-      return FALSE;
+      return false;
     }
@@ -2071,3 +2072,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -2142,3 +2143,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -2147,6 +2148,6 @@
     if ( arg_no == 1 )
-        return FALSE;
+        return false;
     else
     {
-        return TRUE;
+        return true;
     }
@@ -2158,5 +2159,5 @@
       descriptionItemArg = GetArgChunk();
-      return FALSE;
+      return false;
     }
-    return TRUE;
+    return true;
   }
@@ -2199,3 +2200,3 @@
     }
-    return TRUE;
+    return true;
   }
@@ -2228,3 +2229,3 @@
       citeCount ++;
-      return FALSE;
+      return false;
     }
@@ -2232,3 +2233,3 @@
       TexOutput(_T("<P>\n"));
-    return TRUE;
+    return true;
   }
@@ -2243,3 +2244,3 @@
       TexOutput(_T("<HR>\n"));
-      return TRUE;
+      return true;
     }
@@ -2258,3 +2259,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -2269,3 +2270,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -2319,3 +2320,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -2371,3 +2372,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -2417,3 +2418,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -2454,3 +2455,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -2506,3 +2507,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -2528,3 +2529,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -2537,3 +2538,3 @@
       {
-        bool isPicture = FALSE;
+        bool isPicture = false;
         ParseColourString(val, &isPicture);
@@ -2553,3 +2554,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -2567,3 +2568,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -2581,3 +2582,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -2595,3 +2596,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -2609,3 +2610,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -2623,3 +2624,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -2645,3 +2646,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -2652,5 +2653,5 @@
     if (arg_no == 1)
-      return TRUE;
+      return true;
     else
-      return FALSE;
+      return false;
     break;
@@ -2666,6 +2667,6 @@
         currentRowNumber = 0;
-        inTabular = TRUE;
-        startRows = TRUE;
-        tableVerticalLineLeft = FALSE;
-        tableVerticalLineRight = FALSE;
+        inTabular = true;
+        startRows = true;
+        tableVerticalLineLeft = false;
+        tableVerticalLineRight = false;
 
@@ -2694,3 +2695,3 @@
 
-        return FALSE;
+        return false;
       }
@@ -2700,3 +2701,3 @@
       TexOutput(_T("</TABLE>\n"));
-      inTabular = FALSE;
+      inTabular = false;
     }
@@ -2740,3 +2741,3 @@
       SetCurrentOutput(Chapters);
-      return FALSE;
+      return false;
     }
@@ -2745,3 +2746,3 @@
     }
-    return TRUE;
+    return true;
   }
@@ -2760,3 +2761,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -2787,3 +2788,3 @@
         {
-          return TRUE;
+          return true;
         }
@@ -2797,3 +2798,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -2804,3 +2805,3 @@
     if (useWord)
-        return FALSE;
+        return false;
     static int currentLevelNo = 1;
@@ -2849,3 +2850,3 @@
             CurrentOutput1 = outputFile;
-          return FALSE;
+          return false;
         }
@@ -2853,3 +2854,3 @@
         {
-          return TRUE;
+          return true;
         }
@@ -2858,3 +2859,3 @@
       }
-      return TRUE;
+      return true;
     }
@@ -2866,3 +2867,3 @@
         }
-        return TRUE;
+        return true;
     }
@@ -2872,3 +2873,3 @@
   }
-  return TRUE;
+  return true;
 }
@@ -2878,8 +2879,8 @@
   fileId = 0;
-  inVerbatim = FALSE;
+  inVerbatim = false;
   indentLevel = 0;
-  inTabular = FALSE;
-  startRows = FALSE;
-  tableVerticalLineLeft = FALSE;
-  tableVerticalLineRight = FALSE;
+  inTabular = false;
+  startRows = false;
+  tableVerticalLineLeft = false;
+  tableVerticalLineRight = false;
   noColumns = 0;
@@ -2920,3 +2921,3 @@
       OnError(_T("Cannot open output file!"));
-      return FALSE;
+      return false;
     }
@@ -3089,6 +3090,6 @@
 
-    return TRUE;
+    return true;
   }
 
-  return FALSE;
+  return false;
 }
diff -bu1 -r wxWindows/utils/tex2rtf/src/readshg.cpp wxWin/utils/tex2rtf/src/readshg.cpp
--- wxWindows/utils/tex2rtf/src/readshg.cpp	2004-01-08 12:54:29.000000000 +0100
+++ wxWin/utils/tex2rtf/src/readshg.cpp	2004-03-04 19:21:25.000000000 +0100
@@ -8,3 +8,3 @@
 // Author:      Petr Smilauer
-// Modified by:
+// Modified by: Wlodzimiez ABX Skiba 2003/2004 Unicode support
 // Created:     01/01/99
@@ -118,3 +118,3 @@
   if (n == 0)
-    return FALSE;
+    return false;
 
@@ -134,3 +134,3 @@
     delete[] hotspots;
-    return FALSE;
+    return false;
   }
@@ -159,3 +159,3 @@
   delete[] hotspots;
-  return TRUE;
+  return true;
 }
diff -bu1 -r wxWindows/utils/tex2rtf/src/readshg.h wxWin/utils/tex2rtf/src/readshg.h
--- wxWindows/utils/tex2rtf/src/readshg.h	2004-01-08 12:54:29.000000000 +0100
+++ wxWin/utils/tex2rtf/src/readshg.h	2004-03-04 19:20:21.000000000 +0100
@@ -8,3 +8,3 @@
 // Author:      Petr Smilauer
-// Modified by:
+// Modified by: Wlodzimiez ABX Skiba 2003/2004 Unicode support
 // Created:     01/01/99
diff -bu1 -r wxWindows/utils/tex2rtf/src/rtfutils.cpp wxWin/utils/tex2rtf/src/rtfutils.cpp
--- wxWindows/utils/tex2rtf/src/rtfutils.cpp	2004-02-22 12:29:39.000000000 +0100
+++ wxWin/utils/tex2rtf/src/rtfutils.cpp	2004-03-04 19:21:20.000000000 +0100
@@ -4,3 +4,3 @@
 // Author:      Julian Smart
-// Modified by:
+// Modified by: Wlodzimiez ABX Skiba 2003/2004 Unicode support
 // Created:     7.9.93
@@ -67,13 +67,13 @@
 // Are we in verbatim mode? If so, format differently.
-static bool inVerbatim = FALSE;
+static bool inVerbatim = false;
 
 // We're in a series of PopRef topics, so don't output section headings
-bool inPopRefSection = FALSE;
+bool inPopRefSection = false;
 
 // Green colour?
-static bool hotSpotColour = TRUE;
-static bool hotSpotUnderline = TRUE;
+static bool hotSpotColour = true;
+static bool hotSpotUnderline = true;
 
 // Transparency (WHITE = transparent)
-static bool bitmapTransparency = TRUE;
+static bool bitmapTransparency = true;
 
@@ -111,4 +111,4 @@
 // of listoffigures/listoftables
-static bool inFigure = FALSE;
-static bool inTable = FALSE;
+static bool inFigure = false;
+static bool inTable = false;
 
@@ -126,6 +126,6 @@
   if (CurrentChapterName && (wxStrcmp(CurrentChapterName, _T("popups")) == 0))
-    return TRUE;
+    return true;
   if (CurrentSectionName && (wxStrcmp(CurrentSectionName, _T("popups")) == 0))
-    return TRUE;
-  return FALSE;
+    return true;
+  return false;
 }
@@ -192,3 +192,3 @@
     }
-    texTopic->hasChildren = TRUE;
+    texTopic->hasChildren = true;
   }
@@ -208,6 +208,6 @@
   for (i = l; i < 5; i++)
-    ContentsLevels[i] = FALSE;
+    ContentsLevels[i] = false;
     
   // There are always books on the top level
-  ContentsLevels[0] = TRUE;
+  ContentsLevels[0] = true;
 }
@@ -294,3 +294,3 @@
         // remainder of sections at this level.
-        ContentsLevels[level-1] = TRUE;
+        ContentsLevels[level-1] = true;
       }
@@ -308,3 +308,3 @@
         wxFprintf(WinHelpContentsFile, _T("%d %s=%s\n"), level+1, title, topicName);
-        ContentsLevels[level-1] = TRUE;
+        ContentsLevels[level-1] = true;
       }
@@ -541,3 +541,3 @@
   if (!fd)
-    return FALSE;
+    return false;
 
@@ -582,3 +582,3 @@
   fclose(fd);
-  return TRUE;
+  return true;
 }
@@ -596,3 +596,3 @@
 {
-  bool changed = FALSE;
+  bool changed = false;
   int ptr = 0;
@@ -612,3 +612,3 @@
         i ++;
-        changed = TRUE;
+        changed = true;
       }
@@ -628,3 +628,3 @@
               !isspace(chunk->value[i+2]))))
-//        if (TRUE)
+//        if (true)
         {
@@ -646,3 +646,3 @@
           // iteration
-          changed = TRUE;
+          changed = true;
         }
@@ -659,3 +659,3 @@
       i += 2;
-      changed = TRUE;
+      changed = true;
     }
@@ -665,3 +665,3 @@
       i += 1;
-      changed = TRUE;
+      changed = true;
     }
@@ -672,3 +672,3 @@
       i += 1;
-      changed = TRUE;
+      changed = true;
     }
@@ -679,3 +679,3 @@
       i += 1;
-      changed = TRUE;
+      changed = true;
     }
@@ -719,3 +719,3 @@
       if (def && (def->macroId == ltVERBATIM || def->macroId == ltVERB))
-        inVerbatim = TRUE;
+        inVerbatim = true;
 
@@ -730,3 +730,3 @@
       if (def && (def->macroId == ltVERBATIM || def->macroId == ltVERB))
-        inVerbatim = FALSE;
+        inVerbatim = false;
 
@@ -1170,3 +1170,3 @@
   
-      startedSections = TRUE;
+      startedSections = true;
 
@@ -1326,3 +1326,3 @@
       }
-      startedSections = TRUE;
+      startedSections = true;
 
@@ -1493,3 +1493,3 @@
       }
-      startedSections = TRUE;
+      startedSections = true;
 
@@ -1654,3 +1654,3 @@
 
-      startedSections = TRUE;
+      startedSections = true;
 
@@ -1826,5 +1826,5 @@
         TexOutput(_T("K{\\footnote {K} "));
-        suppressNameDecoration = TRUE;
+        suppressNameDecoration = true;
         TraverseChildrenFromChunk(currentMember);
-        suppressNameDecoration = FALSE;
+        suppressNameDecoration = false;
         TexOutput(_T("}\n"));
@@ -1835,5 +1835,5 @@
         TexOutput(_T("{\\xe\\v {"));
-        suppressNameDecoration = TRUE;  // Necessary so don't print "(\\bf" etc.
+        suppressNameDecoration = true;  // Necessary so don't print "(\\bf" etc.
         TraverseChildrenFromChunk(currentMember);
-        suppressNameDecoration = FALSE;
+        suppressNameDecoration = false;
         TexOutput(_T("}}"));
@@ -1856,5 +1856,5 @@
         TexOutput(_T("K{\\footnote {K} "));
-        suppressNameDecoration = TRUE;  // Necessary so don't print "(\\bf" etc.
+        suppressNameDecoration = true;  // Necessary so don't print "(\\bf" etc.
         TraverseChildrenFromChunk(currentMember);
-        suppressNameDecoration = FALSE;
+        suppressNameDecoration = false;
         TexOutput(_T("}\n"));
@@ -1865,5 +1865,5 @@
         TexOutput(_T("{\\xe\\v {"));
-        suppressNameDecoration = TRUE;  // Necessary so don't print "(\\bf" etc.
+        suppressNameDecoration = true;  // Necessary so don't print "(\\bf" etc.
         TraverseChildrenFromChunk(currentMember);
-        suppressNameDecoration = FALSE;
+        suppressNameDecoration = false;
         TexOutput(_T("}}"));
@@ -1893,5 +1893,5 @@
         TexOutput(_T("{\\xe\\v {"));
-        suppressNameDecoration = TRUE;  // Necessary so don't print "(\\bf" etc.
+        suppressNameDecoration = true;  // Necessary so don't print "(\\bf" etc.
         TraverseChildrenFromChunk(currentMember);
-        suppressNameDecoration = FALSE;
+        suppressNameDecoration = false;
         TexOutput(_T("}}"));
@@ -1979,6 +1979,7 @@
     {
-      if (TRUE) // (inVerbatim)
+      #if 1 // if(inVerbatim)
         TexOutput(_T("~"));
-      else
+      #else
         TexOutput(_T(" "));
+      #endif
     }
@@ -2091,4 +2092,4 @@
       {
-        OnMacro(ltPAR, 0, TRUE);
-        OnMacro(ltPAR, 0, FALSE);
+        OnMacro(ltPAR, 0, true);
+        OnMacro(ltPAR, 0, false);
         issuedNewParagraph = 2;
@@ -2140,4 +2141,4 @@
         issuedNewParagraph = 0;
-        OnMacro(ltPAR, 0, TRUE);
-        OnMacro(ltPAR, 0, FALSE);
+        OnMacro(ltPAR, 0, true);
+        OnMacro(ltPAR, 0, false);
       }
@@ -2604,3 +2605,2 @@
 		  // and ParSkip == 0.
-
 #if 0
@@ -2784,4 +2784,4 @@
       FakeCurrentSection(_T("Index"));
-      OnMacro(ltPAR, 0, TRUE);
-      OnMacro(ltPAR, 0, FALSE);
+      OnMacro(ltPAR, 0, true);
+      OnMacro(ltPAR, 0, false);
       TexOutput(_T("\\par{\\field{\\*\\fldinst INDEX \\\\h \"\\emdash A\\emdash \"\\\\c \"2\"}{\\fldrslt PRESS F9 TO REFORMAT INDEX}}\n"));
@@ -2794,7 +2794,7 @@
     {
-      FakeCurrentSection(FiguresNameString, FALSE);
-      OnMacro(ltPAR, 0, TRUE);
-      OnMacro(ltPAR, 0, FALSE);
-      OnMacro(ltPAR, 0, TRUE);
-      OnMacro(ltPAR, 0, FALSE);
+      FakeCurrentSection(FiguresNameString, false);
+      OnMacro(ltPAR, 0, true);
+      OnMacro(ltPAR, 0, false);
+      OnMacro(ltPAR, 0, true);
+      OnMacro(ltPAR, 0, false);
       wxChar buf[200];
@@ -2810,7 +2810,7 @@
     {
-      FakeCurrentSection(TablesNameString, FALSE);
-      OnMacro(ltPAR, 0, TRUE);
-      OnMacro(ltPAR, 0, FALSE);
-      OnMacro(ltPAR, 0, TRUE);
-      OnMacro(ltPAR, 0, FALSE);
+      FakeCurrentSection(TablesNameString, false);
+      OnMacro(ltPAR, 0, true);
+      OnMacro(ltPAR, 0, false);
+      OnMacro(ltPAR, 0, true);
+      OnMacro(ltPAR, 0, false);
       wxChar buf[200];
@@ -3171,4 +3171,4 @@
   {
-    if (start) inFigure = TRUE;
-    else inFigure = FALSE;
+    if (start) inFigure = true;
+    else inFigure = false;
     break;
@@ -3177,4 +3177,4 @@
   {
-    if (start) inTable = TRUE;
-    else inTable = FALSE;
+    if (start) inTable = true;
+    else inTable = false;
     break;
@@ -3213,3 +3213,3 @@
       currentSection = GetArgChunk();
-    return FALSE;
+    return false;
   }
@@ -3362,3 +3362,3 @@
       }
-      return FALSE;
+      return false;
     }
@@ -3407,3 +3407,3 @@
 
-        return TRUE;
+        return true;
       }
@@ -3460,3 +3460,3 @@
         }
-        return FALSE;
+        return false;
       }
@@ -3469,3 +3469,3 @@
     {
-      return TRUE;
+      return true;
     }
@@ -3475,3 +3475,3 @@
       {
-        inVerbatim = TRUE;
+        inVerbatim = true;
         TexOutput(_T(" ({\\f3 "));
@@ -3481,5 +3481,5 @@
         TexOutput(_T("})"));
-        inVerbatim = FALSE;
+        inVerbatim = false;
       }
-      return TRUE;
+      return true;
     }
@@ -3522,5 +3522,5 @@
           TexOutput(_T("}"));
-        return TRUE;
+        return true;
       }
-      else return FALSE;
+      else return false;
     }
@@ -3536,5 +3536,5 @@
         contentsLineValue = copystring(GetArgData());
-      return FALSE;
+      return false;
     }
-    else return FALSE;
+    else return false;
   }
@@ -3547,3 +3547,3 @@
     if (arg_no == 3)
-      return FALSE;
+      return false;
       
@@ -3581,3 +3581,3 @@
           delete [] imageDimensions;
-      return FALSE;
+      return false;
     }  
@@ -3714,6 +3714,6 @@
       }
-      return FALSE;
+      return false;
     }
     else
-      return FALSE;
+      return false;
   }
@@ -3727,6 +3727,6 @@
         currentRowNumber = 0;
-        inTabular = TRUE;
-        startRows = TRUE;
-        tableVerticalLineLeft = FALSE;
-        tableVerticalLineRight = FALSE;
+        inTabular = true;
+        startRows = true;
+        tableVerticalLineLeft = false;
+        tableVerticalLineRight = false;
         int currentWidth = 0;
@@ -3753,3 +3753,3 @@
 
-        return FALSE;
+        return false;
       }
@@ -3760,3 +3760,3 @@
       WriteEnvironmentStyles();
-      inTabular = FALSE;
+      inTabular = false;
     }
@@ -3778,4 +3778,4 @@
       PopEnvironmentStyle();
-      OnMacro(ltPAR, 0, TRUE);
-      OnMacro(ltPAR, 0, FALSE);
+      OnMacro(ltPAR, 0, true);
+      OnMacro(ltPAR, 0, false);
     }
@@ -3793,4 +3793,4 @@
       PopEnvironmentStyle();
-      OnMacro(ltPAR, 0, TRUE);
-      OnMacro(ltPAR, 0, FALSE);
+      OnMacro(ltPAR, 0, true);
+      OnMacro(ltPAR, 0, false);
     }
@@ -3813,4 +3813,4 @@
       PopEnvironmentStyle();
-      OnMacro(ltPAR, 0, TRUE);
-      OnMacro(ltPAR, 0, FALSE);
+      OnMacro(ltPAR, 0, true);
+      OnMacro(ltPAR, 0, false);
     }
@@ -3832,3 +3832,3 @@
       TexOutput(buf);
-      return FALSE;
+      return false;
     }
@@ -3848,3 +3848,3 @@
 
-      return FALSE;
+      return false;
     }
@@ -3863,3 +3863,3 @@
       }
-      return FALSE;
+      return false;
     }
@@ -3872,3 +3872,3 @@
       descriptionItemArg = GetArgChunk();
-      return FALSE;
+      return false;
     }
@@ -3900,3 +3900,3 @@
     }
-    return TRUE;
+    return true;
   }
@@ -3950,3 +3950,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -4002,3 +4002,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -4048,3 +4048,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -4085,3 +4085,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -4140,3 +4140,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -4162,3 +4162,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -4184,3 +4184,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -4227,5 +4227,5 @@
 	}
-	return TRUE;
+        return true;
       }
-      return TRUE;
+      return true;
     }
@@ -4235,3 +4235,3 @@
       {
-        TexOutput(_T(" {\\super \\chftn{\\footnote \\fs20 {\\super \\chftn}"), TRUE);
+        TexOutput(_T(" {\\super \\chftn{\\footnote \\fs20 {\\super \\chftn}"), true);
       }
@@ -4239,5 +4239,5 @@
       {
-        TexOutput(_T("}}"), TRUE);
+        TexOutput(_T("}}"), true);
       }
-      return TRUE;
+      return true;
     }
@@ -4256,4 +4256,7 @@
 	}
-	else TexOutput(_T("}"));
-	return TRUE;
+        else
+        {
+          TexOutput(_T("}"));
+        }
+        return true;
       }
@@ -4286,3 +4289,3 @@
         }
-        return TRUE;
+        return true;
       }
@@ -4292,6 +4295,6 @@
       if (arg_no == 1)
-        return TRUE;
+        return true;
       if (start)
       {
-        TexOutput(_T(" {\\super \\chftn{\\footnote \\fs20 {\\super \\chftn}"), TRUE);
+        TexOutput(_T(" {\\super \\chftn{\\footnote \\fs20 {\\super \\chftn}"), true);
       }
@@ -4299,5 +4302,5 @@
       {
-        TexOutput(_T("}}"), TRUE);
+        TexOutput(_T("}}"), true);
       }
-      return TRUE;
+      return true;
     }
@@ -4308,5 +4311,5 @@
     if (start && (arg_no == 1))
-      return FALSE;
+      return false;
     else
-      return TRUE;
+      return true;
   }
@@ -4319,3 +4322,3 @@
 
-    if (winHelp) return FALSE;
+    if (winHelp) return false;
     if (start)
@@ -4359,3 +4362,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -4368,3 +4371,3 @@
 
-    if (winHelp) return FALSE;
+    if (winHelp) return false;
     if (start)
@@ -4408,3 +4411,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -4412,3 +4415,3 @@
   {
-    if (winHelp) return FALSE;
+    if (winHelp) return false;
     // Fake a SetHeader command
@@ -4427,3 +4430,3 @@
       OutputRTFHeaderCommands();
-    return FALSE;
+    return false;
   }
@@ -4431,3 +4434,3 @@
   {
-    if (winHelp) return FALSE;
+    if (winHelp) return false;
     // Fake a SetHeader command
@@ -4454,3 +4457,3 @@
           OutputRTFHeaderCommands();
-        return FALSE;
+        return false;
       }
@@ -4466,3 +4469,3 @@
 
-    if (winHelp) return FALSE;
+    if (winHelp) return false;
     if (start)
@@ -4477,3 +4480,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -4481,5 +4484,5 @@
   {
-    if (winHelp) return FALSE;
+    if (winHelp) return false;
     if (start)
-      return TRUE;
+      return true;
     break;
@@ -4492,3 +4495,3 @@
       currentItemSep = ParseUnitArgument(val);
-      return FALSE;
+      return false;
     }
@@ -4498,3 +4501,3 @@
   {
-    return FALSE;
+    return false;
   }
@@ -4514,3 +4517,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -4524,3 +4527,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -4535,3 +4538,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -4552,3 +4555,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -4569,3 +4572,3 @@
       }
-      return TRUE;
+      return true;
     }
@@ -4583,3 +4586,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -4611,3 +4614,3 @@
       }
-      return TRUE;
+      return true;
     }
@@ -4625,3 +4628,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -4635,3 +4638,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -4645,3 +4648,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -4694,3 +4697,3 @@
       currentRowNumber ++;
-      return TRUE;
+      return true;
     }
@@ -4713,3 +4716,3 @@
           noMultiColumns = wxAtoi(GetArgData());
-          return FALSE;
+          return false;
         }
@@ -4717,3 +4720,3 @@
         {
-          return FALSE;
+          return false;
         }
@@ -4721,3 +4724,3 @@
         {
-          return TRUE;
+          return true;
         }
@@ -4755,3 +4758,3 @@
       TexOutput(buf);
-      return FALSE;
+      return false;
     }
@@ -4773,3 +4776,3 @@
     }
-    return TRUE;
+    return true;
   }
@@ -4799,3 +4802,3 @@
       TexOutput(buf);
-      return FALSE;
+      return false;
     }
@@ -4817,3 +4820,3 @@
     }
-    return TRUE;
+    return true;
     break;
@@ -4836,3 +4839,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -4845,7 +4848,7 @@
       if (wxStrcmp(text, _T("yes")) == 0 || wxStrcmp(text, _T("on")) == 0 || wxStrcmp(text, _T("ok")) == 0)
-        hotSpotColour = TRUE;
+        hotSpotColour = true;
       else
-        hotSpotColour = FALSE;
+        hotSpotColour = false;
     }
-    return FALSE;
+    return false;
   }
@@ -4857,7 +4860,7 @@
       if (wxStrcmp(text, _T("yes")) == 0 || wxStrcmp(text, _T("on")) == 0 || wxStrcmp(text, _T("ok")) == 0)
-        bitmapTransparency = TRUE;
+        bitmapTransparency = true;
       else
-        bitmapTransparency = FALSE;
+        bitmapTransparency = false;
     }
-    return FALSE;
+    return false;
   }
@@ -4869,7 +4872,7 @@
       if (wxStrcmp(text, _T("yes")) == 0 || wxStrcmp(text, _T("on")) == 0 || wxStrcmp(text, _T("ok")) == 0)
-        hotSpotUnderline = TRUE;
+        hotSpotUnderline = true;
       else
-        hotSpotUnderline = FALSE;
+        hotSpotUnderline = false;
     }
-    return FALSE;
+    return false;
   }
@@ -4892,3 +4895,3 @@
       citeCount ++;
-      return FALSE;
+      return false;
     }
@@ -4896,3 +4899,3 @@
       TexOutput(_T("\\par\\pard\\par\n\n"));
-    return TRUE;
+    return true;
   }
@@ -4931,3 +4934,3 @@
 
-      startedSections = TRUE;
+      startedSections = true;
 
@@ -4966,5 +4969,5 @@
 
-      return FALSE;
+      return false;
     }
-    return TRUE;
+    return true;
   }
@@ -4995,3 +4998,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -5022,3 +5025,3 @@
         {
-          return TRUE;
+          return true;
         }
@@ -5032,3 +5035,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -5052,3 +5055,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -5062,3 +5065,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -5067,5 +5070,5 @@
     if (start)
-      inPopRefSection = TRUE;
+      inPopRefSection = true;
     else
-      inPopRefSection = FALSE;
+      inPopRefSection = false;
     break;
@@ -5077,3 +5080,3 @@
     if (!winHelp & useWord)
-        return FALSE;
+        return false;
     static int currentLevelNo = 1;
@@ -5122,3 +5125,3 @@
             CurrentOutput1 = outputFile;
-          return FALSE;
+          return false;
         }
@@ -5126,3 +5129,3 @@
         {
-          return TRUE;
+          return true;
         }
@@ -5131,3 +5134,3 @@
       }
-      return TRUE;
+      return true;
     }
@@ -5139,3 +5142,3 @@
         }
-        return TRUE;
+        return true;
     }
@@ -5145,3 +5148,3 @@
   }
-  return TRUE;
+  return true;
 }
@@ -5151,3 +5154,3 @@
   if (stopRunning)
-      return FALSE;
+      return false;
 
@@ -5159,11 +5162,11 @@
   descriptionItemArg = NULL;
-  inTabular = FALSE;
-  inTable = FALSE;
-  inFigure = FALSE;
-  startRows = FALSE;
-  tableVerticalLineLeft = FALSE;
-  tableVerticalLineRight = FALSE;
+  inTabular = false;
+  inTable = false;
+  inFigure = false;
+  startRows = false;
+  tableVerticalLineLeft = false;
+  tableVerticalLineRight = false;
   noColumns = 0;
-  startedSections = FALSE;
-  inVerbatim = FALSE;
+  startedSections = false;
+  inVerbatim = false;
   browseId = 0;
@@ -5194,3 +5197,3 @@
         OnError(_T("Ouch! Could not open temporary file(s) for writing."));
-        return FALSE;
+        return false;
       }
@@ -5200,3 +5203,3 @@
       OnError(_T("Ouch! Could not open temporary file(s) for writing."));
-      return FALSE;
+      return false;
     }
@@ -5230,3 +5233,3 @@
     if (stopRunning)
-        return FALSE;
+        return false;
 
@@ -5240,3 +5243,3 @@
       OnError(_T("Ouch! Could not open temporary file header.rtf for writing."));
-      return FALSE;
+      return false;
     }
@@ -5247,3 +5250,3 @@
     
-    Tex2RTFYield(TRUE);
+    Tex2RTFYield(true);
     if (winHelp)
@@ -5278,11 +5281,11 @@
       wxConcatFiles(_T("header.rtf"), _T("chapters.rtf"), _T("tmp1.rtf"));
-      Tex2RTFYield(TRUE);
+      Tex2RTFYield(true);
       wxConcatFiles(_T("tmp1.rtf"), _T("sections.rtf"), _T("tmp2.rtf"));
-      Tex2RTFYield(TRUE);
+      Tex2RTFYield(true);
       wxConcatFiles(_T("tmp2.rtf"), _T("subsections.rtf"), _T("tmp3.rtf"));
-      Tex2RTFYield(TRUE);
+      Tex2RTFYield(true);
       wxConcatFiles(_T("tmp3.rtf"), _T("subsubsections.rtf"), _T("tmp4.rtf"));
-      Tex2RTFYield(TRUE);
+      Tex2RTFYield(true);
       wxConcatFiles(_T("tmp4.rtf"), _T("popups.rtf"), OutputFile);
-      Tex2RTFYield(TRUE);
+      Tex2RTFYield(true);
 
@@ -5296,3 +5299,3 @@
       wxConcatFiles(_T("header.rtf"), _T("chapters.rtf"), _T("tmp1.rtf"));
-      Tex2RTFYield(TRUE);
+      Tex2RTFYield(true);
       if (wxFileExists(OutputFile))
@@ -5319,3 +5322,3 @@
       delete [] cwdStr;
-      Tex2RTFYield(TRUE);
+      Tex2RTFYield(true);
       wxRemoveFile(_T("tmp1.rtf"));
@@ -5343,5 +5346,5 @@
       WriteHPJ(OutputFile);
-    return TRUE;
+    return true;
   }
-  return FALSE;
+  return false;
 }
diff -bu1 -r wxWindows/utils/tex2rtf/src/rtfutils.h wxWin/utils/tex2rtf/src/rtfutils.h
--- wxWindows/utils/tex2rtf/src/rtfutils.h	2004-01-08 12:54:29.000000000 +0100
+++ wxWin/utils/tex2rtf/src/rtfutils.h	2004-03-04 19:20:16.000000000 +0100
@@ -4,3 +4,3 @@
 // Author:      Julian Smart
-// Modified by:
+// Modified by: Wlodzimiez ABX Skiba 2003/2004 Unicode support
 // Created:     7.9.93
diff -bu1 -r wxWindows/utils/tex2rtf/src/symbols.h wxWin/utils/tex2rtf/src/symbols.h
--- wxWindows/utils/tex2rtf/src/symbols.h	2002-08-07 12:56:22.000000000 +0200
+++ wxWin/utils/tex2rtf/src/symbols.h	2004-03-04 19:20:12.000000000 +0100
@@ -4,3 +4,3 @@
 // Author:      Julian Smart
-// Modified by:
+// Modified by: Wlodzimiez ABX Skiba 2003/2004 Unicode support
 // Created:     2002-03-26
diff -bu1 -r wxWindows/utils/tex2rtf/src/table.cpp wxWin/utils/tex2rtf/src/table.cpp
--- wxWindows/utils/tex2rtf/src/table.cpp	2004-01-08 12:54:29.000000000 +0100
+++ wxWin/utils/tex2rtf/src/table.cpp	2004-03-04 19:21:17.000000000 +0100
@@ -4,3 +4,3 @@
 // Author:      Julian Smart
-// Modified by:
+// Modified by: Wlodzimiez ABX Skiba 2003/2004 Unicode support
 // Created:     01/01/99
@@ -44,7 +44,7 @@
 ColumnData TableData[40];
-bool inTabular = FALSE;
+bool inTabular = false;
 
-bool startRows = FALSE;
-bool tableVerticalLineLeft = FALSE;
-bool tableVerticalLineRight = FALSE;
+bool startRows = false;
+bool tableVerticalLineLeft = false;
+bool tableVerticalLineRight = false;
 int noColumns = 0;   // Current number of columns in table
@@ -64,3 +64,3 @@
   int len = wxStrlen(value);
-  bool isBorder = FALSE;
+  bool isBorder = false;
   while (i < len)
@@ -71,3 +71,3 @@
       i ++;
-      isBorder = TRUE;
+      isBorder = true;
     }
@@ -76,6 +76,6 @@
       TableData[noColumns].leftBorder = isBorder;
-      TableData[noColumns].rightBorder = FALSE;
+      TableData[noColumns].rightBorder = false;
       TableData[noColumns].justification = 'l';
       TableData[noColumns].width = 2000; // Estimate
-      TableData[noColumns].absWidth = FALSE;
+      TableData[noColumns].absWidth = false;
 //      TableData[noColumns].spacing = ??
@@ -83,3 +83,3 @@
       i ++;
-      isBorder = FALSE;
+      isBorder = false;
     }
@@ -88,6 +88,6 @@
       TableData[noColumns].leftBorder = isBorder;
-      TableData[noColumns].rightBorder = FALSE;
+      TableData[noColumns].rightBorder = false;
       TableData[noColumns].justification = 'c';
       TableData[noColumns].width = defaultTableColumnWidth; // Estimate
-      TableData[noColumns].absWidth = FALSE;
+      TableData[noColumns].absWidth = false;
 //      TableData[noColumns].spacing = ??
@@ -95,3 +95,3 @@
       i ++;
-      isBorder = FALSE;
+      isBorder = false;
     }
@@ -100,6 +100,6 @@
       TableData[noColumns].leftBorder = isBorder;
-      TableData[noColumns].rightBorder = FALSE;
+      TableData[noColumns].rightBorder = false;
       TableData[noColumns].justification = 'r';
       TableData[noColumns].width = 2000; // Estimate
-      TableData[noColumns].absWidth = FALSE;
+      TableData[noColumns].absWidth = false;
 //      TableData[noColumns].spacing = ??
@@ -107,3 +107,3 @@
       i ++;
-      isBorder = FALSE;
+      isBorder = false;
     }
@@ -137,9 +137,9 @@
       TableData[noColumns].leftBorder = isBorder;
-      TableData[noColumns].rightBorder = FALSE;
+      TableData[noColumns].rightBorder = false;
       TableData[noColumns].justification = 'l';
       TableData[noColumns].width = 20*ParseUnitArgument(numberBuf);
-      TableData[noColumns].absWidth = TRUE;
+      TableData[noColumns].absWidth = true;
 //      TableData[noColumns].spacing = ??
       noColumns ++;
-      isBorder = FALSE;
+      isBorder = false;
     }
@@ -151,3 +151,3 @@
       delete[] buf;
-      return FALSE;
+      return false;
     }
@@ -155,4 +155,4 @@
   if (isBorder)
-    TableData[noColumns-1].rightBorder = TRUE;
-  return TRUE;
+    TableData[noColumns-1].rightBorder = true;
+  return true;
 }
diff -bu1 -r wxWindows/utils/tex2rtf/src/table.h wxWin/utils/tex2rtf/src/table.h
--- wxWindows/utils/tex2rtf/src/table.h	2004-01-08 12:54:29.000000000 +0100
+++ wxWin/utils/tex2rtf/src/table.h	2004-03-04 19:20:07.000000000 +0100
@@ -4,3 +4,3 @@
 // Author:      Julian Smart
-// Modified by:
+// Modified by: Wlodzimiez ABX Skiba 2003/2004 Unicode support
 // Created:     7.9.93
diff -bu1 -r wxWindows/utils/tex2rtf/src/tex2any.cpp wxWin/utils/tex2rtf/src/tex2any.cpp
--- wxWindows/utils/tex2rtf/src/tex2any.cpp	2004-01-08 12:54:29.000000000 +0100
+++ wxWin/utils/tex2rtf/src/tex2any.cpp	2004-03-04 19:21:09.000000000 +0100
@@ -4,3 +4,3 @@
 // Author:      Julian Smart
-// Modified by:
+// Modified by: Wlodzimiez ABX Skiba 2003/2004 Unicode support
 // Created:     01/01/99
@@ -109,12 +109,12 @@
 int             authorFont = LargeFont2;
-int             mirrorMargins = TRUE;
-bool            winHelp = FALSE;  // Output in Windows Help format if TRUE, linear otherwise
-bool            isInteractive = FALSE;
-bool            runTwice = FALSE;
+int             mirrorMargins = true;
+bool            winHelp = false;  // Output in Windows Help format if TRUE, linear otherwise
+bool            isInteractive = false;
+bool            runTwice = false;
 int             convertMode = TEX_RTF;
-bool            checkCurleyBraces = FALSE;
-bool            checkSyntax = FALSE;
-bool            headerRule = FALSE;
-bool            footerRule = FALSE;
-bool            compatibilityMode = FALSE; // If TRUE, maximum Latex compatibility
+bool            checkCurleyBraces = false;
+bool            checkSyntax = false;
+bool            headerRule = false;
+bool            footerRule = false;
+bool            compatibilityMode = false; // If TRUE, maximum Latex compatibility
                                 // (Quality of RTF generation deteriorate)
@@ -127,18 +127,18 @@
 
-bool            useUpButton = TRUE;
+bool            useUpButton = true;
 int             htmlBrowseButtons = HTML_BUTTONS_TEXT;
 
-bool            truncateFilenames = FALSE; // Truncate for DOS
+bool            truncateFilenames = false; // Truncate for DOS
 int             winHelpVersion = 3; // WinHelp Version (3 for Windows 3.1, 4 for Win95)
-bool            winHelpContents = FALSE; // Generate .cnt file for WinHelp 4
-bool            htmlIndex = FALSE; // Generate .htx file for HTML
-bool            htmlFrameContents = FALSE; // Use frames for HTML contents page
+bool            winHelpContents = false; // Generate .cnt file for WinHelp 4
+bool            htmlIndex = false; // Generate .htx file for HTML
+bool            htmlFrameContents = false; // Use frames for HTML contents page
 wxChar         *htmlStylesheet = NULL; // Use this CSS stylesheet for HTML pages
-bool            useHeadingStyles = TRUE; // Insert \s1, s2 etc.
-bool            useWord = TRUE; // Insert proper Word table of contents, etc etc
+bool            useHeadingStyles = true; // Insert \s1, s2 etc.
+bool            useWord = true; // Insert proper Word table of contents, etc etc
 int             contentsDepth = 4; // Depth of Word table of contents
-bool            indexSubsections = TRUE; // Index subsections in linear RTF
+bool            indexSubsections = true; // Index subsections in linear RTF
 // Linear RTF method of including bitmaps. Can be "includepicture", "hex"
 wxChar         *bitmapMethod = copystring(_T("includepicture"));
-bool            upperCaseNames = FALSE;
+bool            upperCaseNames = false;
 // HTML background and text colours
@@ -149,5 +149,5 @@
 wxChar         *followedLinkColourString = NULL;
-bool            combineSubSections = FALSE;
-bool            htmlWorkshopFiles = FALSE;
-bool            ignoreBadRefs = FALSE;
+bool            combineSubSections = false;
+bool            htmlWorkshopFiles = false;
+bool            ignoreBadRefs = false;
 wxChar			*htmlFaceName = NULL;
@@ -205,4 +205,4 @@
 wxChar *TexTmpBibName = NULL;      // Temporary bibliography output file name
-bool isSync = FALSE;             // If TRUE, should not yield to other processes.
-bool stopRunning = FALSE;        // If TRUE, should abort.
+bool isSync = false;             // If TRUE, should not yield to other processes.
+bool stopRunning = false;        // If TRUE, should abort.
 
@@ -210,6 +210,6 @@
 wxChar *currentArgData = NULL;
-bool haveArgData = FALSE; // If TRUE, we're simulating the data.
+bool haveArgData = false; // If TRUE, we're simulating the data.
 TexChunk *currentArgument = NULL;
 TexChunk *nextChunk = NULL;
-bool isArgOptional = FALSE;
+bool isArgOptional = false;
 int noArgs = 0;
@@ -306,3 +306,3 @@
 {
-  *parseToBrace = TRUE;
+  *parseToBrace = true;
   int i = (*pos);
@@ -374,3 +374,3 @@
 
-      *parseToBrace = FALSE;
+      *parseToBrace = false;
     }
@@ -387,4 +387,4 @@
   int j = *pos;
-  bool keepGoing = TRUE;
-  bool moreLines = TRUE;
+  bool keepGoing = true;
+  bool moreLines = true;
   while ((j < len) && keepGoing &&
@@ -402,3 +402,3 @@
       else
-        keepGoing = FALSE;
+        keepGoing = false;
     }
@@ -417,9 +417,9 @@
     *pos = i + 5 + wxStrlen(env);
-    return TRUE;
+    return true;
   }
-  else return FALSE;
+  else return false;
 }
 
-bool readingVerbatim = FALSE;
-bool readInVerbatim = FALSE;  // Within a verbatim, but not nec. verbatiminput
+bool readingVerbatim = false;
+bool readInVerbatim = false;  // Within a verbatim, but not nec. verbatiminput
 
@@ -438,3 +438,3 @@
     buf[0] = 0;
-    return FALSE;
+    return false;
   }
@@ -453,3 +453,3 @@
        OnError((wxChar *)errBuf.c_str());
-       return FALSE;
+       return false;
     }
@@ -458,3 +458,3 @@
          ((bufIndex == 16) && (wxStrncmp(buf, _T("\\end{toocomplex}"), 16) == 0)))
-      readInVerbatim = FALSE;
+      readInVerbatim = false;
 
@@ -503,3 +503,3 @@
              OnError((wxChar *)errBuf.c_str());
-             return FALSE;
+             return false;
           }
@@ -518,3 +518,3 @@
              OnError((wxChar *)errBuf.c_str());
-             return FALSE;
+             return false;
           }
@@ -543,3 +543,3 @@
                    OnError((wxChar *)errBuf.c_str());
-                   return FALSE;
+                   return false;
                 }
@@ -566,3 +566,3 @@
               OnError((wxChar *)errBuf.c_str());
-              return FALSE;
+              return false;
             }
@@ -581,3 +581,3 @@
               OnError((wxChar *)errBuf.c_str());
-              return FALSE;
+              return false;
             }
@@ -653,6 +653,6 @@
       {
-        readingVerbatim = FALSE;
-        readInVerbatim = FALSE;
+        readingVerbatim = false;
+        readInVerbatim = false;
         wxStrcat(buf, _T("\\end{verbatim}\n"));
-        return FALSE;
+        return false;
       }
@@ -718,9 +718,9 @@
       {
-        readingVerbatim = TRUE;
-        readInVerbatim = TRUE;
+        readingVerbatim = true;
+        readInVerbatim = true;
         wxStrcpy(buf, _T("\\begin{verbatim}\n"));
-        return FALSE;
+        return false;
       }
     }
-    return FALSE;
+    return false;
   }
@@ -843,6 +843,6 @@
       wxStrncmp(buf, _T("\\begin{toocomplex}"), 18) == 0)
-    readInVerbatim = TRUE;
+    readInVerbatim = true;
   else if (wxStrncmp(buf, _T("\\end{verbatim}"), 14) == 0 ||
            wxStrncmp(buf, _T("\\end{toocomplex}"), 16) == 0)
-    readInVerbatim = FALSE;
+    readInVerbatim = false;
 
@@ -896,3 +896,3 @@
     }
-    bool end = FALSE;
+    bool end = false;
     int braceCount = 0;
@@ -907,3 +907,3 @@
         if (braceCount == 0)
-          end = TRUE;
+          end = true;
       }
@@ -930,5 +930,5 @@
     }
-    return TRUE;
+    return true;
   }
-  else return FALSE;
+  else return false;
 }
@@ -958,3 +958,3 @@
       OnInform( _T("Halted build due to unrecoverable error.") );
-      stopRunning = TRUE;
+      stopRunning = true;
   }
@@ -975,3 +975,3 @@
   
-  bool eof = FALSE;
+  bool eof = false;
   BigBuffer[0] = 0;
@@ -994,3 +994,3 @@
     {
-      isOptional = TRUE;
+      isOptional = true;
       pos ++;
@@ -999,3 +999,3 @@
     {
-      isOptional = TRUE;
+      isOptional = true;
       pos += 2;
@@ -1150,3 +1150,3 @@
           int i = 0;
-          bool end = FALSE;
+          bool end = false;
           while (!end)
@@ -1160,3 +1160,3 @@
                 wxTex2RTFBuffer[i] = 0;
-                end = TRUE;
+                end = true;
               }
@@ -1193,3 +1193,3 @@
               if (ch == 0)
-                end = TRUE;
+                end = true;
             }
@@ -1258,3 +1258,3 @@
           wxChar *env = NULL;
-          bool tmpParseToBrace = TRUE;
+          bool tmpParseToBrace = true;
           TexMacroDef *def = MatchMacro(buffer, &pos, &env, &tmpParseToBrace);
@@ -1294,3 +1294,3 @@
               wxStrcat(macroBuf, _T("}"));
-              ParseArg(thisArg, children, macroBuf, 0, NULL, TRUE, chunk);
+              ParseArg(thisArg, children, macroBuf, 0, NULL, true, chunk);
             }
@@ -1339,3 +1339,3 @@
             pos = ParseMacroBody(def->name, chunk, chunk->no_args,
-                       buffer, pos, NULL, TRUE, customMacroArgs);
+                       buffer, pos, NULL, true, customMacroArgs);
 
@@ -1350,3 +1350,3 @@
                 wxStrcat(macroBuf, _T("}"));
-                ParseArg(thisArg, children, macroBuf, 0, NULL, TRUE, chunk);
+                ParseArg(thisArg, children, macroBuf, 0, NULL, true, chunk);
               }
@@ -1370,3 +1370,3 @@
           {
-            SoloBlockDef = new TexMacroDef(ltSOLO_BLOCK, _T("solo block"), 1, FALSE);
+            SoloBlockDef = new TexMacroDef(ltSOLO_BLOCK, _T("solo block"), 1, false);
           }
@@ -1394,3 +1394,3 @@
 
-          pos = ParseArg(arg, arg->children, buffer, pos, NULL, TRUE, customMacroArgs);
+          pos = ParseArg(arg, arg->children, buffer, pos, NULL, true, customMacroArgs);
     }
@@ -1600,3 +1600,3 @@
 
-    bool isOptional = FALSE;
+    bool isOptional = false;
 
@@ -1614,3 +1614,3 @@
       {
-        isOptional = TRUE;
+        isOptional = true;
         pos ++;
@@ -1619,3 +1619,3 @@
       {
-        isOptional = TRUE;
+        isOptional = true;
         pos += 2;
@@ -1665,3 +1665,3 @@
   static wxChar *line_buffer;
-  stopRunning = FALSE;
+  stopRunning = false;
   wxStrcpy(TexFileRoot, filename);
@@ -1684,8 +1684,8 @@
     read_a_line(line_buffer);
-    ParseMacroBody(_T("toplevel"), TopLevel, 1, line_buffer, 0, NULL, TRUE);
+    ParseMacroBody(_T("toplevel"), TopLevel, 1, line_buffer, 0, NULL, true);
     if (Inputs[0]) fclose(Inputs[0]);
-    return TRUE;
+    return true;
   }
 
-  return FALSE;
+  return false;
 }
@@ -1715,3 +1715,3 @@
   value = NULL;
-  optional = FALSE;
+  optional = false;
 }
@@ -1827,3 +1827,3 @@
   GetArgData1(currentArgument);
-  haveArgData = FALSE;
+  haveArgData = false;
   return currentArgData;
@@ -1881,3 +1881,3 @@
       if (!childrenOnly)
-        OnMacro(chunk->macroId, chunk->no_args, TRUE);
+        OnMacro(chunk->macroId, chunk->no_args, true);
 
@@ -1895,3 +1895,3 @@
       if (!childrenOnly)
-        OnMacro(chunk->macroId, chunk->no_args, FALSE);
+        OnMacro(chunk->macroId, chunk->no_args, false);
       break;
@@ -1905,5 +1905,5 @@
 
-      // If OnArgument returns FALSE, don't output.
+      // If OnArgument returns false, don't output.
 
-      if (childrenOnly || OnArgument(chunk->macroId, chunk->argn, TRUE))
+      if (childrenOnly || OnArgument(chunk->macroId, chunk->argn, true))
       {
@@ -1927,3 +1927,3 @@
       if (!childrenOnly)
-        (void)OnArgument(chunk->macroId, chunk->argn, FALSE);
+        (void)OnArgument(chunk->macroId, chunk->argn, false);
       break;
@@ -1940,5 +1940,5 @@
         {
-          issuedNewParagraph = FALSE;
+          issuedNewParagraph = false;
         }
-        TexOutput(chunk->value, TRUE);
+        TexOutput(chunk->value, true);
       }
@@ -2017,3 +2017,3 @@
   CurrentInputIndex = 0;
-  haveArgData = FALSE;
+  haveArgData = false;
   noArgs = 0;
@@ -2155,3 +2155,3 @@
   AddMacroDef(ltCOLUMNSEP,           _T("columnsep"), 1);
-  AddMacroDef(ltCOMMENT,             _T("comment"), 1, TRUE);
+  AddMacroDef(ltCOMMENT,             _T("comment"), 1, true);
   AddMacroDef(ltCONG,                _T("cong"), 0);
@@ -2223,3 +2223,3 @@
   AddMacroDef(ltHELPGLOSSARY,        _T("helpglossary"), 1);
-  AddMacroDef(ltHELPIGNORE,          _T("helpignore"), 1, TRUE);
+  AddMacroDef(ltHELPIGNORE,          _T("helpignore"), 1, true);
   AddMacroDef(ltHELPONLY,            _T("helponly"), 1);
@@ -2248,3 +2248,3 @@
   AddMacroDef(ltINDEX,               _T("index"), 1);
-  AddMacroDef(ltINPUT,               _T("input"), 1, TRUE);
+  AddMacroDef(ltINPUT,               _T("input"), 1, true);
   AddMacroDef(ltIOTA,                _T("iota"), 0);
@@ -2316,3 +2316,3 @@
   AddMacroDef(ltNEQ,                 _T("neq"), 0);
-  AddMacroDef(ltNEWCOUNTER,          _T("newcounter"), 1, FALSE, (bool)FORBID_ABSOLUTELY);
+  AddMacroDef(ltNEWCOUNTER,          _T("newcounter"), 1, false, (bool)FORBID_ABSOLUTELY);
   AddMacroDef(ltNEWLINE,             _T("newline"), 0);
@@ -2369,4 +2369,4 @@
   AddMacroDef(ltPROPTO,              _T("propto"), 0);
-  AddMacroDef(ltPSBOXTO,             _T("psboxto"), 1, FALSE, (bool)FORBID_ABSOLUTELY);
-  AddMacroDef(ltPSBOX,               _T("psbox"), 1, FALSE, (bool)FORBID_ABSOLUTELY);
+  AddMacroDef(ltPSBOXTO,             _T("psboxto"), 1, false, (bool)FORBID_ABSOLUTELY);
+  AddMacroDef(ltPSBOX,               _T("psbox"), 1, false, (bool)FORBID_ABSOLUTELY);
   AddMacroDef(ltPSI,                 _T("psi"), 0);
@@ -2441,3 +2441,3 @@
   AddMacroDef(ltSUBSUBSECTION,       _T("subsubsection"), 1);
-  AddMacroDef(ltSUPERTABULAR,        _T("supertabular"), 2, FALSE);
+  AddMacroDef(ltSUPERTABULAR,        _T("supertabular"), 2, false);
   AddMacroDef(ltSURD,                _T("surd"), 0);
@@ -2455,3 +2455,3 @@
   AddMacroDef(ltTABLE,               _T("table"), 1);
-  AddMacroDef(ltTABULAR,             _T("tabular"), 2, FALSE);
+  AddMacroDef(ltTABULAR,             _T("tabular"), 2, false);
   AddMacroDef(ltTAB,                 _T("tab"), 0);
@@ -2582,4 +2582,4 @@
         FakeCurrentSection(AbstractNameString);
-        OnMacro(ltPAR, 0, TRUE);
-        OnMacro(ltPAR, 0, FALSE);
+        OnMacro(ltPAR, 0, true);
+        OnMacro(ltPAR, 0, false);
       }
@@ -2602,8 +2602,8 @@
         FakeCurrentSection(GlossaryNameString);
-        OnMacro(ltPAR, 0, TRUE);
-        OnMacro(ltPAR, 0, FALSE);
+        OnMacro(ltPAR, 0, true);
+        OnMacro(ltPAR, 0, false);
         if ((convertMode == TEX_RTF) && !winHelp)
         {
-          OnMacro(ltPAR, 0, TRUE);
-          OnMacro(ltPAR, 0, FALSE);
+          OnMacro(ltPAR, 0, true);
+          OnMacro(ltPAR, 0, false);
         }
@@ -2619,3 +2619,3 @@
       if (start)
-        TexOutput(_T("<<"), TRUE);
+        TexOutput(_T("<<"), true);
       break;
@@ -2623,3 +2623,3 @@
       if (start)
-        TexOutput(_T(">>"), TRUE);
+        TexOutput(_T(">>"), true);
       break;
@@ -2627,3 +2627,3 @@
       if (start)
-        TexOutput(_T("~"), TRUE);
+        TexOutput(_T("~"), true);
       break;
@@ -2631,3 +2631,3 @@
       if (start)
-        TexOutput(_T("~"), TRUE);
+        TexOutput(_T("~"), true);
       break;
@@ -2635,3 +2635,3 @@
       if (start)
-        TexOutput(_T(" "), TRUE);
+        TexOutput(_T(" "), true);
       break;
@@ -2639,3 +2639,3 @@
       if (start)
-        TexOutput(_T("_"), TRUE);
+        TexOutput(_T("_"), true);
       break;
@@ -2643,3 +2643,3 @@
       if (start)
-        TexOutput(_T("#"), TRUE);
+        TexOutput(_T("#"), true);
       break;
@@ -2647,3 +2647,3 @@
       if (start)
-        TexOutput(_T("&"), TRUE);
+        TexOutput(_T("&"), true);
       break;
@@ -2651,3 +2651,3 @@
       if (start)
-        TexOutput(_T(" "), TRUE);
+        TexOutput(_T(" "), true);
       break;
@@ -2655,3 +2655,3 @@
       if (start)
-        TexOutput(_T("|"), TRUE);
+        TexOutput(_T("|"), true);
       break;
@@ -2659,3 +2659,3 @@
       if (start)
-        TexOutput(_T("%"), TRUE);
+        TexOutput(_T("%"), true);
       break;
@@ -2663,3 +2663,3 @@
       if (start)
-        TexOutput(_T("$"), TRUE);
+        TexOutput(_T("$"), true);
       break;
@@ -2667,3 +2667,3 @@
       if (start)
-        TexOutput(_T(""), TRUE);
+        TexOutput(_T(""), true);
       break;
@@ -2671,3 +2671,3 @@
       if (start)
-        TexOutput(_T(""), TRUE);
+        TexOutput(_T(""), true);
       break;
@@ -2675,3 +2675,3 @@
       if (start)
-        TexOutput(_T("{"), TRUE);
+        TexOutput(_T("{"), true);
       break;
@@ -2679,3 +2679,3 @@
       if (start)
-        TexOutput(_T("}"), TRUE);
+        TexOutput(_T("}"), true);
       break;
@@ -2683,3 +2683,3 @@
       if (start)
-        TexOutput(_T("(c)"), TRUE);
+        TexOutput(_T("(c)"), true);
       break;
@@ -2687,3 +2687,3 @@
       if (start)
-        TexOutput(_T("(r)"), TRUE);
+        TexOutput(_T("(r)"), true);
       break;
@@ -2691,3 +2691,3 @@
       if (start)    
-        TexOutput(_T("\\"), TRUE);
+        TexOutput(_T("\\"), true);
       break;
@@ -2696,3 +2696,3 @@
       if (start)
-        TexOutput(_T("..."), TRUE);
+        TexOutput(_T("..."), true);
       break;
@@ -2700,3 +2700,3 @@
       if (start)
-        TexOutput(_T("|"), TRUE);
+        TexOutput(_T("|"), true);
       break;
@@ -2704,3 +2704,3 @@
       if (start)
-        TexOutput(_T("LaTeX"), TRUE);
+        TexOutput(_T("LaTeX"), true);
       break;
@@ -2708,3 +2708,3 @@
       if (start)
-        TexOutput(_T("TeX"), TRUE);
+        TexOutput(_T("TeX"), true);
       break;
@@ -2712,3 +2712,3 @@
       if (start)
-        TexOutput(_T("£"), TRUE);
+        TexOutput(_T("£"), true);
       break;
@@ -2735,3 +2735,3 @@
         (void) time(&when);
-        TexOutput(wxCtime(&when), TRUE);
+        TexOutput(wxCtime(&when), true);
       }
@@ -3168,3 +3168,3 @@
           }
-          TexOutput(texRef->sectionNumber + i, TRUE);
+          TexOutput(texRef->sectionNumber + i, true);
         }
@@ -3177,4 +3177,4 @@
       }
-      else TexOutput(_T("??"), TRUE);
-      return FALSE;
+      else TexOutput(_T("??"), true);
+      return false;
     }
@@ -3184,3 +3184,3 @@
     {
-      return FALSE;
+      return false;
     }
@@ -3190,3 +3190,3 @@
         DocumentAuthor = GetArgChunk();
-      return FALSE;
+      return false;
     }
@@ -3196,3 +3196,3 @@
         DocumentDate = GetArgChunk();
-      return FALSE;
+      return false;
     }
@@ -3202,3 +3202,3 @@
         DocumentTitle = GetArgChunk();
-      return FALSE;
+      return false;
     }
@@ -3229,9 +3229,9 @@
 
-      OnMacro(ltHELPFONTSIZE, 1, TRUE);
+      OnMacro(ltHELPFONTSIZE, 1, true);
       wxSprintf(currentArgData, _T("%d"), normalFont);
-      haveArgData = TRUE;
-      OnArgument(ltHELPFONTSIZE, 1, TRUE);
-      OnArgument(ltHELPFONTSIZE, 1, FALSE);
-      haveArgData = FALSE;
-      OnMacro(ltHELPFONTSIZE, 1, FALSE);
+      haveArgData = true;
+      OnArgument(ltHELPFONTSIZE, 1, true);
+      OnArgument(ltHELPFONTSIZE, 1, false);
+      haveArgData = false;
+      OnMacro(ltHELPFONTSIZE, 1, false);
     }
@@ -3248,3 +3248,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -3254,3 +3254,3 @@
       BibliographyStyleString = copystring(GetArgData());
-    return FALSE;
+    return false;
   }
@@ -3263,3 +3263,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -3270,3 +3270,3 @@
       LeftHeader = GetArgChunk();
-    return FALSE;
+    return false;
     break;
@@ -3277,3 +3277,3 @@
       LeftFooter = GetArgChunk();
-    return FALSE;
+    return false;
     break;
@@ -3284,3 +3284,3 @@
       CentreHeader = GetArgChunk();
-    return FALSE;
+    return false;
     break;
@@ -3291,3 +3291,3 @@
       CentreFooter = GetArgChunk();
-    return FALSE;
+    return false;
     break;
@@ -3298,3 +3298,3 @@
       RightHeader = GetArgChunk();
-    return FALSE;
+    return false;
     break;
@@ -3305,3 +3305,3 @@
       RightFooter = GetArgChunk();
-    return FALSE;
+    return false;
     break;
@@ -3323,3 +3323,3 @@
         {
-          TexOutput(ref->sectionNumber, TRUE);
+          TexOutput(ref->sectionNumber, true);
           if (wxStrcmp(ref->sectionNumber, _T("??")) == 0)
@@ -3334,6 +3334,6 @@
         {
-          TexOutput(_T(", "), TRUE);
+          TexOutput(_T(", "), true);
         }
       }
-      return FALSE;
+      return false;
     }
@@ -3347,3 +3347,3 @@
       AddCitation(citeKey);
-      return FALSE;
+      return false;
     }
@@ -3362,3 +3362,3 @@
         SetFontSizes(12);
-      return FALSE;
+      return false;
     }
@@ -3370,4 +3370,4 @@
     {
-      TexOutput(_T(" ??"), TRUE);
-      return FALSE;
+      TexOutput(_T(" ??"), true);
+      return false;
     }
@@ -3381,3 +3381,3 @@
       ParSkip = ParseUnitArgument(data);
-      return FALSE;
+      return false;
     }
@@ -3391,3 +3391,3 @@
       ParIndent = ParseUnitArgument(data);
-      return FALSE;
+      return false;
     }
@@ -3419,5 +3419,5 @@
     if (start)
-      TexOutput(_T(" ("), TRUE);
+      TexOutput(_T(" ("), true);
     else
-      TexOutput(_T(")"), TRUE);
+      TexOutput(_T(")"), true);
     break;
@@ -3496,3 +3496,3 @@
       SetCurrentOutputs(save1, save2);
-      return FALSE;
+      return false;
     }
@@ -3517,8 +3517,8 @@
         delete[] s1;
-        return FALSE;
+        return false;
       }
-      else return TRUE;
+      else return true;
 
     }
-    return TRUE;
+    return true;
   }
@@ -3537,8 +3537,8 @@
         delete[] s1;
-        return FALSE;
+        return false;
       }
-      else return TRUE;
+      else return true;
 
     }
-    return TRUE;
+    return true;
   }
@@ -3557,8 +3557,8 @@
         delete[] s1;
-        return FALSE;
+        return false;
       }
-      else return TRUE;
+      else return true;
 
     }
-    return TRUE;
+    return true;
   }
@@ -3567,23 +3567,23 @@
   case ltTWOCOLUMN:
-    return TRUE;
+    return true;
   case ltXLPIGNORE:
-    return ((convertMode == TEX_XLP) ? FALSE : TRUE);
+    return ((convertMode == TEX_XLP) ? false : true);
   case ltXLPONLY:
-    return ((convertMode != TEX_XLP) ? FALSE : TRUE);
+    return ((convertMode != TEX_XLP) ? false : true);
   case ltHTMLIGNORE:
-    return ((convertMode == TEX_HTML) ? FALSE : TRUE);
+    return ((convertMode == TEX_HTML) ? false : true);
   case ltHTMLONLY:
-    return ((convertMode != TEX_HTML) ? FALSE : TRUE);
+    return ((convertMode != TEX_HTML) ? false : true);
   case ltRTFIGNORE:
-    return (((convertMode == TEX_RTF) && !winHelp) ? FALSE : TRUE);
+    return (((convertMode == TEX_RTF) && !winHelp) ? false : true);
   case ltRTFONLY:
-    return (!((convertMode == TEX_RTF) && !winHelp) ? FALSE : TRUE);
+    return (!((convertMode == TEX_RTF) && !winHelp) ? false : true);
   case ltWINHELPIGNORE:
-    return (winHelp ? FALSE : TRUE);
+    return (winHelp ? false : true);
   case ltWINHELPONLY:
-    return (!winHelp ? FALSE : TRUE);
+    return (!winHelp ? false : true);
   case ltLATEXIGNORE:
-    return TRUE;
+    return true;
   case ltLATEXONLY:
-    return FALSE;
+    return false;
   case ltCLINE:
@@ -3657,3 +3657,3 @@
   case ltINSERTATLEVEL:
-    return FALSE;
+    return false;
   case ltTABULAR:
@@ -3701,3 +3701,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -3710,3 +3710,3 @@
   }
-  return TRUE;
+  return true;
 }
diff -bu1 -r wxWindows/utils/tex2rtf/src/tex2any.h wxWin/utils/tex2rtf/src/tex2any.h
--- wxWindows/utils/tex2rtf/src/tex2any.h	2004-01-08 12:54:29.000000000 +0100
+++ wxWin/utils/tex2rtf/src/tex2any.h	2004-03-04 19:19:56.000000000 +0100
@@ -4,3 +4,3 @@
 // Author:      Julian Smart
-// Modified by:
+// Modified by: Wlodzimiez ABX Skiba 2003/2004 Unicode support
 // Created:     7.9.93
@@ -149,8 +149,8 @@
 int ParseArg(TexChunk *thisArg, wxList& children, wxChar *buffer, int pos,
-           wxChar *environment = NULL, bool parseArgToBrace = TRUE, TexChunk *customMacroArgs = NULL);
+           wxChar *environment = NULL, bool parseArgToBrace = true, TexChunk *customMacroArgs = NULL);
 int ParseMacroBody(const wxChar *macro_name, TexChunk *parent, int no_args,
-           wxChar *buffer, int pos, wxChar *environment = NULL, bool parseArgToBrace = TRUE, TexChunk *customMacroArgs = NULL);
+           wxChar *buffer, int pos, wxChar *environment = NULL, bool parseArgToBrace = true, TexChunk *customMacroArgs = NULL);
 void TraverseDocument(void);
-void TraverseFromChunk(TexChunk *chunk, wxNode *thisNode = NULL, bool childrenOnly = FALSE);
-#define TraverseChildrenFromChunk(arg) TraverseFromChunk(arg, NULL, TRUE)
+void TraverseFromChunk(TexChunk *chunk, wxNode *thisNode = NULL, bool childrenOnly = false);
+#define TraverseChildrenFromChunk(arg) TraverseFromChunk(arg, NULL, true)
 void SetCurrentOutput(FILE *fd);
@@ -159,6 +159,6 @@
 extern FILE *CurrentOutput2;
-void AddMacroDef(int the_id, const wxChar *name, int n, bool ignore = FALSE, bool forbidden = FALSE);
+void AddMacroDef(int the_id, const wxChar *name, int n, bool ignore = false, bool forbidden = false);
 void TexInitialize(int bufSize);
 void TexCleanUp(void);
-void TexOutput(const wxChar *s, bool ordinaryText = FALSE);
+void TexOutput(const wxChar *s, bool ordinaryText = false);
 wxChar *GetArgData(TexChunk *chunk);
@@ -176,3 +176,3 @@
 #if !WXWIN_COMPATIBILITY_2
-extern bool StringMatch(const wxChar *one, const wxChar *two, bool subString = TRUE, bool exact = FALSE);
+extern bool StringMatch(const wxChar *one, const wxChar *two, bool subString = true, bool exact = false);
 #endif
@@ -180,3 +180,3 @@
 // Define a variable value from the .ini file
-wxChar *RegisterSetting(wxChar *settingName, wxChar *settingValue, bool interactive = TRUE);
+wxChar *RegisterSetting(wxChar *settingName, wxChar *settingValue, bool interactive = true);
 
@@ -326,3 +326,3 @@
 // Called by Tex2Any to simulate a section
-extern void FakeCurrentSection(wxChar *fakeSection, bool addToContents = TRUE);
+extern void FakeCurrentSection(wxChar *fakeSection, bool addToContents = true);
 
@@ -364,3 +364,3 @@
 // Special yield wrapper
-void Tex2RTFYield(bool force = FALSE);
+void Tex2RTFYield(bool force = false);
 
diff -bu1 -r wxWindows/utils/tex2rtf/src/tex2rtf.cpp wxWin/utils/tex2rtf/src/tex2rtf.cpp
--- wxWindows/utils/tex2rtf/src/tex2rtf.cpp	2004-01-08 12:54:29.000000000 +0100
+++ wxWin/utils/tex2rtf/src/tex2rtf.cpp	2004-03-04 19:21:04.000000000 +0100
@@ -4,3 +4,3 @@
 // Author:      Julian Smart
-// Modified by:
+// Modified by: Wlodzimiez ABX Skiba 2003/2004 Unicode support
 // Created:     7.9.93
@@ -36,3 +36,2 @@
 
-#if defined(NO_GUI) || defined(__UNIX__)
 #if wxUSE_IOSTREAMH
@@ -44,3 +43,2 @@
 #endif
-#endif
 
@@ -65,6 +63,6 @@
 TexChunk *currentMember = NULL;
-bool startedSections = FALSE;
+bool startedSections = false;
 wxChar *contentsString = NULL;
-bool suppressNameDecoration = FALSE;
-bool OkToClose = TRUE;
+bool suppressNameDecoration = false;
+bool OkToClose = true;
 int passNumber = 1;
@@ -129,10 +127,9 @@
 #ifdef NO_GUI
-
-int main(int argc, char **argv)
+    IMPLEMENT_APP_CONSOLE(MyApp)
 #else
-wxMenuBar *menuBar = NULL;
-MyFrame *frame = NULL;
-
-// DECLARE_APP(MyApp)
-IMPLEMENT_APP(MyApp)
+    wxMenuBar *menuBar = NULL;
+    MyFrame *frame = NULL;
+    // DECLARE_APP(MyApp)
+    IMPLEMENT_APP(MyApp)
+#endif
 
@@ -140,3 +137,2 @@
 bool MyApp::OnInit()
-#endif
 {
@@ -153,3 +149,3 @@
 
-  ColourTable.DeleteContents(TRUE);
+  ColourTable.DeleteContents(true);
 
@@ -183,4 +179,4 @@
   }
-
 #endif
+
   if (InputFile)
@@ -190,3 +186,3 @@
   if (!InputFile || !OutputFile)
-    isInteractive = TRUE;
+    isInteractive = true;
 
@@ -199,3 +195,3 @@
       convertMode = TEX_RTF;
-      winHelp = TRUE;
+      winHelp = true;
     }
@@ -205,3 +201,3 @@
       i ++;
-      isInteractive = TRUE;
+      isInteractive = true;
     }
@@ -211,3 +207,3 @@
       i ++;
-      isSync = TRUE;
+      isSync = true;
     }
@@ -231,3 +227,3 @@
       i ++;
-      runTwice = TRUE;
+      runTwice = true;
     }
@@ -264,3 +260,3 @@
           OnError(_T("Incorrect argument for -charset"));
-          return FALSE;
+          return false;
         }
@@ -271,3 +267,3 @@
       i ++;
-      checkCurleyBraces = TRUE;
+      checkCurleyBraces = true;
     }
@@ -276,3 +272,3 @@
       i ++;
-      checkSyntax = TRUE;
+      checkSyntax = true;
     }
@@ -287,3 +283,3 @@
 #endif
-      return FALSE;
+      return false;
     }
@@ -313,3 +309,3 @@
     // Create the main frame window
-    frame = new MyFrame(NULL, -1, _T("Tex2RTF"), wxPoint(-1, -1), wxSize(400, 300));
+    frame = new MyFrame(NULL, wxID_ANY, _T("Tex2RTF"), wxDefaultPosition, wxSize(400, 300));
     frame->CreateStatusBar(2);
@@ -352,4 +348,4 @@
 
-    options_menu->Append(TEX_OPTIONS_CURELY_BRACE, _T("Curley brace matching"), _T("Checks for mismatched curley braces"),TRUE);
-    options_menu->Append(TEX_OPTIONS_SYNTAX_CHECKING, _T("Syntax checking"), _T("Syntax checking for common errors"),TRUE);
+    options_menu->Append(TEX_OPTIONS_CURELY_BRACE, _T("Curley brace matching"), _T("Checks for mismatched curley braces"),true);
+    options_menu->Append(TEX_OPTIONS_SYNTAX_CHECKING, _T("Syntax checking"), _T("Syntax checking for common errors"),true);
 
@@ -371,3 +367,3 @@
     frame->SetMenuBar(menuBar);
-    frame->textWindow = new wxTextCtrl(frame, -1, _T(""), wxPoint(-1, -1), wxSize(-1, -1), wxTE_READONLY|wxTE_MULTILINE);
+    frame->textWindow = new wxTextCtrl(frame, wxID_ANY, _T(""), wxDefaultPosition, wxDefaultSize, wxTE_READONLY|wxTE_MULTILINE);
 
@@ -401,4 +397,4 @@
 
-    frame->Show(TRUE);
-    return TRUE;
+    frame->Show(true);
+    return true;
   }
@@ -424,3 +420,3 @@
 #else
-    return FALSE;
+    return false;
 #endif
@@ -432,3 +428,3 @@
   // Return the main frame window
-  return TRUE;
+  return true;
 #else
@@ -627,4 +623,4 @@
   {
-    stopRunning = TRUE;
-    runTwice = FALSE;
+    stopRunning = true;
+    runTwice = false;
     return;
@@ -647,8 +643,8 @@
       errorCount = 0;
-      menuBar->EnableTop(0, FALSE);
-      menuBar->EnableTop(1, FALSE);
-      menuBar->EnableTop(2, FALSE);
-      menuBar->EnableTop(3, FALSE);
+      menuBar->EnableTop(0, false);
+      menuBar->EnableTop(1, false);
+      menuBar->EnableTop(2, false);
+      menuBar->EnableTop(3, false);
       textWindow->Clear();
-      Tex2RTFYield(TRUE);
+      Tex2RTFYield(true);
       Go();
@@ -666,9 +662,9 @@
       {
-        Tex2RTFYield(TRUE);
+        Tex2RTFYield(true);
         Go();
       }
-      menuBar->EnableTop(0, TRUE);
-      menuBar->EnableTop(1, TRUE);
-      menuBar->EnableTop(2, TRUE);
-      menuBar->EnableTop(3, TRUE);
+      menuBar->EnableTop(0, true);
+      menuBar->EnableTop(1, true);
+      menuBar->EnableTop(2, true);
+      menuBar->EnableTop(3, true);
 }
@@ -677,3 +673,3 @@
 {
-      ChooseInputFile(TRUE);
+      ChooseInputFile(true);
 }
@@ -682,3 +678,3 @@
 {
-      ChooseOutputFile(TRUE);
+      ChooseOutputFile(true);
 }
@@ -738,3 +734,3 @@
       textWindow->Clear();
-      Tex2RTFYield(TRUE);
+      Tex2RTFYield(true);
       ShowCustomMacros();
@@ -745,3 +741,3 @@
       convertMode = TEX_RTF;
-      winHelp = FALSE;
+      winHelp = false;
       InputFile = NULL;
@@ -754,3 +750,3 @@
       convertMode = TEX_RTF;
-      winHelp = TRUE;
+      winHelp = true;
       InputFile = NULL;
@@ -763,3 +759,3 @@
       convertMode = TEX_HTML;
-      winHelp = FALSE;
+      winHelp = false;
       InputFile = NULL;
@@ -907,3 +903,3 @@
   if (!InputFile || !OutputFile || stopRunning)
-    return FALSE;
+    return false;
 
@@ -958,3 +954,3 @@
 
-  bool success = FALSE;
+  bool success = false;
 
@@ -966,3 +962,3 @@
       TexCleanUp();
-      return FALSE;
+      return false;
     }
@@ -976,3 +972,3 @@
 #endif
-    OkToClose = FALSE;
+    OkToClose = false;
     OnInform(_T("Reading LaTeX file..."));
@@ -982,4 +978,4 @@
     {
-        OkToClose = TRUE;
-        return FALSE;
+        OkToClose = true;
+        return false;
     }
@@ -1008,5 +1004,5 @@
     OnInform(_T("*** Aborted by user."));
-    success = FALSE;
-    stopRunning = FALSE;
-    OkToClose = TRUE;
+    success = false;
+    stopRunning = false;
+    OkToClose = true;
   }
@@ -1017,3 +1013,3 @@
     TexCleanUp();
-    startedSections = FALSE;
+    startedSections = false;
 
@@ -1047,4 +1043,4 @@
     errorCount = 0;
-    OkToClose = TRUE;
-    return TRUE;
+    OkToClose = true;
+    return true;
   }
@@ -1052,3 +1048,3 @@
   TexCleanUp();
-  startedSections = FALSE;
+  startedSections = false;
 
@@ -1059,4 +1055,4 @@
   OnInform(_T("Sorry, unsuccessful."));
-  OkToClose = TRUE;
-  return FALSE;
+  OkToClose = true;
+  return false;
 }
@@ -1065,2 +1061,3 @@
 {
+  wxString msg_string = msg;
   errorCount++;
@@ -1068,3 +1065,3 @@
 #ifdef NO_GUI
-  wxSTD cerr << "Error: " << msg << "\n";
+  wxSTD cerr << "Error: " << msg_string.mb_str() << "\n";
   wxSTD cerr.flush();
@@ -1076,3 +1073,3 @@
   {
-    wxSTD cerr << "Error: " << msg << "\n";
+    wxSTD cerr << "Error: " << msg_string.mb_str() << "\n";
     wxSTD cerr.flush();
@@ -1084,3 +1081,3 @@
 #endif
-  Tex2RTFYield(TRUE);
+  Tex2RTFYield(true);
 #endif // NO_GUI
@@ -1090,4 +1087,5 @@
 {
+  wxString msg_string = msg;
 #ifdef NO_GUI
-  wxSTD cout << msg << "\n";
+  wxSTD cout << msg_string.mb_str() << "\n";
   wxSTD cout.flush();
@@ -1104,3 +1102,3 @@
   {
-    wxSTD cout << msg << "\n";
+    wxSTD cout << msg_string.mb_str() << "\n";
     wxSTD cout.flush();
@@ -1114,3 +1112,3 @@
   {
-    Tex2RTFYield(TRUE);
+    Tex2RTFYield(true);
   }
@@ -1161,3 +1159,3 @@
   }
-  return TRUE;
+  return true;
 }
@@ -1203,3 +1201,3 @@
   int i = 0;
-  bool stop = FALSE;
+  bool stop = false;
   // Find first argument (command name)
@@ -1208,3 +1206,3 @@
     if (data[i] == ' ' || data[i] == 0)
-      stop = TRUE;
+      stop = true;
     else
@@ -1229,3 +1227,3 @@
   }
-  return TRUE;
+  return true;
 }
@@ -1273,3 +1271,3 @@
       if (frame)
-        frame->Iconize(TRUE);
+        frame->Iconize(true);
     }
@@ -1279,4 +1277,4 @@
       {
-        frame->Iconize(FALSE);
-        frame->Show(TRUE);
+        frame->Iconize(false);
+        frame->Show(true);
       }
@@ -1286,3 +1284,3 @@
       // Try for a setting
-      wxStrcpy(Tex2RTFLastStatus, RegisterSetting(firstArg, secondArg, FALSE));
+      wxStrcpy(Tex2RTFLastStatus, RegisterSetting(firstArg, secondArg, false));
 #ifndef NO_GUI
@@ -1305,3 +1303,3 @@
   }
-  return TRUE;
+  return true;
 }
diff -bu1 -r wxWindows/utils/tex2rtf/src/tex2rtf.h wxWin/utils/tex2rtf/src/tex2rtf.h
--- wxWindows/utils/tex2rtf/src/tex2rtf.h	2004-01-08 12:54:29.000000000 +0100
+++ wxWin/utils/tex2rtf/src/tex2rtf.h	2004-03-04 19:19:49.000000000 +0100
@@ -4,3 +4,3 @@
 // Author:      Julian Smart
-// Modified by:
+// Modified by: Wlodzimiez ABX Skiba 2003/2004 Unicode support
 // Created:     7.9.93
@@ -11,15 +11,28 @@
 
-#ifndef NO_GUI
 #include "wx/app.h"
-#include "wx/frame.h"
 
-class WXDLLEXPORT wxTextCtrl;
+#ifndef NO_GUI
+    #include "wx/frame.h"
+    #include "wx/textctrl.h"
+    #include "wx/dde.h"
+#endif
 
 // Define a new application type
-class MyApp: public wxApp
+class MyApp: public 
+                    #ifndef NO_GUI
+                        wxApp
+                    #else
+                        wxAppConsole
+                    #endif
 { public:
     bool OnInit();
+#ifdef NO_GUI
+    int OnRun() { return EXIT_SUCCESS; }
+#else
     int OnExit();
+#endif
 };
 
+#ifndef NO_GUI
+
 // Define a new frame type
@@ -54,3 +67,2 @@
 #ifdef __WXMSW__
-#include "wx/dde.h"
 
@@ -149,4 +161,4 @@
 #ifndef NO_GUI
-void ChooseOutputFile(bool force = FALSE);
-void ChooseInputFile(bool force = FALSE);
+void ChooseOutputFile(bool force = false);
+void ChooseInputFile(bool force = false);
 #endif
diff -bu1 -r wxWindows/utils/tex2rtf/src/texutils.cpp wxWin/utils/tex2rtf/src/texutils.cpp
--- wxWindows/utils/tex2rtf/src/texutils.cpp	2004-01-08 12:54:30.000000000 +0100
+++ wxWin/utils/tex2rtf/src/texutils.cpp	2004-03-04 20:45:59.000000000 +0100
@@ -4,3 +4,3 @@
 // Author:      Julian Smart
-// Modified by:
+// Modified by: Wlodzimiez ABX Skiba 2003/2004 Unicode support
 // Created:     7.9.93
@@ -138,4 +138,4 @@
       mac = ltSECTIONHEADINGSTAR;
-    OnMacro(mac, 0, TRUE);
-    OnMacro(mac, 0, FALSE);
+    OnMacro(mac, 0, true);
+    OnMacro(mac, 0, false);
   }
@@ -146,4 +146,4 @@
       mac = ltCHAPTERHEADINGSTAR;
-    OnMacro(mac, 0, TRUE);
-    OnMacro(mac, 0, FALSE);
+    OnMacro(mac, 0, true);
+    OnMacro(mac, 0, false);
   }
@@ -221,3 +221,3 @@
   wxStrcpy(currentArgData, data);
-  haveArgData = TRUE;
+  haveArgData = true;
 }
@@ -226,3 +226,3 @@
 {
-  haveArgData = FALSE;
+  haveArgData = false;
 }
@@ -396,5 +396,6 @@
 {
-  wxSTD ofstream ostr(filename);
+  wxString converter;
+  wxString name = filename;
+  wxSTD ofstream ostr((char const *)name.fn_str());
   if (ostr.bad()) return;
-  wxChar buf[200];
   
@@ -406,7 +407,17 @@
     TexRef *ref = (TexRef *)node->GetData();
-    ostr << ref->refLabel << _T(" ") << (ref->refFile ? ref->refFile : _T("??")) << _T(" ");
-    ostr << (ref->sectionName ? ref->sectionName : _T("??")) << _T(" ");
-    ostr << (ref->sectionNumber ? ref->sectionNumber : _T("??")) << _T("\n");
+    converter = ref->refLabel;
+    ostr << converter.mb_str();
+    ostr << " ";
+    converter = (ref->refFile ? ref->refFile : _T("??"));
+    ostr << converter.mb_str();
+    ostr << " ";
+    converter = (ref->sectionName ? ref->sectionName : _T("??")) ;
+    ostr << converter.mb_str();
+    ostr << " ";
+    converter = (ref->sectionNumber ? ref->sectionNumber : _T("??")) ;
+    ostr << converter.mb_str();
+    ostr << "\n";
     if (!ref->sectionNumber || (wxStrcmp(ref->sectionNumber, _T("??")) == 0 && wxStrcmp(ref->sectionName, _T("??")) == 0))
     {
+      wxChar buf[200];
       wxSprintf(buf, _T("Warning: reference %s not resolved."), ref->refLabel);
@@ -423,3 +434,4 @@
 
-  wxSTD ifstream istr(filename, wxSTD ios::in);
+  wxString name = filename;
+  wxSTD ifstream istr((char const *)name.fn_str(), wxSTD ios::in);
 
@@ -427,6 +439,6 @@
 
-  wxChar label[100];
-  wxChar file[400];
-  wxChar section[100];
-  wxChar sectionName[100];
+  char label[100];
+  char file[400];
+  char section[100];
+  char sectionName[100];
 
@@ -451,2 +463,7 @@
 
+      wxString label_string       = wxString::FromAscii(label);
+      wxString file_string        = wxString::FromAscii(file);
+      wxString sectionName_string = wxString::FromAscii(sectionName);
+      wxString section_string     = wxString::FromAscii(section);
+
       // gt - needed to trick the hash table "TexReferences" into deleting the key 
@@ -455,5 +472,13 @@
       // were massive memory leaks
-      TexReferences.DeleteContents(TRUE);
-      TexReferences.Put(label, new TexRef(label, file, section, sectionName));
-      TexReferences.DeleteContents(FALSE);
+      TexReferences.DeleteContents(true);
+      TexReferences.Put(
+        label_string.c_str(), 
+        new TexRef(
+              label_string.c_str(), 
+              file_string.c_str(), 
+              section_string.c_str(), 
+              sectionName_string.c_str()
+        )
+      );
+      TexReferences.DeleteContents(false);
     }
@@ -517,5 +542,5 @@
   buffer[i] = 0;
-  wxChar ch = istr.peek();
-  bool inQuotes = FALSE;
-  if (ch == _T('"'))
+  char ch = istr.peek();
+  bool inQuotes = false;
+  if (ch == '"')
   {
@@ -523,3 +548,3 @@
     ch = istr.peek();
-    inQuotes = TRUE;
+    inQuotes = true;
   }
@@ -542,4 +567,4 @@
 // Read }-terminated value, taking nested braces into account.
-void BibReadValue(wxSTD istream& istr, wxChar *buffer, bool ignoreBraces = TRUE,
-                  bool quotesMayTerminate = TRUE)
+void BibReadValue(wxSTD istream& istr, wxChar *buffer, bool ignoreBraces = true,
+                  bool quotesMayTerminate = true)
 {
@@ -549,3 +574,3 @@
   char ch = istr.peek();
-  bool stopping = FALSE;
+  bool stopping = false;
   while (!istr.eof() && !stopping)
@@ -570,3 +595,3 @@
       {
-        stopping = TRUE;
+        stopping = true;
         break;
@@ -576,3 +601,3 @@
     {
-      stopping = TRUE;
+      stopping = true;
       break;
@@ -597,7 +622,8 @@
   if (!wxFileExists(filename))
-      return FALSE;
+      return false;
 
+  wxString name = filename;
   wxChar buf[300];
-  wxSTD ifstream istr(filename, wxSTD ios::in);
-  if (istr.bad()) return FALSE;
+  wxSTD ifstream istr((char const *)name.fn_str(), wxSTD ios::in);
+  if (istr.bad()) return false;
 
@@ -622,3 +648,3 @@
       OnError(buf);
-      return FALSE;
+      return false;
     }
@@ -631,6 +657,6 @@
       OnError(buf);
-      return FALSE;
+      return false;
     }
     BibEatWhiteSpace(istr);
-    if (StringMatch(recordType, _T("string"), FALSE, TRUE))
+    if (StringMatch(recordType, _T("string"), false, true))
     {
@@ -643,3 +669,3 @@
         OnError(buf);
-        return FALSE;
+        return false;
       }
@@ -651,3 +677,3 @@
         OnError(buf);
-        return FALSE;
+        return false;
       }
@@ -672,3 +698,3 @@
 
-      bool moreRecords = TRUE;
+      bool moreRecords = true;
       while (moreRecords && !istr.eof())
@@ -679,3 +705,3 @@
         {
-          moreRecords = FALSE;
+          moreRecords = false;
         }
@@ -691,3 +717,3 @@
             OnError(buf);
-            return FALSE;
+            return false;
           }
@@ -708,56 +734,56 @@
           else
-            BibReadValue(istr, fieldValue, TRUE, (ch == _T('"') ? TRUE : FALSE));
+            BibReadValue(istr, fieldValue, true, (ch == _T('"') ? true : false));
 
           // Now we can add a field
-          if (StringMatch(recordField, _T("author"), FALSE, TRUE))
+          if (StringMatch(recordField, _T("author"), false, true))
             bibEntry->author = copystring(fieldValue);
-          else if (StringMatch(recordField, _T("key"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("key"), false, true))
             {}
-          else if (StringMatch(recordField, _T("annotate"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("annotate"), false, true))
             {}
-          else if (StringMatch(recordField, _T("abstract"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("abstract"), false, true))
             {}
-          else if (StringMatch(recordField, _T("edition"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("edition"), false, true))
             {}
-          else if (StringMatch(recordField, _T("howpublished"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("howpublished"), false, true))
             {}
-          else if (StringMatch(recordField, _T("note"), FALSE, TRUE) || StringMatch(recordField, _T("notes"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("note"), false, true) || StringMatch(recordField, _T("notes"), false, true))
             {}
-          else if (StringMatch(recordField, _T("series"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("series"), false, true))
             {}
-          else if (StringMatch(recordField, _T("type"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("type"), false, true))
             {}
-          else if (StringMatch(recordField, _T("keywords"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("keywords"), false, true))
             {}
-          else if (StringMatch(recordField, _T("editor"), FALSE, TRUE) || StringMatch(recordField, _T("editors"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("editor"), false, true) || StringMatch(recordField, _T("editors"), false, true))
             bibEntry->editor= copystring(fieldValue);
-          else if (StringMatch(recordField, _T("title"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("title"), false, true))
             bibEntry->title= copystring(fieldValue);
-          else if (StringMatch(recordField, _T("booktitle"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("booktitle"), false, true))
             bibEntry->booktitle= copystring(fieldValue);
-          else if (StringMatch(recordField, _T("journal"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("journal"), false, true))
             bibEntry->journal= copystring(fieldValue);
-          else if (StringMatch(recordField, _T("volume"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("volume"), false, true))
             bibEntry->volume= copystring(fieldValue);
-          else if (StringMatch(recordField, _T("number"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("number"), false, true))
             bibEntry->number= copystring(fieldValue);
-          else if (StringMatch(recordField, _T("year"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("year"), false, true))
             bibEntry->year= copystring(fieldValue);
-          else if (StringMatch(recordField, _T("month"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("month"), false, true))
             bibEntry->month= copystring(fieldValue);
-          else if (StringMatch(recordField, _T("pages"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("pages"), false, true))
             bibEntry->pages= copystring(fieldValue);
-          else if (StringMatch(recordField, _T("publisher"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("publisher"), false, true))
             bibEntry->publisher= copystring(fieldValue);
-          else if (StringMatch(recordField, _T("address"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("address"), false, true))
             bibEntry->address= copystring(fieldValue);
-          else if (StringMatch(recordField, _T("institution"), FALSE, TRUE) || StringMatch(recordField, _T("school"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("institution"), false, true) || StringMatch(recordField, _T("school"), false, true))
             bibEntry->institution= copystring(fieldValue);
-          else if (StringMatch(recordField, _T("organization"), FALSE, TRUE) || StringMatch(recordField, _T("organisation"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("organization"), false, true) || StringMatch(recordField, _T("organisation"), false, true))
             bibEntry->organization= copystring(fieldValue);
-          else if (StringMatch(recordField, _T("comment"), FALSE, TRUE) || StringMatch(recordField, _T("comments"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("comment"), false, true) || StringMatch(recordField, _T("comments"), false, true))
             bibEntry->comment= copystring(fieldValue);
-          else if (StringMatch(recordField, _T("annote"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("annote"), false, true))
             bibEntry->comment= copystring(fieldValue);
-          else if (StringMatch(recordField, _T("chapter"), FALSE, TRUE))
+          else if (StringMatch(recordField, _T("chapter"), false, true))
             bibEntry->chapter= copystring(fieldValue);
@@ -774,3 +800,3 @@
   }
-  return TRUE;
+  return true;
 }
@@ -781,15 +807,15 @@
 
-  OnMacro(ltNUMBEREDBIBITEM, 2, TRUE);
-  OnArgument(ltNUMBEREDBIBITEM, 1, TRUE);
+  OnMacro(ltNUMBEREDBIBITEM, 2, true);
+  OnArgument(ltNUMBEREDBIBITEM, 1, true);
   TexOutput(ref->sectionNumber);
-  OnArgument(ltNUMBEREDBIBITEM, 1, FALSE);
-  OnArgument(ltNUMBEREDBIBITEM, 2, TRUE);
+  OnArgument(ltNUMBEREDBIBITEM, 1, false);
+  OnArgument(ltNUMBEREDBIBITEM, 2, true);
 
   TexOutput(_T(" "));
-  OnMacro(ltBF, 1, TRUE);
-  OnArgument(ltBF, 1, TRUE);
+  OnMacro(ltBF, 1, true);
+  OnArgument(ltBF, 1, true);
   if (bib->author)
     TexOutput(bib->author);
-  OnArgument(ltBF, 1, FALSE);
-  OnMacro(ltBF, 1, FALSE);
+  OnArgument(ltBF, 1, false);
+  OnMacro(ltBF, 1, false);
   if (bib->author && (wxStrlen(bib->author) > 0) && (bib->author[wxStrlen(bib->author) - 1] != '.'))
@@ -812,3 +838,3 @@
 
-  if (StringMatch(bib->type, _T("article"), FALSE, TRUE))
+  if (StringMatch(bib->type, _T("article"), false, true))
   {
@@ -821,7 +847,7 @@
     {
-      OnMacro(ltIT, 1, TRUE);
-      OnArgument(ltIT, 1, TRUE);
+      OnMacro(ltIT, 1, true);
+      OnArgument(ltIT, 1, true);
       TexOutput(bib->journal);
-      OnArgument(ltIT, 1, FALSE);
-      OnMacro(ltIT, 1, FALSE);
+      OnArgument(ltIT, 1, false);
+      OnMacro(ltIT, 1, false);
     }
@@ -830,7 +856,7 @@
       TexOutput(_T(", "));
-      OnMacro(ltBF, 1, TRUE);
-      OnArgument(ltBF, 1, TRUE);
+      OnMacro(ltBF, 1, true);
+      OnArgument(ltBF, 1, true);
       TexOutput(bib->volume);
-      OnArgument(ltBF, 1, FALSE);
-      OnMacro(ltBF, 1, FALSE);
+      OnArgument(ltBF, 1, false);
+      OnMacro(ltBF, 1, false);
     }
@@ -849,10 +875,10 @@
   }
-  else if (StringMatch(bib->type, _T("book"), FALSE, TRUE) ||
-           StringMatch(bib->type, _T("unpublished"), FALSE, TRUE) ||
-           StringMatch(bib->type, _T("manual"), FALSE, TRUE) ||
-           StringMatch(bib->type, _T("phdthesis"), FALSE, TRUE) ||
-           StringMatch(bib->type, _T("mastersthesis"), FALSE, TRUE) ||
-           StringMatch(bib->type, _T("misc"), FALSE, TRUE) ||
-           StringMatch(bib->type, _T("techreport"), FALSE, TRUE) ||
-           StringMatch(bib->type, _T("booklet"), FALSE, TRUE))
+  else if (StringMatch(bib->type, _T("book"), false, true) ||
+           StringMatch(bib->type, _T("unpublished"), false, true) ||
+           StringMatch(bib->type, _T("manual"), false, true) ||
+           StringMatch(bib->type, _T("phdthesis"), false, true) ||
+           StringMatch(bib->type, _T("mastersthesis"), false, true) ||
+           StringMatch(bib->type, _T("misc"), false, true) ||
+           StringMatch(bib->type, _T("techreport"), false, true) ||
+           StringMatch(bib->type, _T("booklet"), false, true))
   {
@@ -860,12 +886,12 @@
     {
-      OnMacro(ltIT, 1, TRUE);
-      OnArgument(ltIT, 1, TRUE);
+      OnMacro(ltIT, 1, true);
+      OnArgument(ltIT, 1, true);
       TexOutput(bib->title ? bib->title : bib->booktitle);
       TexOutput(_T(". "));
-      OnArgument(ltIT, 1, FALSE);
-      OnMacro(ltIT, 1, FALSE);
+      OnArgument(ltIT, 1, false);
+      OnMacro(ltIT, 1, false);
     }
-    if (StringMatch(bib->type, _T("phdthesis"), FALSE, TRUE))
+    if (StringMatch(bib->type, _T("phdthesis"), false, true))
       TexOutput(_T("PhD thesis. "));
-    if (StringMatch(bib->type, _T("techreport"), FALSE, TRUE))
+    if (StringMatch(bib->type, _T("techreport"), false, true))
       TexOutput(_T("Technical report. "));
@@ -898,6 +924,6 @@
   }
-  else if (StringMatch(bib->type, _T("inbook"), FALSE, TRUE) ||
-           StringMatch(bib->type, _T("inproceedings"), FALSE, TRUE) ||
-           StringMatch(bib->type, _T("incollection"), FALSE, TRUE) ||
-           StringMatch(bib->type, _T("conference"), FALSE, TRUE))
+  else if (StringMatch(bib->type, _T("inbook"), false, true) ||
+           StringMatch(bib->type, _T("inproceedings"), false, true) ||
+           StringMatch(bib->type, _T("incollection"), false, true) ||
+           StringMatch(bib->type, _T("conference"), false, true))
   {
@@ -910,8 +936,8 @@
       TexOutput(_T(", from "));
-      OnMacro(ltIT, 1, TRUE);
-      OnArgument(ltIT, 1, TRUE);
+      OnMacro(ltIT, 1, true);
+      OnArgument(ltIT, 1, true);
       TexOutput(bib->booktitle);
       TexOutput(_T("."));
-      OnArgument(ltIT, 1, FALSE);
-      OnMacro(ltIT, 1, FALSE);
+      OnArgument(ltIT, 1, false);
+      OnMacro(ltIT, 1, false);
     }
@@ -939,7 +965,7 @@
       TexOutput(_T(" "));
-      OnMacro(ltBF, 1, TRUE);
-      OnArgument(ltBF, 1, TRUE);
+      OnMacro(ltBF, 1, true);
+      OnArgument(ltBF, 1, true);
       TexOutput(bib->volume);
-      OnArgument(ltBF, 1, FALSE);
-      OnMacro(ltBF, 1, FALSE);
+      OnArgument(ltBF, 1, false);
+      OnMacro(ltBF, 1, false);
     }
@@ -973,4 +999,4 @@
   }
-  OnArgument(ltNUMBEREDBIBITEM, 2, FALSE);
-  OnMacro(ltNUMBEREDBIBITEM, 2, FALSE);
+  OnArgument(ltNUMBEREDBIBITEM, 2, false);
+  OnMacro(ltNUMBEREDBIBITEM, 2, false);
 }
@@ -984,4 +1010,4 @@
 
-  OnMacro(ltPAR, 0, TRUE);
-  OnMacro(ltPAR, 0, FALSE);
+  OnMacro(ltPAR, 0, true);
+  OnMacro(ltPAR, 0, false);
 
@@ -989,4 +1015,4 @@
   {
-    OnMacro(ltPAR, 0, TRUE);
-    OnMacro(ltPAR, 0, FALSE);
+    OnMacro(ltPAR, 0, true);
+    OnMacro(ltPAR, 0, false);
   }
@@ -1070,8 +1096,8 @@
       wxStrncmp(val, _T("on"), 2) == 0 || wxStrncmp(val, _T("ON"), 2) == 0 ||
-      wxStrncmp(val, _T("true"), 4) == 0 || wxStrncmp(val, _T("TRUE"), 4) == 0 ||
+      wxStrncmp(val, _T("true"), 4) == 0 || wxStrncmp(val, _T("true"), 4) == 0 ||
       wxStrncmp(val, _T("ok"), 2) == 0 || wxStrncmp(val, _T("OK"), 2) == 0 ||
       wxStrncmp(val, _T("1"), 1) == 0)
-    return TRUE;
+    return true;
   else
-    return FALSE;
+    return false;
 }
@@ -1083,3 +1109,3 @@
   wxStrcpy(errorCode, _T("OK"));
-  if (StringMatch(settingName, _T("chapterName"), FALSE, TRUE))
+  if (StringMatch(settingName, _T("chapterName"), false, true))
   {
@@ -1088,3 +1114,3 @@
   }
-  else if (StringMatch(settingName, _T("sectionName"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("sectionName"), false, true))
   {
@@ -1093,3 +1119,3 @@
   }
-  else if (StringMatch(settingName, _T("subsectionName"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("subsectionName"), false, true))
   {
@@ -1098,3 +1124,3 @@
   }
-  else if (StringMatch(settingName, _T("subsubsectionName"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("subsubsectionName"), false, true))
   {
@@ -1103,3 +1129,3 @@
   }
-  else if (StringMatch(settingName, _T("indexName"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("indexName"), false, true))
   {
@@ -1108,3 +1134,3 @@
   }
-  else if (StringMatch(settingName, _T("contentsName"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("contentsName"), false, true))
   {
@@ -1113,3 +1139,3 @@
   }
-  else if (StringMatch(settingName, _T("glossaryName"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("glossaryName"), false, true))
   {
@@ -1118,3 +1144,3 @@
   }
-  else if (StringMatch(settingName, _T("referencesName"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("referencesName"), false, true))
   {
@@ -1123,3 +1149,3 @@
   }
-  else if (StringMatch(settingName, _T("tablesName"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("tablesName"), false, true))
   {
@@ -1128,3 +1154,3 @@
   }
-  else if (StringMatch(settingName, _T("figuresName"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("figuresName"), false, true))
   {
@@ -1133,3 +1159,3 @@
   }
-  else if (StringMatch(settingName, _T("tableName"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("tableName"), false, true))
   {
@@ -1138,3 +1164,3 @@
   }
-  else if (StringMatch(settingName, _T("figureName"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("figureName"), false, true))
   {
@@ -1143,3 +1169,3 @@
   }
-  else if (StringMatch(settingName, _T("abstractName"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("abstractName"), false, true))
   {
@@ -1148,53 +1174,53 @@
   }
-  else if (StringMatch(settingName, _T("chapterFontSize"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("chapterFontSize"), false, true))
     StringToInt(settingValue, &chapterFont);
-  else if (StringMatch(settingName, _T("sectionFontSize"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("sectionFontSize"), false, true))
     StringToInt(settingValue, &sectionFont);
-  else if (StringMatch(settingName, _T("subsectionFontSize"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("subsectionFontSize"), false, true))
     StringToInt(settingValue, &subsectionFont);
-  else if (StringMatch(settingName, _T("titleFontSize"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("titleFontSize"), false, true))
     StringToInt(settingValue, &titleFont);
-  else if (StringMatch(settingName, _T("authorFontSize"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("authorFontSize"), false, true))
     StringToInt(settingValue, &authorFont);
-  else if (StringMatch(settingName, _T("ignoreInput"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("ignoreInput"), false, true))
     IgnorableInputFiles.Add(wxFileNameFromPath(settingValue));
-  else if (StringMatch(settingName, _T("mirrorMargins"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("mirrorMargins"), false, true))
     mirrorMargins = StringTobool(settingValue);
-  else if (StringMatch(settingName, _T("runTwice"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("runTwice"), false, true))
     runTwice = StringTobool(settingValue);
-  else if (StringMatch(settingName, _T("isInteractive"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("isInteractive"), false, true))
     isInteractive = StringTobool(settingValue);
-  else if (StringMatch(settingName, _T("headerRule"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("headerRule"), false, true))
     headerRule = StringTobool(settingValue);
-  else if (StringMatch(settingName, _T("footerRule"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("footerRule"), false, true))
     footerRule = StringTobool(settingValue);
-  else if (StringMatch(settingName, _T("combineSubSections"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("combineSubSections"), false, true))
     combineSubSections = StringTobool(settingValue);
-  else if (StringMatch(settingName, _T("listLabelIndent"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("listLabelIndent"), false, true))
     StringToInt(settingValue, &labelIndentTab);
-  else if (StringMatch(settingName, _T("listItemIndent"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("listItemIndent"), false, true))
     StringToInt(settingValue, &itemIndentTab);
-  else if (StringMatch(settingName, _T("useUpButton"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("useUpButton"), false, true))
     useUpButton = StringTobool(settingValue);
-  else if (StringMatch(settingName, _T("useHeadingStyles"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("useHeadingStyles"), false, true))
     useHeadingStyles = StringTobool(settingValue);
-  else if (StringMatch(settingName, _T("useWord"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("useWord"), false, true))
     useWord = StringTobool(settingValue);
-  else if (StringMatch(settingName, _T("contentsDepth"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("contentsDepth"), false, true))
     StringToInt(settingValue, &contentsDepth);
-  else if (StringMatch(settingName, _T("generateHPJ"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("generateHPJ"), false, true))
     generateHPJ = StringTobool(settingValue);
-  else if (StringMatch(settingName, _T("truncateFilenames"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("truncateFilenames"), false, true))
     truncateFilenames = StringTobool(settingValue);
-  else if (StringMatch(settingName, _T("winHelpVersion"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("winHelpVersion"), false, true))
     StringToInt(settingValue, &winHelpVersion);
-  else if (StringMatch(settingName, _T("winHelpContents"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("winHelpContents"), false, true))
     winHelpContents = StringTobool(settingValue);
-  else if (StringMatch(settingName, _T("htmlIndex"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("htmlIndex"), false, true))
     htmlIndex = StringTobool(settingValue);
-  else if (StringMatch(settingName, _T("htmlWorkshopFiles"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("htmlWorkshopFiles"), false, true))
     htmlWorkshopFiles = StringTobool(settingValue);
-  else if (StringMatch(settingName, _T("htmlFrameContents"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("htmlFrameContents"), false, true))
     htmlFrameContents = StringTobool(settingValue);
-  else if (StringMatch(settingName, _T("htmlStylesheet"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("htmlStylesheet"), false, true))
     {
@@ -1203,7 +1229,7 @@
     }
-  else if (StringMatch(settingName, _T("upperCaseNames"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("upperCaseNames"), false, true))
     upperCaseNames = StringTobool(settingValue);
-  else if (StringMatch(settingName, _T("ignoreBadRefs"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("ignoreBadRefs"), false, true))
     ignoreBadRefs = StringTobool(settingValue);
-  else if (StringMatch(settingName, _T("htmlFaceName"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("htmlFaceName"), false, true))
   {
@@ -1212,3 +1238,3 @@
   }
-  else if (StringMatch(settingName, _T("winHelpTitle"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("winHelpTitle"), false, true))
   {
@@ -1218,7 +1244,7 @@
   }
-  else if (StringMatch(settingName, _T("indexSubsections"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("indexSubsections"), false, true))
     indexSubsections = StringTobool(settingValue);
-  else if (StringMatch(settingName, _T("compatibility"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("compatibility"), false, true))
     compatibilityMode = StringTobool(settingValue);
-  else if (StringMatch(settingName, _T("defaultColumnWidth"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("defaultColumnWidth"), false, true))
   {
@@ -1227,3 +1253,3 @@
   }
-  else if (StringMatch(settingName, _T("bitmapMethod"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("bitmapMethod"), false, true))
   {
@@ -1242,3 +1268,3 @@
   }
-  else if (StringMatch(settingName, _T("htmlBrowseButtons"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("htmlBrowseButtons"), false, true))
   {
@@ -1257,3 +1283,3 @@
   }
-  else if (StringMatch(settingName, _T("backgroundImage"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("backgroundImage"), false, true))
   {
@@ -1261,3 +1287,3 @@
   }
-  else if (StringMatch(settingName, _T("backgroundColour"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("backgroundColour"), false, true))
   {
@@ -1266,3 +1292,3 @@
   }
-  else if (StringMatch(settingName, _T("textColour"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("textColour"), false, true))
   {
@@ -1270,3 +1296,3 @@
   }
-  else if (StringMatch(settingName, _T("linkColour"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("linkColour"), false, true))
   {
@@ -1274,3 +1300,3 @@
   }
-  else if (StringMatch(settingName, _T("followedLinkColour"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("followedLinkColour"), false, true))
   {
@@ -1278,14 +1304,14 @@
   }
-  else if (StringMatch(settingName, _T("conversionMode"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("conversionMode"), false, true))
   {
-    if (StringMatch(settingValue, _T("RTF"), FALSE, TRUE))
+    if (StringMatch(settingValue, _T("RTF"), false, true))
     {
-      winHelp = FALSE; convertMode = TEX_RTF;
+      winHelp = false; convertMode = TEX_RTF;
     }
-    else if (StringMatch(settingValue, _T("WinHelp"), FALSE, TRUE))
+    else if (StringMatch(settingValue, _T("WinHelp"), false, true))
     {
-      winHelp = TRUE; convertMode = TEX_RTF;
+      winHelp = true; convertMode = TEX_RTF;
     }
-    else if (StringMatch(settingValue, _T("XLP"), FALSE, TRUE) ||
-             StringMatch(settingValue, _T("wxHelp"), FALSE, TRUE))
+    else if (StringMatch(settingValue, _T("XLP"), false, true) ||
+             StringMatch(settingValue, _T("wxHelp"), false, true))
     {
@@ -1293,3 +1319,3 @@
     }
-    else if (StringMatch(settingValue, _T("HTML"), FALSE, TRUE))
+    else if (StringMatch(settingValue, _T("HTML"), false, true))
     {
@@ -1304,3 +1330,3 @@
   }
-  else if (StringMatch(settingName, _T("documentFontSize"), FALSE, TRUE))
+  else if (StringMatch(settingName, _T("documentFontSize"), false, true))
   {
@@ -1333,7 +1359,8 @@
   if (!wxFileExists(filename))
-      return FALSE;
+      return false;
 
-  wxSTD ifstream istr(filename, wxSTD ios::in);
+  wxString name = filename;
+  wxSTD ifstream istr((char const *)name.fn_str(), wxSTD ios::in);
 
-  if (istr.bad()) return FALSE;
+  if (istr.bad()) return false;
 
@@ -1362,3 +1389,3 @@
         OnError(_T("Expected = following name: malformed tex2rtf.ini file."));
-        return FALSE;
+        return false;
       }
@@ -1380,3 +1407,3 @@
         OnError(_T("Expected [ followed by number of arguments: malformed tex2rtf.ini file."));
-        return FALSE;
+        return false;
       }
@@ -1387,3 +1414,3 @@
         OnError(_T("Expected ] following number of arguments: malformed tex2rtf.ini file."));
-        return FALSE;
+        return false;
       }
@@ -1394,6 +1421,6 @@
         OnError(_T("Expected { followed by macro body: malformed tex2rtf.ini file."));
-        return FALSE;
+        return false;
       }
       CustomMacro *macro = new CustomMacro(macroName, noArgs, NULL);
-      BibReadValue(istr, macroBody, FALSE, FALSE); // Don't ignore extra braces
+      BibReadValue(istr, macroBody, false, false); // Don't ignore extra braces
       if (wxStrlen(macroBody) > 0)
@@ -1409,3 +1436,3 @@
   OnInform(mbuf);
-  return TRUE;
+  return true;
 }
@@ -1452,3 +1479,3 @@
   int oldPos = *pos;
-  bool keepGoing = TRUE;
+  bool keepGoing = true;
   while ((fieldIndex <= len) && keepGoing)
@@ -1463,3 +1490,3 @@
       *pos = fieldIndex + 1;
-      keepGoing = FALSE;
+      keepGoing = false;
     }
@@ -1468,3 +1495,3 @@
       *pos = fieldIndex + 1;
-      keepGoing = FALSE;
+      keepGoing = false;
     }
@@ -1558,3 +1585,3 @@
 
-        return TRUE;
+        return true;
     }
@@ -1562,3 +1589,3 @@
   }
-  return FALSE;
+  return false;
 }
@@ -1658,3 +1685,3 @@
     filename = NULL;
-  hasChildren = FALSE;
+  hasChildren = false;
   keywords = NULL;
diff -bu1 -r wxWindows/utils/tex2rtf/src/wxhlpblk.h wxWin/utils/tex2rtf/src/wxhlpblk.h
--- wxWindows/utils/tex2rtf/src/wxhlpblk.h	1999-01-02 01:45:37.000000000 +0100
+++ wxWin/utils/tex2rtf/src/wxhlpblk.h	2004-03-04 19:19:45.000000000 +0100
@@ -5,2 +5,3 @@
  * Created:	1993
+ * Modified by: Wlodzimiez ABX Skiba 2003/2004 Unicode support
  * Updated:	
diff -bu1 -r wxWindows/utils/tex2rtf/src/xlputils.cpp wxWin/utils/tex2rtf/src/xlputils.cpp
--- wxWindows/utils/tex2rtf/src/xlputils.cpp	2004-01-08 12:54:30.000000000 +0100
+++ wxWin/utils/tex2rtf/src/xlputils.cpp	2004-03-04 19:20:58.000000000 +0100
@@ -4,3 +4,3 @@
 // Author:      Julian Smart
-// Modified by:
+// Modified by: Wlodzimiez ABX Skiba 2003/2004 Unicode support
 // Created:     7.9.93
@@ -39,5 +39,5 @@
 static int currentTab = 0;
-static bool tableVerticalLineLeft = FALSE;
-static bool tableVerticalLineRight = FALSE;
-static bool inTable = FALSE;
+static bool tableVerticalLineLeft = false;
+static bool tableVerticalLineRight = false;
+static bool inTable = false;
 static int citeCount = 1;
@@ -55,3 +55,3 @@
   for (int i = currentCol; i < tabPos; i++)
-    TexOutput(_T(" "), TRUE);
+    TexOutput(_T(" "), true);
 }
@@ -87,3 +87,3 @@
 
-      startedSections = TRUE;
+      startedSections = true;
       wxFprintf(Contents, _T("\\hy-%d{%ld}{"), hyBLOCK_SMALL_HEADING, id1);
@@ -119,3 +119,3 @@
 
-      startedSections = TRUE;
+      startedSections = true;
 
@@ -211,3 +211,3 @@
 //    if (start)
-//      TexOutput(_T("void"), TRUE);
+//      TexOutput(_T("void"), true);
     break;
@@ -215,3 +215,3 @@
     if (start)
-      TexOutput(_T("\n"), TRUE);
+      TexOutput(_T("\n"), true);
     break;
@@ -222,4 +222,4 @@
       if (ParSkip > 0)
-        TexOutput(_T("\n"), TRUE);
-      TexOutput(_T("\n"), TRUE);
+        TexOutput(_T("\n"), true);
+      TexOutput(_T("\n"), true);
     }
@@ -451,3 +451,3 @@
     if (start)
-      TexOutput(_T("HARDY"), TRUE);
+      TexOutput(_T("HARDY"), true);
     break;
@@ -457,3 +457,3 @@
     if (start)
-      TexOutput(_T("wxCLIPS"), TRUE);
+      TexOutput(_T("wxCLIPS"), true);
     break;
@@ -476,3 +476,3 @@
     {
-      TexOutput(_T("\n------------------------------------------------------------------"), TRUE);
+      TexOutput(_T("\n------------------------------------------------------------------"), true);
     }
@@ -484,3 +484,3 @@
     {
-      TexOutput(_T("--------------------------------------------------------------------------------"), TRUE);
+      TexOutput(_T("--------------------------------------------------------------------------------"), true);
     }
@@ -513,3 +513,3 @@
     if (!start)
-      TexOutput(_T("\n\n"), TRUE);
+      TexOutput(_T("\n\n"), true);
     break;
@@ -570,3 +570,3 @@
       currentSection = GetArgChunk();
-    return FALSE;
+    return false;
   }
@@ -575,7 +575,7 @@
     if (!start && (arg_no == 1))
-      TexOutput(_T(" "), TRUE);
+      TexOutput(_T(" "), true);
     if (start && (arg_no == 3))
-      TexOutput(_T("("), TRUE);
+      TexOutput(_T("("), true);
     if (!start && (arg_no == 3))
-     TexOutput(_T(")"), TRUE);
+     TexOutput(_T(")"), true);
     break;
@@ -585,13 +585,13 @@
     if (!start && (arg_no == 1))
-      TexOutput(_T(" "), TRUE);
+      TexOutput(_T(" "), true);
 
     if (start && (arg_no == 2))
-      TexOutput(_T("(*"), TRUE);
+      TexOutput(_T("(*"), true);
     if (!start && (arg_no == 2))
-      TexOutput(_T(")"), TRUE);
+      TexOutput(_T(")"), true);
 
     if (start && (arg_no == 3))
-      TexOutput(_T("("), TRUE);
+      TexOutput(_T("("), true);
     if (!start && (arg_no == 3))
-      TexOutput(_T(")"), TRUE);
+      TexOutput(_T(")"), true);
     break;
@@ -601,6 +601,6 @@
     if (!start && (arg_no == 1))
-      TexOutput(_T(" "), TRUE);
+      TexOutput(_T(" "), true);
     if (start && (arg_no == 2))
     {
-      TexOutput(_T("("), TRUE);
+      TexOutput(_T("("), true);
       long id = NewBlockId();
@@ -614,3 +614,3 @@
     if (!start && (arg_no == 3))
-     TexOutput(_T(")"), TRUE);
+     TexOutput(_T(")"), true);
     break;
@@ -648,3 +648,3 @@
     if (!start && (arg_no == 1))
-      TexOutput(_T(" "), TRUE);
+      TexOutput(_T(" "), true);
     break;
@@ -653,3 +653,3 @@
   {
-    return FALSE;
+    return false;
   }
@@ -674,3 +674,3 @@
       }
-      return FALSE;
+      return false;
     }
@@ -700,3 +700,3 @@
       
-      return FALSE;
+      return false;
     }
@@ -708,3 +708,3 @@
     {
-      return TRUE;
+      return true;
     }
@@ -716,3 +716,3 @@
         TexOutput(_T(")"));
-      return TRUE;
+      return true;
     }
@@ -725,3 +725,3 @@
       descriptionItemArg = GetArgChunk();
-      return FALSE;
+      return false;
     }
@@ -736,5 +736,5 @@
       {
-        inTable = TRUE;
-        tableVerticalLineLeft = FALSE;
-        tableVerticalLineRight = FALSE;
+        inTable = true;
+        tableVerticalLineLeft = false;
+        tableVerticalLineRight = false;
 
@@ -748,5 +748,5 @@
           if (alignString[0] == '|')
-            tableVerticalLineLeft = TRUE;
+            tableVerticalLineLeft = true;
           if (alignString[len-1] == '|')
-            tableVerticalLineRight = TRUE;
+            tableVerticalLineRight = true;
         }
@@ -770,3 +770,3 @@
 
-        return FALSE;
+        return false;
       }
@@ -775,6 +775,6 @@
     {
-      inTable = FALSE;
+      inTable = false;
     }
     else if (arg_no == 2 && start)
-      return TRUE;
+      return true;
     break;
@@ -789,7 +789,7 @@
     {
-      TexOutput(_T("----------------------------------------------------------------------\n"), TRUE);
-      return TRUE;
+      TexOutput(_T("----------------------------------------------------------------------\n"), true);
+      return true;
     }
     else
-      TexOutput(_T("\n----------------------------------------------------------------------\n"), TRUE);
+      TexOutput(_T("\n----------------------------------------------------------------------\n"), true);
     break;
@@ -813,5 +813,5 @@
       citeCount ++;
-      return FALSE;
+      return false;
     }
-    return TRUE;
+    return true;
   }
@@ -833,3 +833,3 @@
       SetCurrentOutput(Chapters);
-      return FALSE;
+      return false;
     }
@@ -838,3 +838,3 @@
     }
-    return TRUE;
+    return true;
   }
@@ -848,3 +848,3 @@
       TexOutput(_T("\n"));
-    return TRUE;
+    return true;
   }
@@ -898,3 +898,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -950,3 +950,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -996,3 +996,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -1033,3 +1033,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -1085,3 +1085,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -1107,3 +1107,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -1129,3 +1129,3 @@
     }
-    return FALSE;
+    return false;
   }
@@ -1136,3 +1136,3 @@
   }
-  return TRUE;
+  return true;
 }
@@ -1210,5 +1210,5 @@
     wxRemoveFile(_T("index.xlp"));
-    return TRUE;
+    return true;
   }
-  return FALSE;
+  return false;
 }

 	  	 
