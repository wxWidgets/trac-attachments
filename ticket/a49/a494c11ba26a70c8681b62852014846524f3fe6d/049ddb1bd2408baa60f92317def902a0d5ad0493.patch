Index: D:/wxWidgets-2.6.2/src/msw/textctrl.cpp
===================================================================
--- D:/wxWidgets-2.6.2/src/msw/textctrl.cpp	(revision 24)
+++ D:/wxWidgets-2.6.2/src/msw/textctrl.cpp	(working copy)
@@ -2523,7 +2523,7 @@
 
     if ( changeSel )
     {
-        DoSetSelection(position, position, false /* don't scroll caret into view */);
+        DoSetSelection(position, position+1, false /* don't scroll caret into view */);
     }
 
     // get the selection formatting

 	  	 
