Index: src/common/filename.cpp
===================================================================
--- src/common/filename.cpp	(revision 72717)
+++ src/common/filename.cpp	(working copy)
@@ -772,7 +772,7 @@
     if ( !ShouldFollowLink() )
         flags |= wxFILE_EXISTS_NO_FOLLOW;
 
-    return Exists(GetFullPath(), flags);
+    return Exists(GetPath(), flags);
 }
 
 /* static */
