Index: src/common/image.cpp
===================================================================
--- src/common/image.cpp	(revision 59170)
+++ src/common/image.cpp	(working copy)
@@ -234,6 +234,7 @@
     M_IMGDATA->m_height = height;
     M_IMGDATA->m_ok = true;
     M_IMGDATA->m_static = static_data;
+    M_IMGDATA->m_staticAlpha = static_data;
 
     return true;
 }
