Index: samples/dialogs/dialogs.cpp
===================================================================
--- samples/dialogs/dialogs.cpp	(revision 64066)
+++ samples/dialogs/dialogs.cpp	(working copy)
@@ -2643,7 +2643,7 @@
     {
         if ( event.GetEventObject() == m_labels[n] )
         {
-            event.Enable( m_buttons[n]->IsChecked() );
+            event.Show( m_buttons[n]->IsChecked() );
             return;
         }
     }
