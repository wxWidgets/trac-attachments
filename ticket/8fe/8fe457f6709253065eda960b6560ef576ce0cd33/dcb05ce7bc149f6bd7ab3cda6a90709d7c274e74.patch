Index: notebook.cpp
===================================================================
--- notebook.cpp	(revision 72145)
+++ notebook.cpp	(working copy)
@@ -764,6 +764,8 @@
                           true,
                           GetIconIndex(currBook));
     }
+    SetSizeHints(GetBestSize());
+    Fit();
 }
 
 void MyFrame::OnAddPageNoSelect(wxCommandEvent& WXUNUSED(event))
