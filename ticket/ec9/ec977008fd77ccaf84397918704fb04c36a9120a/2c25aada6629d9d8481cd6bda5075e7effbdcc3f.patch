--- C:\Program Files\wxMSW-2.8.0\include\wx\generic\fontdlgg.h	2006-11-16 14:53:08.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\include\wx\generic\fontdlgg.h	2006-11-13 10:50:52.000 +0530
@@ -37,7 +37,6 @@
 enum
 {
     wxID_FONT_UNDERLINE = 3000,
-	wxID_FONT_STRIKETHROUGH,
     wxID_FONT_STYLE,
     wxID_FONT_WEIGHT,
     wxID_FONT_FAMILY,
@@ -91,8 +90,7 @@
     wxChoice *m_styleChoice;
     wxChoice *m_weightChoice;
     wxChoice *m_colourChoice;
-	wxCheckBox *m_underLineCheckBox;
-	wxCheckBox *m_strikeThroughCheckBox;
+    wxCheckBox *m_underLineCheckBox;
 
 #if !USE_SPINCTRL_FOR_POINT_SIZE
     wxChoice   *m_pointSizeChoice;

--- C:\Program Files\wxMSW-2.8.0\include\wx\html\winpars.h	2006-11-16 14:53:08.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\include\wx\html\winpars.h	2006-11-13 10:50:52.000 +0530
@@ -111,9 +111,7 @@
     int GetFontItalic() const {return m_FontItalic;}
     void SetFontItalic(int x) {m_FontItalic = x;}
     int GetFontUnderlined() const {return m_FontUnderlined;}
-	void SetFontUnderlined(int x) {m_FontUnderlined = x;}
-	int GetFontStrikethrough() const {return m_FontStrikethrough;}
-	void SetFontStrikethrough(int x) {m_FontStrikethrough = x;}
+    void SetFontUnderlined(int x) {m_FontUnderlined = x;}
     int GetFontFixed() const {return m_FontFixed;}
     void SetFontFixed(int x) {m_FontFixed = x;}
     wxString GetFontFace() const {return GetFontFixed() ? m_FontFaceFixed : m_FontFaceNormal;}
@@ -169,7 +167,7 @@
     wxHtmlContainerCell *m_Container;
             // current container. See Open/CloseContainer for details.
 
-    int m_FontBold, m_FontItalic, m_FontUnderlined, m_FontStrikethrough, m_FontFixed; // this is not true,false but 1,0, we need it for indexing
+    int m_FontBold, m_FontItalic, m_FontUnderlined, m_FontFixed; // this is not true,false but 1,0, we need it for indexing
     int m_FontSize; /* -2 to +4,  0 is default */
     wxColour m_LinkColor;
     wxColour m_ActualColor;
@@ -194,7 +192,7 @@
 #endif
             // table of loaded fonts. 1st four indexes are 0 or 1, depending on on/off
             // state of these flags (from left to right):
-            // [bold][italic][underlined][strikethrough][fixed_size]
+            // [bold][italic][underlined][fixed_size]
             // last index is font size : from 0 to 6 (remapped from html sizes 1 to 7)
             // Note : this table covers all possible combinations of fonts, but not
             // all of them are used, so many items in table are usually NULL.

--- C:\Program Files\wxMSW-2.8.0\include\wx\msw\font.h	2006-09-22 12:32:55.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\include\wx\msw\font.h	2006-11-13 10:50:52.000 +0530
@@ -29,11 +29,10 @@
            int style,
            int weight,
            bool underlined = false,
-		   bool strikethrough = false,
            const wxString& face = wxEmptyString,
            wxFontEncoding encoding = wxFONTENCODING_DEFAULT)
     {
-        (void)Create(size, family, style, weight, underlined, strikethrough, face, encoding);
+        (void)Create(size, family, style, weight, underlined, face, encoding);
     }
 
     wxFont(const wxSize& pixelSize,
@@ -41,12 +40,11 @@
            int style,
            int weight,
            bool underlined = false,
-		   bool strikethrough = false,
            const wxString& face = wxEmptyString,
            wxFontEncoding encoding = wxFONTENCODING_DEFAULT)
     {
         (void)Create(pixelSize, family, style, weight,
-                     underlined, strikethrough, face, encoding);
+                     underlined, face, encoding);
     }
 
     wxFont(const wxNativeFontInfo& info, WXHFONT hFont = 0)
@@ -61,12 +59,11 @@
                 int style,
                 int weight,
                 bool underlined = false,
-				bool strikethrough = false,
                 const wxString& face = wxEmptyString,
                 wxFontEncoding encoding = wxFONTENCODING_DEFAULT)
     {
         return DoCreate(size, wxDefaultSize, false, family, style,
-                        weight, underlined, strikethrough, face, encoding);
+                        weight, underlined, face, encoding);
     }
 
     bool Create(const wxSize& pixelSize,
@@ -74,12 +71,11 @@
                 int style,
                 int weight,
                 bool underlined = false,
-				bool strikethrough = false,
                 const wxString& face = wxEmptyString,
                 wxFontEncoding encoding = wxFONTENCODING_DEFAULT)
     {
         return DoCreate(-1, pixelSize, true, family, style,
-                        weight, underlined, strikethrough, face, encoding);
+                        weight, underlined, face, encoding);
     }
 
     bool Create(const wxNativeFontInfo& info, WXHFONT hFont = 0);
@@ -97,8 +93,7 @@
     virtual int GetFamily() const;
     virtual int GetStyle() const;
     virtual int GetWeight() const;
-	virtual bool GetUnderlined() const;
-	virtual bool GetStrikethrough() const;
+    virtual bool GetUnderlined() const;
     virtual wxString GetFaceName() const;
     virtual wxFontEncoding GetEncoding() const;
     virtual const wxNativeFontInfo *GetNativeFontInfo() const;
@@ -109,8 +104,7 @@
     virtual void SetStyle(int style);
     virtual void SetWeight(int weight);
     virtual bool SetFaceName(const wxString& faceName);
-	virtual void SetUnderlined(bool underlined);
-	virtual void SetStrikethrough(bool strikethrough);
+    virtual void SetUnderlined(bool underlined);
     virtual void SetEncoding(wxFontEncoding encoding);
 
     virtual bool IsFixedWidth() const;
@@ -140,7 +134,6 @@
                   int style,
                   int weight,
                   bool underlined = false,
-				  bool strikethrough = false,
                   const wxString& face = wxEmptyString,
                   wxFontEncoding encoding = wxFONTENCODING_DEFAULT);
 
--- C:\Program Files\wxMSW-2.8.0\include\wx\richtext\richtextbuffer.h	2006-11-16 13:23:37.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\include\wx\richtext\richtextbuffer.h	2006-11-13 10:50:52.000 +0530
@@ -389,8 +389,7 @@
     bool HasWeight() const { return (GetFlags() & wxTEXT_ATTR_FONT_WEIGHT) != 0; }
     bool HasSize() const { return (GetFlags() & wxTEXT_ATTR_FONT_SIZE) != 0; }
     bool HasItalic() const { return (GetFlags() & wxTEXT_ATTR_FONT_ITALIC) != 0; }
-	bool HasUnderlined() const { return (GetFlags() & wxTEXT_ATTR_FONT_UNDERLINE) != 0; }
-	bool HasStrikethrough() const { return (GetFlags() & wxTEXT_ATTR_FONT_STRIKETHROUGH) != 0; }
+    bool HasUnderlined() const { return (GetFlags() & wxTEXT_ATTR_FONT_UNDERLINE) != 0; }
     bool HasFaceName() const { return (GetFlags() & wxTEXT_ATTR_FONT_FACE) != 0; }
 
     bool HasParagraphSpacingAfter() const { return HasFlag(wxTEXT_ATTR_PARA_SPACING_AFTER); }
@@ -503,8 +502,7 @@
     void SetFontStyle(int fontStyle) { m_fontStyle = fontStyle; m_flags |= wxTEXT_ATTR_FONT_ITALIC; }
     void SetFontWeight(int fontWeight) { m_fontWeight = fontWeight; m_flags |= wxTEXT_ATTR_FONT_WEIGHT; }
     void SetFontFaceName(const wxString& faceName) { m_fontFaceName = faceName; m_flags |= wxTEXT_ATTR_FONT_FACE; }
-	void SetFontUnderlined(bool underlined) { m_fontUnderlined = underlined; m_flags |= wxTEXT_ATTR_FONT_UNDERLINE; }
-	void SetFontStrikethrough(bool strikethrough) { m_fontStrikethrough = strikethrough; m_flags |= wxTEXT_ATTR_FONT_STRIKETHROUGH; }
+    void SetFontUnderlined(bool underlined) { m_fontUnderlined = underlined; m_flags |= wxTEXT_ATTR_FONT_UNDERLINE; }
 
     void SetFlags(long flags) { m_flags = flags; }
 
@@ -537,8 +535,7 @@
     int GetFontSize() const { return m_fontSize; }
     int GetFontStyle() const { return m_fontStyle; }
     int GetFontWeight() const { return m_fontWeight; }
-	bool GetFontUnderlined() const { return m_fontUnderlined; }
-	bool GetFontStrikethrough() const { return m_fontStrikethrough; }
+    bool GetFontUnderlined() const { return m_fontUnderlined; }
     const wxString& GetFontFaceName() const { return m_fontFaceName; }
 
     const wxString& GetCharacterStyleName() const { return m_characterStyleName; }
@@ -567,8 +564,7 @@
     bool HasWeight() const { return (m_flags & wxTEXT_ATTR_FONT_WEIGHT) != 0; }
     bool HasSize() const { return (m_flags & wxTEXT_ATTR_FONT_SIZE) != 0; }
     bool HasItalic() const { return (m_flags & wxTEXT_ATTR_FONT_ITALIC) != 0; }
-	bool HasUnderlined() const { return (m_flags & wxTEXT_ATTR_FONT_UNDERLINE) != 0; }
-	bool HasStrikethrough() const { return (m_flags & wxTEXT_ATTR_FONT_STRIKETHROUGH) != 0; }
+    bool HasUnderlined() const { return (m_flags & wxTEXT_ATTR_FONT_UNDERLINE) != 0; }
     bool HasFaceName() const { return (m_flags & wxTEXT_ATTR_FONT_FACE) != 0; }
     bool HasFont() const { return (m_flags & (wxTEXT_ATTR_FONT)) != 0; }
 
@@ -637,8 +633,7 @@
     int                 m_fontSize;
     int                 m_fontStyle;
     int                 m_fontWeight;
-	bool                m_fontUnderlined;
-	bool                m_fontStrikethrough;
+    bool                m_fontUnderlined;
     wxString            m_fontFaceName;
 
     // Character style
@@ -1724,13 +1719,7 @@
     /// End using underline
     bool EndUnderline() { return EndStyle(); }
 
-	/// Begin using strikethrough
-	bool BeginStrikethrough();
-
-	/// End using strikethrough
-	bool EndStrikethrough() { return EndStyle(); }
-
-	/// Begin using point size
+    /// Begin using point size
     bool BeginFontSize(int pointSize);
 
     /// End using point size

--- C:\Program Files\wxMSW-2.8.0\include\wx\richtext\richtextctrl.h	2006-11-16 14:18:25.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\include\wx\richtext\richtextctrl.h	2006-11-13 10:50:52.000 +0530
@@ -329,13 +329,7 @@
     /// End using underline
     bool EndUnderline() { return GetBuffer().EndUnderline(); }
 
-	/// Begin using strikethrough
-	bool BeginStrikethrough() { return GetBuffer().BeginStrikethrough(); }
-
-	/// End using strikethrough
-	bool EndStrikethrough() { return GetBuffer().EndStrikethrough(); }
-
-	/// Begin using point size
+    /// Begin using point size
     bool BeginFontSize(int pointSize) { return GetBuffer().BeginFontSize(pointSize); }
 
     /// End using point size
@@ -561,10 +555,7 @@
     /// Is all of the selection underlined?
     virtual bool IsSelectionUnderlined();
 
-	/// Is all of the selection strikethrough?
-	virtual bool IsSelectionStrikethrough();
-
-	/// Is all of the selection aligned according to the specified flag?
+    /// Is all of the selection aligned according to the specified flag?
     virtual bool IsSelectionAligned(wxTextAttrAlignment alignment);
 
     /// Apply bold to the selection
@@ -576,10 +567,7 @@
     /// Apply underline to the selection
     virtual bool ApplyUnderlineToSelection();
 
-	/// Apply strikethrough to the selection
-	virtual bool ApplyStrikethroughToSelection();
-
-	/// Apply alignment to the selection
+    /// Apply alignment to the selection
     virtual bool ApplyAlignmentToSelection(wxTextAttrAlignment alignment);
 
     /// Apply a named style to the selection

--- C:\Program Files\wxMSW-2.8.0\include\wx\defs.h	2006-11-16 13:01:38.001 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\include\wx\defs.h	2006-11-13 10:50:52.000 +0530
@@ -1748,7 +1748,6 @@
     wxID_JUSTIFY_RIGHT,
     wxID_JUSTIFY_LEFT,
     wxID_UNDERLINE,
-	wxID_STRIKETHROUGH,
     wxID_INDENT,
     wxID_UNINDENT,
     wxID_ZOOM_100,

--- C:\Program Files\wxMSW-2.8.0\include\wx\font.h	2006-11-16 12:37:35.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\include\wx\font.h	2006-11-13 10:50:52.000 +0530
@@ -121,7 +121,6 @@
         int style,                  // see wxFontStyle enum
         int weight,                 // see wxFontWeight enum
         bool underlined = false,    // not underlined by default
-		bool strikethrough = false, // not strikethrough by default
         const wxString& face = wxEmptyString,              // facename
         wxFontEncoding encoding = wxFONTENCODING_DEFAULT); // ISO8859-X, ...
 
@@ -140,8 +139,7 @@
         int style,                  // see wxFontStyle enum
         int weight,                 // see wxFontWeight enum
         bool underlined = false,    // not underlined by default
-		bool strikethrough = false, // not strikethrough by default
-		const wxString& face = wxEmptyString,              // facename
+        const wxString& face = wxEmptyString,              // facename
         wxFontEncoding encoding = wxFONTENCODING_DEFAULT); // ISO8859-X, ...
 
     // from the font components but using the font flags instead of separate
@@ -174,7 +172,6 @@
     virtual int GetStyle() const = 0;
     virtual int GetWeight() const = 0;
     virtual bool GetUnderlined() const = 0;
-	virtual bool GetStrikethrough() const = 0;
     virtual wxString GetFaceName() const = 0;
     virtual wxFontEncoding GetEncoding() const = 0;
     virtual const wxNativeFontInfo *GetNativeFontInfo() const = 0;
@@ -191,7 +188,6 @@
     virtual void SetStyle( int style ) = 0;
     virtual void SetWeight( int weight ) = 0;
     virtual void SetUnderlined( bool underlined ) = 0;
-	virtual void SetStrikethrough(bool strikethrough) = 0;
     virtual void SetEncoding(wxFontEncoding encoding) = 0;
     virtual bool SetFaceName( const wxString& faceName );
     void SetNativeFontInfo(const wxNativeFontInfo& info)

--- C:\Program Files\wxMSW-2.8.0\include\wx\fontutil.h	2006-11-16 12:38:27.001 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\include\wx\fontutil.h	2006-11-13 10:50:52.000 +0530
@@ -123,7 +123,6 @@
     wxFontStyle   style;
     wxFontWeight  weight;
     bool          underlined;
-	bool          strikethrough;
     wxString      faceName;
     wxFontEncoding encoding;
 #endif // platforms
@@ -157,8 +156,7 @@
         // translate all font parameters
         SetStyle((wxFontStyle)font.GetStyle());
         SetWeight((wxFontWeight)font.GetWeight());
-		SetUnderlined(font.GetUnderlined());
-		SetStrikethrough(font.GetStrikethrough());
+        SetUnderlined(font.GetUnderlined());
 #if defined(__WXMSW__)
         if ( font.IsUsingSizeInPixels() )
             SetPixelSize(font.GetPixelSize());
@@ -187,7 +185,6 @@
     wxFontStyle GetStyle() const;
     wxFontWeight GetWeight() const;
     bool GetUnderlined() const;
-	bool GetStrikethrough() const;
     wxString GetFaceName() const;
     wxFontFamily GetFamily() const;
     wxFontEncoding GetEncoding() const;
@@ -197,7 +194,6 @@
     void SetStyle(wxFontStyle style);
     void SetWeight(wxFontWeight weight);
     void SetUnderlined(bool underlined);
-	void SetStrikethrough(bool strikethrough);
     bool SetFaceName(const wxString& facename);
     void SetFamily(wxFontFamily family);
     void SetEncoding(wxFontEncoding encoding);

--- C:\Program Files\wxMSW-2.8.0\include\wx\gdicmn.h	2006-11-16 12:59:35.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\include\wx\gdicmn.h	2006-11-13 10:50:52.000 +0530
@@ -529,7 +529,6 @@
 public:
     wxFont *FindOrCreateFont(int pointSize, int family, int style, int weight,
                              bool underline = false,
-							 bool strikethrough = false,
                              const wxString& face = wxEmptyString,
                              wxFontEncoding encoding = wxFONTENCODING_DEFAULT);
 #if WXWIN_COMPATIBILITY_2_6

--- C:\Program Files\wxMSW-2.8.0\include\wx\textctrl.h	2006-11-16 13:00:14.001 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\include\wx\textctrl.h	2006-11-13 10:50:52.000 +0530
@@ -174,10 +174,9 @@
 #define wxTEXT_ATTR_FONT_WEIGHT             0x0010
 #define wxTEXT_ATTR_FONT_ITALIC             0x0020
 #define wxTEXT_ATTR_FONT_UNDERLINE          0x0040
-#define wxTEXT_ATTR_FONT_STRIKETHROUGH      0x0080
 #define wxTEXT_ATTR_FONT \
   ( wxTEXT_ATTR_FONT_FACE | wxTEXT_ATTR_FONT_SIZE | wxTEXT_ATTR_FONT_WEIGHT | \
-    wxTEXT_ATTR_FONT_ITALIC | wxTEXT_ATTR_FONT_UNDERLINE | wxTEXT_ATTR_FONT_STRIKETHROUGH)
+    wxTEXT_ATTR_FONT_ITALIC | wxTEXT_ATTR_FONT_UNDERLINE )
 #define wxTEXT_ATTR_ALIGNMENT               0x0080
 #define wxTEXT_ATTR_LEFT_INDENT             0x0100
 #define wxTEXT_ATTR_RIGHT_INDENT            0x0200

--- C:\Program Files\wxMSW-2.8.0\src\common\fontcmn.cpp	2006-11-16 12:40:50.001 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\src\common\fontcmn.cpp	2006-11-13 10:50:38.000 +0530
@@ -135,11 +135,10 @@
                         int style,
                         int weight,
                         bool underlined,
-						bool strikethrough,
                         const wxString& face,
                         wxFontEncoding encoding)
 {
-    return new wxFont(size, family, style, weight, underlined, strikethrough, face, encoding);
+    return new wxFont(size, family, style, weight, underlined, face, encoding);
 }
 
 static inline int flags2Style(int flags)
@@ -165,11 +164,6 @@
     return (flags & wxFONTFLAG_UNDERLINED) != 0;
 }
 
-static inline bool flags2Strikethrough(int flags)
-{
-	return (flags & wxFONTFLAG_STRIKETHROUGH) != 0;
-}
-
 /* static */
 wxFont *wxFontBase::New(int pointSize,
                         wxFontFamily family,
@@ -178,7 +172,7 @@
                         wxFontEncoding encoding)
 {
     return New(pointSize, family, flags2Style(flags), flags2Weight(flags),
-               flags2Underlined(flags), flags2Strikethrough(flags), face, encoding);
+               flags2Underlined(flags), face, encoding);
 }
 
 /* static */
@@ -187,15 +181,14 @@
                         int style,
                         int weight,
                         bool underlined,
-						bool strikethrough,
                         const wxString& face,
                         wxFontEncoding encoding)
 {
 #if defined(__WXMSW__)
     return new wxFont(pixelSize, family, style, weight, underlined,
-                      strikethrough, face, encoding);
+                      face, encoding);
 #else
-    wxFont *self = New(10, family, style, weight, underlined, strikethrough, face, encoding);
+    wxFont *self = New(10, family, style, weight, underlined, face, encoding);
     wxScreenDC dc;
     AdjustFontSize(*(wxFont *)self, dc, pixelSize);
     return self;
@@ -210,7 +203,7 @@
                         wxFontEncoding encoding)
 {
     return New(pixelSize, family, flags2Style(flags), flags2Weight(flags),
-               flags2Underlined(flags), flags2Strikethrough(flags), face, encoding);
+               flags2Underlined(flags), face, encoding);
 }
 
 wxSize wxFontBase::GetPixelSize() const
@@ -259,8 +252,7 @@
     SetFamily(info.family);
     SetStyle(info.style);
     SetWeight(info.weight);
-	SetUnderlined(info.underlined);
-	SetStrikethrough(info.strikethrough);
+    SetUnderlined(info.underlined);
     SetFaceName(info.faceName);
     SetEncoding(info.encoding);
 #else
@@ -345,8 +337,7 @@
             GetFamily() == font.GetFamily() &&
             GetStyle() == font.GetStyle() &&
             GetWeight() == font.GetWeight() &&
-			GetUnderlined() == font.GetUnderlined() &&
-			GetStrikethrough() == font.GetStrikethrough() &&
+            GetUnderlined() == font.GetUnderlined() &&
             GetFaceName().IsSameAs(font.GetFaceName(), false) &&
             GetEncoding() == font.GetEncoding()
            );
@@ -439,7 +430,7 @@
 // These are the generic forms of FromString()/ToString.
 //
 // convert to/from the string representation: format is
-//      version;pointsize;family;style;weight;underlined;strikethrough;facename;encoding
+//      version;pointsize;family;style;weight;underlined;facename;encoding
 
 bool wxNativeFontInfo::FromString(const wxString& s)
 {
@@ -477,12 +468,7 @@
         return false;
     underlined = l != 0;
 
-	token = tokenizer.GetNextToken();
-	if ( !token.ToLong(&l) )
-		return false;
-	strikethrough = l != 0;
-
-	faceName = tokenizer.GetNextToken();
+    faceName = tokenizer.GetNextToken();
 
 #ifndef __WXMAC__
     if( !faceName )
@@ -501,14 +487,13 @@
 {
     wxString s;
 
-    s.Printf(_T("%d;%d;%d;%d;%d;%d;%d;%s;%d"),
+    s.Printf(_T("%d;%d;%d;%d;%d;%d;%s;%d"),
              0,                                 // version
              pointSize,
              family,
              (int)style,
              (int)weight,
-			 underlined,
-			 strikethrough,
+             underlined,
              faceName.GetData(),
              (int)encoding);
 
@@ -521,8 +506,7 @@
     family = wxFONTFAMILY_DEFAULT;
     style = wxFONTSTYLE_NORMAL;
     weight = wxFONTWEIGHT_NORMAL;
-	underlined = false;
-	strikethrough = false;
+    underlined = false;
     faceName.clear();
     encoding = wxFONTENCODING_DEFAULT;
 }
@@ -547,11 +531,6 @@
     return underlined;
 }
 
-bool wxNativeFontInfo::GetStrikethrough() const
-{
-	return strikethrough;
-}
-
 wxString wxNativeFontInfo::GetFaceName() const
 {
     return faceName;
@@ -587,11 +566,6 @@
     underlined = underlined_;
 }
 
-void wxNativeFontInfo::SetStrikethrough(bool strikethrough_)
-{
-	strikethrough = strikethrough_;
-}
-
 bool wxNativeFontInfo::SetFaceName(const wxString& facename_)
 {
     faceName = facename_;
@@ -628,12 +602,7 @@
         desc << _("underlined");
     }
 
-	if ( GetStrikethrough() )
-	{
-		desc << _("strikethrough");
-	}
-
-	switch ( GetWeight() )
+    switch ( GetWeight() )
     {
         default:
             wxFAIL_MSG( _T("unknown font weight") );
@@ -719,10 +688,6 @@
         {
             SetUnderlined(true);
         }
-		else if ( token == _T("strikethrough") || token == _("strikethrough") )
-		{
-			SetStrikethrough(true);
-		}
         else if ( token == _T("light") || token == _("light") )
         {
             SetWeight(wxFONTWEIGHT_LIGHT);

--- C:\Program Files\wxMSW-2.8.0\src\common\gdicmn.cpp	2006-11-16 12:41:32.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\src\common\gdicmn.cpp	2006-11-13 10:50:38.000 +0530
@@ -767,7 +767,6 @@
                                      int style,
                                      int weight,
                                      bool underline,
-									 bool strikethrough,
                                      const wxString& facename,
                                      wxFontEncoding encoding)
 {
@@ -780,8 +779,7 @@
              font->GetPointSize () == pointSize &&
              font->GetStyle () == style &&
              font->GetWeight () == weight &&
-             font->GetUnderlined () == underline &&
-			 font->GetStrikethrough () == strikethrough )
+             font->GetUnderlined () == underline )
         {
             int fontFamily = font->GetFamily();
 
@@ -824,7 +822,7 @@
 
     // font not found, create the new one
     font = NULL;
-    wxFont fontTmp(pointSize, family, style, weight, underline, strikethrough, facename, encoding);
+    wxFont fontTmp(pointSize, family, style, weight, underline, facename, encoding);
     if (fontTmp.Ok())
     {
         font = new wxFont(fontTmp);

--- C:\Program Files\wxMSW-2.8.0\src\common\stockitem.cpp	2006-11-16 12:42:54.001 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\src\common\stockitem.cpp	2006-11-13 10:50:38.000 +0530
@@ -81,8 +81,7 @@
         case wxID_SELECTALL:
         case wxID_STOP:
         case wxID_UNDELETE:
-		case wxID_UNDERLINE:
-		case wxID_STRIKETHROUGH:
+        case wxID_UNDERLINE:
         case wxID_UNDO:
         case wxID_UNINDENT:
         case wxID_YES:
@@ -154,8 +153,7 @@
         STOCKITEM(wxID_SELECTALL,           _("Select all"))
         STOCKITEM(wxID_STOP,                _("&Stop"))
         STOCKITEM(wxID_UNDELETE,            _("Undelete"))
-		STOCKITEM(wxID_UNDERLINE,           _("&Underline"))
-		STOCKITEM(wxID_STRIKETHROUGH,       _("&Strikethrough"))
+        STOCKITEM(wxID_UNDERLINE,           _("&Underline"))
         STOCKITEM(wxID_UNDO,                _("&Undo"))
         STOCKITEM(wxID_UNINDENT,            _("&Unindent"))
         STOCKITEM(wxID_YES,                 _("&Yes"))

--- C:\Program Files\wxMSW-2.8.0\src\generic\fontdlgg.cpp	2006-11-16 14:53:08.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\src\generic\fontdlgg.cpp	2006-11-13 10:50:38.000 +0530
@@ -97,8 +97,7 @@
 IMPLEMENT_DYNAMIC_CLASS(wxGenericFontDialog, wxDialog)
 
 BEGIN_EVENT_TABLE(wxGenericFontDialog, wxDialog)
-	EVT_CHECKBOX(wxID_FONT_UNDERLINE, wxGenericFontDialog::OnChangeFont)
-	EVT_CHECKBOX(wxID_FONT_STRIKETHROUGH, wxGenericFontDialog::OnChangeFont)
+    EVT_CHECKBOX(wxID_FONT_UNDERLINE, wxGenericFontDialog::OnChangeFont)
     EVT_CHOICE(wxID_FONT_STYLE, wxGenericFontDialog::OnChangeFont)
     EVT_CHOICE(wxID_FONT_WEIGHT, wxGenericFontDialog::OnChangeFont)
     EVT_CHOICE(wxID_FONT_FAMILY, wxGenericFontDialog::OnChangeFont)
@@ -404,8 +403,7 @@
     m_styleChoice = (wxChoice*) FindWindow(wxID_FONT_STYLE);
     m_weightChoice = (wxChoice*) FindWindow(wxID_FONT_WEIGHT);
     m_colourChoice = (wxChoice*) FindWindow(wxID_FONT_COLOUR);
-	m_underLineCheckBox = (wxCheckBox*) FindWindow(wxID_FONT_UNDERLINE);
-	m_strikeThroughCheckBox = (wxCheckBox*) FindWindow(wxID_FONT_STRIKETHROUGH);
+    m_underLineCheckBox = (wxCheckBox*) FindWindow(wxID_FONT_UNDERLINE);
 
     m_familyChoice->SetStringSelection( wxFontFamilyIntToString(m_dialogFont.GetFamily()) );
     m_styleChoice->SetStringSelection(wxFontStyleIntToString(m_dialogFont.GetStyle()));
@@ -425,10 +423,6 @@
         m_underLineCheckBox->SetValue(m_dialogFont.GetUnderlined());
     }
 
-	if (m_strikeThroughCheckBox)
-	{
-		m_strikeThroughCheckBox->SetValue(m_dialogFont.GetStrikethrough());
-	}
 #if USE_SPINCTRL_FOR_POINT_SIZE
     spinCtrl->SetValue(m_dialogFont.GetPointSize());
 #else
@@ -460,8 +454,7 @@
     int fontWeight = wxNORMAL;
     int fontStyle = wxNORMAL;
     int fontSize = 12;
-	bool fontUnderline = false;
-	bool fontStrikethrough = false;
+    bool fontUnderline = false;
 
     if (m_fontData.m_initialFont.Ok())
     {
@@ -469,12 +462,11 @@
         fontWeight = m_fontData.m_initialFont.GetWeight();
         fontStyle = m_fontData.m_initialFont.GetStyle();
         fontSize = m_fontData.m_initialFont.GetPointSize();
-		fontUnderline = m_fontData.m_initialFont.GetUnderlined();
-		fontStrikethrough = m_fontData.m_initialFont.GetStrikethrough();
+        fontUnderline = m_fontData.m_initialFont.GetUnderlined();
     }
 
     m_dialogFont = wxFont(fontSize, fontFamily, fontStyle,
-                          fontWeight, fontUnderline, fontStrikethrough);
+                          fontWeight, fontUnderline);
 
     if (m_previewer)
         m_previewer->SetFont(m_dialogFont);
@@ -501,20 +493,14 @@
 
     // Start with previous underline setting, we want to retain it even if we can't edit it
     // m_dialogFont is always initialized because of the call to InitializeFont
-	int fontUnderline = m_dialogFont.GetUnderlined();
-	int fontStrikethrough = m_dialogFont.GetStrikethrough();
+    int fontUnderline = m_dialogFont.GetUnderlined();
 
     if (m_underLineCheckBox)
     {
         fontUnderline = m_underLineCheckBox->GetValue();
     }
 
-	if (m_strikeThroughCheckBox)
-	{
-		fontStrikethrough = m_strikeThroughCheckBox->GetValue();
-	}
-
-    m_dialogFont = wxFont(fontSize, fontFamily, fontStyle, fontWeight, (fontUnderline != 0), (fontStrikethrough != 0));
+    m_dialogFont = wxFont(fontSize, fontFamily, fontStyle, fontWeight, (fontUnderline != 0));
     m_previewer->SetFont(m_dialogFont);
 
     if ( m_colourChoice )

--- C:\Program Files\wxMSW-2.8.0\src\generic\hyperlink.cpp	2006-11-16 14:53:08.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\src\generic\hyperlink.cpp	2006-11-13 10:50:38.000 +0530
@@ -112,8 +112,7 @@
 
     // by default the font of an hyperlink control is underlined
     wxFont f = GetFont();
-	f.SetUnderlined(true);
-	f.SetStrikethrough(false);
+    f.SetUnderlined(true);
     SetFont(f);
 
     SetInitialSize(size);

--- C:\Program Files\wxMSW-2.8.0\src\generic\treectlg.cpp	2006-11-16 11:53:10.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\src\generic\treectlg.cpp	2006-11-13 10:50:38.000 +0530
@@ -819,7 +819,6 @@
                         m_normalFont.GetStyle(),
                         wxBOLD,
                         m_normalFont.GetUnderlined(),
-						m_normalFont.GetStrikethrough(),
                         m_normalFont.GetFaceName(),
                         m_normalFont.GetEncoding());
 }
@@ -1116,8 +1115,7 @@
                         m_normalFont.GetFamily(),
                         m_normalFont.GetStyle(),
                         wxBOLD,
-						m_normalFont.GetUnderlined(),
-						m_normalFont.GetStrikethrough(),
+                        m_normalFont.GetUnderlined(),
                         m_normalFont.GetFaceName(),
                         m_normalFont.GetEncoding());
 

--- C:\Program Files\wxMSW-2.8.0\src\html\helpwnd.cpp	2006-11-16 14:53:08.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\src\html\helpwnd.cpp	2006-11-13 10:50:36.000 +0530
@@ -1242,14 +1242,14 @@
                   _T("<font size=+4>") + content + _T(" +4</font><br>") ;
 
         content = wxString( _T("<html><body><table><tr><td>") ) +
-                  _("Normal face<br>and <u>underlined</u>and <del>strikethrough</del>. ") +
+                  _("Normal face<br>and <u>underlined</u>. ") +
                   _("<i>Italic face.</i> ") +
                   _("<b>Bold face.</b> ") +
                   _("<b><i>Bold italic face.</i></b><br>") +
                   content +
                   wxString( _T("</td><td><tt>") ) +
                   _("Fixed size face.<br> <b>bold</b> <i>italic</i> ") +
-                  _("<b><i>bold italic <u>underlined</u><del>strikethrough</del></i></b><br>") +
+                  _("<b><i>bold italic <u>underlined</u></i></b><br>") +
                   content +
                   _T("</tt></td></tr></table></body></html>");
 

--- C:\Program Files\wxMSW-2.8.0\src\html\m_fonts.cpp	2006-11-16 14:56:43.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\src\html\m_fonts.cpp	2006-11-13 10:50:36.000 +0530
@@ -113,17 +113,14 @@
     TAG_HANDLER_PROC(tag)
     {
         int underlined = m_WParser->GetFontUnderlined();
-		int strikethrough = m_WParser->GetFontStrikethrough();
 
         m_WParser->SetFontUnderlined(true);
-		m_WParser->SetFontStrikethrough(false);
         m_WParser->GetContainer()->InsertCell(
             new wxHtmlFontCell(m_WParser->CreateCurrentFont()));
 
         ParseInner(tag);
 
-		m_WParser->SetFontUnderlined(underlined);
-		m_WParser->SetFontStrikethrough(strikethrough);
+        m_WParser->SetFontUnderlined(underlined);
         m_WParser->GetContainer()->InsertCell(
             new wxHtmlFontCell(m_WParser->CreateCurrentFont()));
         return true;
@@ -212,21 +209,19 @@
 
     TAG_HANDLER_PROC(tag)
     {
-        int old_size, old_b, old_i, old_u, old_s, old_f, old_al;
+        int old_size, old_b, old_i, old_u, old_f, old_al;
         wxHtmlContainerCell *c;
 
         old_size = m_WParser->GetFontSize();
         old_b = m_WParser->GetFontBold();
         old_i = m_WParser->GetFontItalic();
         old_u = m_WParser->GetFontUnderlined();
-		old_s = m_WParser->GetFontStrikethrough();
         old_f = m_WParser->GetFontFixed();
         old_al = m_WParser->GetAlign();
 
         m_WParser->SetFontBold(true);
         m_WParser->SetFontItalic(false);
-		m_WParser->SetFontUnderlined(false);
-		m_WParser->SetFontStrikethrough(false);
+        m_WParser->SetFontUnderlined(false);
         m_WParser->SetFontFixed(false);
 
         if (tag.GetName() == wxT("H1"))
@@ -269,8 +264,7 @@
         m_WParser->SetFontSize(old_size);
         m_WParser->SetFontBold(old_b);
         m_WParser->SetFontItalic(old_i);
-		m_WParser->SetFontUnderlined(old_u);
-		m_WParser->SetFontStrikethrough(old_s);
+        m_WParser->SetFontUnderlined(old_u);
         m_WParser->SetFontFixed(old_f);
         m_WParser->SetAlign(old_al);
 

--- C:\Program Files\wxMSW-2.8.0\src\html\m_links.cpp	2006-11-16 14:53:07.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\src\html\m_links.cpp	2006-11-13 10:50:36.000 +0530
@@ -70,23 +70,20 @@
         {
             wxHtmlLinkInfo oldlnk = m_WParser->GetLink();
             wxColour oldclr = m_WParser->GetActualColor();
-			int oldund = m_WParser->GetFontUnderlined();
-			int oldstr = m_WParser->GetFontStrikethrough();
+            int oldund = m_WParser->GetFontUnderlined();
             wxString name(tag.GetParam( wxT("HREF") )), target;
 
             if (tag.HasParam( wxT("TARGET") )) target = tag.GetParam( wxT("TARGET") );
             m_WParser->SetActualColor(m_WParser->GetLinkColor());
             m_WParser->GetContainer()->InsertCell(new wxHtmlColourCell(m_WParser->GetLinkColor()));
-			m_WParser->SetFontUnderlined(true);
-			m_WParser->SetFontStrikethrough(false);
+            m_WParser->SetFontUnderlined(true);
             m_WParser->GetContainer()->InsertCell(new wxHtmlFontCell(m_WParser->CreateCurrentFont()));
             m_WParser->SetLink(wxHtmlLinkInfo(name, target));
 
             ParseInner(tag);
 
             m_WParser->SetLink(oldlnk);
-			m_WParser->SetFontUnderlined(oldund);
-			m_WParser->SetFontStrikethrough(oldstr);
+            m_WParser->SetFontUnderlined(oldund);
             m_WParser->GetContainer()->InsertCell(new wxHtmlFontCell(m_WParser->CreateCurrentFont()));
             m_WParser->SetActualColor(oldclr);
             m_WParser->GetContainer()->InsertCell(new wxHtmlColourCell(oldclr));

--- C:\Program Files\wxMSW-2.8.0\src\html\m_pre.cpp	2006-11-16 14:53:07.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\src\html\m_pre.cpp	2006-11-13 10:50:36.000 +0530
@@ -85,14 +85,12 @@
 
         int fixed = m_WParser->GetFontFixed(),
             italic = m_WParser->GetFontItalic(),
-			underlined = m_WParser->GetFontUnderlined(),
-			strikethrough = m_WParser->GetFontStrikethrough(),
+            underlined = m_WParser->GetFontUnderlined(),
             bold = m_WParser->GetFontBold(),
             fsize = m_WParser->GetFontSize();
 
         c = m_WParser->GetContainer();
-		m_WParser->SetFontUnderlined(false);
-		m_WParser->SetFontStrikethrough(false);
+        m_WParser->SetFontUnderlined(false);
         m_WParser->SetFontBold(false);
         m_WParser->SetFontItalic(false);
         m_WParser->SetFontFixed(true);
@@ -113,8 +111,7 @@
         m_WParser->CloseContainer();
         c = m_WParser->OpenContainer();
 
-		m_WParser->SetFontUnderlined(underlined);
-		m_WParser->SetFontStrikethrough(strikethrough);
+        m_WParser->SetFontUnderlined(underlined);
         m_WParser->SetFontBold(bold);
         m_WParser->SetFontItalic(italic);
         m_WParser->SetFontFixed(fixed);

--- C:\Program Files\wxMSW-2.8.0\src\html\winpars.cpp	2006-11-16 15:06:47.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\src\html\winpars.cpp	2006-11-13 10:50:36.000 +0530
@@ -196,7 +196,7 @@
     wxHtmlParser::InitParser(source);
     wxASSERT_MSG(m_DC != NULL, wxT("no DC assigned to wxHtmlWinParser!!"));
 
-    m_FontBold = m_FontItalic = m_FontUnderlined = m_FontStrikethrough = m_FontFixed = FALSE;
+    m_FontBold = m_FontItalic = m_FontUnderlined = m_FontFixed = FALSE;
     m_FontSize = 3; //default one
     CreateCurrentFont();           // we're selecting default font into
     m_DC->GetTextExtent( wxT("H"), &m_CharWidth, &m_CharHeight);
@@ -457,7 +457,6 @@
     int fb = GetFontBold(),
         fi = GetFontItalic(),
         fu = GetFontUnderlined(),
-		fd = GetFontStrikethrough(),	//fd for font delete, now XHTML uses "del" instead of strikethrough
         ff = GetFontFixed(),
         fs = GetFontSize() - 1 /*remap from <1;7> to <0;6>*/ ;
 
@@ -486,8 +485,7 @@
                        ff ? wxMODERN : wxSWISS,
                        fi ? wxITALIC : wxNORMAL,
                        fb ? wxBOLD : wxNORMAL,
-                       fu ? true : false,
-					   fd ? true : false, face
+                       fu ? true : false, face
 #if wxUSE_UNICODE
                        );
 #else

--- C:\Program Files\wxMSW-2.8.0\src\msw\font.cpp	2006-11-16 12:44:32.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\src\msw\font.cpp	2006-11-13 10:50:34.001 +0530
@@ -72,13 +72,12 @@
     wxPROPERTY( Family, int  , SetFamily, GetFamily, (int)wxDEFAULT , 0 /*flags*/ , wxT("Helpstring") , wxT("group")) // wxFontFamily
     wxPROPERTY( Style, int , SetStyle, GetStyle, (int)wxNORMAL , 0 /*flags*/ , wxT("Helpstring") , wxT("group")) // wxFontStyle
     wxPROPERTY( Weight, int , SetWeight, GetWeight, (int)wxNORMAL , 0 /*flags*/ , wxT("Helpstring") , wxT("group")) // wxFontWeight
-	wxPROPERTY( Underlined, bool , SetUnderlined, GetUnderlined, false , 0 /*flags*/ , wxT("Helpstring") , wxT("group"))
-	wxPROPERTY( Strikethrough, bool , SetStrikethrough, GetStrikethrough, false , 0 /*flags*/ , wxT("Helpstring") , wxT("group"))
+    wxPROPERTY( Underlined, bool , SetUnderlined, GetUnderlined, false , 0 /*flags*/ , wxT("Helpstring") , wxT("group"))
     wxPROPERTY( Face, wxString , SetFaceName, GetFaceName, EMPTY_MACROVALUE , 0 /*flags*/ , wxT("Helpstring") , wxT("group"))
     wxPROPERTY( Encoding, wxFontEncoding , SetEncoding, GetEncoding, wxFONTENCODING_DEFAULT , 0 /*flags*/ , wxT("Helpstring") , wxT("group"))
 wxEND_PROPERTIES_TABLE()
 
-wxCONSTRUCTOR_6( wxFont , int , Size , int , Family , int , Style , int , Weight , bool , Underlined , bool , Strikethrough , wxString , Face )
+wxCONSTRUCTOR_6( wxFont , int , Size , int , Family , int , Style , int , Weight , bool , Underlined , wxString , Face )
 
 wxBEGIN_HANDLERS_TABLE(wxFont)
 wxEND_HANDLERS_TABLE()
@@ -106,7 +105,7 @@
     wxFontRefData()
     {
         Init(-1, wxSize(0,0), false, wxFONTFAMILY_DEFAULT, wxFONTSTYLE_NORMAL,
-             wxFONTWEIGHT_NORMAL, false, false, wxEmptyString,
+             wxFONTWEIGHT_NORMAL, false, wxEmptyString,
              wxFONTENCODING_DEFAULT);
     }
 
@@ -117,12 +116,11 @@
                   int style,
                   int weight,
                   bool underlined,
-				  bool strikethrough,
                   const wxString& faceName,
                   wxFontEncoding encoding)
     {
         Init(size, pixelSize, sizeUsingPixels, family, style, weight,
-             underlined, strikethrough, faceName, encoding);
+             underlined, faceName, encoding);
     }
 
     wxFontRefData(const wxNativeFontInfo& info, WXHFONT hFont = 0)
@@ -140,7 +138,7 @@
         {
             Init(data.m_pointSize, data.m_pixelSize, data.m_sizeUsingPixels,
                  data.m_family, data.m_style, data.m_weight,
-                 data.m_underlined, data.m_strikethrough, data.m_faceName, data.m_encoding);
+                 data.m_underlined, data.m_faceName, data.m_encoding);
         }
     }
 
@@ -192,13 +190,7 @@
                                   : m_underlined;
     }
 
-	bool GetStrikethrough() const
-	{
-		return m_nativeFontInfoOk ? m_nativeFontInfo.GetStrikethrough()
-			: m_strikethrough;
-	}
-
-	wxString GetFaceName() const
+    wxString GetFaceName() const
     {
         wxString s;
         if ( m_nativeFontInfoOk )
@@ -282,15 +274,7 @@
             m_underlined = underlined;
     }
 
-	void SetStrikethrough(bool strikethrough)
-	{
-		if ( m_nativeFontInfoOk )
-			m_nativeFontInfo.SetStrikethrough(strikethrough);
-		else
-			m_strikethrough = strikethrough;
-	}
-
-	void SetEncoding(wxFontEncoding encoding)
+    void SetEncoding(wxFontEncoding encoding)
     {
         if ( m_nativeFontInfoOk )
             m_nativeFontInfo.SetEncoding(encoding);
@@ -313,7 +297,6 @@
               int style,
               int weight,
               bool underlined,
-			  bool strikethrough,
               const wxString& faceName,
               wxFontEncoding encoding);
 
@@ -326,8 +309,7 @@
     int           m_family;
     int           m_style;
     int           m_weight;
-	bool          m_underlined;
-	bool          m_strikethrough;
+    bool          m_underlined;
     wxString      m_faceName;
     wxFontEncoding m_encoding;
 
@@ -354,7 +336,6 @@
                          int style,
                          int weight,
                          bool underlined,
-						 bool strikethrough,
                          const wxString& faceName,
                          wxFontEncoding encoding)
 {
@@ -365,8 +346,7 @@
     m_family = family;
     m_style = style;
     m_weight = weight;
-	m_underlined = underlined;
-	m_strikethrough = strikethrough;
+    m_underlined = underlined;
     m_faceName = faceName;
     m_encoding = encoding;
 
@@ -482,11 +462,6 @@
     return lf.lfUnderline != 0;
 }
 
-bool wxNativeFontInfo::GetStrikethrough() const
-{
-	return lf.lfStrikeOut != 0;
-}
-
 wxString wxNativeFontInfo::GetFaceName() const
 {
     return lf.lfFaceName;
@@ -594,11 +569,6 @@
     lf.lfUnderline = underlined;
 }
 
-void wxNativeFontInfo::SetStrikethrough(bool strikethrough)
-{
-	lf.lfStrikeOut = strikethrough;
-}
-
 bool wxNativeFontInfo::SetFaceName(const wxString& facename)
 {
     size_t len = WXSIZEOF(lf.lfFaceName);
@@ -856,7 +826,6 @@
                       int style,
                       int weight,
                       bool underlined,
-					  bool strikethrough,
                       const wxString& faceName,
                       wxFontEncoding encoding)
 {
@@ -871,7 +840,7 @@
 
     m_refData = new wxFontRefData(pointSize, pixelSize, sizeUsingPixels,
                                   family, style, weight,
-                                  underlined, strikethrough, faceName, encoding);
+                                  underlined, faceName, encoding);
 
     RealizeResource();
 
@@ -1016,15 +985,6 @@
     RealizeResource();
 }
 
-void wxFont::SetStrikethrough(bool strikethrough)
-{
-	Unshare();
-
-	M_FONTDATA->SetStrikethrough(strikethrough);
-
-	RealizeResource();
-}
-
 void wxFont::SetEncoding(wxFontEncoding encoding)
 {
     Unshare();
@@ -1096,14 +1056,6 @@
     return M_FONTDATA->GetUnderlined();
 }
 
-
-bool wxFont::GetStrikethrough() const
-{
-	wxCHECK_MSG( Ok(), false, wxT("invalid font") );
-
-	return M_FONTDATA->GetStrikethrough();
-}
-
 wxString wxFont::GetFaceName() const
 {
     wxCHECK_MSG( Ok(), wxEmptyString, wxT("invalid font") );

--- C:\Program Files\wxMSW-2.8.0\src\msw\graphics.cpp	2006-11-16 14:53:08.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\src\msw\graphics.cpp	2006-11-13 10:50:34.001 +0530
@@ -596,10 +596,8 @@
     int style = FontStyleRegular;
     if ( font.GetStyle() == wxFONTSTYLE_ITALIC )
         style |= FontStyleItalic;
-	if ( font.GetUnderlined() )
-		style |= FontStyleUnderline;
-	if ( font.GetStrikethrough() )
-		style |= FontStyleStrikethrough;
+    if ( font.GetUnderlined() )
+        style |= FontStyleUnderline;
     if ( font.GetWeight() == wxFONTWEIGHT_BOLD )
         style |= FontStyleBold;
     m_font = new Font( s , size , style );

--- C:\Program Files\wxMSW-2.8.0\src\msw\textctrl.cpp	2006-11-16 12:46:17.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\src\msw\textctrl.cpp	2006-11-13 10:50:34.001 +0530
@@ -2467,7 +2467,7 @@
         //     but using it doesn't seem to hurt neither so leaving it for now
 
         cf.dwMask |= CFM_FACE | CFM_SIZE | CFM_CHARSET |
-                     CFM_ITALIC | CFM_BOLD | CFM_UNDERLINE | CFM_STRIKEOUT;
+                     CFM_ITALIC | CFM_BOLD | CFM_UNDERLINE;
 
         // fill in data from LOGFONT but recalculate lfHeight because we need
         // the real height in twips and not the negative number which
@@ -2498,10 +2498,6 @@
             cf.dwEffects |= CFE_UNDERLINE;
         }
 
-		if ( lf.lfStrikeOut )
-		{
-			cf.dwEffects |= CFE_STRIKEOUT;
-		}
         // strikeout fonts are not supported by wxWidgets
     }
 
@@ -2716,12 +2712,7 @@
     else
         lf.lfUnderline = FALSE;
 
-	if (cf.dwEffects & CFE_STRIKEOUT)
-		lf.lfStrikeOut = TRUE;
-	else
-		lf.lfStrikeOut = FALSE;
-
-	if (cf.dwEffects & CFE_BOLD)
+    if (cf.dwEffects & CFE_BOLD)
         lf.lfWeight = FW_BOLD;
     else
         lf.lfWeight = FW_NORMAL;

--- C:\Program Files\wxMSW-2.8.0\src\richtext\richtextbuffer.cpp	2006-11-16 14:53:07.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\src\richtext\richtextbuffer.cpp	2006-11-13 10:50:38.000 +0530
@@ -1970,29 +1970,7 @@
                 currentStyle.SetFlags(currentStyle.GetFlags() | wxTEXT_ATTR_FONT_UNDERLINE);
             }
         }
-
-		if (style.HasStrikethrough() && !wxHasStyle(multipleStyleAttributes, wxTEXT_ATTR_FONT_STRIKETHROUGH))
-		{
-			if (currentStyle.GetFont().Ok() && currentStyle.HasStrikethrough())
-			{
-				if (currentStyle.GetFont().GetStrikethrough() != style.GetFont().GetStrikethrough())
-				{
-					// Clash of style - mark as such
-					multipleStyleAttributes |= wxTEXT_ATTR_FONT_STRIKETHROUGH;
-					currentStyle.SetFlags(currentStyle.GetFlags() & ~wxTEXT_ATTR_FONT_STRIKETHROUGH);
-				}
-			}
-			else
-			{
-				if (!currentStyle.GetFont().Ok())
-					wxSetFontPreservingStyles(currentStyle, *wxNORMAL_FONT);
-				wxFont font(currentStyle.GetFont());
-				font.SetStrikethrough(style.GetFont().GetStrikethrough());
-				wxSetFontPreservingStyles(currentStyle, font);
-				currentStyle.SetFlags(currentStyle.GetFlags() | wxTEXT_ATTR_FONT_STRIKETHROUGH);
-			}
-		}
-}
+    }
 
     if (style.HasTextColour() && !wxHasStyle(multipleStyleAttributes, wxTEXT_ATTR_TEXT_COLOUR))
     {
@@ -5080,25 +5058,13 @@
 /// Begin using underline
 bool wxRichTextBuffer::BeginUnderline()
 {
-	wxFont font(GetBasicStyle().GetFont());
-	font.SetUnderlined(true);
-
-	wxTextAttrEx attr;
-	attr.SetFont(font, wxTEXT_ATTR_FONT_UNDERLINE);
-
-	return BeginStyle(attr);
-}
-
-/// Begin using strikethrough
-bool wxRichTextBuffer::BeginStrikethrough()
-{
-	wxFont font(GetBasicStyle().GetFont());
-	font.SetStrikethrough(true);
+    wxFont font(GetBasicStyle().GetFont());
+    font.SetUnderlined(true);
 
-	wxTextAttrEx attr;
-	attr.SetFont(font, wxTEXT_ATTR_FONT_STRIKETHROUGH);
+    wxTextAttrEx attr;
+    attr.SetFont(font, wxTEXT_ATTR_FONT_UNDERLINE);
 
-	return BeginStyle(attr);
+    return BeginStyle(attr);
 }
 
 /// Begin using point size
@@ -5812,7 +5778,7 @@
         {
             font = (*wxTheFontList->FindOrCreateFont(attr.GetFont().GetPointSize(), attr.GetFont().GetFamily(),
                         attr.GetFont().GetStyle(), attr.GetFont().GetWeight(), attr.GetFont().GetUnderlined(),
-                        attr.GetFont().GetStrikethrough(), attr.GetBulletFont()));
+                        attr.GetBulletFont()));
         }
         else if (attr.GetFont().Ok())
             font = attr.GetFont();
@@ -6431,8 +6397,7 @@
         attr1.GetFont().GetStyle() == attr2.GetFontStyle() &&
         attr1.GetFont().GetWeight() == attr2.GetFontWeight() &&
         attr1.GetFont().GetFaceName() == attr2.GetFontFaceName() &&
-		attr1.GetFont().GetUnderlined() == attr2.GetFontUnderlined() &&
-		attr1.GetFont().GetStrikethrough() == attr2.GetFontStrikethrough() &&
+        attr1.GetFont().GetUnderlined() == attr2.GetFontUnderlined() &&
         attr1.GetTextEffects() == attr2.GetTextEffects() &&
         attr1.GetTextEffectFlags() == attr2.GetTextEffectFlags() &&
         attr1.GetAlignment() == attr2.GetAlignment() &&
@@ -6481,13 +6446,9 @@
         attr1.GetFont().GetStyle() != attr2.GetFont().GetStyle())
         return false;
 
-	if ((flags & wxTEXT_ATTR_FONT_UNDERLINE) && attr1.GetFont().Ok() && attr2.GetFont().Ok() &&
-		attr1.GetFont().GetUnderlined() != attr2.GetFont().GetUnderlined())
-		return false;
-
-	if ((flags & wxTEXT_ATTR_FONT_STRIKETHROUGH) && attr1.GetFont().Ok() && attr2.GetFont().Ok() &&
-		attr1.GetFont().GetStrikethrough() != attr2.GetFont().GetStrikethrough())
-		return false;
+    if ((flags & wxTEXT_ATTR_FONT_UNDERLINE) && attr1.GetFont().Ok() && attr2.GetFont().Ok() &&
+        attr1.GetFont().GetUnderlined() != attr2.GetFont().GetUnderlined())
+        return false;
 
     if ((flags & wxTEXT_ATTR_ALIGNMENT) && attr1.GetAlignment() != attr2.GetAlignment())
         return false;
@@ -6591,13 +6552,9 @@
         attr1.GetFont().GetStyle() != attr2.GetFontStyle())
         return false;
 
-	if ((flags & wxTEXT_ATTR_FONT_UNDERLINE) && attr1.GetFont().Ok() &&
-		attr1.GetFont().GetUnderlined() != attr2.GetFontUnderlined())
-		return false;
-
-	if ((flags & wxTEXT_ATTR_FONT_STRIKETHROUGH) && attr1.GetFont().Ok() &&
-		attr1.GetFont().GetStrikethrough() != attr2.GetFontStrikethrough())
-		return false;
+    if ((flags & wxTEXT_ATTR_FONT_UNDERLINE) && attr1.GetFont().Ok() &&
+        attr1.GetFont().GetUnderlined() != attr2.GetFontUnderlined())
+        return false;
 
     if ((flags & wxTEXT_ATTR_ALIGNMENT) && attr1.GetAlignment() != attr2.GetAlignment())
         return false;
@@ -6723,17 +6680,11 @@
             font.SetWeight(style.GetFont().GetWeight());
         }
 
-		if (style.GetFlags() & wxTEXT_ATTR_FONT_UNDERLINE)
-		{
-			destStyle.SetFlags(destStyle.GetFlags() | wxTEXT_ATTR_FONT_UNDERLINE);
-			font.SetUnderlined(style.GetFont().GetUnderlined());
-		}
-
-		if (style.GetFlags() & wxTEXT_ATTR_FONT_STRIKETHROUGH)
-		{
-			destStyle.SetFlags(destStyle.GetFlags() | wxTEXT_ATTR_FONT_STRIKETHROUGH);
-			font.SetStrikethrough(style.GetFont().GetStrikethrough());
-		}
+        if (style.GetFlags() & wxTEXT_ATTR_FONT_UNDERLINE)
+        {
+            destStyle.SetFlags(destStyle.GetFlags() | wxTEXT_ATTR_FONT_UNDERLINE);
+            font.SetUnderlined(style.GetFont().GetUnderlined());
+        }
 
         if (font != destStyle.GetFont())
         {
@@ -6845,7 +6796,7 @@
     if (((style.GetFlags() & (wxTEXT_ATTR_FONT)) == (wxTEXT_ATTR_FONT)) && !compareWith)
     {
         destStyle.SetFont(wxFont(style.GetFontSize(), destStyle.GetFont().Ok() ? destStyle.GetFont().GetFamily() : wxDEFAULT,
-            style.GetFontStyle(), style.GetFontWeight(), style.GetFontUnderlined(), style.GetFontStrikethrough(), style.GetFontFaceName()));
+            style.GetFontStyle(), style.GetFontWeight(), style.GetFontUnderlined(), style.GetFontFaceName()));
     }
     else if (style.GetFlags() & (wxTEXT_ATTR_FONT))
     {
@@ -6916,20 +6867,7 @@
             }
         }
 
-		if (style.GetFlags() & wxTEXT_ATTR_FONT_STRIKETHROUGH)
-		{
-			if (compareWith && compareWith->HasStrikethrough() && compareWith->GetFontStrikethrough() == style.GetFontStrikethrough())
-			{
-				// The same as currently displayed, so don't set
-			}
-			else
-			{
-				destStyle.SetFlags(destStyle.GetFlags() | wxTEXT_ATTR_FONT_STRIKETHROUGH);
-				font.SetStrikethrough(style.GetFontStrikethrough());
-			}
-		}
-
-		if (font != destStyle.GetFont())
+        if (font != destStyle.GetFont())
         {
             int oldFlags = destStyle.GetFlags();
 
@@ -7221,8 +7159,7 @@
     m_fontSize = 12;
     m_fontStyle = wxNORMAL;
     m_fontWeight = wxNORMAL;
-	m_fontUnderlined = false;
-	m_fontStrikethrough = false;
+    m_fontUnderlined = false;
 
     m_paragraphSpacingAfter = 0;
     m_paragraphSpacingBefore = 0;
@@ -7249,8 +7186,7 @@
     m_fontSize = attr.m_fontSize;
     m_fontStyle = attr.m_fontStyle;
     m_fontWeight = attr.m_fontWeight;
-	m_fontUnderlined = attr.m_fontUnderlined;
-	m_fontStrikethrough = attr.m_fontStrikethrough;
+    m_fontUnderlined = attr.m_fontUnderlined;
     m_fontFaceName = attr.m_fontFaceName;
     m_textEffects = attr.m_textEffects;
     m_textEffectFlags = attr.m_textEffectFlags;
@@ -7379,8 +7315,7 @@
             GetFontSize() == attr.GetFontSize() &&
             GetFontStyle() == attr.GetFontStyle() &&
             GetFontWeight() == attr.GetFontWeight() &&
-			GetFontUnderlined() == attr.GetFontUnderlined() &&
-			GetFontStrikethrough() == attr.GetFontStrikethrough() &&
+            GetFontUnderlined() == attr.GetFontUnderlined() &&
             GetFontFaceName() == attr.GetFontFaceName() &&
             
             GetURL() == attr.GetURL();
@@ -7389,7 +7324,7 @@
 // Create font from font attributes.
 wxFont wxRichTextAttr::CreateFont() const
 {
-    wxFont font(m_fontSize, wxDEFAULT, m_fontStyle, m_fontWeight, m_fontUnderlined, m_fontStrikethrough, m_fontFaceName);
+    wxFont font(m_fontSize, wxDEFAULT, m_fontStyle, m_fontWeight, m_fontUnderlined, m_fontFaceName);
 #ifdef __WXMAC__
     font.SetNoAntiAliasing(true);
 #endif
@@ -7405,8 +7340,7 @@
     m_fontSize = font.GetPointSize();
     m_fontStyle = font.GetStyle();
     m_fontWeight = font.GetWeight();
-	m_fontUnderlined = font.GetUnderlined();
-	m_fontStrikethrough = font.GetStrikethrough();
+    m_fontUnderlined = font.GetUnderlined();
     m_fontFaceName = font.GetFaceName();
 
     return true;
@@ -7445,11 +7379,8 @@
     if (attr.HasItalic())
         newAttr.SetFontStyle(attr.GetFontStyle());
 
-	if (attr.HasUnderlined())
-		newAttr.SetFontUnderlined(attr.GetFontUnderlined());
-
-	if (attr.HasStrikethrough())
-		newAttr.SetFontStrikethrough(attr.GetFontStrikethrough());
+    if (attr.HasUnderlined())
+        newAttr.SetFontUnderlined(attr.GetFontUnderlined());
 
     if (attr.HasFaceName())
         newAttr.SetFontFaceName(attr.GetFontFaceName());
@@ -7667,16 +7598,11 @@
                 flags |= wxTEXT_ATTR_FONT_FACE;
                 font.SetFaceName(attr.GetFont().GetFaceName());
             }
-			if (attr.HasUnderlined())
-			{
-				flags |= wxTEXT_ATTR_FONT_UNDERLINE;
-				font.SetUnderlined(attr.GetFont().GetUnderlined());
-			}
-			if (attr.HasStrikethrough())
-			{
-				flags |= wxTEXT_ATTR_FONT_STRIKETHROUGH;
-				font.SetStrikethrough(attr.GetFont().GetStrikethrough());
-			}
+            if (attr.HasUnderlined())
+            {
+                flags |= wxTEXT_ATTR_FONT_UNDERLINE;
+                font.SetUnderlined(attr.GetFont().GetUnderlined());
+            }
             newAttr.SetFont(font);
             newAttr.SetFlags(newAttr.GetFlags()|flags);
         }

--- C:\Program Files\wxMSW-2.8.0\src\richtext\richtextctrl.cpp	2006-11-16 14:18:22.001 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\src\richtext\richtextctrl.cpp	2006-11-13 10:50:38.000 +0530
@@ -2789,36 +2789,6 @@
     return false;
 }
 
-/// Is all of the selection strikethrough?
-bool wxRichTextCtrl::IsSelectionStrikethrough()
-{
-	if (HasSelection())
-	{
-		wxRichTextRange range = GetInternalSelectionRange();
-		wxRichTextAttr attr;
-		attr.SetFlags(wxTEXT_ATTR_FONT_STRIKETHROUGH);
-		attr.SetFontStrikethrough(true);
-
-		return HasCharacterAttributes(range, attr);
-	}
-	else
-	{
-		// If no selection, then we need to combine current style with default style
-		// to see what the effect would be if we started typing.
-		wxRichTextAttr attr;
-		attr.SetFlags(wxTEXT_ATTR_FONT_STRIKETHROUGH);
-		long pos = GetAdjustedCaretPosition(GetCaretPosition());
-
-		if (GetStyle(pos, attr))
-		{
-			if (IsDefaultStyleShowing())
-				wxRichTextApplyStyle(attr, GetDefaultStyleEx());
-			return attr.GetFontStrikethrough();
-		}
-	}
-	return false;
-}
-
 /// Apply bold to the selection
 bool wxRichTextCtrl::ApplyBoldToSelection()
 {
@@ -2852,7 +2822,7 @@
 {
     wxRichTextAttr attr;
     attr.SetFlags(wxTEXT_ATTR_FONT_UNDERLINE);
-	attr.SetFontUnderlined(!IsSelectionUnderlined());
+    attr.SetFontUnderlined(!IsSelectionUnderlined());
 
     if (HasSelection())
         return SetStyleEx(GetSelectionRange(), attr, wxRICHTEXT_SETSTYLE_WITH_UNDO|wxRICHTEXT_SETSTYLE_OPTIMIZE|wxRICHTEXT_SETSTYLE_CHARACTERS_ONLY);
@@ -2861,20 +2831,6 @@
     return true;
 }
 
-/// Apply strikethrough to the selection
-bool wxRichTextCtrl::ApplyStrikethroughToSelection()
-{
-	wxRichTextAttr attr;
-	attr.SetFlags(wxTEXT_ATTR_FONT_STRIKETHROUGH);
-	attr.SetFontStrikethrough(!IsSelectionStrikethrough());
-
-	if (HasSelection())
-		return SetStyleEx(GetSelectionRange(), attr, wxRICHTEXT_SETSTYLE_WITH_UNDO|wxRICHTEXT_SETSTYLE_OPTIMIZE|wxRICHTEXT_SETSTYLE_CHARACTERS_ONLY);
-	else
-		SetAndShowDefaultStyle(attr);
-	return true;
-}
-
 /// Is all of the selection aligned according to the specified flag?
 bool wxRichTextCtrl::IsSelectionAligned(wxTextAttrAlignment alignment)
 {

--- C:\Program Files\wxMSW-2.8.0\src\richtext\richtexthtml.cpp	2006-11-16 14:18:25.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\src\richtext\richtexthtml.cpp	2006-11-13 10:50:38.000 +0530
@@ -288,10 +288,8 @@
         str << wxT("<b>");
     if (thisStyle.GetFont().GetStyle() == wxITALIC)
         str << wxT("<i>");
-	if (thisStyle.GetFont().GetUnderlined())
-		str << wxT("<u>");
-	if (thisStyle.GetFont().GetStrikethrough())
-		str << wxT("<del>");
+    if (thisStyle.GetFont().GetUnderlined())
+        str << wxT("<u>");
     
     if (thisStyle.HasURL())
         str << wxT("<a href=\"") << thisStyle.GetURL() << wxT("\">");
@@ -304,10 +302,8 @@
     if (thisStyle.HasURL())
         str << wxT("</a>");
 
-	if (thisStyle.GetFont().GetUnderlined())
-		str << wxT("</u>");
-	if (thisStyle.GetFont().GetStrikethrough())
-		str << wxT("</del>");
+    if (thisStyle.GetFont().GetUnderlined())
+        str << wxT("</u>");
     if (thisStyle.GetFont().GetStyle() == wxITALIC)
         str << wxT("</i>");
     if (thisStyle.GetFont().GetWeight() == wxBOLD)

--- C:\Program Files\wxMSW-2.8.0\src\richtext\richtextstyles.cpp	2006-11-16 14:57:00.001 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\src\richtext\richtextstyles.cpp	2006-11-13 10:50:38.000 +0530
@@ -576,33 +576,26 @@
 
     bool hasBold = false;
     bool hasItalic = false;
-	bool hasUnderline = false;
-	bool hasStrikethrough = false;
+    bool hasUnderline = false;
 
     if (def->GetStyle().GetFontWeight() == wxBOLD)
         hasBold = true;
     if (def->GetStyle().GetFontStyle() == wxITALIC)
         hasItalic = true;
-	if (def->GetStyle().GetFontUnderlined())
-		hasUnderline = true;
-	if (def->GetStyle().GetFontStrikethrough())
-		hasStrikethrough = true;
+    if (def->GetStyle().GetFontUnderlined())
+        hasUnderline = true;
 
     if (hasBold)
         str << wxT("<b>");
     if (hasItalic)
         str << wxT("<i>");
-	if (hasUnderline)
-		str << wxT("<u>");
-	if (hasStrikethrough)
-		str << wxT("<del>");
+    if (hasUnderline)
+        str << wxT("<u>");
 
     str += def->GetName();
 
-	if (hasUnderline)
-		str << wxT("</u>");
-	if (hasStrikethrough)
-		str << wxT("</del>");
+    if (hasUnderline)
+        str << wxT("</u>");
     if (hasItalic)
         str << wxT("</i>");
     if (hasBold)

--- C:\Program Files\wxMSW-2.8.0\src\richtext\richtextsymboldlg.cpp	2006-11-16 14:56:43.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\src\richtext\richtextsymboldlg.cpp	2006-11-13 10:50:38.000 +0530
@@ -573,7 +573,7 @@
 
     if (!fontNameToUse.empty())
     {
-        font = wxFont(14, wxDEFAULT, wxNORMAL, wxNORMAL, false, false, fontNameToUse);
+        font = wxFont(14, wxDEFAULT, wxNORMAL, wxNORMAL, false, fontNameToUse);
     }
     else
         font = *wxNORMAL_FONT;

--- C:\Program Files\wxMSW-2.8.0\src\richtext\richtextxml.cpp	2006-11-16 14:18:25.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\src\richtext\richtextxml.cpp	2006-11-13 10:50:38.000 +0530
@@ -908,10 +908,7 @@
         if (attr.HasUnderlined())
             str << wxT(" fontunderlined=\"") << (int) attr.GetFont().GetUnderlined() << wxT("\"");
 
-		if (attr.HasStrikethrough())
-			str << wxT(" fontstrikethrough=\"") << (int) attr.GetFont().GetStrikethrough() << wxT("\"");
-
-		if (attr.HasFaceName())
+        if (attr.HasFaceName())
             str << wxT(" fontface=\"") << attr.GetFont().GetFaceName() << wxT("\"");
     }
 
@@ -1017,7 +1014,6 @@
     int fontWeight = wxNORMAL;
     int fontStyle = wxNORMAL;
     bool fontUnderlined = false;
-	bool fontStrikethrough = false;
     
     int fontFlags = 0;
 
@@ -1058,17 +1054,10 @@
         fontFlags |= wxTEXT_ATTR_FONT_UNDERLINE;
     }
     
-	value = node->GetPropVal(wxT("fontstrikethrough"), wxEmptyString);
-	if (!value.empty())
-	{
-		fontStrikethrough = wxAtoi(value) != 0;
-		fontFlags |= wxTEXT_ATTR_FONT_STRIKETHROUGH;
-	}
-
-	attr.SetFlags(fontFlags);
+    attr.SetFlags(fontFlags);
     
     if (attr.HasFlag(wxTEXT_ATTR_FONT))
-        attr.SetFont(* wxTheFontList->FindOrCreateFont(fontSize, fontFamily, fontStyle, fontWeight, fontUnderlined, fontStrikethrough, fontFacename));
+        attr.SetFont(* wxTheFontList->FindOrCreateFont(fontSize, fontFamily, fontStyle, fontWeight, fontUnderlined, fontFacename));
 
     // Restore correct font flags
     attr.SetFlags(fontFlags);

--- C:\Program Files\wxMSW-2.8.0\src\xrc\xmlres.cpp	2006-11-16 14:53:07.000 +0530
+++ C:\Documents and Settings\pbolia\My Documents\Setup\wxMSW-2.8.0-rc1\src\xrc\xmlres.cpp	2006-11-13 10:50:36.000 +0530
@@ -1393,11 +1393,7 @@
     bool hasUnderlined = HasParam(wxT("underlined"));
     bool underlined = hasUnderlined ? GetBool(wxT("underlined"), false) : false;
 
-	// strikethrough
-	bool hasStrikethrough = HasParam(wxT("strikethrough"));
-	bool strikethrough = hasStrikethrough ? GetBool(wxT("strikethrough"), false) : false;
-
-	// family and facename
+    // family and facename
     int ifamily = wxDEFAULT;
     bool hasFamily = HasParam(wxT("family"));
     if (hasFamily)
@@ -1458,10 +1454,8 @@
             font.SetStyle(istyle);
         if (hasWeight)
             font.SetWeight(iweight);
-		if (hasUnderlined)
-			font.SetUnderlined(underlined);
-		if (hasStrikethrough)
-			font.SetStrikethrough(strikethrough);
+        if (hasUnderlined)
+            font.SetUnderlined(underlined);
         if (hasFamily)
             font.SetFamily(ifamily);
         if (hasFacename)
@@ -1473,7 +1467,7 @@
     {
         font = wxFont(isize == -1 ? wxNORMAL_FONT->GetPointSize() : isize,
                       ifamily, istyle, iweight,
-                      underlined, strikethrough, facename, enc);
+                      underlined, facename, enc);
     }
 
     m_node = oldnode;
@@ -1732,8 +1726,7 @@
     stdID(wxID_JUSTIFY_FILL);
     stdID(wxID_JUSTIFY_RIGHT);
     stdID(wxID_JUSTIFY_LEFT);
-	stdID(wxID_UNDERLINE);
-	stdID(wxID_STRIKETHROUGH);
+    stdID(wxID_UNDERLINE);
     stdID(wxID_INDENT);
     stdID(wxID_UNINDENT);
     stdID(wxID_ZOOM_100);


 	  	 
