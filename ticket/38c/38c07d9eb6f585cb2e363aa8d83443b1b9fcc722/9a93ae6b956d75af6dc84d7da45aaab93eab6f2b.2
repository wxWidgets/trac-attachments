--- /usr/src/cvs/wxwindows/wxWindows/samples/scroll/scroll.cpp	2001-11-22 10:11:30.000000000 +0100
+++ scroll.cpp	2002-09-23 14:40:08.000000000 +0200
@@ -258,6 +258,17 @@
     dc.DrawText( "Some text", 140, 140 );
 
     dc.DrawRectangle( 100, 160, 200, 200 );
+
+// line bug 2
+// in Gtk1 line is always redrawn black (sometimes for the first time is drawn in color)
+// in Gtk2 some part's are black some in color (only when scrolling wiht big speed??)
+    dc.SetPen (*wxGREEN_PEN);
+    dc.DrawLine( 330, 150, 400, 500 );
+// size bug - ScrolledWindow should be 10*50=500 x 10*100=1000
+// let's draw rectangle in bottom right corner
+// and to the right and to the bottom there are 10 more pixels (and LMB there
+// reports position bigger then 500x1000
+    dc.DrawRectangle (450, 950, 50, 50);
 }
 
 void MyCanvas::OnQueryPosition( wxCommandEvent &WXUNUSED(event) )

 	  	 
