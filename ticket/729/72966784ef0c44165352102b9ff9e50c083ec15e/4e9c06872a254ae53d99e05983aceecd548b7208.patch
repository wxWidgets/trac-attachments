Index: include/wx/headerctrl.h
===================================================================
--- include/wx/headerctrl.h	(révision 70939)
+++ include/wx/headerctrl.h	(copie de travail)
@@ -352,7 +352,7 @@
     void Init();
 
     // bring the column count in sync with the number of columns we store
-    void UpdateColumnCount() { SetColumnCount(m_cols.size()); }
+    void UpdateColumnCount() { SetColumnCount((int)m_cols.size()); }
 
 
     // all our current columns
