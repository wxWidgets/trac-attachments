Index: src/msw/notebook.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/notebook.cpp,v
retrieving revision 1.176
diff -u -r1.176 notebook.cpp
--- src/msw/notebook.cpp	2006/05/30 07:42:57	1.176
+++ src/msw/notebook.cpp	2006/06/01 12:10:01
@@ -598,13 +598,13 @@
     }
     if ( HasFlag(wxBK_LEFT) || HasFlag(wxBK_RIGHT) )
     {
-        sizeTotal.x += tabSize.x + 7;
-        sizeTotal.y += 7;
+        sizeTotal.x += tabSize.x + 8;
+        sizeTotal.y += 8;
     }
     else
     {
-        sizeTotal.x += 7;
-        sizeTotal.y += tabSize.y + 7;
+        sizeTotal.x += 8;
+        sizeTotal.y += tabSize.y + 8;
     }
 
     return sizeTotal;

 	  	 
