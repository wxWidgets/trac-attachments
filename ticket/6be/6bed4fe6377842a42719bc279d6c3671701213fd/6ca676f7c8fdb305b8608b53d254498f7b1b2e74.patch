Index: auibook.cpp
===================================================================
--- auibook.cpp	(revision 48219)
+++ auibook.cpp	(working copy)
@@ -3072,6 +3072,8 @@
                                bool select,
                                const wxBitmap& bitmap)
 {
+    page->Reparent(this);
+
     wxAuiNotebookPage info;
     info.window = page;
     info.caption = caption;

 	  	 
