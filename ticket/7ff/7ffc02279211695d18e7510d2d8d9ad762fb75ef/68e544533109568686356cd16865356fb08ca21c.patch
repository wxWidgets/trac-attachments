Index: dataview.cpp
===================================================================
--- dataview.cpp	(revision 65142)
+++ dataview.cpp	(working copy)
@@ -180,7 +180,8 @@
 
         RenderText(m_value,
                    0, // no offset
-                   wxRect(dc->GetTextExtent(m_value)).CentreIn(rect),
+                   rect,
+                   //wxRect(dc->GetTextExtent(m_value)).CentreIn(rect),
                    dc,
                    state);
         return true;
