Index: interface/wx/buffer.h
===================================================================
--- interface/wx/buffer.h	(revision 70597)
+++ interface/wx/buffer.h	(working copy)
@@ -289,7 +289,7 @@
         @param size
             size of the new buffer.
     */
-    wxMemoryBuffer(size_t size = DefBufSize);
+    wxMemoryBuffer(size_t size = wxMemoryBufferData::DefBufSize);
 
     /**
         Append a single byte to the buffer.
