Index: cocoa/cursor.h
===================================================================
--- cocoa/cursor.h	(revision 49117)
+++ cocoa/cursor.h	(working copy)
@@ -39,7 +39,7 @@
 public:
     wxCursor();
 
-    wxCursor(const wxString& name, wxBitmapType type = wxCURSOR_DEFAULT_TYPE,
+    wxCursor(const wxString& name, wxBitmapType type = wxBITMAP_DEFAULT_TYPE,
              int hotSpotX = 0, int hotSpotY = 0);
 
     wxCursor(wxStockCursor id) { InitFromStock(id); }
