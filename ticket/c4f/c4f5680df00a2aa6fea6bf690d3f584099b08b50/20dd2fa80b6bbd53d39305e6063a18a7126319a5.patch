Index: configure.in
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/configure.in,v
retrieving revision 1.1003
diff -b -u -2 -r1.1003 configure.in
--- configure.in	2005/05/27 13:18:46	1.1003
+++ configure.in	2005/05/29 15:52:11
@@ -3297,5 +3297,5 @@
 
 
-dnl ODBC is handled seperately for MSW 
+dnl ODBC is handled separately for MSW 
 if test "$TOOLKIT" != "MSW" ; then
 
@@ -3469,5 +3469,5 @@
         if test "x$OPENGL_LIBS" = "x"; then
             dnl it should be an error and not a warning because OpenGL is not on
-            dnl by default and so if it had been explicitely requested, we
+            dnl by default and so if it had been explicitly requested, we
             dnl shouldn't just fall back to compiling the library without it
             AC_MSG_ERROR(OpenGL libraries not available)
Index: build/bakefiles/wxwin.py
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/build/bakefiles/wxwin.py,v
retrieving revision 1.27
diff -b -u -2 -r1.27 wxwin.py
--- build/bakefiles/wxwin.py	2005/03/21 00:10:46	1.27
+++ build/bakefiles/wxwin.py	2005/05/29 15:52:11
@@ -110,9 +110,9 @@
         for l in lines:
             if not l.startswith('#define'): continue
-            splitted = l.strip().split()
-            if splitted[0] != '#define': continue
-            if len(splitted) < 3: continue
-            name = splitted[1]
-            value = splitted[2]
+            splitline = l.strip().split()
+            if splitline[0] != '#define': continue
+            if len(splitline) < 3: continue
+            name = splitline[1]
+            value = splitline[2]
             if value == None: continue
             if name == 'wxMAJOR_VERSION': major = int(value)
Index: build/bakefiles/wxpresets/bakefile_quickstart.txt
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/build/bakefiles/wxpresets/bakefile_quickstart.txt,v
retrieving revision 1.3
diff -b -u -2 -r1.3 bakefile_quickstart.txt
--- build/bakefiles/wxpresets/bakefile_quickstart.txt	2005/05/12 10:55:10	1.3
+++ build/bakefiles/wxpresets/bakefile_quickstart.txt	2005/05/29 15:52:11
@@ -150,5 +150,5 @@
 
     exe         create an executable file
-    dll         create a shared libary
+    dll         create a shared library
     lib         create a static library
     module      create a library that is loaded at runtime (i.e. a plugin)
Index: build/bakefiles/wxpresets/presets/wx_win32.bkl
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/build/bakefiles/wxpresets/presets/wx_win32.bkl,v
retrieving revision 1.8
diff -b -u -2 -r1.8 wx_win32.bkl
--- build/bakefiles/wxpresets/presets/wx_win32.bkl	2005/05/11 21:29:28	1.8
+++ build/bakefiles/wxpresets/presets/wx_win32.bkl	2005/05/29 15:52:12
@@ -35,5 +35,5 @@
     <!--    configurations you can have (in the build menu),        -->
     <!--    and in autoconf is enabled with enable-xxx=xx.          -->
-    <!--    For other compilers a seperate configuration            -->
+    <!--    For other compilers a separate configuration            -->
     <!--    file is created (such as config.gcc on gcc)             -->
     <!--    which has several options a user can modify.            -->
@@ -156,5 +156,5 @@
     <!--    The "base class" of all our build targets               -->
     <!--    This links with the appropriate native                  -->
-    <!--    libraries required by the platform, the libaries        -->
+    <!--    libraries required by the platform, the libraries       -->
     <!--    we want for our stuff, and the wxWindows libs.          -->
 
Index: contrib/docs/latex/mmedia/sndbase.tex
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/contrib/docs/latex/mmedia/sndbase.tex,v
retrieving revision 1.2
diff -b -u -2 -r1.2 sndbase.tex
--- contrib/docs/latex/mmedia/sndbase.tex	2000/03/14 15:18:18	1.2
+++ contrib/docs/latex/mmedia/sndbase.tex	2005/05/29 15:52:12
@@ -31,6 +31,6 @@
 \twocolwidtha{7cm}
 \begin{twocollist}\itemsep=0pt
-\twocolitem{{\bf wxSOUND\_NOERR}}{No error occured}
-\twocolitem{{\bf wxSOUND\_IOERR}}{An input/output error occured, it may concern
+\twocolitem{{\bf wxSOUND\_NOERR}}{No error occurred}
+\twocolitem{{\bf wxSOUND\_IOERR}}{An input/output error occurred, it may concern
 either a driver or a file}
 \twocolitem{{\bf wxSOUND\_INVFRMT}}{The sound format passed to the function is
@@ -272,5 +272,5 @@
 \constfunc{wxSoundError}{GetError}{\void}
 
-It returns the last error which occured.
+It returns the last error which occurred.
 
 \membersection{wxSoundStream::GetLastAccess}\label{wxsoundstreamgetlastaccess}
@@ -291,4 +291,4 @@
 \func{void}{OnSoundEvent}{\param{int }{evt}}
 
-It is called by the wxSoundStream when a new sound event occured.
+It is called by the wxSoundStream when a new sound event occurred.
 
Index: contrib/include/wx/fl/controlbar.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/contrib/include/wx/fl/controlbar.h,v
retrieving revision 1.21
diff -b -u -2 -r1.21 controlbar.h
--- contrib/include/wx/fl/controlbar.h	2005/05/23 10:11:55	1.21
+++ contrib/include/wx/fl/controlbar.h	2005/05/29 15:52:13
@@ -889,5 +889,5 @@
     DECLARE_DYNAMIC_CLASS( cbBarInfo )
 public:
-    // textual name, by which this bar is refered in layout-customization dialogs
+    // textual name, by which this bar is referred in layout-customization dialogs
     wxString      mName;
 
Index: contrib/include/wx/foldbar/captionbar.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/contrib/include/wx/foldbar/captionbar.h,v
retrieving revision 1.5
diff -b -u -2 -r1.5 captionbar.h
--- contrib/include/wx/foldbar/captionbar.h	2004/12/10 12:00:52	1.5
+++ contrib/include/wx/foldbar/captionbar.h	2005/05/29 15:52:14
@@ -25,5 +25,5 @@
 #define wxFPB_EXTRA_X 10
 #define wxFPB_EXTRA_Y 4
-#define wxFPB_BMP_RIGHTSPACE 2    // pixels of the bmp to be alligned from the right filled with space
+#define wxFPB_BMP_RIGHTSPACE 2    // pixels of the bmp to be aligned from the right filled with space
 
 enum
Index: contrib/include/wx/foldbar/foldpanelbar.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/contrib/include/wx/foldbar/foldpanelbar.h,v
retrieving revision 1.7
diff -b -u -2 -r1.7 foldpanelbar.h
--- contrib/include/wx/foldbar/foldpanelbar.h	2004/12/10 12:00:52	1.7
+++ contrib/include/wx/foldbar/foldpanelbar.h	2005/05/29 15:52:14
@@ -97,5 +97,5 @@
     This control is easy to use. Simply create it as a child for a panel or sash window, and populate panels with
     wxFoldPanelBar::AddFoldPanel(). Then use the wxFoldPanelBar::AddFoldPanelWindow() to add wxWindow derived controls
-    to the current fold panel. Use wxFoldPanelBar::AddFoldPanelSeperator() to put separators between the groups of
+    to the current fold panel. Use wxFoldPanelBar::AddFoldPanelSeparator() to put separators between the groups of
     controls that need a visual separator to group them together. After all is constructed, the user can fold
     the panels by doubleclicking on the bar or single click on the arrow, which will indicate the collapsed or
@@ -126,10 +126,10 @@
 
     /** Returns the length of the panels that are expanded and collapsed. This is useful to determine
-        quickly what size is used to display, and what is left at the bottom (right) to allign
+        quickly what size is used to display, and what is left at the bottom (right) to align
         the collapsed panels. */
     int GetPanelsLength(int &collapsed, int &expanded);
 
     /** Reposition all the collapsed panels to the bottom. When it is not possible to
-        allign them to the bottom, stick them behind the visible panels. The Rect holds the
+        align them to the bottom, stick them behind the visible panels. The Rect holds the
         slack area left between last repositioned panel and the bottom panels. This needs to
         get a refresh */
@@ -168,7 +168,7 @@
     /** Adds a wxWindow derived class to the referenced wxFoldPanel. IMPORTANT: Make the to be created window,
         child of the wxFoldPanel. See example that follows. The flags to be used are:
-        - wxFPB_ALIGN_WIDTH: Which means the wxWindow to be added will be alligned to fit the width of the
+        - wxFPB_ALIGN_WIDTH: Which means the wxWindow to be added will be aligned to fit the width of the
           wxFoldPanel when it is resized. Very handy for sizer items, buttons and text boxes.
-        - wxFPB_ALIGN_LEFT: Alligns left instead of fitting the width of the child window to be added. Use either
+        - wxFPB_ALIGN_LEFT: Aligns left instead of fitting the width of the child window to be added. Use either
           this one or wxFPB_ALIGN_WIDTH.
 
@@ -194,4 +194,4 @@
 
-            // now add a text ctrl. Also very easy. Allign this on width so that when the control gets wider
+            // now add a text ctrl. Also very easy. Align this on width so that when the control gets wider
             // the text control also sizes along.
             m_pnl->AddFoldPanelWindow(item, new wxTextCtrl(item.GetParent(), wxID_ANY, wxT("Comment")), wxFPB_ALIGN_WIDTH, wxFPB_DEFAULT_SPACING, 20);
@@ -207,5 +207,5 @@
                            int rightSpacing = wxFPB_DEFAULT_RIGHTSPACING);
 
-    /** Adds a seperator line to the current wxFoldPanel. The seperator is a simple line which is drawn and is no
-        real component. It can be used to seperate groups of controls which belong to eachother. The colour is
+    /** Adds a separator line to the current wxFoldPanel. The separator is a simple line which is drawn and is no
+        real component. It can be used to separate groups of controls which belong to each other. The colour is
         adjustable, and it takes the same ySpacing, leftSpacing and rightSpacing as AddFoldPanelWindow(). */
Index: contrib/include/wx/foldbar/foldpanelitem.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/contrib/include/wx/foldbar/foldpanelitem.h,v
retrieving revision 1.5
diff -b -u -2 -r1.5 foldpanelitem.h
--- contrib/include/wx/foldbar/foldpanelitem.h	2004/12/10 12:00:52	1.5
+++ contrib/include/wx/foldbar/foldpanelitem.h	2005/05/29 15:52:14
@@ -122,5 +122,5 @@
 
     // resize the element, whatever it is. A separator or
-    // line will be always alligned by width or hight
+    // line will be always aligned by width or height
     // depending on orientation of the whole panel
 
@@ -128,5 +128,5 @@
         if(m_flags & wxFPB_ALIGN_WIDTH)
         {
-            // allign by taking full width
+            // align by taking full width
             int mySize = size - m_leftSpacing - m_rightSpacing;
 
@@ -186,5 +186,5 @@
 
     /** Add a window item to the list of items on this panel. The flags are wxFPB_ALIGN_LEFT for a non sizing
-        window element, and wxFPB_ALIGN_WIDTH for a width alligned item. The ySpacing parameter reserves a number
+        window element, and wxFPB_ALIGN_WIDTH for a width aligned item. The ySpacing parameter reserves a number
         of pixels before the window element, and leftSpacing is an indent. rightSpacing is only relevant when the
         style wxFPB_ALIGN_WIDTH is chosen. */
Index: contrib/include/wx/mmedia/sndbase.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/contrib/include/wx/mmedia/sndbase.h,v
retrieving revision 1.5
diff -b -u -2 -r1.5 sndbase.h
--- contrib/include/wx/mmedia/sndbase.h	2004/11/22 20:05:24	1.5
+++ contrib/include/wx/mmedia/sndbase.h	2005/05/29 15:52:14
@@ -46,6 +46,6 @@
 // ---------------------
 // wxSoundError:
-//   - wxSOUND_NOERR: No error occured
-//   - wxSOUND_IOERR: an input/output error occured, it may concern either
+//   - wxSOUND_NOERR: No error occurred
+//   - wxSOUND_IOERR: an input/output error occurred, it may concern either
 //                    a driver or a file
 //   - wxSOUND_INVFRMT: the sound format passed to the function is invalid.
@@ -84,5 +84,5 @@
 // wxSoundCallback(stream, evt, cdata): C callback for sound event.
 //    - stream: current wxSoundStream
-//    - evt: the sound event which has occured, it may be wxSOUND_INPUT,
+//    - evt: the sound event which has occurred, it may be wxSOUND_INPUT,
 //            wxSOUND_OUTPUT or wxSOUND_DUPLEX
 //    - cdata: User callback data
Index: contrib/src/fl/dyntbar.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/contrib/src/fl/dyntbar.cpp,v
retrieving revision 1.13
diff -b -u -2 -r1.13 dyntbar.cpp
--- contrib/src/fl/dyntbar.cpp	2004/06/07 16:02:23	1.13
+++ contrib/src/fl/dyntbar.cpp	2005/05/29 15:52:15
@@ -427,5 +427,5 @@
             }
 
-            // Do we need to set a new size to a seperator object?
+            // Do we need to set a new size to a separator object?
             if ( info.mpToolWnd )
             {
Index: contrib/src/foldbar/foldpanelbar.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/contrib/src/foldbar/foldpanelbar.cpp,v
retrieving revision 1.5
diff -b -u -2 -r1.5 foldpanelbar.cpp
--- contrib/src/foldbar/foldpanelbar.cpp	2005/02/10 15:29:58	1.5
+++ contrib/src/foldbar/foldpanelbar.cpp	2005/05/29 15:52:16
@@ -353,5 +353,5 @@
 
     // assumed here that all the panels that are expanded
-    // are positioned after eachother from 0,0 to end.
+    // are positioned after each other from 0,0 to end.
 
     for(size_t j = 0; j < m_panels.GetCount(); j++)
Index: contrib/src/mmedia/sndcpcm.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/contrib/src/mmedia/sndcpcm.cpp,v
retrieving revision 1.6
diff -b -u -2 -r1.6 sndcpcm.cpp
--- contrib/src/mmedia/sndcpcm.cpp	2004/11/22 20:10:24	1.6
+++ contrib/src/mmedia/sndcpcm.cpp	2005/05/29 15:52:17
@@ -378,5 +378,5 @@
         delete[] m_prebuffer;
 
-    // We try to minimize the need of dynamic memory allocation by preallocating a buffer. But
+    // We try to minimize the need for dynamic memory allocation by preallocating a buffer. But
     // to be sure it will be efficient we minimize the best size.
     if (m_multiplier_in < m_multiplier_out) {
Index: contrib/src/mmedia/sndwin.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/contrib/src/mmedia/sndwin.cpp,v
retrieving revision 1.10
diff -b -u -2 -r1.10 sndwin.cpp
--- contrib/src/mmedia/sndwin.cpp	2004/11/22 20:10:26	1.10
+++ contrib/src/mmedia/sndwin.cpp	2005/05/29 15:52:17
@@ -383,5 +383,5 @@
 // queue. It uses AllocHeader() for each element.
 //
-// Once it has allocated all blocks, it returns true and if an error occured
+// Once it has allocated all blocks, it returns true and if an error occurred
 // it returns false.
 // -------------------------------------------------------------------------
Index: contrib/src/stc/scintilla/src/AutoComplete.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/contrib/src/stc/scintilla/src/AutoComplete.h,v
retrieving revision 1.6
diff -b -u -2 -r1.6 AutoComplete.h
--- contrib/src/stc/scintilla/src/AutoComplete.h	2003/04/19 19:59:50	1.6
+++ contrib/src/stc/scintilla/src/AutoComplete.h	2005/05/29 15:52:17
@@ -16,5 +16,5 @@
 	char fillUpChars[256];
 	char separator;
-	char typesep; // Type seperator
+	char typesep; // Type separator
 
 public:
@@ -51,5 +51,5 @@
 	char GetSeparator();
 
-	/// The typesep character is used for seperating the word from the type
+	/// The typesep character is used for separating the word from the type
 	void SetTypesep(char separator_);
 	char GetTypesep();
Index: contrib/src/stc/scintilla/src/LexMetapost.cxx
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/contrib/src/stc/scintilla/src/LexMetapost.cxx,v
retrieving revision 1.1
diff -b -u -2 -r1.1 LexMetapost.cxx
--- contrib/src/stc/scintilla/src/LexMetapost.cxx	2004/02/06 00:02:50	1.1
+++ contrib/src/stc/scintilla/src/LexMetapost.cxx	2005/05/29 15:52:17
@@ -161,6 +161,6 @@
 
 	// 0  no keyword highlighting
-	// 1  metapost keyword hightlighting
-	// 2+ metafun keyword hightlighting
+	// 1  metapost keyword highlighting
+	// 2+ metafun keyword highlighting
 
 	int extraInterface = 0 ;
Index: contrib/utils/convertrc/rc2wxr.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/contrib/utils/convertrc/rc2wxr.cpp,v
retrieving revision 1.15
diff -b -u -2 -r1.15 rc2wxr.cpp
--- contrib/utils/convertrc/rc2wxr.cpp	2005/03/31 19:10:25	1.15
+++ contrib/utils/convertrc/rc2wxr.cpp	2005/05/29 15:52:18
@@ -400,5 +400,5 @@
 
 
-bool rc2wxr::Seperator(int ch)
+bool rc2wxr::Separator(int ch)
 
 {
@@ -514,5 +514,5 @@
 
 
-while (Seperator(ch))
+while (Separator(ch))
 
 {
@@ -542,5 +542,5 @@
 
 
-while (!Seperator(ch))
+while (!Separator(ch))
 
 {
Index: contrib/utils/convertrc/rc2wxr.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/contrib/utils/convertrc/rc2wxr.h,v
retrieving revision 1.6
diff -b -u -2 -r1.6 rc2wxr.h
--- contrib/utils/convertrc/rc2wxr.h	2005/03/31 19:10:26	1.6
+++ contrib/utils/convertrc/rc2wxr.h	2005/05/29 15:52:18
@@ -31,5 +31,5 @@
     void ParseTextCtrl();
     void ParsePushButton();
-    bool Seperator(int ch);
+    bool Separator(int ch);
     void ParseGroupBox();
     void ReadRect(int & x, int & y, int & width, int & height);
Index: contrib/utils/convertrc/rc2xml.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/contrib/utils/convertrc/rc2xml.cpp,v
retrieving revision 1.19
diff -b -u -2 -r1.19 rc2xml.cpp
--- contrib/utils/convertrc/rc2xml.cpp	2005/03/31 19:10:26	1.19
+++ contrib/utils/convertrc/rc2xml.cpp	2005/05/29 15:52:18
@@ -343,5 +343,5 @@
 
 
-bool rc2xml::Seperator(int ch)
+bool rc2xml::Separator(int ch)
 {
 //if ((ch==' ')|(ch==',')|(ch==13)|(ch==10)|(ch=='|')|(ch=='\t'))
@@ -388,5 +388,5 @@
 }
 
-wxString rc2xml::GetToken(bool *listseperator)
+wxString rc2xml::GetToken(bool *listseparator)
 {
     wxString token=wxEmptyString;
@@ -406,5 +406,5 @@
     }
 
-    while (Seperator(ch))
+    while (Separator(ch))
     {
     ReadChar(ch);
@@ -419,5 +419,5 @@
 
 
-    while (!Seperator(ch))
+    while (!Separator(ch))
     {
     token += (char)ch;
@@ -428,6 +428,6 @@
         m_done = true;
 
-    if (listseperator)
-      *listseperator = (ch == ',');
+    if (listseparator)
+      *listseparator = (ch == ',');
     return token;
 }
Index: contrib/utils/convertrc/rc2xml.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/contrib/utils/convertrc/rc2xml.h,v
retrieving revision 1.9
diff -b -u -2 -r1.9 rc2xml.h
--- contrib/utils/convertrc/rc2xml.h	2005/03/31 19:10:26	1.9
+++ contrib/utils/convertrc/rc2xml.h	2005/05/29 15:52:18
@@ -63,8 +63,8 @@
     void ParseCheckBox(wxString phrase,wxString varname);
     void ParsePushButton(wxString phrase, wxString varname);
-    bool Seperator(int ch);
+    bool Separator(int ch);
     void ParseGroupBox(wxString phrase, wxString varname);
     bool ReadRect(int & x, int & y, int & width, int & height);
-    wxString GetToken(bool *listseperator = 0);
+    wxString GetToken(bool *listseparator = 0);
     wxString GetQuoteField();
     wxString GetStringQuote();
Index: demos/bombs/readme.txt
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/demos/bombs/readme.txt,v
retrieving revision 1.1
diff -b -u -2 -r1.1 readme.txt
--- demos/bombs/readme.txt	2000/01/08 15:27:37	1.1
+++ demos/bombs/readme.txt	2005/05/29 15:52:18
@@ -8,5 +8,5 @@
 
 2. Installation
-If you are reading this file, I suppose you have succesfully unpacked the
+If you are reading this file, I suppose you have successfully unpacked the
 files in a directory of your hard disk :-). You should already have
 installed wxWin on your system.
Index: demos/dbbrowse/browsedb.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/demos/dbbrowse/browsedb.cpp,v
retrieving revision 1.32
diff -b -u -2 -r1.32 browsedb.cpp
--- demos/dbbrowse/browsedb.cpp	2005/01/31 18:09:45	1.32
+++ demos/dbbrowse/browsedb.cpp	2005/05/29 15:52:19
@@ -58,5 +58,5 @@
     // msg += _T("\n");
     /* Scan through each database connection displaying
-    * any ODBC errors that have occured. */
+    * any ODBC errors that have occurred. */
     wxDbList *pDbList;
     for (pDbList = PtrBegDbList; pDbList; pDbList = pDbList->PtrNext)
Index: demos/dbbrowse/dbbrowse.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/demos/dbbrowse/dbbrowse.cpp,v
retrieving revision 1.20
diff -b -u -2 -r1.20 dbbrowse.cpp
--- demos/dbbrowse/dbbrowse.cpp	2005/03/31 18:52:50	1.20
+++ demos/dbbrowse/dbbrowse.cpp	2005/05/29 15:52:19
@@ -236,5 +236,5 @@
     frame->pDoc->p_Splitter->pDoc     = frame->pDoc;       // ControlBase: saving the Sash
     //---------------------------------------------------------------------------------------
-    //-- Problem : GetClientSize(Width,Hight) are not the same as the values given in the ---
+    //-- Problem : GetClientSize(Width,Height) are not the same as the values given in the ---
     //--            construction of the Frame.                                            ---
     //-- Solved  : GetClientSize is called here and the difference is noted. When the     ---
Index: demos/dbbrowse/pgmctrl.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/demos/dbbrowse/pgmctrl.cpp,v
retrieving revision 1.18
diff -b -u -2 -r1.18 pgmctrl.cpp
--- demos/dbbrowse/pgmctrl.cpp	2005/01/31 18:09:46	1.18
+++ demos/dbbrowse/pgmctrl.cpp	2005/05/29 15:52:19
@@ -167,7 +167,7 @@
         {
             if (p_ProgramCfg->DeleteAll()) // Default Diretory for wxFileSelector
-                Temp0 = _("-I-> wxConfigBase.p_ProgramCfg->DeleteAll() was succesfull.");
+                Temp0 = _("-I-> wxConfigBase.p_ProgramCfg->DeleteAll() was successful.");
             else
-                Temp0 = _("-E-> wxConfigBase.p_ProgramCfg->DeleteAll() was not succesfull !");
+                Temp0 = _("-E-> wxConfigBase.p_ProgramCfg->DeleteAll() was not successful !");
             wxBell();     // Ding_a_Ling
             Treffer++;
Index: demos/dbbrowse/de/dbbrowse.po
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/demos/dbbrowse/de/dbbrowse.po,v
retrieving revision 1.8
diff -b -u -2 -r1.8 dbbrowse.po
--- demos/dbbrowse/de/dbbrowse.po	2000/03/12 09:11:36	1.8
+++ demos/dbbrowse/de/dbbrowse.po	2005/05/29 15:52:20
@@ -95,5 +95,5 @@
 #: ../browsedb.cpp:117
 msgid   "\n"
-        "-E-> BrowserDB::OnStartDB() : DB CONNECTION ERROR : A problem occured while trying to get a connection to the data source"
+        "-E-> BrowserDB::OnStartDB() : DB CONNECTION ERROR : A problem occurred while trying to get a connection to the data source"
 msgstr  "\n"
         "-E-> BrowserDB::OnStartDB() : DB VERBINDUNGSFEHLER : Verbindungsaufbau zur Datenquelle nicht möglich"
@@ -301,5 +301,5 @@
 
 #: ../pgmctrl.cpp:165
-msgid   "-E-> wxConfigBase.p_ProgramCfg->DeleteAll() was not succesfull !"
+msgid   "-E-> wxConfigBase.p_ProgramCfg->DeleteAll() was not successful !"
 msgstr  "-E-> wxConfigBase.p_ProgramCfg->DeleteAll() war nicht Erfolgreich !"
 
@@ -366,5 +366,5 @@
 
 #: ../pgmctrl.cpp:163
-msgid   "-I-> wxConfigBase.p_ProgramCfg->DeleteAll() was succesfull."
+msgid   "-I-> wxConfigBase.p_ProgramCfg->DeleteAll() was successful."
 msgstr  "-I-> wxConfigBase.p_ProgramCfg->DeleteAll() war Erfolgreich !"
 
Index: docs/latex/wx/array.tex
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/docs/latex/wx/array.tex,v
retrieving revision 1.31
diff -b -u -2 -r1.31 array.tex
--- docs/latex/wx/array.tex	2004/10/22 19:15:30	1.31
+++ docs/latex/wx/array.tex	2005/05/29 15:52:20
@@ -165,5 +165,5 @@
 
 Note that the above macros are generally only useful for 
-wxObject types.  There are seperate macros for declaring an array of a simple type, 
+wxObject types.  There are separate macros for declaring an array of a simple type, 
 such as an int.
 
Index: docs/latex/wx/datetime.tex
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/docs/latex/wx/datetime.tex,v
retrieving revision 1.27
diff -b -u -2 -r1.27 datetime.tex
--- docs/latex/wx/datetime.tex	2005/02/22 15:09:48	1.27
+++ docs/latex/wx/datetime.tex	2005/05/29 15:52:21
@@ -98,5 +98,5 @@
 
 \helpref{GetMonthName()}{wxdatetimegetmonthname} and
-\helpref{GetWeekDayName}{wxdatetimegetweekdayname} functions use the followign
+\helpref{GetWeekDayName}{wxdatetimegetweekdayname} functions use the following
 flags:
 
Index: docs/latex/wx/debugrpt.tex
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/docs/latex/wx/debugrpt.tex,v
retrieving revision 1.5
diff -b -u -2 -r1.5 debugrpt.tex
--- docs/latex/wx/debugrpt.tex	2005/04/17 16:35:04	1.5
+++ docs/latex/wx/debugrpt.tex	2005/05/29 15:52:22
@@ -167,5 +167,5 @@
 
 Returns \true if file could be added successfully, \false if an IO error
-occured.
+occurred.
 
 
Index: docs/latex/wx/keyevent.tex
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/docs/latex/wx/keyevent.tex,v
retrieving revision 1.22
diff -b -u -2 -r1.22 keyevent.tex
--- docs/latex/wx/keyevent.tex	2004/10/06 19:46:11	1.22
+++ docs/latex/wx/keyevent.tex	2005/05/29 15:52:22
@@ -42,5 +42,5 @@
 
 {\bf Note:} If a key down ({\tt EVT\_KEY\_DOWN}) event is caught and
-the event handler does not call {\tt event.Skip()} then the coresponding
+the event handler does not call {\tt event.Skip()} then the corresponding
 char event ({\tt EVT\_CHAR}) will not happen.  This is by design and
 enables the programs that handle both types of events to be a bit
Index: docs/latex/wx/sizeritem.tex
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/docs/latex/wx/sizeritem.tex,v
retrieving revision 1.6
diff -b -u -2 -r1.6 sizeritem.tex
--- docs/latex/wx/sizeritem.tex	2005/03/21 23:49:46	1.6
+++ docs/latex/wx/sizeritem.tex	2005/05/29 15:52:22
@@ -146,5 +146,5 @@
 \constfunc{wxWindow*}{GetWindow}{\void}
 
-If this item is trackign a window then return it.  NULL otherwise.
+If this item is tracking a window then return it.  NULL otherwise.
 
 \membersection{wxSizerItem::IsShown}\label{wxsizeritemisshown}
Index: docs/latex/wx/socket.tex
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/docs/latex/wx/socket.tex,v
retrieving revision 1.43
diff -b -u -2 -r1.43 socket.tex
--- docs/latex/wx/socket.tex	2005/02/22 15:09:53	1.43
+++ docs/latex/wx/socket.tex	2005/05/29 15:52:23
@@ -561,6 +561,6 @@
 This is mostly used on UNIX-based systems to allow rapid starting and stopping of a server - 
 otherwise you may have to wait several minutes for the port to become available.
-This option can have suprising platform dependent behavior, check the documentation for
-your platforms implementation of setsockopt().
+This option can have surprising platform dependent behavior, check the documentation for
+your platform's implementation of setsockopt().
 
 So:
Index: docs/latex/wx/uri.tex
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/docs/latex/wx/uri.tex,v
retrieving revision 1.15
diff -b -u -2 -r1.15 uri.tex
--- docs/latex/wx/uri.tex	2005/04/03 03:48:04	1.15
+++ docs/latex/wx/uri.tex	2005/05/29 15:52:23
@@ -99,5 +99,5 @@
 \constfunc{wxString}{BuildURI}{\void}
 
-Builds the URI from its individual components and adds proper seperators.
+Builds the URI from its individual components and adds proper separators.
 
 If the URI is not a reference or is not resolved, 
@@ -110,5 +110,5 @@
 \constfunc{wxString}{BuildUnescapedURI}{\void}
 
-Builds the URI from its individual components, adds proper seperators, and
+Builds the URI from its individual components, adds proper separators, and
 returns escape sequences to normal characters.
 
Index: docs/latex/wx/wxstring.tex
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/docs/latex/wx/wxstring.tex,v
retrieving revision 1.53
diff -b -u -2 -r1.53 wxstring.tex
--- docs/latex/wx/wxstring.tex	2005/05/22 16:56:10	1.53
+++ docs/latex/wx/wxstring.tex	2005/05/29 15:52:24
@@ -1258,5 +1258,5 @@
 
 Note that the exact usage of this depends on whether on not wxUSE\_STL is enabled.  If
-wxUSE\_STL is enabled, wxStringBuffer creates a seperate empty character buffer, and
+wxUSE\_STL is enabled, wxStringBuffer creates a separate empty character buffer, and
 if wxUSE\_STL is disabled, it uses GetWriteBuf() from wxString, keeping the same buffer
 wxString uses intact.  In other words, relying on wxStringBuffer containing the old 
@@ -1323,5 +1323,5 @@
 
 Note that the exact usage of this depends on whether on not wxUSE\_STL is enabled.  If
-wxUSE\_STL is enabled, wxStringBuffer creates a seperate empty character buffer, and
+wxUSE\_STL is enabled, wxStringBuffer creates a separate empty character buffer, and
 if wxUSE\_STL is disabled, it uses GetWriteBuf() from wxString, keeping the same buffer
 wxString uses intact.  In other words, relying on wxStringBuffer containing the old 
Index: docs/tech/tn0014.txt
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/docs/tech/tn0014.txt,v
retrieving revision 1.17
diff -b -u -2 -r1.17 tn0014.txt
--- docs/tech/tn0014.txt	2005/05/18 13:58:18	1.17
+++ docs/tech/tn0014.txt	2005/05/29 15:52:24
@@ -112,5 +112,5 @@
 
 <object_ref> node is identical to <object>, except that it does _not_ have
-"class" property and has additional required property "ref". It's concept is
+"class" property and has additional required property "ref". Its concept is
 similar to Unix symlinks: value of the "ref" property is equal to the value of
 "name" property of some existing node (called referred node) in the resources
Index: include/msvc/wx/setup.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/msvc/wx/setup.h,v
retrieving revision 1.4
diff -b -u -2 -r1.4 setup.h
--- include/msvc/wx/setup.h	2005/04/18 07:24:21	1.4
+++ include/msvc/wx/setup.h	2005/05/29 15:52:24
@@ -12,5 +12,5 @@
 // VC++ IDE predefines _DEBUG and _UNICODE for the new projects itself, but
 // the other symbols (WXUSINGDLL, __WXUNIVERSAL__, ...) should be defined
-// explicitely!
+// explicitly!
 
 #ifdef _MSC_VER
Index: include/wx/app.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/app.h,v
retrieving revision 1.126
diff -b -u -2 -r1.126 app.h
--- include/wx/app.h	2005/01/10 17:20:48	1.126
+++ include/wx/app.h	2005/05/29 15:52:25
@@ -264,5 +264,5 @@
     // dialog box)
     //
-    // the arguments are the place where the assert occured, the text of the
+    // the arguments are the place where the assert occurred, the text of the
     // assert itself and the user-specified message
 #ifdef __WXDEBUG__
Index: include/wx/cmdline.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/cmdline.h,v
retrieving revision 1.25
diff -b -u -2 -r1.25 cmdline.h
--- include/wx/cmdline.h	2004/12/25 01:58:35	1.25
+++ include/wx/cmdline.h	2005/05/29 15:52:25
@@ -175,5 +175,5 @@
     // parse the command line, return 0 if ok, -1 if "-h" or "--help" option
     // was encountered and the help message was given or a positive value if a
-    // syntax error occured
+    // syntax error occurred
     //
     // if showUsage is true, Usage() is called in case of syntax error or if
Index: include/wx/datetime.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/datetime.h,v
retrieving revision 1.62
diff -b -u -2 -r1.62 datetime.h
--- include/wx/datetime.h	2005/05/13 12:39:15	1.62
+++ include/wx/datetime.h	2005/05/29 15:52:26
@@ -1151,5 +1151,5 @@
     }
 
-        // return this timespan with inversed sign
+        // return this timespan with opposite sign
     wxTimeSpan Negate() const { return wxTimeSpan(-GetValue()); }
         // negate the value of the timespan
Index: include/wx/db.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/db.h,v
retrieving revision 1.96
diff -b -u -2 -r1.96 db.h
--- include/wx/db.h	2005/05/22 22:57:15	1.96
+++ include/wx/db.h	2005/05/29 15:52:27
@@ -790,5 +790,5 @@
 #if wxODBC_BACKWARD_COMPATABILITY
 //#################################################################################
-//############### DEPRECATED functions for backward compatability #################
+//############### DEPRECATED functions for backward compatibility #################
 //#################################################################################
 
Index: include/wx/dbtable.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/dbtable.h,v
retrieving revision 1.45
diff -b -u -2 -r1.45 dbtable.h
--- include/wx/dbtable.h	2005/03/16 16:18:18	1.45
+++ include/wx/dbtable.h	2005/05/29 15:52:27
@@ -361,5 +361,5 @@
 #if wxODBC_BACKWARD_COMPATABILITY
 //#################################################################################
-//############### DEPRECATED functions for backward compatability #################
+//############### DEPRECATED functions for backward compatibility #################
 //#################################################################################
 
Index: include/wx/defs.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/defs.h,v
retrieving revision 1.500
diff -b -u -2 -r1.500 defs.h
--- include/wx/defs.h	2005/05/22 18:19:03	1.500
+++ include/wx/defs.h	2005/05/29 15:52:29
@@ -1900,5 +1900,5 @@
 };
 
-/*  the values of the format constants should be the same as correspondign */
+/*  the values of the format constants should be the same as corresponding */
 /*  CF_XXX constants in Windows API */
 enum wxDataFormatId
Index: include/wx/dynlib.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/dynlib.h,v
retrieving revision 1.52
diff -b -u -2 -r1.52 dynlib.h
--- include/wx/dynlib.h	2005/03/16 16:18:18	1.52
+++ include/wx/dynlib.h	2005/05/29 15:52:29
@@ -250,5 +250,5 @@
     // return all modules/shared libraries in the address space of this process
     //
-    // returns an empty array if not implemented or an error occured
+    // returns an empty array if not implemented or an error occurred
     static wxDynamicLibraryDetailsArray ListLoaded();
 
Index: include/wx/event.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/event.h,v
retrieving revision 1.232
diff -b -u -2 -r1.232 event.h
--- include/wx/event.h	2005/05/22 11:21:18	1.232
+++ include/wx/event.h	2005/05/29 15:52:31
@@ -2411,5 +2411,5 @@
     //     It is part of wxBase which doesn't use validators and the code
     //     is compiled out when building wxBase w/o GUI classes, which affects
-    //     binary compatiblity and wxBase library can't be used by GUI
+    //     binary compatibility and wxBase library can't be used by GUI
     //     ports.
     virtual bool TryValidator(wxEvent& WXUNUSED(event)) { return false; }
Index: include/wx/ffile.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/ffile.h,v
retrieving revision 1.21
diff -b -u -2 -r1.21 ffile.h
--- include/wx/ffile.h	2005/02/19 15:52:07	1.21
+++ include/wx/ffile.h	2005/05/29 15:52:31
@@ -63,5 +63,5 @@
   bool ReadAll(wxString *str, wxMBConv& conv = wxConvUTF8);
     // returns number of bytes read - use Eof() and Error() to see if an error
-    // occured or not
+    // occurred or not
   size_t Read(void *pBuf, size_t nCount);
     // returns the number of bytes written
@@ -93,5 +93,5 @@
     // is end of file reached?
   bool Eof() const { return feof(m_fp) != 0; }
-    // is an error occured?
+    // has an error occurred?
   bool Error() const { return ferror(m_fp) != 0; }
     // get the file name
Index: include/wx/file.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/file.h,v
retrieving revision 1.45
diff -b -u -2 -r1.45 file.h
--- include/wx/file.h	2005/03/16 16:18:18	1.45
+++ include/wx/file.h	2005/05/29 15:52:31
@@ -126,5 +126,5 @@
     // is end of file reached?
   bool Eof() const;
-    // has an error occured?
+    // has an error occurred?
   bool Error() const { return m_error; }
     // type such as disk or pipe
Index: include/wx/font.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/font.h,v
retrieving revision 1.49
diff -b -u -2 -r1.49 font.h
--- include/wx/font.h	2004/12/20 12:43:48	1.49
+++ include/wx/font.h	2005/05/29 15:52:31
@@ -227,5 +227,5 @@
     // encoding, but may be changed by the application using
     // SetDefaultEncoding() to make all subsequent fonts created without
-    // specifing encoding parameter using this encoding
+    // specifying encoding parameter using this encoding
     static wxFontEncoding ms_encodingDefault;
 };
Index: include/wx/intl.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/intl.h,v
retrieving revision 1.56
diff -b -u -2 -r1.56 intl.h
--- include/wx/intl.h	2004/09/16 18:13:25	1.56
+++ include/wx/intl.h	2005/05/29 15:52:31
@@ -379,5 +379,5 @@
              const wxChar *szLocale = (const wxChar *) NULL,     // locale (for setlocale)
              bool bLoadDefault = true,                           // preload wxstd.mo?
-             bool bConvertEncoding = false)                      // convert Win<->Unix if neccessary?
+             bool bConvertEncoding = false)                      // convert Win<->Unix if necessary?
         {
             DoCommonInit();
@@ -408,5 +408,5 @@
     ~wxLocale();
 
-    // Try to get user's (or OS's) prefered language setting.
+    // Try to get user's (or OS's) preferred language setting.
     // Return wxLANGUAGE_UNKNOWN if language-guessing algorithm failed
     static int GetSystemLanguage();
Index: include/wx/menuitem.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/menuitem.h,v
retrieving revision 1.34
diff -b -u -2 -r1.34 menuitem.h
--- include/wx/menuitem.h	2005/03/16 16:18:20	1.34
+++ include/wx/menuitem.h	2005/05/29 15:52:31
@@ -130,5 +130,5 @@
     wxString      m_text,           // label of the item
                   m_help;           // the help string for the item
-    wxItemKind    m_kind;           // seperator/normal/check/radio item?
+    wxItemKind    m_kind;           // separator/normal/check/radio item?
     bool          m_isChecked;      // is checked?
     bool          m_isEnabled;      // is enabled?
Index: include/wx/mimetype.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/mimetype.h,v
retrieving revision 1.37
diff -b -u -2 -r1.37 mimetype.h
--- include/wx/mimetype.h	2005/01/21 18:48:20	1.37
+++ include/wx/mimetype.h	2005/05/29 15:52:32
@@ -321,5 +321,5 @@
 
     // other operations: return true if there were no errors or false if there
-    // were some unreckognized entries (the good entries are always read anyhow)
+    // were some unrecognized entries (the good entries are always read anyhow)
     //
     // FIXME: These ought to be private ??
Index: include/wx/rawbmp.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/rawbmp.h,v
retrieving revision 1.27
diff -b -u -2 -r1.27 rawbmp.h
--- include/wx/rawbmp.h	2005/03/01 17:49:27	1.27
+++ include/wx/rawbmp.h	2005/05/29 15:52:32
@@ -154,5 +154,5 @@
 // the (most common) native bitmap format without alpha support
 #if defined(__WXMSW__)
-    // under MSW the RGB components are inversed, they're in BGR order
+    // under MSW the RGB components are reversed, they're in BGR order
     typedef wxPixelFormat<unsigned char, 24, 2, 1, 0> wxNativePixelFormat;
 
Index: include/wx/setup_inc.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/setup_inc.h,v
retrieving revision 1.22
diff -b -u -2 -r1.22 setup_inc.h
--- include/wx/setup_inc.h	2005/05/23 11:10:15	1.22
+++ include/wx/setup_inc.h	2005/05/29 15:52:33
@@ -965,5 +965,5 @@
 
 // Default is 0.  Set to 1 to use the deprecated classes, enum types, function,
-// member variables.  With a setting of 1, full backward compatability with the
+// member variables.  With a setting of 1, full backward compatibility with the
 // 2.0.x release is possible. It is STRONGLY recommended that this be set to 0,
 // as future development will be done only on the non-deprecated
Index: include/wx/spinbutt.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/spinbutt.h,v
retrieving revision 1.33
diff -b -u -2 -r1.33 spinbutt.h
--- include/wx/spinbutt.h	2005/03/08 20:53:16	1.33
+++ include/wx/spinbutt.h	2005/05/29 15:52:33
@@ -40,5 +40,5 @@
 {
 public:
-    // ctor intiializes the range with the default (0..100) values
+    // ctor initializes the range with the default (0..100) values
     wxSpinButtonBase() { m_min = 0; m_max = 100; }
 
Index: include/wx/stream.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/stream.h,v
retrieving revision 1.68
diff -b -u -2 -r1.68 stream.h
--- include/wx/stream.h	2005/04/17 16:50:09	1.68
+++ include/wx/stream.h	2005/05/29 15:52:33
@@ -224,5 +224,5 @@
     // do read up to size bytes of data into the provided buffer
     //
-    // this method should return 0 if EOF has been reached or an error occured
+    // this method should return 0 if EOF has been reached or an error occurred
     // (m_lasterror should be set accordingly as well) or the number of bytes
     // read
Index: include/wx/treebase.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/treebase.h,v
retrieving revision 1.51
diff -b -u -2 -r1.51 treebase.h
--- include/wx/treebase.h	2005/05/23 10:28:17	1.51
+++ include/wx/treebase.h	2005/05/29 15:52:33
@@ -360,5 +360,5 @@
 #define EVT_TREE_BEGIN_RDRAG(id, fn) wx__DECLARE_TREEEVT(BEGIN_RDRAG, id, fn)
 
-// GetItem() is the item on which the drop occured (if any) and GetPoint() the
+// GetItem() is the item on which the drop occurred (if any) and GetPoint() the
 // current mouse coords
 #define EVT_TREE_END_DRAG(id, fn) wx__DECLARE_TREEEVT(END_DRAG, id, fn)
Index: include/wx/html/helpdata.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/html/helpdata.h,v
retrieving revision 1.34
diff -b -u -2 -r1.34 helpdata.h
--- include/wx/html/helpdata.h	2004/10/11 16:30:25	1.34
+++ include/wx/html/helpdata.h	2005/05/29 15:52:34
@@ -154,6 +154,6 @@
 
 
-// State information of a search action. I'd have prefered to make this a nested
-// class inside wxHtmlHelpData, but that's against coding standards :-(
+// State information of a search action. I'd have preferred to make this a
+// nested class inside wxHtmlHelpData, but that's against coding standards :-(
 // Never construct this class yourself, obtain a copy from
 // wxHtmlHelpData::PrepareKeywordSearch(const wxString& key)
Index: include/wx/html/htmlwin.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/html/htmlwin.h,v
retrieving revision 1.65
diff -b -u -2 -r1.65 htmlwin.h
--- include/wx/html/htmlwin.h	2005/02/17 23:55:53	1.65
+++ include/wx/html/htmlwin.h	2005/05/29 15:52:34
@@ -90,5 +90,5 @@
     // it is NOT address/filename of HTML document. If you want to
     // specify document location, use LoadPage() istead
-    // Return value : false if an error occured, true otherwise
+    // Return value : false if an error occurred, true otherwise
     bool SetPage(const wxString& source);
 
Index: include/wx/mac/setup0.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/mac/setup0.h,v
retrieving revision 1.64
diff -b -u -2 -r1.64 setup0.h
--- include/wx/mac/setup0.h	2005/04/20 16:14:01	1.64
+++ include/wx/mac/setup0.h	2005/05/29 15:52:35
@@ -985,5 +985,5 @@
 
 // Default is 0.  Set to 1 to use the deprecated classes, enum types, function,
-// member variables.  With a setting of 1, full backward compatability with the
+// member variables.  With a setting of 1, full backward compatibility with the
 // 2.0.x release is possible. It is STRONGLY recommended that this be set to 0,
 // as future development will be done only on the non-deprecated
Index: include/wx/msw/crashrpt.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/msw/crashrpt.h,v
retrieving revision 1.6
diff -b -u -2 -r1.6 crashrpt.h
--- include/wx/msw/crashrpt.h	2005/01/17 02:03:59	1.6
+++ include/wx/msw/crashrpt.h	2005/05/29 15:52:35
@@ -25,5 +25,5 @@
 enum
 {
-    // we always report where the crash occured
+    // we always report where the crash occurred
     wxCRASH_REPORT_LOCATION = 0,
 
Index: include/wx/msw/dcclient.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/msw/dcclient.h,v
retrieving revision 1.20
diff -b -u -2 -r1.20 dcclient.h
--- include/wx/msw/dcclient.h	2004/05/23 20:51:27	1.20
+++ include/wx/msw/dcclient.h	2005/05/29 15:52:35
@@ -47,5 +47,5 @@
 
 protected:
-    // intiialize the newly created DC
+    // initialize the newly created DC
     void InitDC();
 
Index: include/wx/msw/menu.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/msw/menu.h,v
retrieving revision 1.60
diff -b -u -2 -r1.60 menu.h
--- include/wx/msw/menu.h	2005/03/17 23:44:53	1.60
+++ include/wx/msw/menu.h	2005/05/29 15:52:35
@@ -202,5 +202,5 @@
     const wxAcceleratorTable& GetAccelTable() const { return m_accelTable; }
 
-    // update the accel table (must be called after adding/deletign a menu)
+    // update the accel table (must be called after adding/deleting a menu)
     void RebuildAccelTable();
 #endif // wxUSE_ACCEL
Index: include/wx/msw/setup0.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/msw/setup0.h,v
retrieving revision 1.208
diff -b -u -2 -r1.208 setup0.h
--- include/wx/msw/setup0.h	2005/04/10 14:49:01	1.208
+++ include/wx/msw/setup0.h	2005/05/29 15:52:36
@@ -984,5 +984,5 @@
 
 // Default is 0.  Set to 1 to use the deprecated classes, enum types, function,
-// member variables.  With a setting of 1, full backward compatability with the
+// member variables.  With a setting of 1, full backward compatibility with the
 // 2.0.x release is possible. It is STRONGLY recommended that this be set to 0,
 // as future development will be done only on the non-deprecated
Index: include/wx/msw/setup_microwin.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/msw/setup_microwin.h,v
retrieving revision 1.25
diff -b -u -2 -r1.25 setup_microwin.h
--- include/wx/msw/setup_microwin.h	2005/02/27 20:31:19	1.25
+++ include/wx/msw/setup_microwin.h	2005/05/29 15:52:37
@@ -826,5 +826,5 @@
 
 // Default is 0.  Set to 1 to use the deprecated classes, enum types, function,
-// member variables.  With a setting of 1, full backward compatability with the
+// member variables.  With a setting of 1, full backward compatibility with the
 // 2.0.x release is possible. It is STRONGLY recommended that this be set to 0,
 // as future development will be done only on the non-deprecated
Index: include/wx/msw/wince/setup.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/msw/wince/setup.h,v
retrieving revision 1.59
diff -b -u -2 -r1.59 setup.h
--- include/wx/msw/wince/setup.h	2005/05/13 11:40:50	1.59
+++ include/wx/msw/wince/setup.h	2005/05/29 15:52:37
@@ -984,5 +984,5 @@
 
 // Default is 0.  Set to 1 to use the deprecated classes, enum types, function,
-// member variables.  With a setting of 1, full backward compatability with the
+// member variables.  With a setting of 1, full backward compatibility with the
 // 2.0.x release is possible. It is STRONGLY recommended that this be set to 0,
 // as future development will be done only on the non-deprecated
Index: include/wx/os2/menu.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/os2/menu.h,v
retrieving revision 1.21
diff -b -u -2 -r1.21 menu.h
--- include/wx/os2/menu.h	2005/03/17 23:44:55	1.21
+++ include/wx/os2/menu.h	2005/05/29 15:52:37
@@ -246,5 +246,5 @@
 
     //
-    // Update the accel table (must be called after adding/deletign a menu)
+    // Update the accel table (must be called after adding/deleting a menu)
     //
     void                      RebuildAccelTable(void);
Index: include/wx/os2/stdpaths.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/os2/stdpaths.h,v
retrieving revision 1.1
diff -b -u -2 -r1.1 stdpaths.h
--- include/wx/os2/stdpaths.h	2004/11/06 22:13:59	1.1
+++ include/wx/os2/stdpaths.h	2005/05/29 15:52:38
@@ -25,5 +25,5 @@
     // this function is automatically called from application initialisation.
     // If you override the initialisation routine, you should call it
-    // explicitely yourself.
+    // explicitly yourself.
     static void SetInstallPrefix(const wxString& prefix);
 
Index: include/wx/palmos/dcclient.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/palmos/dcclient.h,v
retrieving revision 1.2
diff -b -u -2 -r1.2 dcclient.h
--- include/wx/palmos/dcclient.h	2005/01/18 21:14:24	1.2
+++ include/wx/palmos/dcclient.h	2005/05/29 15:52:38
@@ -47,5 +47,5 @@
 
 protected:
-    // intiialize the newly created DC
+    // initialize the newly created DC
     void InitDC();
 
Index: include/wx/palmos/menu.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/palmos/menu.h,v
retrieving revision 1.5
diff -b -u -2 -r1.5 menu.h
--- include/wx/palmos/menu.h	2005/03/17 23:44:57	1.5
+++ include/wx/palmos/menu.h	2005/05/29 15:52:38
@@ -175,5 +175,5 @@
     const wxAcceleratorTable& GetAccelTable() const { return m_accelTable; }
 
-    // update the accel table (must be called after adding/deletign a menu)
+    // update the accel table (must be called after adding/deleting a menu)
     void RebuildAccelTable();
 #endif // wxUSE_ACCEL
Index: include/wx/palmos/setup0.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/palmos/setup0.h,v
retrieving revision 1.20
diff -b -u -2 -r1.20 setup0.h
--- include/wx/palmos/setup0.h	2005/03/09 07:29:27	1.20
+++ include/wx/palmos/setup0.h	2005/05/29 15:52:38
@@ -940,5 +940,5 @@
 
 // Default is 0.  Set to 1 to use the deprecated classes, enum types, function,
-// member variables.  With a setting of 1, full backward compatability with the
+// member variables.  With a setting of 1, full backward compatibility with the
 // 2.0.x release is possible. It is STRONGLY recommended that this be set to 0,
 // as future development will be done only on the non-deprecated
Index: include/wx/protocol/ftp.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/protocol/ftp.h,v
retrieving revision 1.20
diff -b -u -2 -r1.20 ftp.h
--- include/wx/protocol/ftp.h	2004/09/18 14:24:48	1.20
+++ include/wx/protocol/ftp.h	2005/05/29 15:52:38
@@ -130,5 +130,5 @@
 
     // get the server reply, return the first character of the reply code,
-    // '1'..'5' for normal FTP replies, 0 (*not* '0') if an error occured
+    // '1'..'5' for normal FTP replies, 0 (*not* '0') if an error occurred
     char GetResult();
 
Index: include/wx/univ/setup0.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/univ/setup0.h,v
retrieving revision 1.42
diff -b -u -2 -r1.42 setup0.h
--- include/wx/univ/setup0.h	2005/05/14 21:16:45	1.42
+++ include/wx/univ/setup0.h	2005/05/29 15:52:39
@@ -1022,5 +1022,5 @@
 
 // Default is 0.  Set to 1 to use the deprecated classes, enum types, function,
-// member variables.  With a setting of 1, full backward compatability with the
+// member variables.  With a setting of 1, full backward compatibility with the
 // 2.0.x release is possible. It is STRONGLY recommended that this be set to 0,
 // as future development will be done only on the non-deprecated
Index: include/wx/unix/stdpaths.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/unix/stdpaths.h,v
retrieving revision 1.2
diff -b -u -2 -r1.2 stdpaths.h
--- include/wx/unix/stdpaths.h	2004/10/20 00:35:22	1.2
+++ include/wx/unix/stdpaths.h	2005/05/29 15:52:39
@@ -24,5 +24,5 @@
     // under some systems (currently only Linux) the program directory can be
     // determined automatically but for portable programs you should always set
-    // it explicitely
+    // it explicitly
     void SetInstallPrefix(const wxString& prefix);
 
Index: include/wx/xrc/xmlres.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/include/wx/xrc/xmlres.h,v
retrieving revision 1.42
diff -b -u -2 -r1.42 xmlres.h
--- include/wx/xrc/xmlres.h	2005/04/08 14:34:00	1.42
+++ include/wx/xrc/xmlres.h	2005/05/29 15:52:39
@@ -311,5 +311,5 @@
     // Should check for validity.
     // parent is a higher-level object (usually window, dialog or panel)
-    // that is often neccessary to create the resource.
+    // that is often necessary to create the resource.
     // If instance is non-NULL it should not create a new instance via 'new' but
     // should rather use this one, and call its Create method.
Index: samples/console/console.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/samples/console/console.cpp,v
retrieving revision 1.189
diff -b -u -2 -r1.189 console.cpp
--- samples/console/console.cpp	2005/04/20 07:18:45	1.189
+++ samples/console/console.cpp	2005/05/29 15:52:42
@@ -2996,5 +2996,5 @@
         wxPrintf(_T("Full name from wxVCard API: %s\n"), value.c_str());
 
-        // now show how to deal with multiply occuring properties
+        // now show how to deal with multiply occurring properties
         DumpVCardAddresses(vcard);
         DumpVCardPhoneNumbers(vcard);
Index: samples/db/listdb.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/samples/db/listdb.cpp,v
retrieving revision 1.27
diff -b -u -2 -r1.27 listdb.cpp
--- samples/db/listdb.cpp	2004/12/28 18:32:40	1.27
+++ samples/db/listdb.cpp	2005/05/29 15:52:42
@@ -80,5 +80,5 @@
 /*
  * This function will return the exact string(s) from the database engine
- * indicating all error conditions which have just occured during the
+ * indicating all error conditions which have just occurred during the
  * last call to the database engine.
  *
Index: samples/dialup/nettest.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/samples/dialup/nettest.cpp,v
retrieving revision 1.14
diff -b -u -2 -r1.14 nettest.cpp
--- samples/dialup/nettest.cpp	2005/03/30 19:10:00	1.14
+++ samples/dialup/nettest.cpp	2005/05/29 15:52:42
@@ -273,5 +273,5 @@
     if ( wxGetApp().GetDialer()->HangUp() )
     {
-        wxLogStatus(this, wxT("Connection was succesfully terminated."));
+        wxLogStatus(this, wxT("Connection was successfully terminated."));
     }
     else
Index: samples/dnd/dnd.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/samples/dnd/dnd.cpp,v
retrieving revision 1.97
diff -b -u -2 -r1.97 dnd.cpp
--- samples/dnd/dnd.cpp	2004/12/19 14:41:45	1.97
+++ samples/dnd/dnd.cpp	2005/05/29 15:52:43
@@ -1731,5 +1731,5 @@
         default:
         case wxDragError:
-            wxLogError(wxT("An error occured during drag and drop operation"));
+            wxLogError(wxT("An error occurred during drag and drop operation"));
             break;
 
Index: samples/drawing/drawing.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/samples/drawing/drawing.cpp,v
retrieving revision 1.77
diff -b -u -2 -r1.77 drawing.cpp
--- samples/drawing/drawing.cpp	2005/03/30 19:10:07	1.77
+++ samples/drawing/drawing.cpp	2005/05/29 15:52:44
@@ -670,5 +670,5 @@
     // Added by JACS to demonstrate bizarre behaviour.
     // With a size of 70, we get a missing red RHS,
-    // and the hight is too small, so we get yellow
+    // and the height is too small, so we get yellow
     // showing. With a size of 40, it draws as expected:
     // it just shows a white rectangle with red outline.
Index: samples/except/except.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/samples/except/except.cpp,v
retrieving revision 1.11
diff -b -u -2 -r1.11 except.cpp
--- samples/except/except.cpp	2005/03/19 16:14:58	1.11
+++ samples/except/except.cpp	2005/05/29 15:52:44
@@ -80,5 +80,5 @@
     virtual bool OnInit();
 
-    // 2nd-level exception handling: we get all the exceptions occuring in any
+    // 2nd-level exception handling: we get all the exceptions occurring in any
     // event handler here
     virtual bool OnExceptionInMainLoop();
Index: samples/html/test/test.htm
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/samples/html/test/test.htm,v
retrieving revision 1.12
diff -b -u -2 -r1.12 test.htm
--- samples/html/test/test.htm	2005/05/13 17:48:25	1.12
+++ samples/html/test/test.htm	2005/05/29 15:52:44
@@ -83,5 +83,5 @@
 <UL>
 <LI>
-deeper nesting for check of mark allignment </LI>
+deeper nesting for check of mark alignment </LI>
 
 <LI>
Index: samples/mediaplayer/mediaplayer.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/samples/mediaplayer/mediaplayer.cpp,v
retrieving revision 1.17
diff -b -u -2 -r1.17 mediaplayer.cpp
--- samples/mediaplayer/mediaplayer.cpp	2005/03/18 19:45:31	1.17
+++ samples/mediaplayer/mediaplayer.cpp	2005/05/29 15:52:45
@@ -18,5 +18,5 @@
 // To use this sample, simply select Open File from the file menu,
 // select the file you want to play - and MediaPlayer will play the file in a
-// new notebook page, showing video if neccessary.
+// new notebook page, showing video if necessary.
 //
 // You can select one of the menu options, or move the slider around
Index: samples/richedit/kbList.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/samples/richedit/kbList.cpp,v
retrieving revision 1.3
diff -b -u -2 -r1.3 kbList.cpp
--- samples/richedit/kbList.cpp	2004/08/06 17:27:18	1.3
+++ samples/richedit/kbList.cpp	2005/05/29 15:52:46
@@ -28,5 +28,5 @@
  * 2) "redesign" of "Folder properties" dialog and bug corrected, small change to
  *    MInputBox (it was too wide)
- * 3) bug in ProvFC when it didn't reckognize the books as being in the correct
+ * 3) bug in ProvFC when it didn't recognize the books as being in the correct
  *    format (=> messages "can't reopen book") corrected
  * 4) I tried to enhance MDialog_About(), but it didn't really work... oh well,
Index: samples/sockets/client.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/samples/sockets/client.cpp,v
retrieving revision 1.30
diff -b -u -2 -r1.30 client.cpp
--- samples/sockets/client.cpp	2005/05/22 14:52:05	1.30
+++ samples/sockets/client.cpp	2005/05/29 15:52:46
@@ -291,5 +291,5 @@
   // Connect(addr, false) will issue a nonblocking connection request
   // and return immediately. If the return value is true, then the
-  // connection has been already succesfully established. If it is
+  // connection has been already successfully established. If it is
   // false, you must wait for the request to complete, either with
   // WaitOnConnect() or by watching wxSOCKET_CONNECTION / LOST
Index: src/cocoa/combobox.mm
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/cocoa/combobox.mm,v
retrieving revision 1.11
diff -b -u -2 -r1.11 combobox.mm
--- src/cocoa/combobox.mm	2005/03/26 05:51:07	1.11
+++ src/cocoa/combobox.mm	2005/05/29 15:52:47
@@ -14,9 +14,9 @@
 //
 // Impl notes:
-// There is no custom data source because doing so unneccesaraly sacrifices
+// There is no custom data source because doing so unnecessarily sacrifices
 // some native autocompletion behavior (we would have to make our own - 
 // the SimpleComboBox sample does so in the developer folder that
 // comes with OSX).  One reason you might want this would be to have
-// only one array or be able to display numbers by returned an NSNumber
+// only one array or be able to display numbers returned by an NSNumber
 // from the methods.
 //
Index: src/cocoa/mediactrl.mm
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/cocoa/mediactrl.mm,v
retrieving revision 1.3
diff -b -u -2 -r1.3 mediactrl.mm
--- src/cocoa/mediactrl.mm	2005/02/04 17:56:49	1.3
+++ src/cocoa/mediactrl.mm	2005/05/29 15:52:47
@@ -203,5 +203,5 @@
         Cleanup();
 
-    //Note that ExitMovies() is not neccessary...
+    //Note that ExitMovies() is not necessary...
     ExitMovies();
 }
@@ -325,5 +325,5 @@
     //Here, if the parent of the control has a sizer - we
     //tell it to recalculate the size of this control since
-    //the user opened a seperate media file
+    //the user opened a separate media file
     //
     m_ctrl->InvalidateBestSize();
Index: src/cocoa/utilsexc.mm
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/cocoa/utilsexc.mm,v
retrieving revision 1.4
diff -b -u -2 -r1.4 utilsexc.mm
--- src/cocoa/utilsexc.mm	2005/04/22 01:13:45	1.4
+++ src/cocoa/utilsexc.mm	2005/05/29 15:52:47
@@ -160,19 +160,19 @@
         [wxNSStringWithWxString(command) componentsSeparatedByString:@"\""];
         
-    NSMutableArray* theSeperatedArguments = 
+    NSMutableArray* theSeparatedArguments = 
         [NSMutableArray arrayWithCapacity:10];
         
     for (unsigned i = 0; i < [theQuoteArguments count]; ++i)
     {
-        [theSeperatedArguments addObjectsFromArray:
+        [theSeparatedArguments addObjectsFromArray:
             [[theQuoteArguments objectAtIndex:i] componentsSeparatedByString:@" "]
         ];
         
         if(++i < [theQuoteArguments count])
-            [theSeperatedArguments addObject:[theQuoteArguments objectAtIndex:i]];
+            [theSeparatedArguments addObject:[theQuoteArguments objectAtIndex:i]];
     }
     
-    [theTask setLaunchPath:[theSeperatedArguments objectAtIndex:0]];
-    [theTask setArguments:theSeperatedArguments];
+    [theTask setLaunchPath:[theSeparatedArguments objectAtIndex:0]];
+    [theTask setArguments:theSeparatedArguments];
     [theTask launch];
     
Index: src/common/bookctrl.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/common/bookctrl.cpp,v
retrieving revision 1.13
diff -b -u -2 -r1.13 bookctrl.cpp
--- src/common/bookctrl.cpp	2004/11/23 11:44:56	1.13
+++ src/common/bookctrl.cpp	2005/05/29 15:52:47
@@ -129,5 +129,5 @@
     }
 
-    // convert display area to window area, adding the size neccessary for the
+    // convert display area to window area, adding the size necessary for the
     // tabs
     wxSize best = CalcSizeFromPage(bestSize);
Index: src/common/datetime.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/common/datetime.cpp,v
retrieving revision 1.130
diff -b -u -2 -r1.130 datetime.cpp
--- src/common/datetime.cpp	2005/05/08 23:35:34	1.130
+++ src/common/datetime.cpp	2005/05/29 15:52:49
@@ -2310,10 +2310,10 @@
                     // - 2038) whose Jan 1 falls on the same week day as the Jan 1
                     // of the real year. Then make a copy of the format and
-                    // replace all occurences of YEAR in it with some unique
+                    // replace all occurrences of YEAR in it with some unique
                     // string not appearing anywhere else in it, then use
                     // strftime() to format the date in year YEAR and then replace
                     // YEAR back by the real year and the unique replacement
-                    // string back with YEAR. Notice that "all occurences of YEAR"
-                    // means all occurences of 4 digit as well as 2 digit form!
+                    // string back with YEAR. Notice that "all occurrences of YEAR"
+                    // means all occurrences of 4 digit as well as 2 digit form!
                     //
                     // the bugs: we assume that neither of %c nor %x contains any
@@ -2373,6 +2373,6 @@
                         strYear2.Printf(_T("%d"), year % 100);
 
-                        // find two strings not occuring in format (this is surely
-                        // not optimal way of doing it... improvements welcome!)
+                        // find two strings not occurring in format (this is surely
+                        // not the optimal way of doing it... improvements welcome!)
                         wxString fmt = format;
                         wxString replacement = (wxChar)-1;
@@ -2388,5 +2388,5 @@
                         }
 
-                        // replace all occurences of year with it
+                        // replace all occurrences of year with it
                         bool wasReplaced = fmt.Replace(strYear, replacement) > 0;
                         if ( !wasReplaced )
@@ -2415,5 +2415,5 @@
                                                     &tmAdjusted);
 
-                        // now replace the occurence of 1999 with the real year
+                        // now replace the occurrence of 1999 with the real year
                         wxString strYearReal, strYearReal2;
                         strYearReal.Printf(_T("%04d"), yearReal);
@@ -2422,5 +2422,5 @@
                         str.Replace(strYear2, strYearReal2);
 
-                        // and replace back all occurences of replacement string
+                        // and replace back all occurrences of replacement string
                         if ( wasReplaced )
                         {
Index: src/common/db.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/common/db.cpp,v
retrieving revision 1.122
diff -b -u -2 -r1.122 db.cpp
--- src/common/db.cpp	2005/05/22 22:57:11	1.122
+++ src/common/db.cpp	2005/05/29 15:52:52
@@ -180,5 +180,5 @@
     if (SQLAllocEnv(&Henv) != SQL_SUCCESS)
     {
-        wxLogDebug(wxT("A problem occured while trying to get a connection to the data source"));
+        wxLogDebug(wxT("A problem occurred while trying to get a connection to the data source"));
         return false;
     }
@@ -1782,5 +1782,5 @@
  * This function is called internally whenever an error condition prevents the user's
  * request from being executed.  This function will query the datasource as to the
- * actual error(s) that just occured on the previous request of the datasource.
+ * actual error(s) that just occurred on the previous request of the datasource.
  *
  * The function will retrieve each error condition from the datasource and
@@ -1789,7 +1789,7 @@
  * window and program execution will be paused until the user presses a key.
  *
- * This function always returns a false, so that functions which call this function
+ * This function always returns false, so that functions which call this function
  * can have a line like "return (DispAllErrors(henv, hdbc));" to indicate the failure
- * of the users request, so that the calling code can then process the error msg log
+ * of the user's request, so that the calling code can then process the error message log.
  */
 {
@@ -2508,5 +2508,5 @@
  *            This is how the end of the array is detected.
  *        2) This function returns an array of wxDbColInf structures.  If no columns
- *            were found, or an error occured, this pointer will be zero (null).  THE
+ *            were found, or an error occurred, this pointer will be zero (null).  THE
  *            CALLING FUNCTION IS RESPONSIBLE FOR DELETING THE MEMORY RETURNED WHEN IT
  *            IS FINISHED WITH IT.  i.e.
@@ -2598,5 +2598,5 @@
             }
             if (retcode != SQL_SUCCESS)
-            {  // Error occured, abort
+            {  // Error occurred, abort
                 DispAllErrors(henv, hdbc, hstmt);
                 if (colInf)
@@ -2656,5 +2656,5 @@
             }
             if (retcode != SQL_NO_DATA_FOUND)
-            {  // Error occured, abort
+            {  // Error occurred, abort
                 DispAllErrors(henv, hdbc, hstmt);
                 if (colInf)
@@ -2754,5 +2754,5 @@
         }
         if (retcode != SQL_SUCCESS)
-        {  // Error occured, abort
+        {  // Error occurred, abort
             DispAllErrors(henv, hdbc, hstmt);
             if (colInf)
@@ -2830,5 +2830,5 @@
         }
         if (retcode != SQL_NO_DATA_FOUND)
-        {  // Error occured, abort
+        {  // Error occurred, abort
             DispAllErrors(henv, hdbc, hstmt);
             if (colInf)
@@ -3010,5 +3010,5 @@
         }
         if (retcode != SQL_SUCCESS)
-        {  // Error occured, abort
+        {  // Error occurred, abort
             DispAllErrors(henv, hdbc, hstmt);
             if (colInf)
@@ -3106,5 +3106,5 @@
         }
         if (retcode != SQL_NO_DATA_FOUND)
-        {  // Error occured, abort
+        {  // Error occurred, abort
             DispAllErrors(henv, hdbc, hstmt);
             if (colInf)
@@ -3258,5 +3258,5 @@
     }
     if (retcode != SQL_SUCCESS)
-    {  // Error occured, abort
+    {  // Error occurred, abort
         DispAllErrors(henv, hdbc, hstmt);
         SQLFreeStmt(hstmt, SQL_CLOSE);
@@ -3269,5 +3269,5 @@
 
     if (retcode != SQL_NO_DATA_FOUND)
-    {  // Error occured, abort
+    {  // Error occurred, abort
         DispAllErrors(henv, hdbc, hstmt);
         SQLFreeStmt(hstmt, SQL_CLOSE);
@@ -3290,5 +3290,5 @@
  * --          : pColInf is set to NULL and numCols to 0;         ------
  * --          : returns pDbInf (wxDbInf)                         ------
- * --            - if unsuccesfull (pDbInf == NULL)               ------
+ * --            - if unsuccessful (pDbInf == NULL)               ------
  * --          : pColInf can be filled with GetColumns(..);       ------
  * --          : numCols   can be filled with GetColumnCount(..); ------
@@ -4334,5 +4334,5 @@
     setupStr.Printf(wxT("DSN=%s%cDescription=%s%cDefaultDir=%s%c"),dsn,2,description,2,defDir,2);
 
-    // Replace the separator from above with the '\0' seperator needed
+    // Replace the separator from above with the '\0' separator needed
     // by the SQLConfigDataSource() function
     int k;
@@ -4416,5 +4416,5 @@
  *
  * The following functions are all DEPRECATED and are included for
- * backward compatability reasons only
+ * backward compatibility reasons only
  *
  ********************************************************************
Index: src/common/dcbase.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/common/dcbase.cpp,v
retrieving revision 1.38
diff -b -u -2 -r1.38 dcbase.cpp
--- src/common/dcbase.cpp	2005/03/01 19:13:24	1.38
+++ src/common/dcbase.cpp	2005/05/29 15:52:53
@@ -374,5 +374,5 @@
 
 // Each element of the widths array will be the width of the string up to and
-// including the coresponding character in text.  This is the generic
+// including the corresponding character in text.  This is the generic
 // implementation, the port-specific classes should do this with native APIs
 // if available and if faster.  Note: pango_layout_index_to_pos is much slower
Index: src/common/filefn.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/common/filefn.cpp,v
retrieving revision 1.239
diff -b -u -2 -r1.239 filefn.cpp
--- src/common/filefn.cpp	2005/05/25 16:05:22	1.239
+++ src/common/filefn.cpp	2005/05/29 15:52:53
@@ -184,6 +184,6 @@
         "C:\Program" and "Files"
         */
-//        wxT(" ;"); // Don't seperate with colon in DOS (used for drive)
-        wxT(";"); // Don't seperate with colon in DOS (used for drive)
+//        wxT(" ;"); // Don't separate with colon in DOS (used for drive)
+        wxT(";"); // Don't separate with colon in DOS (used for drive)
 #else
         wxT(" :;");
Index: src/common/ftp.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/common/ftp.cpp,v
retrieving revision 1.60
diff -b -u -2 -r1.60 ftp.cpp
--- src/common/ftp.cpp	2004/11/22 17:45:19	1.60
+++ src/common/ftp.cpp	2005/05/29 15:52:54
@@ -388,5 +388,5 @@
     }
 
-    // If we get here the operation has been succesfully completed
+    // If we get here the operation has been successfully completed
     // Set the status-member
     m_currentTransfermode = transferMode;
Index: src/common/gifdecod.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/common/gifdecod.cpp,v
retrieving revision 1.36
diff -b -u -2 -r1.36 gifdecod.cpp
--- src/common/gifdecod.cpp	2005/01/31 22:21:11	1.36
+++ src/common/gifdecod.cpp	2005/05/29 15:52:54
@@ -510,5 +510,5 @@
                     found, Or if the maximum number of passes has
                     been reached, exit the loop, and stop image
-                    decoding (At this point the image is succesfully
+                    decoding (At this point the image is successfully
                     decoded).
                     If we don't loop, but merely set y to some other
Index: src/common/intl.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/common/intl.cpp,v
retrieving revision 1.156
diff -b -u -2 -r1.156 intl.cpp
--- src/common/intl.cpp	2005/03/29 18:12:26	1.156
+++ src/common/intl.cpp	2005/05/29 15:52:56
@@ -1793,5 +1793,5 @@
     {
         // 0. Make sure the lang is according to latest ISO 639
-        //    (this is neccessary because glibc uses iw and in instead
+        //    (this is necessary because glibc uses iw and in instead
         //    of he and id respectively).
 
Index: src/common/popupcmn.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/common/popupcmn.cpp,v
retrieving revision 1.56
diff -b -u -2 -r1.56 popupcmn.cpp
--- src/common/popupcmn.cpp	2005/05/06 13:39:21	1.56
+++ src/common/popupcmn.cpp	2005/05/29 15:52:57
@@ -492,5 +492,5 @@
     wxPoint pos = event.GetPosition();
 
-    // scrollbar on which the click occured
+    // scrollbar on which the click occurred
     wxWindow *sbar = NULL;
 
Index: src/common/regex.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/common/regex.cpp,v
retrieving revision 1.33
diff -b -u -2 -r1.33 regex.cpp
--- src/common/regex.cpp	2005/02/04 09:37:55	1.33
+++ src/common/regex.cpp	2005/05/29 15:52:57
@@ -297,5 +297,5 @@
 
         default:
-            // an error occured
+            // an error occurred
             wxLogError(_("Failed to match '%s' in regular expression: %s"),
                        str, GetErrorMsg(rc, !conv).c_str());
Index: src/common/stdpbase.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/common/stdpbase.cpp,v
retrieving revision 1.6
diff -b -u -2 -r1.6 stdpbase.cpp
--- src/common/stdpbase.cpp	2005/03/07 22:37:53	1.6
+++ src/common/stdpbase.cpp	2005/05/29 15:52:57
@@ -76,5 +76,5 @@
     wxString subdir(dir);
 
-    // empty string indicates that an error has occured, don't touch it then
+    // empty string indicates that an error has occurred, don't touch it then
     if ( !subdir.empty() )
     {
Index: src/common/strconv.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/common/strconv.cpp,v
retrieving revision 1.147
diff -b -u -2 -r1.147 strconv.cpp
--- src/common/strconv.cpp	2005/04/13 20:05:56	1.147
+++ src/common/strconv.cpp	2005/05/29 15:52:58
@@ -271,7 +271,7 @@
 
         //Increment to next (sub)string
-        //Note that we have to use strlen here instead of nLen
-        //here because XX2XX gives us the size of the output buffer,
-        //not neccessarly the length of the string
+        //Note that we have to use strlen instead of nLen here
+        //because XX2XX gives us the size of the output buffer,
+        //which is not necessarily the length of the string
         szPos += strlen(szPos) + 1;
     }
@@ -333,7 +333,7 @@
 
         //Increment to next (sub)string
-        //Note that we have to use wxWcslen here instead of nLen
-        //here because XX2XX gives us the size of the output buffer,
-        //not neccessarly the length of the string
+        //Note that we have to use wxWcslen instead of nLen here
+        //because XX2XX gives us the size of the output buffer,
+        //which is not necessarily the length of the string
         szPos += wxWcslen(szPos) + 1;
     }
Index: src/common/tbarbase.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/common/tbarbase.cpp,v
retrieving revision 1.77
diff -b -u -2 -r1.77 tbarbase.cpp
--- src/common/tbarbase.cpp	2005/04/12 07:33:20	1.77
+++ src/common/tbarbase.cpp	2005/05/29 15:52:58
@@ -3,5 +3,5 @@
 // Purpose:     wxToolBarBase implementation
 // Author:      Julian Smart
-// Modified by: VZ at 11.12.99 (wxScrollableToolBar splitted off)
+// Modified by: VZ at 11.12.99 (wxScrollableToolBar split off)
 // Created:     04/01/98
 // RCS-ID:      $Id: tbarbase.cpp,v 1.77 2005/04/12 07:33:20 JS Exp $
Index: src/common/wincmn.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/common/wincmn.cpp,v
retrieving revision 1.231
diff -b -u -2 -r1.231 wincmn.cpp
--- src/common/wincmn.cpp	2005/05/25 12:40:53	1.231
+++ src/common/wincmn.cpp	2005/05/29 15:53:00
@@ -501,5 +501,5 @@
     wxRect clientrect = wxGetClientDisplayRect();
 
-    // NB: in wxMSW, negative position may not neccessary mean "out of screen",
+    // NB: in wxMSW, negative position may not necessarily mean "out of screen",
     //     but it may mean that the window is placed on other than the main
     //     display. Therefore we only make sure centered window is on the main display
Index: src/common/zstream.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/common/zstream.cpp,v
retrieving revision 1.50
diff -b -u -2 -r1.50 zstream.cpp
--- src/common/zstream.cpp	2005/03/30 16:49:33	1.50
+++ src/common/zstream.cpp	2005/05/29 15:53:00
@@ -57,3 +57,3 @@
 
 #if WXWIN_COMPATIBILITY_2_4
-  // treat compatibilty mode as auto
+  // treat compatibility mode as auto
Index: src/generic/calctrl.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/generic/calctrl.cpp,v
retrieving revision 1.67
diff -b -u -2 -r1.67 calctrl.cpp
--- src/generic/calctrl.cpp	2005/05/26 17:33:41	1.67
+++ src/generic/calctrl.cpp	2005/05/29 15:53:02
@@ -1222,11 +1222,11 @@
             {
                 // special case: interval 7 days or less not in same week
-                // split in two seperate intervals
+                // split in two separate intervals
                 wxDateTime tfd = fromdate + wxDateSpan::Days(7-fd);
                 wxDateTime ftd = tfd + wxDateSpan::Day();
 #if DEBUG_PAINT
-                wxLogDebug("Highlight: Seperate segments");
+                wxLogDebug("Highlight: Separate segments");
 #endif
-                // draw seperately
+                // draw separately
                 HighlightRange(pDC, fromdate, tfd, pPen, pBrush);
                 HighlightRange(pDC, ftd, todate, pPen, pBrush);
Index: src/generic/dirdlgg.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/generic/dirdlgg.cpp,v
retrieving revision 1.59
diff -b -u -2 -r1.59 dirdlgg.cpp
--- src/generic/dirdlgg.cpp	2005/04/05 16:09:38	1.59
+++ src/generic/dirdlgg.cpp	2005/05/29 15:53:02
@@ -153,5 +153,5 @@
 
     // 1) dir ctrl
-    m_dirCtrl = NULL; // this is neccessary, event handler called from
+    m_dirCtrl = NULL; // this is necessary, event handler called from
                       // wxGenericDirCtrl would crash otherwise!
     long dirStyle = wxDIRCTRL_DIR_ONLY | wxDEFAULT_CONTROL_BORDER;
Index: src/generic/gridctrl.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/generic/gridctrl.cpp,v
retrieving revision 1.15
diff -b -u -2 -r1.15 gridctrl.cpp
--- src/generic/gridctrl.cpp	2004/06/17 16:22:34	1.15
+++ src/generic/gridctrl.cpp	2005/05/29 15:53:03
@@ -353,5 +353,5 @@
     //       space at the end of the line. But it
     //       is invisible , simplifies the size calculation
-    //       and ensures tokens are seperated in the display
+    //       and ensures tokens are separated in the display
     tok += _T(" ");
 
Index: src/generic/listctrl.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/generic/listctrl.cpp,v
retrieving revision 1.372
diff -b -u -2 -r1.372 listctrl.cpp
--- src/generic/listctrl.cpp	2005/05/09 19:58:55	1.372
+++ src/generic/listctrl.cpp	2005/05/29 15:53:05
@@ -1929,5 +1929,5 @@
         int xpos = 0;
 
-        // find the column where this event occured
+        // find the column where this event occurred
         int col,
             countCol = m_owner->GetColumnCount();
@@ -3025,5 +3025,5 @@
     else
     {
-        // This is neccessary , because after a DnD operation in
+        // This is necessary, because after a DnD operation in
         // from and to ourself, the up event is swallowed by the
         // DnD code. So on next non-up event (which means here and
Index: src/generic/logg.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/generic/logg.cpp,v
retrieving revision 1.90
diff -b -u -2 -r1.90 logg.cpp
--- src/generic/logg.cpp	2005/03/14 20:00:42	1.90
+++ src/generic/logg.cpp	2005/05/29 15:53:06
@@ -1123,5 +1123,5 @@
 
 #ifdef __WXGTK__
-    // VS: this is neccessary in order to force frame redraw under
+    // VS: this is necessary in order to force frame redraw under
     // WindowMaker or fvwm2 (and probably other broken WMs).
     // Otherwise, detailed list wouldn't be displayed.
Index: src/gtk/dnd.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/gtk/dnd.cpp,v
retrieving revision 1.92
diff -b -u -2 -r1.92 dnd.cpp
--- src/gtk/dnd.cpp	2005/03/21 23:42:16	1.92
+++ src/gtk/dnd.cpp	2005/05/29 15:53:06
@@ -403,5 +403,5 @@
         wxLogTrace(TRACE_DND, wxT( "Drop target: OnData returned TRUE") );
 
-        /* tell GTK that data transfer was successfull */
+        /* tell GTK that data transfer was successful */
         gtk_drag_finish( context, TRUE, FALSE, time );
     }
@@ -410,5 +410,5 @@
         wxLogTrace(TRACE_DND, wxT( "Drop target: OnData returned FALSE") );
 
-        /* tell GTK that data transfer was not successfull */
+        /* tell GTK that data transfer was not successful */
         gtk_drag_finish( context, FALSE, FALSE, time );
     }
Index: src/html/chm.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/html/chm.cpp,v
retrieving revision 1.8
diff -b -u -2 -r1.8 chm.cpp
--- src/html/chm.cpp	2004/12/26 09:16:29	1.8
+++ src/html/chm.cpp	2005/05/29 15:53:07
@@ -71,5 +71,5 @@
     bool Contains(const wxString& pattern);
 
-    /// get a string for the last error occured
+    /// get a string for the last error which occurred
     const wxString GetLastErrorMessage();
 
@@ -750,7 +750,7 @@
     /// Open a file
     virtual wxFSFile* OpenFile(wxFileSystem& fs, const wxString& location);
-    /// Find first occurence of spec
+    /// Find first occurrence of spec
     virtual wxString FindFirst(const wxString& spec, int flags = 0);
-    /// Find next occurence of spec
+    /// Find next occurrence of spec
     virtual wxString FindNext();
 
Index: src/html/helpfrm.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/html/helpfrm.cpp,v
retrieving revision 1.123
diff -b -u -2 -r1.123 helpfrm.cpp
--- src/html/helpfrm.cpp	2005/03/23 13:43:39	1.123
+++ src/html/helpfrm.cpp	2005/05/29 15:53:08
@@ -966,8 +966,8 @@
     wxTreeItemId roots[MAX_ROOTS];
     // VS: this array holds information about whether we've set item icon at
-    //     given level. This is neccessary because m_Data has flat structure
+    //     given level. This is necessary because m_Data has a flat structure
     //     and there's no way of recognizing if some item has subitems or not.
     //     We set the icon later: when we find an item with level=n, we know
-    //     that the last item with level=n-1 was folder with subitems, so we
+    //     that the last item with level=n-1 was afolder with subitems, so we
     //     set its icon accordingly
     bool imaged[MAX_ROOTS];
@@ -985,7 +985,7 @@
             if (m_hfStyle & wxHF_MERGE_BOOKS)
                 // VS: we don't want book nodes, books' content should
-                //    appear under tree's root. This line will create "fake"
+                //    appear under tree's root. This line will create a "fake"
                 //    record about book node so that the rest of this look
-                //    will believe there really _is_ book node and will
+                //    will believe there really _is_ a book node and will
                 //    behave correctly.
                 roots[1] = roots[0];
@@ -1011,5 +1011,5 @@
                          new wxHtmlHelpHashData(i, roots[it->level + 1]));
 
-        // Set the icon for the node one level up in the hiearachy,
+        // Set the icon for the node one level up in the hierarchy,
         // unless already done (see comment above imaged[] declaration)
         if (!imaged[it->level])
Index: src/html/htmlcell.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/html/htmlcell.cpp,v
retrieving revision 1.99
diff -b -u -2 -r1.99 htmlcell.cpp
--- src/html/htmlcell.cpp	2005/01/13 20:48:48	1.99
+++ src/html/htmlcell.cpp	2005/05/29 15:53:08
@@ -730,5 +730,5 @@
         }
 
-        // force new line if occured:
+        // force new line if occurred:
         if ((cell == NULL) ||
             (xpos + nextWordWidth > s_width && cell->IsLinebreakAllowed()))
Index: src/html/htmltag.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/html/htmltag.cpp,v
retrieving revision 1.46
diff -b -u -2 -r1.46 htmltag.cpp
--- src/html/htmltag.cpp	2005/02/13 19:40:03	1.46
+++ src/html/htmltag.cpp	2005/05/29 15:53:09
@@ -459,5 +459,5 @@
 wxString wxHtmlTag::GetAllParams() const
 {
-    // VS: this function is for backward compatiblity only,
+    // VS: this function is for backward compatibility only,
     //     never used by wxHTML
     wxString s;
Index: src/mac/carbon/cfsocket.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/mac/carbon/cfsocket.cpp,v
retrieving revision 1.3
diff -b -u -2 -r1.3 cfsocket.cpp
--- src/mac/carbon/cfsocket.cpp	2004/05/23 20:52:29	1.3
+++ src/mac/carbon/cfsocket.cpp	2005/05/29 15:53:10
@@ -2056,5 +2056,5 @@
 
 /* GSocket_GetError:
- *  Returns the last error occured for this socket. Note that successful
+ *  Returns the last error which occurred for this socket. Note that successful
  *  operations do not clear this back to GSOCK_NOERROR, so use it only
  *  after an error.
@@ -2081,5 +2081,5 @@
  *   OUTPUT events will be generated unless an error occurs.
  * GSOCK_CONNECTION:
- *   Connection succesfully established, for client sockets, or incoming
+ *   Connection successfully established, for client sockets, or incoming
  *   client connection, for server sockets. Wait for this event (also watch
  *   out for GSOCK_LOST) after you issue a nonblocking GSocket_Connect() call.
Index: src/mac/carbon/font.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/mac/carbon/font.cpp,v
retrieving revision 1.33
diff -b -u -2 -r1.33 font.cpp
--- src/mac/carbon/font.cpp	2005/04/10 07:02:07	1.33
+++ src/mac/carbon/font.cpp	2005/05/29 15:53:10
@@ -130,5 +130,5 @@
     // ATSU Font Information
     
-    // this is splitted into an ATSU font id that may
+    // this is split into an ATSU font id that may
     // contain some styles (special bold fonts etc) and
     // these are the additional qd styles that are not
Index: src/mac/carbon/fontdlgosx.mm
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/mac/carbon/fontdlgosx.mm,v
retrieving revision 1.4
diff -b -u -2 -r1.4 fontdlgosx.mm
--- src/mac/carbon/fontdlgosx.mm	2004/11/22 17:18:58	1.4
+++ src/mac/carbon/fontdlgosx.mm	2005/05/29 15:53:10
@@ -266,5 +266,5 @@
     //	So we set up delegates for both the color and font panels,
     //  and the if the font panel opens the color panel, we 
-    //  stop the modal loop, and start a seperate modal loop for
+    //  stop the modal loop, and start a separate modal loop for
     //  the color panel until the color panel closes, switching
     //  back to the font panel modal loop once it does close.
Index: src/mac/carbon/gsocket.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/mac/carbon/gsocket.cpp,v
retrieving revision 1.7
diff -b -u -2 -r1.7 gsocket.cpp
--- src/mac/carbon/gsocket.cpp	2005/05/04 18:55:48	1.7
+++ src/mac/carbon/gsocket.cpp	2005/05/29 15:53:11
@@ -697,5 +697,5 @@
  *  to establish a client connection to a server using the peer address
  *  as established with GSocket_SetPeer(). Returns GSOCK_NOERROR if the
- *  connection has been succesfully established, or one of the error
+ *  connection has been successfully established, or one of the error
  *  codes listed below. Note that for nonblocking sockets, a return
  *  value of GSOCK_WOULDBLOCK doesn't mean a failure. The connection
@@ -975,5 +975,5 @@
 
 /* GSocket_GetError:
- *  Returns the last error occured for this socket. Note that successful
+ *  Returns the last error which occurred for this socket. Note that successful
  *  operations do not clear this back to GSOCK_NOERROR, so use it only
  *  after an error.
@@ -1000,5 +1000,5 @@
  *   OUTPUT events will be generated unless an error occurs.
  * GSOCK_CONNECTION:
- *   Connection succesfully established, for client sockets, or incoming
+ *   Connection successfully established, for client sockets, or incoming
  *   client connection, for server sockets. Wait for this event (also watch
  *   out for GSOCK_LOST) after you issue a nonblocking GSocket_Connect() call.
@@ -1535,5 +1535,5 @@
  *   OUTPUT events will be generated unless an error occurs.
  * GSOCK_CONNECTION:
- *   Connection succesfully established, for client sockets, or incoming
+ *   Connection successfully established, for client sockets, or incoming
  *   client connection, for server sockets. Wait for this event (also watch
  *   out for GSOCK_LOST) after you issue a nonblocking GSocket_Connect() call.
Index: src/mac/carbon/mediactrl.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/mac/carbon/mediactrl.cpp,v
retrieving revision 1.12
diff -b -u -2 -r1.12 mediactrl.cpp
--- src/mac/carbon/mediactrl.cpp	2005/03/09 18:53:45	1.12
+++ src/mac/carbon/mediactrl.cpp	2005/05/29 15:53:11
@@ -225,5 +225,5 @@
         Cleanup();
 
-    //Note that ExitMovies() is not neccessary...
+    //Note that ExitMovies() is not necessary...
     ExitMovies();
 }
@@ -468,5 +468,5 @@
     //Here, if the parent of the control has a sizer - we
     //tell it to recalculate the size of this control since
-    //the user opened a seperate media file
+    //the user opened a separate media file
     //
     m_ctrl->InvalidateBestSize();
Index: src/mac/carbon/slider.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/mac/carbon/slider.cpp,v
retrieving revision 1.43
diff -b -u -2 -r1.43 slider.cpp
--- src/mac/carbon/slider.cpp	2005/05/10 06:26:59	1.43
+++ src/mac/carbon/slider.cpp	2005/05/29 15:53:12
@@ -434,5 +434,5 @@
     // when wxSlider has it's size hardcoded, we're not allowed to
     // change the size. But when the control has labels, we DO need
-    // to resize the internal Mac control to accomodate the text labels.
+    // to resize the internal Mac control to accommodate the text labels.
     // We need to trick the wxWidgets resize mechanism so that we can
     // resize the slider part of the control ONLY.
Index: src/mac/carbon/sound.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/mac/carbon/sound.cpp,v
retrieving revision 1.17
diff -b -u -2 -r1.17 sound.cpp
--- src/mac/carbon/sound.cpp	2005/02/14 20:12:08	1.17
+++ src/mac/carbon/sound.cpp	2005/05/29 15:53:12
@@ -93,5 +93,5 @@
         Stop();
 
-        //Note that ExitMovies() is not neccessary, but
+        //Note that ExitMovies() is not necessary, but
         //the docs are fuzzy on whether or not TerminateQTML is
         ExitMovies();
Index: src/mac/carbon/taskbar.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/mac/carbon/taskbar.cpp,v
retrieving revision 1.23
diff -b -u -2 -r1.23 taskbar.cpp
--- src/mac/carbon/taskbar.cpp	2005/03/09 18:54:52	1.23
+++ src/mac/carbon/taskbar.cpp	2005/05/29 15:53:12
@@ -282,5 +282,5 @@
                                         theItem->GetText(), //text label
                                         theItem->GetHelp(), //status bar help string
-                                        theItem->GetKind(), //menu flags - checkable, seperator, etc.
+                                        theItem->GetKind(), //menu flags - checkable, separator, etc.
                                         theItem->GetSubMenu() //submenu
                                         ));
Index: src/mac/carbon/textctrl.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/mac/carbon/textctrl.cpp,v
retrieving revision 1.154
diff -b -u -2 -r1.154 textctrl.cpp
--- src/mac/carbon/textctrl.cpp	2005/05/10 06:26:59	1.154
+++ src/mac/carbon/textctrl.cpp	2005/05/29 15:53:14
@@ -654,5 +654,5 @@
     {
         // unfortunately CW 8 is not able to correctly deduce the template types, so we have
-        // to instantiate explicitely
+        // to instantiate explicitly
         wxMacMPRemoteGUICall<wxTextCtrl,wxString>( this , &wxTextCtrl::WriteText , str ) ;
         return ;
Index: src/mac/carbon/toplevel.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/mac/carbon/toplevel.cpp,v
retrieving revision 1.147
diff -b -u -2 -r1.147 toplevel.cpp
--- src/mac/carbon/toplevel.cpp	2005/05/21 16:52:56	1.147
+++ src/mac/carbon/toplevel.cpp	2005/05/29 15:53:15
@@ -1197,5 +1197,5 @@
     {
         // There is a bug in 10.2.X for ::GetRootControl returning the window view instead of 
-        // the content view, so we have to retrieve it explicitely
+        // the content view, so we have to retrieve it explicitly
         HIViewFindByID( HIViewGetRoot( (WindowRef) m_macWindow ) , kHIViewWindowContentID , 
             m_peer->GetControlRefAddr() ) ;
Index: src/mac/carbon/uma.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/mac/carbon/uma.cpp,v
retrieving revision 1.67
diff -b -u -2 -r1.67 uma.cpp
--- src/mac/carbon/uma.cpp	2005/03/30 04:52:01	1.67
+++ src/mac/carbon/uma.cpp	2005/05/29 15:53:15
@@ -674,5 +674,5 @@
     {
 //        bool isHighlighted = IsWindowHighlited( inWindowRef ) ;
-//        if ( inActivate != isHightlited )
+//        if ( inActivate != isHighlighted )
         GrafPtr port ;
         GetPort( &port ) ;
Index: src/mac/classic/app.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/mac/classic/app.cpp,v
retrieving revision 1.10
diff -b -u -2 -r1.10 app.cpp
--- src/mac/classic/app.cpp	2005/04/04 10:34:56	1.10
+++ src/mac/classic/app.cpp	2005/05/29 15:53:16
@@ -390,5 +390,5 @@
 // due to the rather low-level event API of wxWidgets, we cannot use RunApplicationEventLoop
 // but have to use ReceiveNextEvent dealing with events manually, therefore we also have
-// deal with clicks in the menu bar explicitely
+// to deal with clicks in the menu bar explicitly
 
 pascal OSStatus wxMacWindowEventHandler( EventHandlerCallRef handler , EventRef event , void *data ) ;
Index: src/mac/classic/bitmap.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/mac/classic/bitmap.cpp,v
retrieving revision 1.3
diff -b -u -2 -r1.3 bitmap.cpp
--- src/mac/classic/bitmap.cpp	2004/05/23 20:52:39	1.3
+++ src/mac/classic/bitmap.cpp	2005/05/29 15:53:17
@@ -169,5 +169,5 @@
         newColors = nil;
 
-    /* If no errors occured, return a handle to the new off-screen PixMap */
+    /* If no errors occurred, return a handle to the new off-screen PixMap */
     if (error != noErr)
         {
Index: src/mac/classic/gsocket.c
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/mac/classic/gsocket.c,v
retrieving revision 1.1
diff -b -u -2 -r1.1 gsocket.c
--- src/mac/classic/gsocket.c	2004/03/20 07:42:59	1.1
+++ src/mac/classic/gsocket.c	2005/05/29 15:53:18
@@ -707,5 +707,5 @@
  *  to establish a client connection to a server using the peer address
  *  as established with GSocket_SetPeer(). Returns GSOCK_NOERROR if the
- *  connection has been succesfully established, or one of the error
+ *  connection has been successfully established, or one of the error
  *  codes listed below. Note that for nonblocking sockets, a return
  *  value of GSOCK_WOULDBLOCK doesn't mean a failure. The connection
@@ -985,5 +985,5 @@
 
 /* GSocket_GetError:
- *  Returns the last error occured for this socket. Note that successful
+ *  Returns the last error which occurred for this socket. Note that successful
  *  operations do not clear this back to GSOCK_NOERROR, so use it only
  *  after an error.
@@ -1010,5 +1010,5 @@
  *   OUTPUT events will be generated unless an error occurs.
  * GSOCK_CONNECTION:
- *   Connection succesfully established, for client sockets, or incoming
+ *   Connection successfully established, for client sockets, or incoming
  *   client connection, for server sockets. Wait for this event (also watch
  *   out for GSOCK_LOST) after you issue a nonblocking GSocket_Connect() call.
@@ -1535,5 +1535,5 @@
  *   OUTPUT events will be generated unless an error occurs.
  * GSOCK_CONNECTION:
- *   Connection succesfully established, for client sockets, or incoming
+ *   Connection successfully established, for client sockets, or incoming
  *   client connection, for server sockets. Wait for this event (also watch
  *   out for GSOCK_LOST) after you issue a nonblocking GSocket_Connect() call.
Index: src/mac/classic/toplevel.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/mac/classic/toplevel.cpp,v
retrieving revision 1.5
diff -b -u -2 -r1.5 toplevel.cpp
--- src/mac/classic/toplevel.cpp	2005/01/18 15:06:28	1.5
+++ src/mac/classic/toplevel.cpp	2005/05/29 15:53:19
@@ -951,5 +951,5 @@
             {
                 // This is not right if the second mouse down
-                // event occured in a differen window. We
+                // event occurred in a different window. We
                 // correct this in MacDispatchMouseEvent.
                 if ( controlDown )
Index: src/msw/bitmap.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/bitmap.cpp,v
retrieving revision 1.130
diff -b -u -2 -r1.130 bitmap.cpp
--- src/msw/bitmap.cpp	2005/05/25 17:06:47	1.130
+++ src/msw/bitmap.cpp	2005/05/29 15:53:20
@@ -338,5 +338,5 @@
     if ( !refData->m_hasAlpha )
     {
-        // the mask returned by GetIconInfo() is inversed compared to the usual
+        // the mask returned by GetIconInfo() is inverted compared to the usual
         // wxWin convention
         refData->SetMask(wxInvertMask(iconInfo.hbmMask, w, h));
@@ -436,5 +436,5 @@
         // we assume that it is in XBM format which is not quite the same as
         // the format CreateBitmap() wants because the order of bytes in the
-        // line is inversed!
+        // line is reversed!
         const size_t bytesPerLine = (width + 7) / 8;
         const size_t padding = bytesPerLine % 2;
Index: src/msw/datectrl.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/datectrl.cpp,v
retrieving revision 1.21
diff -b -u -2 -r1.21 datectrl.cpp
--- src/msw/datectrl.cpp	2005/05/18 02:23:00	1.21
+++ src/msw/datectrl.cpp	2005/05/29 15:53:20
@@ -101,5 +101,5 @@
     // presumably earlier versions 4.70 and 4.71, date time picker not being
     // supported in < 4.70 anyhow) it does not do it and we have to initialize
-    // it explicitely
+    // it explicitly
     static bool s_initDone = false; // MT-ok: used from GUI thread only
     if ( !s_initDone )
Index: src/msw/dcprint.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/dcprint.cpp,v
retrieving revision 1.57
diff -b -u -2 -r1.57 dcprint.cpp
--- src/msw/dcprint.cpp	2005/03/24 20:01:48	1.57
+++ src/msw/dcprint.cpp	2005/05/29 15:53:20
@@ -402,5 +402,5 @@
             !DrawBitmapUsingStretchDIBits(GetHdc(), bmp, x, y) )
     {
-        // no support for StretchDIBits() or an error occured if we got here
+        // no support for StretchDIBits() or an error occurred if we got here
         wxMemoryDC memDC;
         memDC.SelectObject(bmp);
Index: src/msw/dialup.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/dialup.cpp,v
retrieving revision 1.51
diff -b -u -2 -r1.51 dialup.cpp
--- src/msw/dialup.cpp	2005/03/30 16:49:39	1.51
+++ src/msw/dialup.cpp	2005/05/29 15:53:21
@@ -506,5 +506,5 @@
         else
         {
-            // an error occured
+            // an error occurred
             wxLogError(_("Cannot find active dialup connection: %s"),
                        GetErrorString(dwRet).c_str());
Index: src/msw/evtloop.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/evtloop.cpp,v
retrieving revision 1.27
diff -b -u -2 -r1.27 evtloop.cpp
--- src/msw/evtloop.cpp	2004/12/12 11:47:57	1.27
+++ src/msw/evtloop.cpp	2005/05/29 15:53:21
@@ -117,5 +117,5 @@
     wxWindow *wndThis = wxGetWindowFromHWND((WXHWND)hwnd);
 
-    // this may happen if the event occured in a standard modeless dialog (the
+    // this may happen if the event occurred in a standard modeless dialog (the
     // only example of which I know of is the find/replace dialog) - then call
     // IsDialogMessage() to make TAB navigation in it work
Index: src/msw/glcanvas.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/glcanvas.cpp,v
retrieving revision 1.31
diff -b -u -2 -r1.31 glcanvas.cpp
--- src/msw/glcanvas.cpp	2004/10/15 15:29:31	1.31
+++ src/msw/glcanvas.cpp	2005/05/29 15:53:21
@@ -477,5 +477,5 @@
         PFD_DOUBLEBUFFER,        /* support double-buffering */
         PFD_TYPE_RGBA,            /* color type */
-        16,                /* prefered color depth */
+        16,                /* preferred color depth */
         0, 0, 0, 0, 0, 0,        /* color bits (ignored) */
         0,                /* no alpha buffer */
@@ -786,5 +786,5 @@
         PFD_DOUBLEBUFFER,        /* support double-buffering */
         PFD_TYPE_RGBA,            /* color type */
-        16,                /* prefered color depth */
+        16,                /* preferred color depth */
         0, 0, 0, 0, 0, 0,        /* color bits (ignored) */
         0,                /* no alpha buffer */
Index: src/msw/gsocket.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/gsocket.cpp,v
retrieving revision 1.15
diff -b -u -2 -r1.15 gsocket.cpp
--- src/msw/gsocket.cpp	2005/05/04 18:55:50	1.15
+++ src/msw/gsocket.cpp	2005/05/29 15:53:22
@@ -517,5 +517,5 @@
 *  Implemented as a GSocket function because clients (ie, wxSocketServer)
 *  don't have access to the GSocket struct information.
-*  Returns true if the flag was set correctly, false if an error occured
+*  Returns true if the flag was set correctly, false if an error occurred
 *  (ie, if the parameter was NULL)
 */
@@ -536,5 +536,5 @@
  *  to establish a client connection to a server using the peer address
  *  as established with GSocket_SetPeer(). Returns GSOCK_NOERROR if the
- *  connection has been succesfully established, or one of the error
+ *  connection has been successfully established, or one of the error
  *  codes listed below. Note that for nonblocking sockets, a return
  *  value of GSOCK_WOULDBLOCK doesn't mean a failure. The connection
@@ -938,5 +938,5 @@
 
 /* GSocket_GetError:
- *  Returns the last error occured for this socket. Note that successful
+ *  Returns the last error occurred for this socket. Note that successful
  *  operations do not clear this back to GSOCK_NOERROR, so use it only
  *  after an error.
@@ -963,5 +963,5 @@
  *   OUTPUT events will be generated unless an error occurs.
  * GSOCK_CONNECTION:
- *   Connection succesfully established, for client sockets, or incoming
+ *   Connection successfully established, for client sockets, or incoming
  *   client connection, for server sockets. Wait for this event (also watch
  *   out for GSOCK_LOST) after you issue a nonblocking GSocket_Connect() call.
Index: src/msw/imaglist.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/imaglist.cpp,v
retrieving revision 1.28
diff -b -u -2 -r1.28 imaglist.cpp
--- src/msw/imaglist.cpp	2005/02/27 10:36:57	1.28
+++ src/msw/imaglist.cpp	2005/05/29 15:53:22
@@ -204,5 +204,5 @@
     if ( !ok )
     {
-        wxLogLastError(wxT("ImageList_Add()"));
+        wxLogLastError(wxT("ImageList_Replace()"));
     }
 
Index: src/msw/listctrl.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/listctrl.cpp,v
retrieving revision 1.228
diff -b -u -2 -r1.228 listctrl.cpp
--- src/msw/listctrl.cpp	2005/05/25 21:15:47	1.228
+++ src/msw/listctrl.cpp	2005/05/29 15:53:24
@@ -2291,5 +2291,5 @@
         case LVN_ENDLABELEDITA:
         case LVN_ENDLABELEDITW:
-            // logic here is inversed compared to all the other messages
+            // logic here is inverted compared to all the other messages
             *result = event.IsAllowed();
 
Index: src/msw/main.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/main.cpp,v
retrieving revision 1.51
diff -b -u -2 -r1.51 main.cpp
--- src/msw/main.cpp	2005/05/22 14:45:02	1.51
+++ src/msw/main.cpp	2005/05/29 15:53:24
@@ -94,5 +94,5 @@
 
 // ----------------------------------------------------------------------------
-// wrapper wxEntry catching all Win32 exceptions occuring in a wx program
+// wrapper wxEntry catching all Win32 exceptions occurring in a wx program
 // ----------------------------------------------------------------------------
 
Index: src/msw/mediactrl.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/mediactrl.cpp,v
retrieving revision 1.34
diff -b -u -2 -r1.34 mediactrl.cpp
--- src/msw/mediactrl.cpp	2005/05/25 12:54:58	1.34
+++ src/msw/mediactrl.cpp	2005/05/29 15:53:25
@@ -1118,5 +1118,5 @@
         /*{*/ \
             /*wxFAIL_MSG( wxString::Format(wxT("DirectShow error \"%s\" ")*/\
-                                         /*wxT("occured at line %i in ")*/ \
+                                         /*wxT("occurred at line %i in ")*/ \
                                          /*wxT("mediactrl.cpp"),*/ \
                                             /*szError, __LINE__) );*/ \
@@ -1124,5 +1124,5 @@
         /*else*/ \
             wxFAIL_MSG( wxString::Format(wxT("Unknown error (%i) ") \
-                                         wxT("occured at") \
+                                         wxT("occurred at") \
                                          wxT(" line %i in mediactrl.cpp."), \
                                          (int)hrdsv, __LINE__) ); \
@@ -1194,5 +1194,5 @@
     // so we need to get rid of those, and create with
     // wxCLIP_CHILDREN, so that if the driver/backend
-    // is a child window, it refereshes properly
+    // is a child window, it refreshes properly
     //
     if ( !ctrl->wxControl::Create(parent, id, pos, size,
@@ -1839,5 +1839,5 @@
     //Here, if the parent of the control has a sizer - we
     //tell it to recalculate the size of this control since
-    //the user opened a seperate media file
+    //the user opened a separate media file
     //
     m_ctrl->InvalidateBestSize();
@@ -2265,5 +2265,5 @@
     if(m_lib.IsOk())
     {
-        //Note that ExitMovies() is not neccessary, but
+        //Note that ExitMovies() is not necessary, but
         //the docs are fuzzy on whether or not TerminateQTML is
         m_lib.ExitMovies();
@@ -2442,5 +2442,5 @@
     //Here, if the parent of the control has a sizer - we
     //tell it to recalculate the size of this control since
-    //the user opened a seperate media file
+    //the user opened a separate media file
     //
     m_ctrl->InvalidateBestSize();
Index: src/msw/menu.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/menu.cpp,v
retrieving revision 1.113
diff -b -u -2 -r1.113 menu.cpp
--- src/msw/menu.cpp	2005/04/02 15:16:53	1.113
+++ src/msw/menu.cpp	2005/05/29 15:53:26
@@ -647,5 +647,5 @@
     {
         // get the checked status of the command: notice that menuState is the
-        // old state of the menu, so the test for MF_CHECKED must be inversed
+        // old state of the menu, so the test for MF_CHECKED must be inverted
         UINT menuState = ::GetMenuState(GetHmenu(), id, MF_BYCOMMAND);
         SendEvent(id, !(menuState & MF_CHECKED));
Index: src/msw/ownerdrw.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/ownerdrw.cpp,v
retrieving revision 1.56
diff -b -u -2 -r1.56 ownerdrw.cpp
--- src/msw/ownerdrw.cpp	2005/05/17 16:03:25	1.56
+++ src/msw/ownerdrw.cpp	2005/05/29 15:53:26
@@ -86,5 +86,5 @@
 };
 
-// these static variables are by the wxMSWSystemMenuFontModule object
+// these static variables are from the wxMSWSystemMenuFontModule object
 // and reflect the system settings returned by the Win32 API's
 // SystemParametersInfo() call.
@@ -160,6 +160,6 @@
 
   // if we have a valid accel string, then pad out
-  // the menu string so the menu and accel string are not
-  // placed ontop of eachother.
+  // the menu string so that the menu and accel string are not
+  // placed on top of each other.
   if ( !m_strAccel.empty() )
    {
@@ -280,5 +280,5 @@
 
 
-  // using native API because it reckognizes '&'
+  // using native API because it recognizes '&'
   int nPrevMode = SetBkMode(hdc, TRANSPARENT);
   HBRUSH hbr = CreateSolidBrush(colBack),
@@ -323,5 +323,5 @@
 
   // ::SetTextAlign(hdc, TA_RIGHT) doesn't work with DSS_DISABLED or DSS_MONO
-  // as last parameter in DrawState() (at least with Windows98). So we have
+  // as the last parameter in DrawState() (at least with Windows98). So we have
   // to take care of right alignment ourselves.
   if ( !m_strAccel.empty() )
@@ -395,5 +395,5 @@
           nBmpHeight = bmp.GetHeight();
 
-      // there should be enough place!
+      // there should be enough space!
       wxASSERT((nBmpWidth <= rc.GetWidth()) && (nBmpHeight <= rc.GetHeight()));
 
Index: src/msw/stackwalk.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/stackwalk.cpp,v
retrieving revision 1.5
diff -b -u -2 -r1.5 stackwalk.cpp
--- src/msw/stackwalk.cpp	2005/02/09 14:19:49	1.5
+++ src/msw/stackwalk.cpp	2005/05/29 15:53:26
@@ -135,7 +135,7 @@
     // if symbol information is corrupted and we crash, the exception is going
     // to be ignored when we're called from WalkFromException() because of the
-    // except handler there returning EXCEPTION_CONTINUE_EXECUTION, but we'd be
-    // left in an inconsistent state, so deal with it explicitely here (even if
-    // normally we should never crash, of course...)
+    // exception handler there returning EXCEPTION_CONTINUE_EXECUTION, but we'd
+    // be left in an inconsistent state, so deal with it explicitly here (even
+    // if normally we should never crash, of course...)
 #ifdef _CPPUNWIND
     try
Index: src/msw/taskbar.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/taskbar.cpp,v
retrieving revision 1.45
diff -b -u -2 -r1.45 taskbar.cpp
--- src/msw/taskbar.cpp	2005/04/23 18:59:32	1.45
+++ src/msw/taskbar.cpp	2005/05/29 15:53:26
@@ -144,6 +144,6 @@
 bool wxTaskBarIcon::SetIcon(const wxIcon& icon, const wxString& tooltip)
 {
-    // NB: we have to create the window lazily because of backward compatiblity,
-    //     old aplications may create wxTaskBarIcon instance before wxApp
+    // NB: we have to create the window lazily because of backward compatibility,
+    //     old applications may create a wxTaskBarIcon instance before wxApp
     //     is initialized (as samples/taskbar used to do)
     if (!m_win)
Index: src/msw/tooltip.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/tooltip.cpp,v
retrieving revision 1.41
diff -b -u -2 -r1.41 tooltip.cpp
--- src/msw/tooltip.cpp	2005/05/17 16:03:25	1.41
+++ src/msw/tooltip.cpp	2005/05/29 15:53:27
@@ -142,5 +142,5 @@
         LPPOINT ppt = (LPPOINT)lParam;
 
-        // the window on which event occured
+        // the window on which event occurred
         HWND hwnd = ::WindowFromPoint(*ppt);
 
Index: src/msw/toplevel.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/toplevel.cpp,v
retrieving revision 1.114
diff -b -u -2 -r1.114 toplevel.cpp
--- src/msw/toplevel.cpp	2005/04/06 21:15:17	1.114
+++ src/msw/toplevel.cpp	2005/05/29 15:53:27
@@ -942,5 +942,5 @@
                                 dllUser32.GetSymbol(_T("FlashWindowEx"));
 
-        // we can safely unload user32.dll here, it's goign to remain loaded as
+        // we can safely unload user32.dll here, it's going to remain loaded as
         // long as the program is running anyhow
     }
Index: src/msw/utils.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/utils.cpp,v
retrieving revision 1.174
diff -b -u -2 -r1.174 utils.cpp
--- src/msw/utils.cpp	2005/05/22 12:10:55	1.174
+++ src/msw/utils.cpp	2005/05/29 15:53:28
@@ -730,5 +730,5 @@
 
                 // EnumWindows() has nice semantics: it returns 0 if it found
-                // something or if an error occured and non zero if it
+                // something or if an error occurred and non zero if it
                 // enumerated all the window
                 if ( !::EnumWindows(wxEnumFindByPidProc, (LPARAM)&params) )
@@ -739,5 +739,5 @@
                         // tell the app to close
                         //
-                        // NB: this is the harshest way, the app won't have
+                        // NB: this is the harshest way, the app won't have an
                         //     opportunity to save any files, for example, but
                         //     this is probably what we want here. If not we
@@ -1479,5 +1479,5 @@
 
   pclassname is a pointer to a caller stored classname, which must initially be
-  NULL. classname is the desired wndclass classname. If function succesfully
+  NULL. classname is the desired wndclass classname. If function successfully
   registers the class, pclassname will be set to classname.
  */
Index: src/msw/utilsexc.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/utilsexc.cpp,v
retrieving revision 1.79
diff -b -u -2 -r1.79 utilsexc.cpp
--- src/msw/utilsexc.cpp	2005/03/01 10:19:24	1.79
+++ src/msw/utilsexc.cpp	2005/05/29 15:53:28
@@ -386,5 +386,5 @@
         }
 
-        // don't try to continue reading from a pipe if an error occured or if
+        // don't try to continue reading from a pipe if an error occurred or if
         // it had been closed
         ::CloseHandle(m_hInput);
@@ -418,5 +418,5 @@
     }
 
-    // bytesRead is set to 0, as desired, if an error occured
+    // bytesRead is set to 0, as desired, if an error occurred
     return bytesRead;
 }
Index: src/msw/utilsgui.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/utilsgui.cpp,v
retrieving revision 1.10
diff -b -u -2 -r1.10 utilsgui.cpp
--- src/msw/utilsgui.cpp	2004/09/07 11:11:05	1.10
+++ src/msw/utilsgui.cpp	2005/05/29 15:53:29
@@ -268,5 +268,5 @@
 #else
     // this function is called from wxDC ctor so it is called a *lot* of times
-    // hence we optimize it a bit but doign the check only once
+    // hence we optimize it a bit but doing the check only once
     //
     // this should be MT safe as only the GUI thread (holding the GUI mutex)
Index: src/msw/window.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/window.cpp,v
retrieving revision 1.618
diff -b -u -2 -r1.618 window.cpp
--- src/msw/window.cpp	2005/05/25 13:02:59	1.618
+++ src/msw/window.cpp	2005/05/29 15:53:32
@@ -821,5 +821,5 @@
                           &scrollInfo) )
     {
-        // Not neccessarily an error, if there are no scrollbars yet.
+        // Not necessarily an error, if there are no scrollbars yet.
         // wxLogLastError(_T("GetScrollInfo"));
     }
@@ -5048,5 +5048,5 @@
                                   &scrollInfo) )
             {
-                // Not neccessarily an error, if there are no scrollbars yet.
+                // Not necessarily an error, if there are no scrollbars yet.
                 // wxLogLastError(_T("GetScrollInfo"));
             }
Index: src/msw/ole/automtn.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/ole/automtn.cpp,v
retrieving revision 1.41
diff -b -u -2 -r1.41 automtn.cpp
--- src/msw/ole/automtn.cpp	2004/12/14 20:42:07	1.41
+++ src/msw/ole/automtn.cpp	2005/05/29 15:53:32
@@ -1232,5 +1232,5 @@
 
         default:
-            wsprintf(szBuf, L"%s: Unknown error occured.", szMember);
+            wsprintf(szBuf, L"%s: Unknown error occurred.", szMember);
             break;
     }
Index: src/msw/ole/dataobj.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/ole/dataobj.cpp,v
retrieving revision 1.85
diff -b -u -2 -r1.85 dataobj.cpp
--- src/msw/ole/dataobj.cpp	2005/03/10 18:54:53	1.85
+++ src/msw/ole/dataobj.cpp	2005/05/29 15:53:33
@@ -107,5 +107,5 @@
 
     // normally, wxDataObject controls our lifetime (i.e. we're deleted when it
-    // is), but in some cases, the situation is inversed, that is we delete it
+    // is), but in some cases, the situation is reversed, that is we delete it
     // when this object is deleted - setting this flag enables such logic
     void SetDeleteFlag() { m_mustDelete = true; }
@@ -501,5 +501,5 @@
                             pBuf = m_pDataObject->GetSizeFromBuffer( pBuf, &size, format );
                             if (! format.IsStandard() ) {
-                                // see GetData for coresponding increment
+                                // see GetData for corresponding increment
                                 size -= m_pDataObject->GetBufferOffset( format  );
                             }
Index: src/msw/ole/droptgt.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/msw/ole/droptgt.cpp,v
retrieving revision 1.41
diff -b -u -2 -r1.41 droptgt.cpp
--- src/msw/ole/droptgt.cpp	2005/03/08 22:50:46	1.41
+++ src/msw/ole/droptgt.cpp	2005/05/29 15:53:33
@@ -261,5 +261,5 @@
 // Params  : [in] IDataObject *pIDataSource     the data to paste
 //           [in] DWORD        grfKeyState      kbd & mouse state
-//           [in] POINTL       pt               where the drop occured?
+//           [in] POINTL       pt               where the drop occurred?
 //           [ouy]DWORD       *pdwEffect        operation effect
 // Notes   :
Index: src/os2/bitmap.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/os2/bitmap.cpp,v
retrieving revision 1.47
diff -b -u -2 -r1.47 bitmap.cpp
--- src/os2/bitmap.cpp	2005/04/27 01:19:36	1.47
+++ src/os2/bitmap.cpp	2005/05/29 15:53:34
@@ -195,5 +195,5 @@
         // We assume that it is in XBM format which is not quite the same as
         // the format CreateBitmap() wants because the order of bytes in the
-        // line is inversed!
+        // line is reversed!
         //
         const size_t                nBytesPerLine = (nWidth + 7) / 8;
Index: src/os2/dc.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/os2/dc.cpp,v
retrieving revision 1.69
diff -b -u -2 -r1.69 dc.cpp
--- src/os2/dc.cpp	2005/05/16 16:22:05	1.69
+++ src/os2/dc.cpp	2005/05/29 15:53:36
@@ -1388,5 +1388,5 @@
 
                 //
-                // Bitmap must be ina double-word alligned address so we may
+                // Bitmap must be in a double-word aligned address so we may
                 // have some padding to worry about
                 //
Index: src/os2/dcclient.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/os2/dcclient.cpp,v
retrieving revision 1.36
diff -b -u -2 -r1.36 dcclient.cpp
--- src/os2/dcclient.cpp	2005/04/27 01:19:38	1.36
+++ src/os2/dcclient.cpp	2005/05/29 15:53:36
@@ -181,5 +181,5 @@
     delete pFont;
     //
-    // OS/2 default vertical character allignment needs to match the other OS's
+    // OS/2 default vertical character alignment needs to match the other OS's
     //
     ::GpiSetTextAlignment((HPS)GetHPS(), TA_NORMAL_HORIZ, TA_BOTTOM);
Index: src/os2/ownerdrw.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/os2/ownerdrw.cpp,v
retrieving revision 1.22
diff -b -u -2 -r1.22 ownerdrw.cpp
--- src/os2/ownerdrw.cpp	2005/04/27 01:19:41	1.22
+++ src/os2/ownerdrw.cpp	2005/05/29 15:53:36
@@ -78,6 +78,6 @@
     //
     // If we have a valid accel string, then pad out
-    // the menu string so the menu and accel string are not
-    // placed ontop of eachother.
+    // the menu string so that the menu and accel string are not
+    // placed on top of each other.
     if (!m_strAccel.empty() )
     {
@@ -93,5 +93,5 @@
     {
         //
-        // Measure the accelerator string, and add it's width to
+        // Measure the accelerator string, and add its width to
         // the total item width, plus 16 (Accelerators are right justified,
         // with the right edge of the text rectangle 16 pixels left of
@@ -109,6 +109,6 @@
 
     //
-    // Add space at the end of the menu for the submenu expansion arrow
-    // this will also allow offsetting the accel string from the right edge
+    // Add space at the end of the menu for the submenu expansion arrow.
+    // This will also allow offsetting the accel string from the right edge
     //
     *pWidth = (size_t)(*pWidth + GetDefaultMarginWidth() * 1.5);
@@ -211,5 +211,5 @@
 
     //
-    // Base on the status of the menu item pick the right colors
+    // Based on the status of the menu item, pick the right colors
     //
     if (eStatus & wxODSelected)
@@ -292,12 +292,12 @@
     // Unfortunately, unlike Win32, PM has no owner drawn specific text
     // drawing methods like ::DrawState that can cleanly handle accel
-    // pneumonics and deal, automatically, with various states, so we have
+    // mnemonics and deal, automatically, with various states, so we have
     // to handle them ourselves. Notice Win32 can't handle \t in ownerdrawn
-    // strings either.  We cannot handle mneumonics either.  We display
-    // it, though, in hopes we can figure it out some day.
+    // strings either.  We cannot handle mnemonics either.  We display
+    // them, though, in the hope we can figure them out some day.
     //
 
     //
-    // Display main text and accel text separately to allign better
+    // Display main text and accel text separately to align better
     //
     wxString                        sTgt = wxT("\t");
@@ -308,5 +308,5 @@
     size_t                          nCharWidth;
     size_t                          nHeight;
-    bool                            bFoundMneumonic = FALSE;
+    bool                            bFoundMnemonic = FALSE;
     bool                            bFoundAccel = FALSE;
 
@@ -323,5 +323,5 @@
 
     //
-    // Deal with the mneumonic character
+    // Deal with the mnemonic character
     //
     sTgt = wxT("~");
@@ -331,5 +331,5 @@
         wxString                    sTmp = sFullString;
 
-        bFoundMneumonic = TRUE;
+        bFoundMnemonic = TRUE;
         sTmp.Remove(nIndex);
         rDC.GetTextExtent( sTmp
@@ -354,8 +354,8 @@
                       ,(PCH)sFullString.c_str()
                      );
-    if (bFoundMneumonic)
+    if (bFoundMnemonic)
     {
         //
-        // Underline the mneumonic -- still won't work, but at least it "looks" right
+        // Underline the mnemonic -- still won't work, but at least it "looks" right
         //
         wxPen                       vPen;
Index: src/os2/utilsgui.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/os2/utilsgui.cpp,v
retrieving revision 1.9
diff -b -u -2 -r1.9 utilsgui.cpp
--- src/os2/utilsgui.cpp	2005/04/27 01:19:43	1.9
+++ src/os2/utilsgui.cpp	2005/05/29 15:53:37
@@ -940,5 +940,5 @@
 
     //
-    // Bitmap must be ina double-word alligned address so we may
+    // Bitmap must be in a double-word aligned address so we may
     // have some padding to worry about
     //
Index: src/tiff/tif_open.c
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/tiff/tif_open.c,v
retrieving revision 1.4
diff -b -u -2 -r1.4 tif_open.c
--- src/tiff/tif_open.c	2004/11/19 22:29:45	1.4
+++ src/tiff/tif_open.c	2005/05/29 15:53:37
@@ -217,5 +217,5 @@
 	 * that can optimize operations on data by using a particular
 	 * bit order.  By default the library returns data in MSB2LSB
-	 * bit order for compatibiltiy with older versions of this
+	 * bit order for compatibility with older versions of this
 	 * library.  Returning data in the bit order of the native cpu
 	 * makes the most sense but also requires applications to check
Index: src/tiff/tif_pixarlog.c
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/tiff/tif_pixarlog.c,v
retrieving revision 1.3
diff -b -u -2 -r1.3 tif_pixarlog.c
--- src/tiff/tif_pixarlog.c	2004/11/19 22:29:45	1.3
+++ src/tiff/tif_pixarlog.c	2005/05/29 15:53:38
@@ -44,5 +44,5 @@
  * the range 0-1.  In the case of IEEE values, the 0-1 range is assumed to
  * be the normal linear color range, in addition over 1 values are
- * accepted up to a value of about 25.0 to encode "hot" hightlights and such.
+ * accepted up to a value of about 25.0 to encode "hot" highlights and such.
  * The encoding is lossless for 8-bit values, slightly lossy for the
  * other bit depths.  The actual color precision should be better
Index: src/univ/combobox.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/univ/combobox.cpp,v
retrieving revision 1.41
diff -b -u -2 -r1.41 combobox.cpp
--- src/univ/combobox.cpp	2005/02/16 20:34:30	1.41
+++ src/univ/combobox.cpp	2005/05/29 15:53:38
@@ -577,5 +577,5 @@
         m_combo->ProcessEvent(event2);
     }
-    //else: ignore the events resultign from just moving the mouse initially
+    //else: ignore the events resulting from just moving the mouse initially
 }
 
@@ -615,5 +615,5 @@
     // we're about to go away anyhow (normally this shouldn't happen at all,
     // but I don't put assert here as it just might do on other platforms and
-    // it doesn't break anythign anyhow)
+    // it doesn't break anything anyhow)
     if ( this == wxWindow::GetCapture() )
     {
Index: src/univ/scrolbar.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/univ/scrolbar.cpp,v
retrieving revision 1.23
diff -b -u -2 -r1.23 scrolbar.cpp
--- src/univ/scrolbar.cpp	2004/12/20 01:08:13	1.23
+++ src/univ/scrolbar.cpp	2005/05/29 15:53:39
@@ -257,5 +257,5 @@
                                bool refresh)
 {
-    // we only refresh everythign when the range changes, thumb position
+    // we only refresh everything when the range changes, thumb position
     // changes are handled in OnIdle
     bool needsRefresh = (range != m_range) ||
Index: src/univ/slider.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/univ/slider.cpp,v
retrieving revision 1.15
diff -b -u -2 -r1.15 slider.cpp
--- src/univ/slider.cpp	2004/11/05 21:03:44	1.15
+++ src/univ/slider.cpp	2005/05/29 15:53:39
@@ -23,7 +23,7 @@
 
    What we really need is probably a more fine grain control on labels, i.e. we
-   should be able to select if we show nothign at all, the current value only
+   should be able to select if we show nothing at all, the current value only
    or the value and the limits - the current approach is just that of the
-   greatest common denominator.
+   lowest common denominator.
 
    TODO:
Index: src/univ/toolbar.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/univ/toolbar.cpp,v
retrieving revision 1.33
diff -b -u -2 -r1.33 toolbar.cpp
--- src/univ/toolbar.cpp	2004/11/05 21:03:45	1.33
+++ src/univ/toolbar.cpp	2005/05/29 15:53:40
@@ -592,5 +592,5 @@
         if (tool->IsSeparator() && !HasFlag(wxTB_FLAT))
         {
-            // Draw seperators only in flat mode
+            // Draw separators only in flat mode
             continue;
         }
Index: src/univ/themes/metal.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/univ/themes/metal.cpp,v
retrieving revision 1.14
diff -b -u -2 -r1.14 metal.cpp
--- src/univ/themes/metal.cpp	2004/11/05 21:05:50	1.14
+++ src/univ/themes/metal.cpp	2005/05/29 15:53:40
@@ -80,6 +80,6 @@
         Arrow_Disabled,
         Arrow_Pressed,
-        Arrow_Inversed,
-        Arrow_InversedDisabled,
+        Arrow_Inverted,
+        Arrow_InvertedDisabled,
         Arrow_StateMax
     };
@@ -367,10 +367,10 @@
         }
 
-        // create the inversed bitmap but only for the right arrow as we only
+        // create the inverted bitmap but only for the right arrow as we only
         // use it for the menus
         if ( n == Arrow_Right )
         {
-            m_bmpArrows[Arrow_Inversed][n].Create(w, h);
-            dcInverse.SelectObject(m_bmpArrows[Arrow_Inversed][n]);
+            m_bmpArrows[Arrow_Inverted][n].Create(w, h);
+            dcInverse.SelectObject(m_bmpArrows[Arrow_Inverted][n]);
             dcInverse.Clear();
             dcInverse.Blit(0, 0, w, h,
@@ -379,9 +379,9 @@
             dcInverse.SelectObject(wxNullBitmap);
 
-            mask = new wxMask(m_bmpArrows[Arrow_Inversed][n], *wxBLACK);
-            m_bmpArrows[Arrow_Inversed][n].SetMask(mask);
+            mask = new wxMask(m_bmpArrows[Arrow_Inverted][n], *wxBLACK);
+            m_bmpArrows[Arrow_Inverted][n].SetMask(mask);
 
-            m_bmpArrows[Arrow_InversedDisabled][n].Create(w, h);
-            dcInverse.SelectObject(m_bmpArrows[Arrow_InversedDisabled][n]);
+            m_bmpArrows[Arrow_InvertedDisabled][n].Create(w, h);
+            dcInverse.SelectObject(m_bmpArrows[Arrow_InvertedDisabled][n]);
             dcInverse.Clear();
             dcInverse.Blit(0, 0, w, h,
@@ -390,6 +390,6 @@
             dcInverse.SelectObject(wxNullBitmap);
 
-            mask = new wxMask(m_bmpArrows[Arrow_InversedDisabled][n], *wxBLACK);
-            m_bmpArrows[Arrow_InversedDisabled][n].SetMask(mask);
+            mask = new wxMask(m_bmpArrows[Arrow_InvertedDisabled][n], *wxBLACK);
+            m_bmpArrows[Arrow_InvertedDisabled][n].SetMask(mask);
         }
 
Index: src/univ/themes/win32.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/univ/themes/win32.cpp,v
retrieving revision 1.75
diff -b -u -2 -r1.75 win32.cpp
--- src/univ/themes/win32.cpp	2005/02/11 10:42:05	1.75
+++ src/univ/themes/win32.cpp	2005/05/29 15:53:42
@@ -137,6 +137,6 @@
         Arrow_Disabled,
         Arrow_Pressed,
-        Arrow_Inversed,
-        Arrow_InversedDisabled,
+        Arrow_Inverted,
+        Arrow_InvertedDisabled,
         Arrow_StateMax
     };
@@ -1678,10 +1678,10 @@
         }
 
-        // create the inversed bitmap but only for the right arrow as we only
+        // create the inverted bitmap but only for the right arrow as we only
         // use it for the menus
         if ( n == Arrow_Right )
         {
-            m_bmpArrows[Arrow_Inversed][n].Create(w, h);
-            dcInverse.SelectObject(m_bmpArrows[Arrow_Inversed][n]);
+            m_bmpArrows[Arrow_Inverted][n].Create(w, h);
+            dcInverse.SelectObject(m_bmpArrows[Arrow_Inverted][n]);
             dcInverse.Clear();
             dcInverse.Blit(0, 0, w, h,
@@ -1690,9 +1690,9 @@
             dcInverse.SelectObject(wxNullBitmap);
 
-            mask = new wxMask(m_bmpArrows[Arrow_Inversed][n], *wxBLACK);
-            m_bmpArrows[Arrow_Inversed][n].SetMask(mask);
+            mask = new wxMask(m_bmpArrows[Arrow_Inverted][n], *wxBLACK);
+            m_bmpArrows[Arrow_Inverted][n].SetMask(mask);
 
-            m_bmpArrows[Arrow_InversedDisabled][n].Create(w, h);
-            dcInverse.SelectObject(m_bmpArrows[Arrow_InversedDisabled][n]);
+            m_bmpArrows[Arrow_InvertedDisabled][n].Create(w, h);
+            dcInverse.SelectObject(m_bmpArrows[Arrow_InvertedDisabled][n]);
             dcInverse.Clear();
             dcInverse.Blit(0, 0, w, h,
@@ -1701,6 +1701,6 @@
             dcInverse.SelectObject(wxNullBitmap);
 
-            mask = new wxMask(m_bmpArrows[Arrow_InversedDisabled][n], *wxBLACK);
-            m_bmpArrows[Arrow_InversedDisabled][n].SetMask(mask);
+            mask = new wxMask(m_bmpArrows[Arrow_InvertedDisabled][n], *wxBLACK);
+            m_bmpArrows[Arrow_InvertedDisabled][n].SetMask(mask);
         }
 
@@ -2844,5 +2844,5 @@
        H         D B
        H         D B
-       H         D B   where H is hightlight colour
+       H         D B   where H is highlight colour
        H         D B         D    dark grey
        H         D B         B    black
@@ -3198,8 +3198,8 @@
         wxArrowStyle arrowStyle;
         if ( flags & wxCONTROL_DISABLED )
-            arrowStyle = flags & wxCONTROL_SELECTED ? Arrow_InversedDisabled
+            arrowStyle = flags & wxCONTROL_SELECTED ? Arrow_InvertedDisabled
                                                     : Arrow_Disabled;
         else if ( flags & wxCONTROL_SELECTED )
-            arrowStyle = Arrow_Inversed;
+            arrowStyle = Arrow_Inverted;
         else
             arrowStyle = Arrow_Normal;
Index: src/unix/gsocket.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/unix/gsocket.cpp,v
retrieving revision 1.41
diff -b -u -2 -r1.41 gsocket.cpp
--- src/unix/gsocket.cpp	2005/05/07 13:35:09	1.41
+++ src/unix/gsocket.cpp	2005/05/29 15:53:43
@@ -633,5 +633,5 @@
  *  to establish a client connection to a server using the peer address
  *  as established with GSocket_SetPeer(). Returns GSOCK_NOERROR if the
- *  connection has been succesfully established, or one of the error
+ *  connection has been successfully established, or one of the error
  *  codes listed below. Note that for nonblocking sockets, a return
  *  value of GSOCK_WOULDBLOCK doesn't mean a failure. The connection
@@ -1093,5 +1093,5 @@
 
 /* GSocket_GetError:
- *  Returns the last error occured for this socket. Note that successful
+ *  Returns the last error occurred for this socket. Note that successful
  *  operations do not clear this back to GSOCK_NOERROR, so use it only
  *  after an error.
@@ -1118,5 +1118,5 @@
  *   OUTPUT events will be generated unless an error occurs.
  * GSOCK_CONNECTION:
- *   Connection succesfully established, for client sockets, or incoming
+ *   Connection successfully established, for client sockets, or incoming
  *   client connection, for server sockets. Wait for this event (also watch
  *   out for GSOCK_LOST) after you issue a nonblocking GSocket_Connect() call.
Index: src/unix/utilsx11.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/src/unix/utilsx11.cpp,v
retrieving revision 1.25
diff -b -u -2 -r1.25 utilsx11.cpp
--- src/unix/utilsx11.cpp	2005/03/21 23:42:24	1.25
+++ src/unix/utilsx11.cpp	2005/05/29 15:53:44
@@ -199,5 +199,5 @@
 //     GTK+ >= 2.1.2 uses it as the only method of making windows fullscreen
 //     (that's why wxGTK will *not* switch to using gtk_window_fullscreen
-//     unless it has better compatiblity with older WMs).
+//     unless it has better compatibility with older WMs).
 //
 //     
@@ -409,5 +409,5 @@
     }
 
-    // it is neccessary to unmap the window, otherwise kwin will ignore us:
+    // it is necessary to unmap the window, otherwise kwin will ignore us:
     XSync(display, False);
     
@@ -436,10 +436,10 @@
     if (!fullscreen)
     {
-        // NB: like many other WMs, kwin ignores first request for window
+        // NB: like many other WMs, kwin ignores the first request for a window
         //     position change after the window was mapped. This additional
         //     move+resize event will ensure that the window is restored in
-        //     exactly same position as before it was made fullscreen (because
-        //     wxTopLevelWindow::ShowFullScreen will call SetSize, thus
-        //     setting the position for second time).
+	//     exactly the same position as before it was made fullscreen
+	//     (because wxTopLevelWindow::ShowFullScreen will call SetSize, thus
+        //     setting the position for the second time).
         XMoveResizeWindow(display, w,
                           origRect->x, origRect->y,
Index: utils/HelpGen/src/scriptbinder.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/utils/HelpGen/src/scriptbinder.h,v
retrieving revision 1.5
diff -b -u -2 -r1.5 scriptbinder.h
--- utils/HelpGen/src/scriptbinder.h	2005/05/24 09:06:20	1.5
+++ utils/HelpGen/src/scriptbinder.h	2005/05/29 15:53:44
@@ -369,5 +369,5 @@
 
     // fopenOptions arg. is string passed to fopen() method,
-    // returns true, if saving was successfull
+    // returns true, if saving was successful
 
     virtual bool SaveDocument( const char*    fname,
Index: utils/HelpGen/src/sourcepainter.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/utils/HelpGen/src/sourcepainter.h,v
retrieving revision 1.4
diff -b -u -2 -r1.4 sourcepainter.h
--- utils/HelpGen/src/sourcepainter.h	2005/05/23 15:22:09	1.4
+++ utils/HelpGen/src/sourcepainter.h	2005/05/29 15:53:44
@@ -97,5 +97,5 @@
 
     // returns vector of block descriptors, see SPBlockListT definition
-    // (block descriptors can be used for fast custom hightlighted text generation)
+    // (block descriptors can be used for fast custom highlighted text generation)
 
     SPBlockListT& GetBlocks();
Index: utils/HelpGen/src/wxstllst.h
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/utils/HelpGen/src/wxstllst.h,v
retrieving revision 1.2
diff -b -u -2 -r1.2 wxstllst.h
--- utils/HelpGen/src/wxstllst.h	2001/11/18 12:25:12	1.2
+++ utils/HelpGen/src/wxstllst.h	2005/05/29 15:53:44
@@ -25,5 +25,5 @@
 #include <new.h>
 
-// VERSION:: 0.2 (copy-constructor/adign-op added)
+// VERSION:: 0.2 (copy-constructor/assign-op added)
 
 // FOR NOW:: class-member operators "new" and "delete"
Index: utils/tex2rtf/docs/tex2rtf.tex
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/utils/tex2rtf/docs/tex2rtf.tex,v
retrieving revision 1.8
diff -b -u -2 -r1.8 tex2rtf.tex
--- utils/tex2rtf/docs/tex2rtf.tex	2005/01/06 15:51:05	1.8
+++ utils/tex2rtf/docs/tex2rtf.tex	2005/05/29 15:53:47
@@ -849,5 +849,5 @@
 \rtfsp\commandrefn{helprefn}{helprefn}, and \commandrefn{popref}{popref}.
 Tex2RTF outputs help files that may be read linearly using the
-\rtfsp$<<$ and $>>$ buttons, and an additonal Up button for
+\rtfsp$<<$ and $>>$ buttons, with an additional Up button for
 ease of navigation.
 
@@ -925,5 +925,5 @@
 Optional arguments are specified using square brackets or parentheses.
 
-The braces that start command arguments must not be seperated from
+The braces that start command arguments must not be separated from
 the other arguments by whitespace. For example, the following produces
 an error:
Index: utils/wxrc/wxrc.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/utils/wxrc/wxrc.cpp,v
retrieving revision 1.45
diff -b -u -2 -r1.45 wxrc.cpp
--- utils/wxrc/wxrc.cpp	2005/02/01 20:34:39	1.45
+++ utils/wxrc/wxrc.cpp	2005/05/29 15:53:47
@@ -206,5 +206,5 @@
         { wxCMD_LINE_OPTION, _T("o"), _T("output"),  _T("output file [resource.xrs/cpp]"), (wxCmdLineParamType)0, 0 },
 #if 0 // not yet implemented
-        { wxCMD_LINE_OPTION, _T("l"), _T("list-of-handlers"),  _T("output list of neccessary handlers to this file"), (wxCmdLineParamType)0, 0 },
+        { wxCMD_LINE_OPTION, _T("l"), _T("list-of-handlers"),  _T("output list of necessary handlers to this file"), (wxCmdLineParamType)0, 0 },
 #endif
         { wxCMD_LINE_PARAM,  NULL, NULL, _T("input file(s)"),
Index: wxPython/contrib/gizmos/wxCode/src/treelistctrl.cpp
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/wxPython/contrib/gizmos/wxCode/src/treelistctrl.cpp,v
retrieving revision 1.20
diff -b -u -2 -r1.20 treelistctrl.cpp
--- wxPython/contrib/gizmos/wxCode/src/treelistctrl.cpp	2005/05/22 01:48:22	1.20
+++ wxPython/contrib/gizmos/wxCode/src/treelistctrl.cpp	2005/05/29 15:53:50
@@ -1399,5 +1399,5 @@
         int xpos = 0;
 
-        // find the column where this event occured
+        // find the column where this event occurred
         int countCol = GetColumnCount();
         for (int col = 0; col < countCol; col++)
Index: wxPython/demo/Main.py
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/wxPython/demo/Main.py,v
retrieving revision 1.166
diff -b -u -2 -r1.166 Main.py
--- wxPython/demo/Main.py	2005/05/29 00:48:53	1.166
+++ wxPython/demo/Main.py	2005/05/29 15:53:51
@@ -938,5 +938,5 @@
 
         # Main Label
-        self.box.Add(wx.StaticText(self, -1, "An error has occured while trying to run the demo")
+        self.box.Add(wx.StaticText(self, -1, "An error has occurred while trying to run the demo")
                      , 0, wx.ALIGN_CENTER | wx.TOP, 10)
 
@@ -1405,5 +1405,5 @@
         # o The RunTest() for all samples must now return a window that can
         #   be palced in a tab in the main notebook.
-        # o If an error occurs (or has occured before) an error tab is created.
+        # o If an error occurs (or has occurred before) an error tab is created.
         
         if module is not None:
Index: wxPython/samples/ide/activegrid/tool/DebuggerHarness.py
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/wxPython/samples/ide/activegrid/tool/DebuggerHarness.py,v
retrieving revision 1.2
diff -b -u -2 -r1.2 DebuggerHarness.py
--- wxPython/samples/ide/activegrid/tool/DebuggerHarness.py	2005/05/19 00:21:48	1.2
+++ wxPython/samples/ide/activegrid/tool/DebuggerHarness.py	2005/05/29 15:53:52
@@ -108,5 +108,5 @@
         else: 
             exc_type_name = exc_type.__name__
-        message = "Exception occured: " + repr(exc_type_name) + " See locals.__exception__ for details."
+        message = "Exception occurred: " + repr(exc_type_name) + " See locals.__exception__ for details."
         traceback.print_exception(exc_type, exc_value, exc_traceback)
         self._harness.interaction(message, frame, message)
Index: wxPython/samples/ide/activegrid/tool/process.py
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/wxPython/samples/ide/activegrid/tool/process.py,v
retrieving revision 1.1
diff -b -u -2 -r1.1 process.py
--- wxPython/samples/ide/activegrid/tool/process.py	2005/04/08 22:51:48	1.1
+++ wxPython/samples/ide/activegrid/tool/process.py	2005/05/29 15:53:54
@@ -1925,5 +1925,5 @@
         self.__buf = ''
         # A state change is defined as the buffer being closed or a
-        # write occuring.
+        # write occurring.
         if mutex is not None:
             self._mutex = mutex
Index: wxPython/src/_mimetype.i
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/wxPython/src/_mimetype.i,v
retrieving revision 1.7
diff -b -u -2 -r1.7 _mimetype.i
--- wxPython/src/_mimetype.i	2005/03/09 22:28:41	1.7
+++ wxPython/src/_mimetype.i	2005/05/29 15:53:54
@@ -344,5 +344,5 @@
 
     // other operations: return True if there were no errors or False if there
-    // were some unreckognized entries (the good entries are always read anyhow)
+    // were some unrecognized entries (the good entries are always read anyhow)
     //
 
Index: wxPython/src/html.i
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/wxPython/src/html.i,v
retrieving revision 1.67
diff -b -u -2 -r1.67 html.i
--- wxPython/src/html.i	2005/03/09 22:28:39	1.67
+++ wxPython/src/html.i	2005/05/29 15:53:55
@@ -832,5 +832,5 @@
     // it is NOT address/filename of HTML document. If you want to
     // specify document location, use LoadPage() istead
-    // Return value : False if an error occured, True otherwise
+    // Return value : False if an error occurred, True otherwise
     bool SetPage(const wxString& source);
 
Index: wxPython/wx/lib/foldpanelbar.py
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/wxPython/wx/lib/foldpanelbar.py,v
retrieving revision 1.3
diff -b -u -2 -r1.3 foldpanelbar.py
--- wxPython/wx/lib/foldpanelbar.py	2005/05/18 02:33:37	1.3
+++ wxPython/wx/lib/foldpanelbar.py	2005/05/29 15:53:56
@@ -1076,5 +1076,5 @@
               text boxes.
 
-            * FPB_ALIGN_LEFT: Alligns left instead of fitting the
+            * FPB_ALIGN_LEFT: Aligns left instead of fitting the
               width of the child window to be added. Use either this
               one or FPB_ALIGN_WIDTH.
Index: wxPython/wx/lib/pydocview.py
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/wxPython/wx/lib/pydocview.py,v
retrieving revision 1.8
diff -b -u -2 -r1.8 pydocview.py
--- wxPython/wx/lib/pydocview.py	2005/05/19 00:21:39	1.8
+++ wxPython/wx/lib/pydocview.py	2005/05/29 15:53:58
@@ -296,5 +296,5 @@
         config = wx.ConfigBase_Get()
         if config.ReadInt("MDIFrameMaximized", False):
-            # wxBug: On maximize, statusbar leaves a residual that needs to be refereshed, happens even when user does it
+            # wxBug: On maximize, statusbar leaves a residual that needs to be refreshed, happens even when user does it
             self.Maximize()
 
Index: wxPython/wx/tools/XRCed/tree.py
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/wxPython/wx/tools/XRCed/tree.py,v
retrieving revision 1.15
diff -b -u -2 -r1.15 tree.py
--- wxPython/wx/tools/XRCed/tree.py	2005/05/26 01:10:04	1.15
+++ wxPython/wx/tools/XRCed/tree.py	2005/05/29 15:53:59
@@ -623,5 +623,5 @@
         # Update tools
         g.tools.UpdateUI()
-        # Hightlighting is done in OnIdle
+        # Highlighting is done in OnIdle
         self.pendingHighLight = self.selection
 
@@ -649,5 +649,5 @@
         size = obj.GetSize()
         # Highlight
-        # Nagative positions are not working wuite well
+        # Negative positions are not working quite well
         if g.testWin.highLight:
             g.testWin.highLight.Replace(pos, size)
@@ -671,5 +671,5 @@
             self.SetItemBold(g.testWin.item, False)
         self.CreateTestWin(item)
-        # Maybe an error occured, so we need to test
+        # Maybe an error occurred, so we need to test
         if g.testWin: self.SetItemBold(g.testWin.item)
 
Index: wxPython/wx/tools/XRCed/xrced.py
===================================================================
RCS file: /pack/cvsroots/wxwidgets/wxWidgets/wxPython/wx/tools/XRCed/xrced.py,v
retrieving revision 1.21
diff -b -u -2 -r1.21 xrced.py
--- wxPython/wx/tools/XRCed/xrced.py	2005/05/26 19:23:01	1.21
+++ wxPython/wx/tools/XRCed/xrced.py	2005/05/29 15:53:59
@@ -101,5 +101,5 @@
         # Load our own resources
         self.res = wxXmlResource('')
-        # !!! Blocking of assert failure occuring in older unicode builds
+        # !!! Blocking of assert failure occurring in older unicode builds
         try:
             self.res.Load(os.path.join(basePath, 'xrced.xrc'))

 	  	 
